/**
 * Compatibility export for Chat consumers.
 *
 * The palette belongs to the platform theme runtime. Chat imports the same
 * contract as every other authenticated product surface.
 */
export * from '../../../platform/platformThemePalette';
