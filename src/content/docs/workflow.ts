import type { ProductDocs } from './_types';

const workflow: ProductDocs = {
  "slug": "workflow",
  "productName": "XENO Workflow",
  "tagline": "Local-first, AI-native visual workflow automation — a Zapier / Make / n8n alternative built on the XENO ecosystem.",
  "seo": {
    "title": "XENO Workflow Documentation",
    "description": "Build, run, and debug automations visually. Over 50 built-in nodes, a checkpointed execution engine, an expression language, an encrypted credential vault, and local + cloud AI — all running on your own machine."
  },
  "sections": [
    {
      "title": "Getting Started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Workflow is, how it is built, and what works today in the v0.3.0 alpha.",
          "body": "## What XENO Workflow is\n\nXENO Workflow is a desktop application for building automations visually. You drag nodes onto an infinite canvas, wire them together, and the engine runs the graph node by node — reading data out of one node's output ports and feeding it into the next. It is the XENO ecosystem's answer to Zapier, Make, n8n, and Power Automate, with two deliberate differences:\n\n- **Local-first.** Workflows execute on your own machine. Your data, credentials, and API calls never have to leave it. HTTP requests and email are sent from the app's own main process rather than a browser sandbox.\n- **XENO-native.** It ships nodes for the rest of the XENO ecosystem (Pixel, Motion, Sound, 3D, Architect, Engine) and an AI Agent node backed by XENO's local + cloud LLM runtime.\n\nEverything is a **node**. A trigger node starts a run; action and transform nodes do the work; logic nodes branch and merge. Connections carry typed data between ports.\n\n## Maturity: alpha (v0.3.0)\n\nXENO Workflow is under active development. This documentation describes what is actually implemented in the current build and is explicit about what is stubbed or planned — no page here promises a capability the app does not have yet.\n\nWhat works today:\n\n- A custom infinite canvas with typed data wires and Blueprints-style execution pins (see [The nodes and the canvas](/docs/workflow/nodes-and-canvas)).\n- 146 built-in node types across triggers, data/transform, logic, external services, and AI (see [Node library](/docs/workflow/node-library)).\n- A graph execution engine with per-node checkpoints, retry/backoff, error strategies, and a dead-letter queue (see [The execution engine](/docs/workflow/execution-engine)).\n- An expression language for wiring data through node configuration (see [Expressions and variables](/docs/workflow/expressions-and-variables)).\n- Real integrations: HTTP, SMTP email, Slack, Discord, GitHub issues, S3/R2, plus an AI Agent node backed by local (xeno-rt) or cloud LLMs (see [Integrations, scheduling and AI](/docs/workflow/integrations-scheduling-and-ai)).\n- Local persistence: the `.xflow` project format, autosave, crash recovery, and execution history.\n- An agent capability port (new in v0.3.0), so an agent in another process can discover and drive the app over local HTTP — bound to 127.0.0.1 only, with a bearer token minted per launch.\n\nWhat is not finished yet (and is documented as such where relevant):\n\n- The database nodes (PostgreSQL, MySQL, Redis) are placeholders that report they need a driver.\n- The XENO creative-app nodes (Pixel, Motion, Sound, 3D, Architect, Engine) and the AI Model node are pass-through stubs that require the target app or xeno-lib wiring. The 54 fine-grained creative-app nodes added in v0.3.0 are real implementations, but they call a host bridge Workflow does not yet inject, so they report that the target app is not running.\n- Of the trigger nodes, only Manual, CRON Schedule, and Webhook are wired to live schedulers/servers; the rest place on the canvas but do not yet run a live watcher/poller.\n- The engine is a token-based scheduler with dead-path elimination, real iteration, and BPMN parallel joins.\n\n## How it is built\n\n| Layer | Technology |\n|---|---|\n| Desktop shell | Electron 34 |\n| UI | React 19 + TypeScript + Tailwind CSS |\n| Canvas | Custom DOM-hybrid renderer with a WebGPU/WebGL2 overview layer |\n| Canvas virtualization | Rust R-tree spatial index (napi-rs) |\n| Execution engine | TypeScript (pure logic, framework-free) |\n| State | Zustand 5 |\n| Scheduling / servers | node-cron + a built-in HTTP webhook server |\n| Email | nodemailer (SMTP) |\n| AI | @xeno-corporation/xeno-agent-sdk DualLLMProvider (local xeno-rt + cloud) |\n\nA note on the engine: the part of XENO Workflow written in Rust is the **canvas scene graph** — a spatial index that keeps the canvas fast when a workflow holds a very large number of nodes. The workflow **execution** engine itself is TypeScript and framework-free, which keeps every node's `execute()` function unit-testable in isolation.\n\n## Where to go next\n\n- New here? Start with [Installation](/docs/workflow/installation), then [Build your first workflow](/docs/workflow/quickstart).\n- Want the mental model? Read [The nodes and the canvas](/docs/workflow/nodes-and-canvas) and [The execution engine](/docs/workflow/execution-engine).\n- Looking for a specific node? Jump to the [Node library](/docs/workflow/node-library)."
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Build and run XENO Workflow from source, including the native canvas module, and where it stores your files.",
          "body": "## Status\n\nXENO Workflow is an alpha and is distributed as source in the `xeno-workflow` repository. Packaged installers are produced with `electron-builder`, and the XENO Hub launcher is the intended distribution channel; until a build is published there, run it from source as below.\n\n## Prerequisites\n\n- **Node.js** 20+ and npm.\n- **A Rust toolchain** — the canvas uses a native module (`src/native/scene-graph`) compiled through napi-rs. On Windows this needs the MSVC build tools; on macOS/Linux the standard `cargo` toolchain.\n- The sibling repos it links to locally: `@xeno/core` and `@xeno-corporation/xeno-agent-sdk` are referenced as `file:` dependencies, so keep the XENO monorepo checkout intact.\n\n## Build and run\n\n```bash\n# From the xeno-workflow/ directory\nnpm install\n\n# Compile the native canvas scene graph (Rust to .node)\nnpm run build:native\n\n# Start the app in development\nnpm run dev\n```\n\nIf the native module has not been built, the renderer still boots — the preload bridge loads the scene graph lazily and surfaces a descriptive error instead of crashing, but viewport virtualization for very large graphs is unavailable until you run `npm run build:native`.\n\n## Other commands\n\n```bash\nnpm run build        # Production build (electron-vite)\nnpm run typecheck    # TypeScript, no emit\nnpm run test         # Vitest unit + integration suite\nnpm run test:native  # Smoke-test the Rust scene graph\nnpm run package:win  # Build a Windows installer\nnpm run package:mac  # Build a macOS installer\nnpm run package:linux# Build a Linux installer\n```\n\nEach `npm run package:*` runs `build:native`, the electron-vite build, and electron-builder in sequence.\n\n## Where your files live\n\nXENO Workflow keeps everything under your home directory, in `~/.xeno/workflows/`:\n\n| Path | Contents |\n|---|---|\n| `*.xflow` | Saved workflows (JSON, see [Node library](/docs/workflow/node-library)) |\n| `.autosave/` | Autosaved copies used for crash recovery (offered for 24 hours) |\n| `.recent.json` | Recently opened workflows (up to 20) |\n| `.executions/` | Execution history (last 200 runs, plus an index) |\n| `.schedules.json` | Registered CRON schedules |\n| `.credentials.vault` | Encrypted credential store (see [Integrations](/docs/workflow/integrations-scheduling-and-ai)) |\n| `.dead-letter-queue.json` | Failed executions captured for inspection |\n\nWindow position and size are stored separately in the app's user-data directory. Nothing here is uploaded anywhere by the app itself.\n\n## Next\n\nWith the app running, follow [Build your first workflow](/docs/workflow/quickstart)."
        },
        {
          "slug": "quickstart",
          "title": "Build your first workflow",
          "description": "Create a manual to transform to HTTP workflow, run it, and read the execution log.",
          "body": "This walkthrough builds a small workflow that starts manually, shapes some data, and posts it to a URL. It touches the four things every workflow uses: a trigger, an action, wires, and the execution log.\n\n## 1. Start a workflow and add a trigger\n\nOpen the **node palette** on the left. Every run needs a starting node — a node with no incoming connections. Drag a **Manual Trigger** onto the canvas. It has no inputs and outputs a `timestamp` and a `trigger` payload.\n\nTriggers are the top category in the palette. Besides Manual Trigger you will find CRON Schedule, Webhook, File Watcher, Email, and more — though only Manual, CRON, and Webhook are wired to live schedulers/servers today (see [Integrations, scheduling and AI](/docs/workflow/integrations-scheduling-and-ai)).\n\n## 2. Add a transform\n\nDrag a **Set Variable** node (Data category) onto the canvas. In the **node inspector** on the right, set its `Name` to `greeting` and its `Value` to a literal or an expression such as:\n\n```\nHello from XENO Workflow at {{ timestamp() }}\n```\n\nThe `{{ … }}` syntax is the expression language — `timestamp()` is a built-in function that returns the current ISO-8601 time. See [Expressions and variables](/docs/workflow/expressions-and-variables) for the full list.\n\n## 3. Wire the nodes\n\nDrag from the Manual Trigger's output port to the Set Variable's input port. A bezier wire connects them, colored by the data type flowing across it. Data ports only connect to compatible types — the canvas rejects an incompatible drop.\n\nNodes also have white **execution pins** that control the order things run in (the Blueprints/Unreal dual-wire model). For a simple linear flow you can rely on the data wires; execution pins matter once you branch. See [The nodes and the canvas](/docs/workflow/nodes-and-canvas).\n\n## 4. Add an action and run\n\nDrag an **HTTP Request** node (External category). Set its `URL` and `Method` (e.g. `POST` to a test endpoint), and wire the Set Variable output into the HTTP node's `Body` input. In the body you can reference upstream data with expressions.\n\nRun the workflow from the toolbar. As it executes:\n\n- Each node shows its status as it runs, then settles to success or error.\n- The **execution log** streams timestamped entries with per-node status and duration.\n- After every node completes, the engine writes a **checkpoint** — a snapshot of node outputs and variables at that point. That is what powers time-travel debugging.\n\n## 5. Save it\n\nSave the workflow. XENO Workflow writes a `.xflow` file — a JSON document containing your nodes, edges, variables, and metadata. While you work, the app autosaves in the background; if it ever closes unexpectedly, it offers to recover your unsaved work on next launch.\n\n## Where to go next\n\n- Understand what the engine did: [The execution engine](/docs/workflow/execution-engine).\n- Shape data between nodes: [Expressions and variables](/docs/workflow/expressions-and-variables).\n- Browse everything you can drag onto the canvas: [Node library](/docs/workflow/node-library)."
        }
      ]
    },
    {
      "title": "Core Concepts",
      "pages": [
        {
          "slug": "nodes-and-canvas",
          "title": "The nodes and the canvas",
          "description": "The node model, data ports vs execution pins, the dual-wire graph, and how the canvas stays fast at scale.",
          "body": "## The node model\n\nEvery node type in XENO Workflow is a definition registered in a central **node registry**. A definition declares its category and appearance, its input and output ports, its configuration fields, its default error handling, and an `execute()` function. In TypeScript terms:\n\n```typescript\ninterface NodeTypeDefinition {\n  type: string          // unique id, e.g. 'http-request'\n  name: string\n  category: NodeCategoryId\n  visualCategory: 'trigger' | 'action' | 'transform' | 'logic'\n  inputs: PortDefinition[]\n  outputs: PortDefinition[]\n  execInputs?: ExecPortDefinition[]   // optional; derived from category if omitted\n  execOutputs?: ExecPortDefinition[]\n  pure?: boolean        // data-only node, no execution pins\n  configFields: ConfigField[]\n  errorHandling: ErrorHandlingConfig\n  execute: (inputs, config, ctx) => Promise<Record<string, unknown>>\n}\n```\n\nWhen you drag a node onto the canvas you create an **instance** of a type — with its own id, label, position, and config. The instance references the type; the type owns the behavior. Because `execute()` is a plain async function with no React or DOM dependency, each node is independently unit-testable, and the same definitions drive both the UI and the engine.\n\n## Two kinds of ports, two kinds of wires\n\nXENO Workflow uses the dual-wire model popularized by Unreal Engine's Blueprints:\n\n- **Data ports** are typed and carry values. They sit on the left (inputs) and right (outputs) edges of a node and are colored by their data type. The supported port types are `string`, `number`, `boolean`, `object`, `array`, `image`, `audio`, `video`, and `any`.\n- **Execution pins** are white and control *when* a node runs, not *what* data flows.\n\nMost nodes have their execution pins derived automatically from their category — a trigger has one exec output and no exec input; everything else has one in and one out. Nodes with branching control flow declare their exec outputs explicitly: **If / Else** exposes `true` and `false`; **Switch** exposes one pin per case plus a default; **For Each** exposes `each` and `done`. Pure data nodes (formatters, math, getters) have no exec pins at all — their outputs are computed on demand by whoever reads them.\n\nType compatibility is checked as you draw a wire: the connection type-checker rejects drops between incompatible data types, and `any` accepts anything.\n\n## Reroute knots\n\nDrop a data wire onto a **knot** and XENO Workflow inserts a hidden `reroute` node — a small pass-through with one `any` input and one `any` output. Reroutes let you route wires cleanly around the graph without changing the data. They never appear in the palette; they exist only to organize connections.\n\n## The canvas\n\nThe canvas is a custom, XENO-owned renderer — not a third-party graph library. (An earlier plan to build on React Flow, and a later pure-WebGPU plan, were both reversed; the repo's own architecture notes record this.) It is a **DOM-hybrid** design:\n\n- The **nodes you can see** are rendered as real DOM/React elements. That gives pixel-perfect templates, crisp text, native inline widgets, tooltips, and accessibility.\n- A **Rust scene graph** (an R-tree spatial index, compiled through napi-rs) holds the workflow's nodes and edges as data and answers one question extremely fast: *which nodes and edges fall inside the current viewport?* Only that visible set is mounted in the DOM, so a workflow can hold a very large number of nodes without the DOM trying to render all of them.\n- **Edges** are drawn as SVG bezier wires behind the nodes.\n- When you zoom far enough out that individual nodes would be sub-pixel, an **overview layer** (drawn on a single `<canvas>` via WebGPU or WebGL2) takes over and shows the graph as a density map rather than full templates.\n\nA renderer-agnostic input router and camera handle pan, zoom, marquee selection, hover, drag-move, and connection dragging.\n\n### On the performance targets\n\nThe repo's architecture aims at studio-grade scale — very large graphs held without crashing, and smooth pan/zoom at high node counts — and the scene-graph virtualization above is the mechanism meant to get there. Treat the specific numeric targets in the repo (e.g. 100K+/1M-node ceilings) as engineering goals for the canvas, not as guarantees of the current alpha build.\n\n## Next\n\nSee how these nodes actually run in [The execution engine](/docs/workflow/execution-engine)."
        },
        {
          "slug": "execution-engine",
          "title": "The execution engine",
          "description": "How a workflow runs: topological order, checkpoints and time-travel, error strategies, retry with backoff, and the dead-letter queue.",
          "body": "## How a run works\n\nWhen you run a workflow, the engine:\n\n1. Builds a directed graph from your nodes and edges (adjacency + reverse-adjacency).\n2. Finds the **start nodes** — nodes with no incoming edges.\n3. Computes an execution order with a **topological sort** (Kahn's algorithm).\n4. Executes nodes in that order, one at a time.\n\nFor each node it collects inputs from upstream node outputs (following the wires), resolves any `{{ expressions }}` in the node's config against the current variables and node outputs, runs the node's `execute()` with error handling, records the result, and writes a checkpoint.\n\nThe engine is a **token-based, exec-driven scheduler with dead-path elimination (DPE)**, the model BPEL uses. A branching node declares which of its execution outputs actually fired, and the engine schedules only that branch — every node on the path not taken is marked **dead** rather than run, and deadness propagates downstream so that joins downstream of a dead branch resolve correctly instead of hanging. The same rule covers failure: if a node's upstream dependency failed and no error route reaches it, it is marked **skipped**.\n\nIteration is real, not a single pass. Iterator nodes (For Each, Split) emit their body once per item into a fresh child scope, the engine drains that body to completion, and only then fires the iterator's `done` output with the collected results. There are no graph cycles — cycles are rejected at run start, with collect wires exempt.\n\nParallel joins follow BPMN: a node with an `all` execution-input policy (Aggregate) waits for every incoming branch, while the default `any` policy fires on the first.\n\n## Execution context\n\nEvery node's `execute()` receives a context object with the tools it needs:\n\n```typescript\ninterface ExecutionContext {\n  workflowId: string\n  executionId: string\n  nodeId: string\n  checkpoint: (data) => Promise<void>\n  log: (level, message) => void\n  getVariable: (name) => unknown\n  setVariable: (name, value) => void\n  getNodeOutput: (nodeId, portId) => unknown\n  abortSignal: AbortSignal\n}\n```\n\nThe `abortSignal` is how cancellation works: cancelling a running execution aborts the signal, and well-behaved nodes (Delay, HTTP, the Code node's timeout race, and the retry backoff wait) stop promptly.\n\n## Checkpoints and time-travel\n\nAfter every successful node, the engine saves an **execution checkpoint** — a snapshot of all node results and variable state at that moment. Checkpoints accumulate through the run and make debugging concrete: you can inspect the exact data at any point in the pipeline, and the replay tooling can step through a completed execution.\n\nCompleted runs are persisted to `~/.xeno/workflows/.executions/` (the most recent 200, with an index), so you can reopen and inspect a run after the fact.\n\n## Error handling strategies\n\nErrors are handled **per node**. A node's definition ships a default strategy, and you can override it on the instance. The available strategies are:\n\n| Strategy | Behavior |\n|---|---|\n| `break` | Stop the workflow and mark it failed (the default for most nodes) |\n| `resume` | Continue using the node's fallback output instead of failing |\n| `retry` | Re-run the node with backoff (configured separately) before applying a terminal strategy |\n| `commit` | Checkpoint partial results, then fail |\n| `rollback` | Unwind the failed branch, then fail |\n| `error_route` | Emit an error marker so flow redirects to a designated error-handling node |\n\nRetry behavior is orthogonal: any node can carry a retry config that runs first, and only after retries are exhausted does the terminal strategy apply.\n\n## Retry with backoff\n\nWhen a node retries, the retry config controls how:\n\n```typescript\ninterface RetryConfig {\n  maxRetries: number          // e.g. 3\n  backoffType: 'fixed' | 'exponential' | 'linear'\n  initialDelay: number        // ms\n  maxDelay: number            // ms cap\n  retryOn: ErrorType[]        // which error classes to retry\n}\n```\n\nErrors are automatically **classified** from their message into `timeout`, `network`, `rate_limit`, `server_error`, `validation`, or `unknown`, and a retry fires only when the class is listed in `retryOn` (or when `unknown` is listed). Delays grow according to `backoffType`, carry a small random jitter (10%) to avoid thundering herds, and are capped at `maxDelay`. The backoff wait respects the abort signal, so cancelling stops it immediately.\n\nMany built-in nodes ship sensible defaults — HTTP Request, for instance, retries on `timeout`, `network`, and `server_error` with exponential backoff.\n\n## The dead-letter queue\n\nWhen a node ultimately fails, the engine records it in a **dead-letter queue** (DLQ): the workflow, execution, node, the error and its classified type, the inputs, and the config at failure time. The in-memory DLQ holds up to 1000 entries and is persisted to disk (`.dead-letter-queue.json`), giving you a durable record of what failed and why, ready for inspection or a manual re-run.\n\n## Next\n\nMost real workflows shape data between nodes with expressions — see [Expressions and variables](/docs/workflow/expressions-and-variables)."
        },
        {
          "slug": "expressions-and-variables",
          "title": "Expressions and variables",
          "description": "The double-brace expression language: variable paths, node references, built-in functions, arithmetic, and workflow variables.",
          "body": "## Expressions\n\nAnywhere a node's configuration accepts a value, you can embed an expression in `{{ … }}`. Before a node runs, the engine resolves every expression in its config against the current state and passes the resolved config to `execute()`.\n\nA field that is a **single** expression returns the raw evaluated value (so `{{ data.items }}` stays an array). An expression mixed with surrounding text is coerced to a string (so `Count: {{ length(data.items) }}` becomes `Count: 3`).\n\n## What you can write\n\n- **Variable paths** — `{{ myVar }}`, `{{ data.user.email }}`, `{{ data.items[0].name }}`. Dotted and single-index bracket paths both work.\n- **Node references** — `{{ $node.<nodeId>.<port> }}` reads a specific upstream node's output port directly.\n- **Literals** — strings (`{{ \"hello\" }}`), numbers (`{{ 42 }}`), booleans, and `null`.\n- **Arithmetic** — `{{ price * 1.1 }}`, `{{ count + 1 }}`. String `+` concatenates.\n- **Comparisons** — `===`, `!==`, `==`, `!=`, `>=`, `<=`, `>`, `<`.\n- **Logical** — `&&`, `||`.\n- **Ternary** — `{{ score > 50 ? \"pass\" : \"fail\" }}`.\n- **Function calls** — see below.\n\nExpression evaluation is a purpose-built parser (not JavaScript `eval`), and path resolution is hardened against prototype-pollution: keys like `__proto__`, `prototype`, and `constructor` are never traversed.\n\n## Built-in functions\n\nThe expression language ships a standard library you can call inline. A representative set:\n\n- **Strings** — `upper`, `lower`, `trim`, `replace(v, search, repl)`, `split(v, sep)`, `join(v, sep)`, `substring`, `length`, `contains`, `startsWith`, `endsWith`.\n- **Numbers** — `round(v, decimals)`, `floor`, `ceil`, `abs`, `min`, `max`, `random`, `parseInt`, `parseFloat`.\n- **Dates** — `now()` (epoch ms), `timestamp()` (ISO string), `formatDate(v, 'date' | 'time')`.\n- **Collections** — `keys`, `values`, `entries`, `first`, `last`, `flatten`, `unique`, `sort`, `reverse`, `slice`, `map(v, key)`, `filter(v, key, value)`.\n- **Types** — `typeof`, `isNull`, `isArray`, `isObject`, `isNumber`, `isString`, `toBool`, `toNumber`, `toString`.\n- **JSON** — `jsonParse`, `jsonStringify(v, indent)`.\n- **Conditional** — `if(cond, a, b)`, `coalesce(...)` (first non-null), `default(v, fallback)`.\n\nFunctions nest and compose, e.g. `{{ upper(first(split(data.name, \" \"))) }}`.\n\n## Workflow variables\n\nA workflow carries a bag of **variables** that persist across the whole run. Any node can read and write them through its execution context (`getVariable` / `setVariable`), and expressions can reference them by name. The **Set Variable** node is the direct way to store a value; from then on `{{ thatName }}` resolves to it anywhere downstream.\n\nVariables are included in every checkpoint, so when you inspect an execution you see the variable state at each step alongside node outputs.\n\n## The Code node vs. expressions\n\nExpressions are for lightweight, inline shaping. When you need real logic, the **Code** node runs JavaScript with a configurable timeout. It receives your input as `data` and a restricted `ctx` (`log`, `getVariable`, `setVariable`), and it deliberately shadows dangerous globals (`process`, `require`, `fetch`, `window`, and more) as undefined to keep scripts sandboxed. Return a value and it becomes the node's `result` output.\n\n## Next\n\nSee which nodes accept which fields in the [Node library](/docs/workflow/node-library)."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "node-library",
          "title": "Node library",
          "description": "Every built-in node by category — triggers, data/transform, logic, external, XENO and AI — plus the .xflow project format.",
          "body": "XENO Workflow ships 146 built-in node types — 70 core nodes plus the 76 XENO ecosystem nodes, which became reachable in v0.3.0. This page is the catalog, grouped by palette category. Node type ids (in `code`) are what appears in the saved `.xflow` file and in AI-generated workflows.\n\n## Triggers\n\nTriggers start a run and have no data inputs.\n\n| Type | Node | What it does |\n|---|---|---|\n| `manual-trigger` | Manual Trigger | Start a run by hand, with an optional JSON payload |\n| `cron-trigger` | CRON Schedule | Fire on a CRON expression (persisted via node-cron) |\n| `webhook-trigger` | Webhook | Fire on an inbound HTTP request; exposes HMAC-verification and IP-whitelist fields |\n| `file-watch-trigger` | File Watcher | Fire when files in a directory change |\n| `email-trigger` | Email Trigger | Fire when new mail arrives (IMAP) |\n| `database-trigger` | Database Trigger | Fire when new rows appear |\n| `app-event-trigger` | App Event | Fire on XENO app events (file saved, export complete, …) |\n| `schedule-trigger` | Schedule | Fire on an interval or at a specific time |\n\nToday only **Manual**, **CRON Schedule**, and **Webhook** are wired to live main-process schedulers/servers (see [Integrations, scheduling and AI](/docs/workflow/integrations-scheduling-and-ai)). The others place on the canvas and are configurable, but their live watchers/pollers (IMAP, file-watch, DB-poll, app-event bus) are not implemented yet — their nodes currently emit placeholder output.\n\n## Data and transform\n\nMost of these are **pure** nodes — no side effects, outputs computed from inputs.\n\n| Type | Node | Highlights |\n|---|---|---|\n| `json-transform` | JSON Transform | Emit a JSON template as the result |\n| `set-variable` | Set Variable | Store a value into a workflow variable |\n| `code` | Code | Run sandboxed JavaScript with a timeout |\n| `text-formatter` | Text Formatter | uppercase/lowercase/title/trim/replace/regex/split/join/truncate/slug/template/extract/strip-HTML/URL-encode/decode |\n| `number-formatter` | Number Formatter | round/floor/ceil/abs/format/currency/percentage/clamp and basic math |\n| `date-formatter` | Date Formatter | parse/format/add/subtract/diff/before/after |\n| `array-operations` | Array Operations | filter/map/sort/unique/flatten/chunk/zip/slice/compact/sum/average/group-by |\n| `object-operations` | Object Operations | pick/omit/merge/flatten/unflatten/keys/values/has/set/delete/rename |\n| `csv-parser` | CSV Parser | CSV to/from JSON with configurable delimiter and header |\n| `xml-parser` | XML Parser | Simple flat XML to JSON |\n| `html-parser` | HTML Parser | Extract text, links, images, and meta tags |\n| `base64` | Base64 | Encode / decode |\n\n## Logic\n\n| Type | Node | What it does |\n|---|---|---|\n| `if-else` | If / Else | Branch on a condition (expression, exists, truthy, equals, gt, lt, contains, regex) |\n| `switch` | Switch | Route a value to one of several cases, plus a default |\n| `loop` | For Each | Iterate over an array (emits `item` / `index` / `done`) |\n| `delay` | Delay | Wait a configurable duration, respecting cancellation |\n| `merge` | Merge | Combine two inputs (combine / array / first) |\n| `split` | Split | Fan an array out item by item |\n| `parallel-branch` | Parallel Branch | Fan one payload to multiple branch outputs |\n| `aggregate` | Aggregate | Collect branch results (array / merge / first) |\n| `sub-workflow` | Sub-Workflow | Invoke another workflow as a step |\n| `error-handler` | Error Handler | Catch an upstream error and continue or fall back |\n| `wait-for-approval` | Wait for Approval | Human-in-the-loop pause with approve/reject outputs |\n\nAs described in [The execution engine](/docs/workflow/execution-engine), these nodes are scheduled by a token-based executor with dead-path elimination: only the branch a node actually fires is run, and iterator nodes drain their body per item before firing `done`.\n\n## External services\n\n| Type | Node | Status |\n|---|---|---|\n| `http-request` | HTTP Request | Works. GET/POST/PUT/PATCH/DELETE with Basic/Bearer/API-key auth, run through the app's main process |\n| `email-send` | Send Email | Works. SMTP via nodemailer |\n| `email-read` | Read Email | IMAP read (scaffold — returns empty) |\n| `slack-send` | Slack: Send | Works. Bot token or incoming webhook URL |\n| `slack-listen` | Slack: Listen | Slack event listener (scaffold) |\n| `discord-send` | Discord: Send | Works. Discord webhook |\n| `github-create-issue` | GitHub: Issue | Works. Creates an issue via the REST API |\n| `github-webhook` | GitHub: Webhook | GitHub event listener (scaffold) |\n| `s3-upload` | S3: Upload | Works. PUT to S3/R2-compatible storage |\n| `s3-download` | S3: Download | Works. GET from S3/R2-compatible storage |\n| `postgres-query` | PostgreSQL | Not available yet — reports it needs the `pg` driver |\n| `mysql-query` | MySQL | Not available yet — reports it needs the `mysql2` driver |\n| `redis` | Redis | Not available yet — reports it needs the `ioredis` driver |\n\nUntil the database drivers land, query a database through a REST endpoint with HTTP Request, or use the Code node against a proxy — which is exactly what the database nodes' error messages suggest.\n\n## XENO and AI\n\n| Type | Node | Status |\n|---|---|---|\n| `xeno-agent` | AI Agent | Works. Sends prompts to a local (xeno-rt) or cloud LLM; supports a system prompt, temperature, max tokens, and JSON mode |\n| `xeno-lib-ai` | AI Model | Selects a task model (background removal, upscale, denoise, face restore, depth, segment, OCR, style, colorize, inpaint, voice clone, music gen, noise reduce); wiring to xeno-lib is in development, so it currently passes input through |\n| `xeno-pixel` | XENO Pixel | In development — pass-through stub, needs XENO Pixel running |\n| `xeno-motion` | XENO Motion | In development — pass-through stub, needs XENO Motion running |\n| `xeno-sound` | XENO Sound | In development — pass-through stub, needs XENO Sound running |\n| `xeno-3d` | XENO 3D | In development — pass-through stub, needs XENO 3D running |\n| `xeno-architect` | XENO Architect | In development — pass-through stub |\n| `xeno-engine` | XENO Engine | In development — pass-through stub |\n\nThe creative-app nodes above currently pass their input through and log the requested operation; they light up fully once the target app is running and exposes its API. The AI Agent node is fully functional — see [Integrations, scheduling and AI](/docs/workflow/integrations-scheduling-and-ai).\n\n## The XENO ecosystem node families (new in v0.3.0)\n\nAlongside the eight single nodes above, XENO Workflow ships **76 fine-grained ecosystem nodes** across eight families. These were present but **unreachable** in every build up to and including v0.2.0 — they were defined and tested but never registered, so they did not appear in the palette at all. As of v0.3.0 they are registered and reachable.\n\n| Family | Nodes | What it covers | Status |\n|---|---|---|---|\n| Agent | 7 | `agent-prompt-llm`, `agent-tool-call`, `agent-conversation`, `agent-multi-agent`, `agent-rag`, `agent-classify`, `agent-extract` | **Works.** Runs in the app's own main process — `agent-tool-call` drives a real xeno-agent-sdk tool loop. Needs XENO account credentials. |\n| LibAI | 16 | Background removal, upscale, denoise, face restore, depth, segment, OCR, style, colorize, inpaint, voice clone, music gen, noise reduce and related task models | Routed through the xeno-rt task server. Reports a clear error when that runtime is not available. |\n| Pixel | 12 | Open, resize, crop, filter, export and other image operations | Requires XENO Pixel's host bridge (see below). |\n| Motion | 10 | Timeline, clip, render and export operations | Requires XENO Motion's host bridge. |\n| Sound | 10 | Track, effect, mix and export operations | Requires XENO Sound's host bridge. |\n| 3D | 8 | Scene, mesh, material and render operations | Requires XENO 3D's host bridge. |\n| Architect | 7 | Model, floor-plan and drawing operations | Requires XENO Architect's host bridge. |\n| Engine | 6 | Build, test, deploy, asset-pipeline and scene-export operations | Requires XENO Engine's host bridge. |\n\n**Be clear about what \"reachable\" means.** The 54 creative-app nodes (Pixel, Motion, Sound, 3D, Architect, Engine) are real implementations written against a host bridge — `window.xenoPixel` and its siblings — that **XENO Workflow does not yet inject**. You can find them in the palette, place them, and configure them; running one reports that the target app is not running rather than doing the work or silently passing data through. The Agent family is the exception: it runs against Workflow's own main process and works today.\n\n## The .xflow project format\n\nSaving a workflow writes a `.xflow` file: a JSON document with a schema version wrapping the workflow.\n\n```json\n{\n  \"xflowVersion\": 1,\n  \"workflow\": {\n    \"id\": \"…\",\n    \"name\": \"My workflow\",\n    \"description\": \"\",\n    \"version\": 1,\n    \"nodes\": [ /* id, type, label, config, position */ ],\n    \"edges\": [ /* id, source, sourcePort, target, targetPort */ ],\n    \"variables\": {},\n    \"createdAt\": \"…\",\n    \"updatedAt\": \"…\"\n  }\n}\n```\n\nBecause it is plain JSON, `.xflow` files are diff-friendly and easy to keep in version control. XENO Workflow also reads a bare workflow JSON object (one with `id` and `nodes`) on import. Files are validated on load — files that are too large (over 10 MB) or malformed are rejected rather than partially imported."
        },
        {
          "slug": "integrations-scheduling-and-ai",
          "title": "Integrations, scheduling and AI",
          "description": "How triggers connect to the CRON scheduler and webhook server, how credentials are stored, and how the AI nodes and AI Workflow Builder work.",
          "body": "This page covers the parts of XENO Workflow that reach outside a single run: the schedulers and servers behind triggers, the credential vault, and the AI features.\n\n## Scheduling: CRON\n\nThe **CRON Schedule** trigger is backed by `node-cron` running in the app's main process. When you register a schedule, XENO Workflow:\n\n- Validates the CRON expression.\n- Starts a live cron job that, when it fires, notifies the renderer to run the workflow.\n- **Persists** the schedule to `~/.xeno/workflows/.schedules.json` and re-loads enabled schedules on the next launch, so they survive restarts.\n\nUnregistering a schedule stops the job and removes it from disk. All cron jobs are stopped cleanly when the app quits.\n\n## The webhook server\n\nThe **Webhook** trigger is served by a built-in HTTP server in the main process (default port 9100). Registering a webhook route adds a `path` + `method` mapping; an inbound request that matches is answered with your configured response code and body, and its parsed body, headers, query, and method are handed to the workflow. The server starts on the first registered route and shuts down when the last one is removed. The webhook trigger node also carries fields for HMAC-signature verification and an IP whitelist.\n\n## HTTP and email run in-process\n\nNetwork-facing nodes execute in the app's own main process (over IPC) rather than the renderer sandbox:\n\n- **HTTP Request** validates the URL and protocol (http/https only), enforces a timeout, supports Basic / Bearer / API-key auth, and returns the parsed body, status, and headers.\n- **Send Email** uses `nodemailer` over SMTP (implicit TLS when the port is 465, otherwise STARTTLS).\n\nRunning these in the main process is what makes local-first automation practical: no browser CORS constraints, and your traffic stays on your machine.\n\n## The credential vault\n\nRather than hard-coding secrets into node config, XENO Workflow provides an encrypted **credential vault**. On disk it lives at `~/.xeno/workflows/.credentials.vault`, encrypted with the operating system's secure storage (Electron `safeStorage`) when available; the app also exposes whether OS-level encryption is available, so you know which protection you have. When encryption is unavailable the vault is written unencrypted, so treat that case accordingly.\n\n## AI Agent node\n\nThe **AI Agent** node (`xeno-agent`) is fully functional. It sends a prompt — with optional structured context — to a large language model and returns the text response, an optional parsed JSON object (in JSON mode), and token-usage stats. It is configurable with a system prompt, temperature, and max tokens.\n\nRouting is handled by the agent SDK's `DualLLMProvider`, which prefers a **local** model served by xeno-rt and falls back to the **cloud** (api.xenostudio.ai). You can query provider status and switch modes from the app. If no provider is reachable, the node fails with a clear message rather than silently doing nothing.\n\n## AI Workflow Builder\n\nXENO Workflow can turn a plain-language description into a starting workflow. The builder first detects intents from your prompt with keyword matching — phrases like \"send a slack message\", \"when a github issue is created\", \"every hour\", or \"filter the data\" map to real registered node types — and assembles a connected graph. When an LLM provider is connected, the app can additionally consult it to refine the design. The result is a normal editable workflow: a scaffold to adjust, not a black box.\n\nThe app also exposes focused AI helpers over the same provider, including generating a workflow from a description and explaining what a given node does.\n\n## The AI Model node vs. the AI Agent node\n\nDon't confuse the two AI nodes:\n\n- **AI Agent** (`xeno-agent`) talks to an LLM for reasoning, generation, and structured extraction — and it works today.\n- **AI Model** (`xeno-lib-ai`) is for task-specific models (upscale, background removal, OCR, transcription, and so on) served by xeno-lib. Its model catalog is present in the node, but the execution wiring to xeno-lib is in development, so today it passes its input through.\n\n## Next\n\nReturn to the [Node library](/docs/workflow/node-library) for the full node catalog, or [The execution engine](/docs/workflow/execution-engine) for how failures from these integrations are retried and captured."
        }
      ]
    }
  ]
};

export default workflow;
