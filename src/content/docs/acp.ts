import type { ProductDocs } from './_types';

const acp: ProductDocs = {
  "slug": "acp",
  "productName": "XENO ACP",
  "tagline": "One protocol, any ACP coding agent — drive Claude Code, Codex, Gemini and more in-process or behind an OpenAI-compatible gateway.",
  "seo": {
    "title": "XENO ACP documentation",
    "description": "Embed or serve any Agent Client Protocol (ACP) agent through XENO ACP — an in-process engine, an OpenAI-compatible HTTP gateway, and a XENO-owned ACP agent endpoint."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO ACP is, the two surfaces it exposes, and where it fits in the XENO ecosystem.",
          "body": "# Introduction\n\n**XENO ACP** is the XENO ecosystem's single way to drive any [Agent Client Protocol](https://agentclientprotocol.com) (ACP) coding agent. It connects to approved ACP agents — Claude Code, Codex, OpenCode, Gemini CLI, Kimi, GLM — and re-exposes them to the rest of the ecosystem two ways: as an embeddable in-process library, and as an OpenAI-compatible HTTP gateway.\n\n> ACP is to AI coding agents what **LSP** is to language servers: one protocol, any client can drive any agent. XENO ACP plays the **client** side of ACP and re-exposes the agents to everything else.\n\n## Why it exists\n\nThe XENO ecosystem already standardizes raw LLM inference behind an OpenAI-compatible surface (`xeno-rt`). But agentic coding tools run a full **agentic loop** — they read files, run terminals, call MCP servers, and edit code — over protocol surfaces like ACP over stdio, not a chat API. XENO ACP is the **Layer 3 (Agent & Automation)** component that turns \"any ACP agent\" into \"a capability the ecosystem can call,\" embedded or over the wire.\n\nWhere `xeno-rt` serves *models*, XENO ACP serves *agents* — tools that act in a real workspace.\n\n## Two surfaces\n\n- **In-process** — [`@xeno-acp/core`](/docs/acp/embed-api) is a framework-free engine that a XENO agentic project imports directly. It returns a **structured** event stream (tool calls, plans, thoughts, diffs, usage) at full fidelity.\n- **Over HTTP** — [`@xeno-acp/server`](/docs/acp/http-gateway) is a thin OpenAI-compatible shell on top of the core. Simple consumers hit one `/v1/chat/completions` endpoint and route by `model` id; the agent output is **flattened** to text/SSE.\n\nFlattening only changes *presentation*. The agent still runs every tool inside its workspace — flattening just chooses how much of that is narrated back over the chat stream.\n\nA third package, [`@xeno-acp/agent`](/docs/acp/agent-endpoint), is the inverse: a XENO-owned ACP **agent-side** endpoint another client can spawn and drive over stdio.\n\n## Where it fits\n\n**Layer 3 — Agent & Automation.** XENO ACP is a sibling to `xeno-agent-sdk` (tool registry / message protocol) and `xeno-rt` (raw inference). Its intended in-process consumers are `xeno-agent-sdk`, `xeno-workflow`, `xeno-use`, `xeno-agent-cli`, and `xeno-hub`; over HTTP, anything that speaks the OpenAI chat format can call it. During the alpha these integrations are still being wired — the `xeno-hub` embed bridge is on the roadmap, not shipped.\n\n## Status\n\n> **Version `0.1.0-alpha.2` — private alpha.** XENO ACP is both an ACP **client/gateway** and an ACP **agent endpoint**. It is licensed [Apache-2.0](/docs/acp/agent-endpoint) and built on the Apache-2.0 ACP SDK. This is a **private alpha checkpoint, not a production-readiness claim**, and not an official-provider ACP certification. The packages are workspace-internal and run from source (see [Installation](/docs/acp/installation)).\n\n## Next steps\n\n- [Installation](/docs/acp/installation) — clone, install, and build the workspace\n- [Quickstart](/docs/acp/quickstart) — start the gateway and drive an agent\n- [Architecture](/docs/acp/architecture) — how the engine, sessions, and flattener fit together"
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Clone the XENO ACP workspace, install dependencies, and build the packages.",
          "body": "# Installation\n\nXENO ACP is a pnpm + TypeScript monorepo targeting **Node 20 or newer**. During the private alpha the packages are workspace-internal (`@xeno-acp/core`, `@xeno-acp/server`, `@xeno-acp/agent`) and are **run from source** — they are not published to a public npm registry.\n\n## Requirements\n\n- **Node.js 20+**\n- **pnpm 9** (the repo pins `pnpm@9.12.0`)\n- One or more ACP agents installed and authenticated on your machine, if you want to drive real agents (see [Agents and authentication](/docs/acp/agents-and-auth)). The hermetic smoke tests need no real agent.\n\n## Install and build\n\n```bash\ngit clone https://github.com/XENO-CORPORATION/xeno-acp\ncd xeno-acp\npnpm install\npnpm run build   # builds @xeno-acp/core, /server, and /agent\n```\n\n`pnpm run build` compiles every package under `packages/*`. The core engine must be built before the server or agent can typecheck against it; the `build` and `pretest` scripts handle that order for you.\n\n## Configure\n\nCopy the example config and edit it to enable the agents and API keys you want:\n\n```bash\ncp xeno-acp.config.example.jsonc xeno-acp.config.jsonc\n```\n\nThe config file is gitignored. String values support `${ENV_VAR}` expansion. See [HTTP gateway](/docs/acp/http-gateway) for the full schema; [Agents and authentication](/docs/acp/agents-and-auth) covers enabling agents.\n\n## Verify the install\n\nRun the hermetic test and smoke suites — they drive a deterministic **fixture ACP agent**, never a real provider CLI, and never read your real config or credentials:\n\n```bash\npnpm test          # vitest (unit + integration); builds first via pretest\npnpm run smoke     # boot the gateway against the fixture agent, end-to-end\npnpm run scan      # source security scan (fails on active-risk/secret-risk)\npnpm run quality   # typecheck + build + scan + test + smokes + git diff --check\n```\n\nIf `pnpm run smoke` prints a completed turn against the fixture, your build is healthy.\n\n## Next steps\n\n- [Quickstart](/docs/acp/quickstart) — start the gateway and send your first request\n- [Agents and authentication](/docs/acp/agents-and-auth) — enable a real ACP agent"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Start the OpenAI-compatible gateway and drive a configured ACP agent, or embed the engine in-process.",
          "body": "# Quickstart\n\nThis walks you from a built workspace to a first agent turn. If you have not built the repo yet, do [Installation](/docs/acp/installation) first.\n\n## 1. Start the gateway\n\n```bash\npnpm install\ncp xeno-acp.config.example.jsonc xeno-acp.config.jsonc   # edit agents + keys\npnpm --filter @xeno-acp/server dev                        # starts the gateway\n```\n\nBy default the server listens on `http://127.0.0.1:8787` in `bearer` auth mode, with `dev-key` configured as a local key in the example config. The root `pnpm run dev` script does the same after building core; `pnpm start` runs the built server.\n\n## 2. List the enabled agents\n\nEach enabled agent appears as an OpenAI model object. The `id` is what you pass as `model`.\n\n```bash\ncurl -s localhost:8787/v1/models -H \"authorization: Bearer dev-key\" | jq\n```\n\n## 3. Drive an agent (streaming)\n\n```bash\ncurl -N localhost:8787/v1/chat/completions \\\n  -H \"authorization: Bearer dev-key\" -H \"content-type: application/json\" \\\n  -d '{\"model\":\"claude-code\",\"stream\":true,\n       \"messages\":[{\"role\":\"user\",\"content\":\"List the files in the workspace and summarize them.\"}]}'\n```\n\nThe response is a standard `chat.completion.chunk` SSE stream ending in `data: [DONE]`. The agent runs its real tools inside an isolated sandbox workspace; by default only its message text is streamed back. To narrate tool calls, plans, and thoughts, add the [`xeno` extension](/docs/acp/http-gateway) with `\"verbose\": true`.\n\n> The `model` accepts an agent id (`claude-code`) or `agentId:submodel` (`gemini:gemini-3-pro-preview`) — see [the HTTP gateway reference](/docs/acp/http-gateway).\n\n## 4. Or run without any provider\n\nFor a hermetic, no-provider end-to-end check, drive the fixture agent:\n\n```bash\npnpm run smoke\n```\n\n## Embed it instead\n\nIf you are building another XENO project, skip HTTP entirely and consume the structured stream in-process:\n\n```ts\nimport { AcpManager, loadConfig } from \"@xeno-acp/core\";\n\nconst acp = new AcpManager(loadConfig());\nfor await (const ev of acp.prompt(\"claude-code\", {\n  messages: [{ role: \"user\", content: \"Refactor utils.ts and run the tests.\" }],\n})) {\n  if (ev.type === \"text\") process.stdout.write(ev.text);\n  else if (ev.type === \"tool_call\") console.log(\"[tool]\", ev.title, ev.status);\n}\nawait acp.dispose();\n```\n\nSee the full [Embedding API](/docs/acp/embed-api).\n\n## Next steps\n\n- [Architecture](/docs/acp/architecture) — what happens inside a prompt turn\n- [Safety and sandboxing](/docs/acp/safety-and-sandboxing) — what an agent can and cannot touch\n- [HTTP gateway](/docs/acp/http-gateway) — the full OpenAI-compatible contract"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "architecture",
          "title": "Architecture",
          "description": "The layered design: a framework-free core engine, a thin HTTP shell, and the ACP wire underneath.",
          "body": "# Architecture\n\nXENO ACP is three packages layered over the official ACP SDK. This page covers *how* it is built; for the public contract see the [HTTP gateway](/docs/acp/http-gateway) and [Embedding API](/docs/acp/embed-api) references.\n\n## Layers\n\n```\n@xeno-acp/server  ──depends on──▶  @xeno-acp/core  ──depends on──▶  @agentclientprotocol/sdk\n   (HTTP, OpenAI)                     (engine)                         (ACP wire, Apache-2.0)\n```\n\n`core` has **no HTTP and no knowledge of OpenAI**. `server` is a thin translation shell: OpenAI request → core call → flattened OpenAI response. Any other front-end (a CLI, a `xeno-hub` embed, gRPC) can sit on `core` the same way.\n\n## The core engine\n\nThe engine (`@xeno-acp/core`) is a small set of focused modules:\n\n- **AcpManager** — the public entry point. A lazy pool of agent processes (one per agent id), a per-agent concurrency semaphore, and the `prompt()` async iterator that both in-process consumers and the HTTP server build on.\n- **AgentProcess** — spawns the agent, bridges its stdio to the ACP SDK as an NDJSON stream, holds a `ClientSideConnection`, runs `initialize` once, dispatches each `session/update` to the right session, and restarts on crash.\n- **Session** — one ACP session id plus an async event queue. `prompt()` drives a turn and yields structured events until the turn's `StopReason`.\n- **Capabilities** — the ACP **Client** implementation: `requestPermission`, `readTextFile`, `writeTextFile`, and terminal methods, all scoped to the session workspace.\n- **flatten()** — projects the structured `AcpEvent` stream into OpenAI-shaped text/SSE deltas for the HTTP side.\n\n## The prompt turn\n\n```\nAcpManager.prompt(agentId, opts)\n  └─ acquire concurrency slot for agentId\n  └─ AgentProcess (lazy spawn + initialize once)\n       └─ newSession({ cwd: workspace })   → sessionId\n       └─ register Session(sessionId) in the dispatch map\n  └─ Session drives the turn:\n       ├─ connection.prompt({ sessionId, prompt })   ── resolves at end of turn\n       └─ meanwhile: Client.sessionUpdate(n) ──by sessionId──▶ Session.queue.push(event)\n  └─ async-iterate Session events  ──▶ caller (or flatten() for HTTP)\n  └─ on turn end: push {type:'done', stopReason}; close queue; release slot\n```\n\nThe key design point: **one long-lived subprocess per agent id, one cheap ACP session per request.** Spawning an agent (often via `npx`) is expensive; ACP sessions are not. Because the `Client` handler is per-*connection*, `AgentProcess` routes each session notification to the right `Session` by its session id.\n\n## The initialize handshake\n\nOnce per process, the gateway advertises the client capabilities it implements:\n\n```ts\nawait conn.initialize({\n  protocolVersion: acp.PROTOCOL_VERSION,   // 1\n  clientCapabilities: {\n    fs: { readTextFile: true, writeTextFile: true },\n    terminal: true,\n  },\n});\n```\n\nIf a `newSession` call returns an `auth_required` error, the process is marked **needs-auth** and surfaced in `/healthz` and as an `error` event. You authenticate that agent's own CLI once; the gateway never performs OAuth itself.\n\n## Concurrency and lifecycle\n\n- A per-agent semaphore (`maxConcurrentSessions`, default 4) bounds in-flight turns; excess requests queue.\n- If a subprocess exits unexpectedly, in-flight sessions get an `error` event and the next request lazily respawns.\n- `AcpManager.dispose()` cancels turns, closes sessions, and kills subprocesses.\n\n## Why build clean instead of forking a gateway\n\nExisting tools prove the pattern but each carries its own framework, scope, and license surface. Building directly on the official Apache-2.0 ACP SDK gives XENO a clean license story, native config conventions (`~/.xeno/`, OpenAI-compatible like `xeno-rt`), and a `core` that embeds cleanly into the rest of the ecosystem.\n\n## Next steps\n\n- [Agents and authentication](/docs/acp/agents-and-auth) — the agents the client drives\n- [Safety and sandboxing](/docs/acp/safety-and-sandboxing) — the containment model\n- [Embedding API](/docs/acp/embed-api) — the structured `AcpEvent` stream in detail"
        },
        {
          "slug": "agents-and-auth",
          "title": "Agents and authentication",
          "description": "The config-driven agents XENO ACP can drive, how each authenticates with your own logins, and the ToS scope.",
          "body": "# Agents and authentication\n\nXENO ACP drives ACP **agents** as data, not code. Each agent is a config entry describing how to launch it; adding a new ACP agent is a config change, not a code change.\n\n## Built-in agents\n\nBuilt-in definitions ship for six agents. Listing one in your config lets you override its `cmd`, `args`, `env`, or default model.\n\n| id | Launch (default) | ACP | Auth (your own) |\n|---|---|---|---|\n| `claude-code` | `npx -y @zed-industries/claude-code-acp` | adapter | Claude / Anthropic |\n| `codex` | `npx -y @zed-industries/codex-acp` | adapter | ChatGPT / OpenAI |\n| `opencode` | `opencode acp` | native | OpenCode |\n| `gemini` | `gemini --acp` | native | Google login / GEMINI_API_KEY |\n| `kimi` | `kimi acp` | native | Kimi / Moonshot |\n| `glm` | `npx -y glm-acp-agent@latest` | native | Z.AI Coding Plan |\n\n\"adapter\" agents are bridged to ACP by an official Zed adapter package; \"native\" agents speak ACP directly (for example via an `--acp` flag). Any other conforming ACP agent works by adding a config entry.\n\n## Authentication model\n\nXENO ACP does **not** run models and does **not** perform OAuth. Each agent authenticates with **your own** provider credentials through its own CLI — Claude, ChatGPT, Google, Kimi, Z.AI, and so on. The gateway routes to a legitimate first-party client; it does not pool or resell access.\n\nYou authenticate an agent's CLI once (for example `claude`, `codex`, `opencode`, `gemini`, `kimi`, or the GLM agent's own setup). If an agent still needs auth when the gateway tries to open a session, the process is marked **needs-auth** and surfaced in `/healthz` and as an `error` event.\n\n## Enabling an agent\n\nList the agents you want under `agents` in `xeno-acp.config.jsonc`. Omit an entry to disable that agent.\n\n```jsonc\n\"agents\": [\n  {\n    \"id\": \"claude-code\",\n    \"name\": \"Claude Code\",\n    \"cmd\": \"npx\",\n    \"args\": [\"-y\", \"@zed-industries/claude-code-acp\"],\n    \"defaultModel\": null\n  }\n]\n```\n\nOn Windows, native CLIs installed via npm are `.cmd` shims; the process resolver launches them through `cmd.exe /c` for you while keeping `shell:false` (see [Safety and sandboxing](/docs/acp/safety-and-sandboxing)).\n\n## Deployment scope and ToS posture (LOCKED)\n\nThe scope is **one owner, many internal consumers** — the XENO apps. This matters legally:\n\n- Each agent uses **your own** provider credentials. The gateway routes; it does not pool or resell.\n- The sanctioned path is the official ACP adapters and native `--acp` modes, driven by the legitimate first-party clients.\n- **Do not** re-expose this gateway to third parties on a single shared subscription — that crosses into reselling subscription access, which violates provider ToS. Multi-tenant use would require bring-your-own-credentials *per tenant* and a ToS review, and is explicitly out of scope for the alpha.\n\nPer-app API keys (see [HTTP gateway](/docs/acp/http-gateway)) exist for attribution, per-app agent allowlists, and limits — all consumers are the owner's apps.\n\n## Next steps\n\n- [Safety and sandboxing](/docs/acp/safety-and-sandboxing) — the containment guarantees and known gaps\n- [HTTP gateway](/docs/acp/http-gateway) — configuring per-app keys"
        },
        {
          "slug": "safety-and-sandboxing",
          "title": "Safety and sandboxing",
          "description": "How agent file, terminal, and process actions are contained — and the safety gaps the alpha is honest about.",
          "body": "# Safety and sandboxing\n\nAn ACP agent can read files, write files, and run terminals. XENO ACP contains that authority — and is explicit about the limits it does *not* yet enforce. Read this before pointing the gateway at a real agent.\n\n## Per-session sandbox workspace\n\nEvery session gets an **isolated workspace directory**: `workspaceRoot/<agentId>/<sessionId>/` (created on demand, removed on session close unless `keepWorkspaces` is set). The default `workspaceRoot` is `./.xeno-acp/workspaces`, which is gitignored.\n\n- `readTextFile` / `writeTextFile` resolve the requested path against the workspace root and **refuse** anything that escapes it — `..` traversal, absolute paths outside the root, or symlink hops.\n- Terminal commands run with `cwd` set to the workspace; output is buffered and exposed through the ACP terminal methods. A configurable timeout (`terminalTimeoutMs`, default 120000) kills long-runners.\n\nAuto-approval plus a writable workspace and a terminal is powerful, so the isolated per-session workspace is what contains the blast radius.\n\n## Permission policy\n\nAgents run **non-interactively**: an agent's `requestPermission` call is answered by the configured permission policy, per agent:\n\n- `auto` (default) — pick the first allow option, so the agent runs headless.\n- `deny` — pick the first reject option.\n- `prompt` — only meaningful for interactive embedders; the HTTP server treats `prompt` as `auto`.\n\n## Spawn safety — no shell\n\nAgent processes and terminal commands are spawned with **`shell:false`** — there is no `shell:true` anywhere in source. The process resolver handles this cross-platform:\n\n- **POSIX** — the command is spawned directly.\n- **Windows** — Node refuses to spawn a `.cmd`/`.bat` under `shell:false` (post-CVE-2024-27980), so the resolver looks the command up via PATH+PATHEXT and routes a `.cmd`/`.bat` target explicitly through `cmd.exe /c`. This keeps `shell:false`, passes args as argv (no concatenated shell string, no shell injection).\n\nThe `command` is treated as a single executable token — shell metacharacters in it are rejected — and `args` are config-driven and passed as argv, so no user prompt ever reaches a spawn argument.\n\n## Known safety gaps (honest scope)\n\nThe alpha is deliberately explicit about what it does **not** enforce:\n\n- **No network egress controls** for agent processes. A real agent can make network calls; the fixture used in tests does not.\n- **Bearer keys are plaintext** in local config — acceptable only for the single-owner / many-internal-apps scope, not for multi-tenant.\n- Terminal resource use is bounded only by a kill-timeout; a crash mid-turn can leak a workspace directory (under `workspaceRoot`, gitignored).\n\nThe agent-side endpoint's sandbox (see [Agent endpoint](/docs/acp/agent-endpoint)) is an in-process, cooperative sandbox for XENO-authored adapters — **not** a hostile-code OS sandbox. Process-level isolation of a separate provider CLI is research/design only in this alpha.\n\n## Testing is hermetic\n\nThe test and smoke suites drive a deterministic **fixture ACP agent** (`tests/fixtures/acp-fixture-agent.mjs`), never a real provider CLI, and never read your real config or credentials. The fixture is driven by the real `ClientSideConnection` exactly like a production agent but performs no model, network, filesystem, or terminal work of its own. `pnpm run scan` additionally runs a source risk scan that fails on active-risk or secret-risk findings.\n\n## Next steps\n\n- [Agents and authentication](/docs/acp/agents-and-auth) — the ToS scope this containment assumes\n- [Agent endpoint](/docs/acp/agent-endpoint) — the approval-gated provider sandbox"
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "http-gateway",
          "title": "HTTP gateway",
          "description": "The OpenAI-compatible endpoints, the xeno request extension, flattening rules, auth, and configuration.",
          "body": "# HTTP gateway\n\n`@xeno-acp/server` is an OpenAI-compatible HTTP shell over the core engine. Existing ecosystem clients and `LLMProvider` implementations work unchanged by pointing their `baseURL` at the gateway. Default listen address is `http://127.0.0.1:8787`.\n\n## Endpoints\n\n### GET /v1/models\n\nLists enabled agents as OpenAI model objects. `id` is the agent id; `owned_by` is `\"xeno-acp\"`. Provider-specific submodel choices are owned by the provider runtime discovery contract, not hardcoded by consumers.\n\n### POST /v1/chat/completions\n\nThe request honors this subset of the OpenAI schema:\n\n| field | meaning |\n|---|---|\n| `model` | agent id (`claude-code`), or `agent:submodel` (`gemini:gemini-3-pro-preview`) |\n| `messages[]` | standard chat messages, rendered into the ACP prompt |\n| `stream` | `true` → SSE deltas; `false` → single JSON completion |\n| `xeno` *(ext)* | optional XENO extension object (below) |\n\nThe response is a standard `chat.completion` (non-stream) or a `chat.completion.chunk` SSE stream ending in `data: [DONE]`. `finish_reason` is mapped from the ACP `StopReason`.\n\n### GET /healthz\n\nLiveness plus per-agent process status and a **non-secret** config summary (auth mode, host/port, engine defaults). It never exposes API keys, env values, or workspace paths.\n\n## Authentication\n\nRequests carry `Authorization: Bearer <key>`. Keys are configured per consuming app. A request is rejected `401` if the key is unknown and the gateway is not in `open` auth mode. `open` mode disables auth and is for local dev only.\n\n## The xeno request extension\n\nXENO-specific controls live under an additive `xeno` key so the rest of the request stays pure OpenAI:\n\n```jsonc\n\"xeno\": {\n  \"session\": \"app:thread-123\",  // reuse a stateful ACP session (default: ephemeral)\n  \"workspace\": \"/abs/path\",     // working dir for this turn (default: per-session sandbox)\n  \"verbose\": true,              // narrate tool_call/plan/thought into the text stream\n  \"mode\": \"code\"                // ACP session mode if the agent advertises modes\n}\n```\n\n> **Session persistence is in progress.** By default each request creates a fresh ACP session and the full conversation is rendered into the prompt (stateless, OpenAI-style). The `xeno.session` field is accepted, but persistent stateful-session reuse is on the v0.1.0 track and currently falls back to an ephemeral session. Treat stateful reuse as not-yet-guaranteed in this alpha.\n\n## Message to prompt mapping\n\nOpenAI `messages[]` are flattened into ACP prompt content for the turn: `system` messages become a leading instruction block, `user`/`assistant` turns are rendered in order, and the trailing `user` message is the active ask. Non-text content maps to ACP content blocks where the agent advertises support; otherwise it is described as text.\n\n## Flattening rules (ACP to OpenAI)\n\nThe agent streams ACP `session/update` notifications; the flattener projects them. `verbose:true` narrates the agent's work into the text stream:\n\n| ACP sessionUpdate | default (verbose:false) | verbose:true |\n|---|---|---|\n| `agent_message_chunk` | content delta (text) | content delta |\n| `agent_thought_chunk` | dropped | thought lines |\n| `tool_call` | dropped | tool title + status line |\n| `tool_call_update` | dropped | status line |\n| `plan` / `plan_update` | dropped | checklist lines |\n| `usage_update` | `usage` in final object | same |\n| others | dropped | dropped |\n\n`StopReason` to `finish_reason`: `end_turn`→`stop`, `max_tokens`→`length`, `max_turn_requests`→`stop`, `refusal`→`content_filter`, `cancelled`→`stop`.\n\n**Flattening never disables tools.** The agent still performs every file, terminal, and MCP action inside its workspace — flattening only chooses how much is narrated back.\n\n## Configuration\n\nString values in the config expand `${VAR}`.\n\n```jsonc\n{\n  \"server\": { \"host\": \"127.0.0.1\", \"port\": 8787, \"auth\": \"bearer\" },\n  \"workspaceRoot\": \"./.xeno-acp/workspaces\",\n  \"keepWorkspaces\": false,\n  \"defaults\": {\n    \"maxConcurrentSessions\": 4,\n    \"permission\": \"auto\",\n    \"terminalTimeoutMs\": 120000,\n    \"initTimeoutMs\": 60000\n  },\n  \"agents\": [ /* see Agents and authentication */ ],\n  \"keys\": [\n    { \"key\": \"dev-key\", \"app\": \"local-dev\", \"agents\": [\"*\"], \"maxConcurrentSessions\": 8 }\n  ]\n}\n```\n\nEach `keys` entry maps an API key to an app name (for attribution), an agent allowlist (`[\"*\"]` for all enabled agents, or a list of ids), and optional limits.\n\n## Versioning\n\nACP wire compatibility is governed by the `protocolVersion` exchanged at `initialize` (currently `1`), independent of this package's semver. The HTTP contract follows OpenAI's stable chat schema; XENO extensions live under the `xeno` key and are additive.\n\n## Next steps\n\n- [Embedding API](/docs/acp/embed-api) — the structured stream the flattener sits on top of\n- [Agents and authentication](/docs/acp/agents-and-auth) — the model ids this endpoint serves"
        },
        {
          "slug": "embed-api",
          "title": "Embedding API",
          "description": "Drive an ACP agent in-process with @xeno-acp/core and consume the full structured AcpEvent stream.",
          "body": "# Embedding API\n\n`@xeno-acp/core` is the framework-free engine. Embed it directly when you want **full-fidelity** structured events — tool calls, plans, thoughts, usage — rather than the flattened OpenAI text the [HTTP gateway](/docs/acp/http-gateway) produces. It has no HTTP and no OpenAI types.\n\n## Loading config and creating a manager\n\n```ts\nimport { AcpManager, loadConfig } from \"@xeno-acp/core\";\n\nconst acp = new AcpManager(loadConfig());\n```\n\n`loadConfig()` resolves and normalizes the same config the server uses (built-in agent defaults merged, `${ENV}` expanded, defaults applied). `AcpManager` takes the resolved config and an optional logger.\n\n## AcpManager methods\n\n- `prompt(agentId, opts): AsyncGenerator<AcpEvent>` — drive one turn against `agentId` (or `agentId:submodel`) and async-iterate structured events ending in `done` (or `error`).\n- `complete(agentId, opts): Promise<{ text, stopReason, usage? }>` — convenience wrapper that runs a turn and collects the full assistant text and stop reason.\n- `listAgents(): AgentDef[]` — the enabled agent definitions.\n- `getAgentDef(id)` / `hasAgent(id)` — look up a single agent.\n- `statuses(): AgentStatus[]` — a status snapshot for every agent (`idle` / `starting` / `ready` / `needs-auth` / `error` / `stopped`, plus active session count and pid).\n- `dispose(): Promise<void>` — cancel turns, close sessions, and kill subprocesses.\n\n## Prompt options\n\n```ts\ninterface PromptOptions {\n  messages: ChatMessage[];   // trailing user message is the active ask\n  session?: string;          // reuse a stateful ACP session (see note below)\n  workspace?: string;        // override the working dir (default: sandboxed session dir)\n  model?: string | null;     // submodel hint for agents that support it\n  mode?: string;             // ACP session mode if the agent advertises modes\n  verbose?: boolean;         // consumer's choice to surface tool/plan/thought events\n  signal?: AbortSignal;      // cancel the in-flight turn\n}\n```\n\nA `ChatMessage` has a `role` (`system` / `user` / `assistant` / `tool`), string `content`, and an optional `name`.\n\n> As with the HTTP side, per-call sessions are ephemeral by default and persistent `session` reuse is on the v0.1.0 track — see the note in the [HTTP gateway](/docs/acp/http-gateway).\n\n## The AcpEvent stream\n\nThe engine yields a discriminated union. Embedders consume it at full fidelity:\n\n```ts\ntype AcpEvent =\n  | { type: \"text\"; text: string }\n  | { type: \"thought\"; text: string }\n  | { type: \"tool_call\"; id: string; title: string; status: string; kind?: string }\n  | { type: \"tool_update\"; id: string; status?: string; title?: string }\n  | { type: \"plan\"; entries: Array<{ content: string; status: string; priority?: string }> }\n  | { type: \"usage\"; inputTokens?: number; outputTokens?: number; totalTokens?: number }\n  | { type: \"raw\"; update: unknown }\n  | { type: \"done\"; stopReason: StopReason }\n  | { type: \"error\"; message: string };\n```\n\nThe HTTP server runs exactly this stream through `flatten()`; the mapping is documented in the [gateway flattening rules](/docs/acp/http-gateway).\n\n## Example: full structured consumption\n\n```ts\nimport { AcpManager, loadConfig } from \"@xeno-acp/core\";\n\nconst acp = new AcpManager(loadConfig());\ntry {\n  for await (const ev of acp.prompt(\"claude-code\", {\n    messages: [{ role: \"user\", content: \"Refactor utils.ts and run the tests.\" }],\n  })) {\n    switch (ev.type) {\n      case \"text\": process.stdout.write(ev.text); break;\n      case \"tool_call\": console.log(\"[tool]\", ev.title, ev.status); break;\n      case \"plan\": console.log(\"[plan]\", ev.entries.map((e) => e.content)); break;\n      case \"usage\": console.log(\"[usage]\", ev.totalTokens); break;\n      case \"error\": console.error(\"[error]\", ev.message); break;\n    }\n  }\n} finally {\n  await acp.dispose();\n}\n```\n\n## Exports\n\nBeyond `AcpManager` and `loadConfig`, the barrel also exports the lower-level building blocks — `Session`, `AgentProcess`, `flatten`, `createWorkspace` / `containPath`, `resolveCommand`, the built-in agent registry (`BUILTIN_AGENTS`, `mergeAgentDef`), agent version-policy helpers, and the logger — for front-ends that need more than the manager.\n\n## Next steps\n\n- [HTTP gateway](/docs/acp/http-gateway) — the OpenAI shell over this engine\n- [Architecture](/docs/acp/architecture) — how a turn flows through the engine"
        },
        {
          "slug": "agent-endpoint",
          "title": "Agent endpoint",
          "description": "The XENO-owned ACP agent-side endpoint: the CLI, its provider adapters, the approval flow, and honest scope.",
          "body": "# Agent endpoint\n\nEverything above plays the **client** side of ACP. `@xeno-acp/agent` is the inverse: a XENO-owned ACP **agent-side** endpoint another client can spawn and drive over stdio. It runs from the built workspace as `node packages/agent/dist/cli.js` (bin name `xeno-acp-agent`).\n\n> **Scope.** This is an early, safety-first surface. Only a deterministic fixture and a small set of approval-gated **local** adapters are runnable; everything else is a blocked placeholder. It is **not** official-provider ACP certification and **not** production-ready.\n\n## Running the endpoint\n\n```bash\n# ACP v1 over stdio (deterministic fixture provider, zero I/O authority)\nnode packages/agent/dist/cli.js agent --provider fixture\n\n# human-readable readiness report\nnode packages/agent/dist/cli.js check\n\n# end-to-end endpoint smoke\npnpm run smoke:agent\n```\n\nIn `agent` mode, **stdout is JSON-RPC only** (ACP v1); all diagnostics go to stderr. Other commands (`check`, `doctor`, `--version`) print human-readable text.\n\n## CLI commands\n\n- `--version` — print the endpoint version.\n- `check` / `doctor` — readiness report: Node version, config source, per-provider status and runnability, and the process supervisor's enforcement classification. Add `--json` for machine output.\n- `support-bundle` — a redacted, metadata-only support bundle (no secret values, no env values, no logs) with an audit timeline.\n- `certify --provider <id>` — a **local preflight only**; it reports whether a provider is ready for a Hub certification handoff. It does not itself certify.\n- `process check` — print the minimal supervisor's platform and **honest** enforcement classification. It runs nothing.\n- `adapter --provider xeno-local` — run the XENO-owned adapter process directly (emits the v1 adapter event protocol, not ACP).\n- `providers <subcommand>` — manage providers (below).\n- `agent --provider <id>` — start the ACP agent-side endpoint for a runnable provider.\n\n## Providers subcommands\n\n```\nproviders list | check                 # providers + status + runnability\nproviders setup    --provider <id>     # read-only step-by-step setup guide\nproviders review   --provider <id>     # policy review for a provider\nproviders models   --provider <id>     # model/default discovery (JSON)\nproviders secrets  --provider <id>     # metadata-only secret-ref binding review\nproviders diagnose --provider <id>     # config + runnability + secrets + process\nproviders test     --provider <id>     # drive one turn against the provider\nproviders approve  --provider <id> (--yes|--dry-run|--interactive)\nproviders approvals                    # list stored approvals\nproviders revoke   --provider <id> --yes\n```\n\n## Provider runnability and the approval flow\n\nA provider only becomes runnable when its **definition + policy + a current manual approval** all line up. Approvals are pinned to a hash of the provider definition and policy; store entries hold hashes and metadata only — **never secret values**. Approving an unimplemented provider records the approval but keeps it **not runnable**. Missing or stale approvals fail closed.\n\nA typical local flow is `providers review → providers secrets → providers approve --yes → providers test → agent`. The `providers setup` command prints exactly these steps for a given provider.\n\n## Runnable providers today\n\n- **`fixture`** — a deterministic provider with zero model/network/fs/terminal authority. Always runnable; used for smoke and certification.\n- **`xeno-local`** — a XENO-owned, deterministic local adapter that proves the real pipeline end to end (definition → policy → approval → safety → sandbox → process supervisor → adapter protocol → ACP). No auth/network/fs-write/terminal authority; approval-gated.\n- **`claude-local`** — a XENO-authored adapter around your installed **Claude Code CLI** (`claude -p … --output-format stream-json`). `third-party`, approval-gated, relies on the Claude CLI's own local auth (loads/stores no secrets), grants no fs-write/terminal.\n- **`codex-local`** — a XENO-authored adapter around your installed **Codex CLI** (`codex exec --json`), run with Codex `--sandbox read-only` and no terminal authority. `third-party`, approval-gated, relies on the Codex CLI's own local auth.\n\nThe base `codex`, `claude`, `gemini`, `opencode`, `kimi`, `glm`, and `hermes` providers remain **blocked, disabled placeholders** on the agent side until their own scoped milestones land. (Note: several of these *are* drivable as ACP *client* agents — see [Agents and authentication](/docs/acp/agents-and-auth); the agent-side endpoint is a separate, narrower surface.)\n\n## Sandbox honesty\n\nThe adapter sandbox is an in-process, dependency-injected sandbox for cooperative, XENO-authored adapters — its network, secrets, filesystem, and terminal capabilities are deny-by-default, policy-gated, and audited. It is **not** a hostile-code OS sandbox. A minimal process supervisor enforces no-shell launch, a clean env allowlist, an absolute cwd, a hard timeout, and bounded+redacted output capture — but it does **not** enforce network or filesystem against a hostile process, and it surfaces exactly that in `process check`. Full OS-level process isolation is research/design only in this alpha.\n\n## Hub certification\n\nSpecific XENO-owned/authored endpoints carry their own distinct XENO Hub certification classifications — the **fixture** endpoint, the **`xeno-local`** adapter, and the XENO-authored `claude-local` / `codex-local` adapters. These are XENO-owned/authored classifications, **not** official-provider ACP certification and **not** a broad third-party certification claim.\n\n## License\n\nXENO ACP is licensed [Apache-2.0](https://github.com/XENO-CORPORATION/xeno-acp), built on the Apache-2.0 ACP SDK. Local provider adapters use the operator's own installed CLIs and credentials.\n\n## Next steps\n\n- [Safety and sandboxing](/docs/acp/safety-and-sandboxing) — the client-side containment model\n- [Introduction](/docs/acp/introduction) — where the agent endpoint sits in the whole product"
        }
      ]
    }
  ]
};

export default acp;
