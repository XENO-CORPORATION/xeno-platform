import type { ProductDocs } from './_types';

const rt: ProductDocs = {
  "slug": "rt",
  "productName": "XENO RT",
  "tagline": "The pure-Rust inference runtime — GGUF LLMs and image models behind one OpenAI-compatible API, CPU or CUDA, fully offline.",
  "seo": {
    "title": "XENO RT documentation",
    "description": "Build, run, and serve local models with XENO RT (xrt) — a from-scratch Rust inference runtime with GGUF support, an OpenAI-compatible server, an agent-adaptive KV cache, CUDA acceleration, and C/Python bindings."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO RT is, and where it fits in the XENO ecosystem.",
          "body": "# Introduction\n\n**XENO RT** (`xrt`) is a from-scratch inference runtime written in pure Rust. It loads GGUF language models with memory-mapped, zero-copy startup, runs them on optimized CPU kernels or a CUDA GPU backend, and serves them behind an **OpenAI-compatible HTTP API** — so anything that talks to OpenAI can talk to a fully local model instead.\n\nIt is positioned as a memory-safe alternative to llama.cpp, prioritizing clean architecture and first-class GGUF compatibility while delivering competitive speed through hand-tuned kernels.\n\n## What it does\n\n- **Runs GGUF models locally** — Llama and Qwen family architectures, loaded straight from a `.gguf` file or the HuggingFace Hub.\n- **Speaks OpenAI** — a drop-in `/v1/chat/completions`, `/v1/completions`, and `/v1/models` server with SSE streaming.\n- **CPU or CUDA** — optimized CPU kernels everywhere, with a feature-gated CUDA-resident backend for GPU acceleration.\n- **Agent-aware KV cache** — a paged key-value cache with an experimental *agent-adaptive* mode that keeps hot tokens in full precision and quantizes the rest.\n- **More than text** — an image-domain endpoint (background removal) and multimodal image inputs when a vision projector is loaded.\n- **Embeddable** — a C ABI and a Python module so other languages can drive the runtime in-process.\n\n## Where it fits\n\nXENO RT is **Layer 2 (Native Engine & Inference)** of the XENO ecosystem. It is the unified local-inference runtime that lets the whole platform work offline: the [XENO Agent SDK](/docs/agent-sdk) and [XENO Agent CLI](/docs/agent-cli) can route reasoning through `xrt` instead of the cloud, and creative apps use it for on-device AI. Because the API is OpenAI-compatible, switching an app between the XENO cloud and a local `xrt` server is a base-URL change.\n\n## Maturity\n\nXENO RT is **beta** (Rust, v0.2.0). The core inference path — GGUF loading, CPU kernels, sampling, the OpenAI server, HuggingFace downloads — is the most mature part and is covered by tests. GPU acceleration, the quantized/adaptive KV cache modes, multimodal, and the vision endpoint are newer; this documentation calls out where a feature is experimental or backend-specific. It is licensed under **Apache-2.0**.\n\n## Next steps\n\n- [Installation](/docs/rt/installation) — build `xrt` from source\n- [Quickstart](/docs/rt/quickstart) — download a model, generate text, start the server\n- [Architecture](/docs/rt/architecture) — the crate map and how a request flows"
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Build XENO RT from source with Cargo, on CPU or with CUDA.",
          "body": "# Installation\n\nXENO RT is distributed as source and built with Cargo. There is no prebuilt binary, npm, or Homebrew channel today — you compile the `xrt` CLI and server yourself.\n\n## Prerequisites\n\n- **Rust 1.76 or newer** — install via [rustup](https://rustup.rs/).\n- **CUDA Toolkit 12.x** — optional, only for the GPU backend.\n\n## Build (CPU only)\n\n```bash\ngit clone https://github.com/XENO-CORPORATION/xeno-rt.git\ncd xeno-rt\ncargo build --release\n```\n\nThis produces two binaries under `target/release/`:\n\n- `xrt-cli` — the command-line tool (`generate`, `chat`, `bench`, `download`).\n- `xrt-server` — the OpenAI-compatible HTTP server.\n\nThroughout the docs, commands are shown via `cargo run` for convenience; you can equally call the built binaries directly.\n\n## Build with CUDA\n\n```bash\ncargo build --release --features cuda\n```\n\nThe CUDA backend is isolated behind the `cuda` feature flag. Building without it (the default) always gives you a working CPU runtime — GPU support is strictly additive. See [Backends & GPU](/docs/rt/backends).\n\n## Run the tests\n\n```bash\ncargo test --workspace\n\n# include the ignored end-to-end smoke tests (full inference pipeline)\ncargo test --workspace -- --include-ignored\n```\n\n## Optional: the bindings\n\nThe [C API](/docs/rt/bindings) and [Python module](/docs/rt/bindings) are separate workspace crates (`xrt-capi`, `xrt-python`) built the same way. See that page for language-specific build steps.\n\nNext: [Quickstart →](/docs/rt/quickstart)"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Download a model, generate text, chat, and start the OpenAI-compatible server.",
          "body": "# Quickstart\n\nOnce [built](/docs/rt/installation), you can download a model and run inference in a couple of commands. All CLI examples use the `xrt-cli` crate.\n\n## 1. Download a model\n\n`xrt-cli download` pulls a GGUF file from the HuggingFace Hub into the local cache (`~/.cache/xrt/models/`):\n\n```bash\ncargo run --release -p xrt-cli -- download \\\n  --repo TheBloke/TinyLlama-1.1B-Chat-v1.0-GGUF \\\n  --file tinyllama-1.1b-chat-v1.0.Q4_0.gguf\n```\n\nThe command prints the local path of the downloaded (or already-cached) file. You can also select by quantization with `--quantization q4_0` instead of an exact `--file`. See [Models & quantization](/docs/rt/models).\n\n## 2. Generate text\n\n```bash\ncargo run --release -p xrt-cli -- generate \\\n  --model ./models/tinyllama-1.1b-chat-v1.0.Q4_0.gguf \\\n  --prompt \"Explain quantum computing in simple terms\" \\\n  --max-tokens 256 --temperature 0.7\n```\n\nOutput streams token-by-token, followed by a timing line (tokens, prefill latency, decode tok/s). Instead of a local `--model`, `generate` also accepts a HuggingFace model directly via `--hf-repo` / `--hf-file`, auto-downloading if needed.\n\n## 3. Interactive chat\n\n`chat` applies the model's chat template (falling back to ChatML) and keeps the conversation in memory:\n\n```bash\ncargo run --release -p xrt-cli -- chat \\\n  --model ./models/tinyllama-1.1b-chat-v1.0.Q4_0.gguf \\\n  --system \"You are a concise assistant.\"\n```\n\n## 4. Start the server\n\n```bash\ncargo run --release -p xrt-server -- \\\n  --model ./models/tinyllama-1.1b-chat-v1.0.Q4_0.gguf \\\n  --host 0.0.0.0 --port 3000\n```\n\nThen call it like OpenAI:\n\n```bash\ncurl http://localhost:3000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"tinyllama\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"Hello!\"}],\n    \"max_tokens\": 128,\n    \"stream\": true\n  }'\n```\n\n## Next steps\n\n- [The OpenAI-compatible server](/docs/rt/server-api) — every endpoint and field\n- [Models & quantization](/docs/rt/models) — what loads and what the formats mean\n- [Backends & GPU](/docs/rt/backends) — run on CUDA\n- [CLI reference](/docs/rt/cli-reference) — all commands and flags"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "architecture",
          "title": "Architecture",
          "description": "The crate map and how an inference request flows through the runtime.",
          "body": "# Architecture\n\nXENO RT is a Cargo workspace of focused crates. Each layer builds on the one below it, from raw tensor types up to the CLI and server.\n\n## The stack\n\n```\n  xrt-cli / xrt-server        CLI + OpenAI API\n  xrt-capi / xrt-python       C ABI + Python bindings\n  xrt-vision                  image-domain ONNX tasks\n──────────────────────────────────────────────────────\n  xrt-runtime                 sessions, sampling, KV cache, backends\n  xrt-models                  Llama forward pass (GQA, transformer)\n  xrt-hub                     HuggingFace download + cache\n──────────────────────────────────────────────────────\n  xrt-kernels    xrt-cuda     CPU kernels / GPU PTX kernels\n  xrt-gguf       xrt-tokenizer GGUF parser / BPE tokenizer\n  xrt-core                    DType, Device, TensorView, errors\n```\n\n## Crate overview\n\n| Crate | Responsibility |\n|---|---|\n| `xrt-core` | Core types: `DType`, `Device`, `TensorView`, error definitions |\n| `xrt-gguf` | GGUF binary parser with mmap, metadata extraction, tensor validation |\n| `xrt-tokenizer` | BPE tokenizer loaded from GGUF metadata; chat-template application |\n| `xrt-kernels` | CPU compute kernels — RMSNorm, RoPE, softmax, SiLU, tiled matmul, dequantization |\n| `xrt-cuda` | CUDA GPU backend — PTX kernels via cudarc (feature-gated) |\n| `xrt-models` | Model implementations — Llama-family forward pass with grouped-query attention |\n| `xrt-runtime` | The runtime — paged KV cache, sampling, sessions, CPU/CUDA backends |\n| `xrt-hub` | HuggingFace download, cache, verification, model aliases |\n| `xrt-vision` | Image-domain ONNX task inference (background removal) |\n| `xrt-cli` | Command-line interface |\n| `xrt-server` | OpenAI-compatible HTTP server |\n| `xrt-capi` | C FFI bindings |\n| `xrt-python` | Python bindings (PyO3) |\n\n## How a generation request flows\n\n1. **Load** — `Runtime::load` (or `load_with_backend`) mmaps the GGUF, parses metadata and tensors, builds the tokenizer, and selects a backend.\n2. **Session** — `runtime.new_session()` creates a `Session` holding its own KV cache and sampler state. Sessions are cheap and independent.\n3. **Encode** — the prompt is tokenized (BPE), optionally via the model's chat template.\n4. **Prefill + decode** — the backend runs the transformer forward pass, writing keys/values into the [paged KV cache](/docs/rt/kv-cache); the [sampler](/docs/rt/configuration) draws each next token (temperature, top-k, top-p, repetition penalty).\n5. **Detokenize + stream** — pieces are decoded back to text and streamed to the caller.\n\n## Design invariants\n\nThe runtime holds a few rules sacred across the ecosystem:\n\n- **OpenAI API compatibility** — `/v1/chat/completions` and `/v1/models` match the OpenAI shape so callers can swap between cloud and local without code changes.\n- **CPU fallback always works** — CUDA is optional; the runtime must function on any x86-64 / aarch64 machine.\n- **GGUF is the model format** — how users get models, first-class and never removed.\n\nRelated: [Backends & GPU](/docs/rt/backends) · [Models & quantization](/docs/rt/models)."
        },
        {
          "slug": "models",
          "title": "Models & quantization",
          "description": "Supported architectures, GGUF quantization formats, and the HuggingFace model hub.",
          "body": "# Models & quantization\n\nXENO RT loads **GGUF** models — the quantized single-file format popularized by llama.cpp — with memory-mapped, zero-copy startup.\n\n## Supported architectures\n\n| Architecture | Examples |\n|---|---|\n| Llama | Llama 2, Llama 3, CodeLlama, TinyLlama |\n| Qwen | Qwen 1.5, Qwen 2 |\n\nThese share the Llama-family transformer with grouped-query attention (GQA), which the runtime implements directly. Architectures outside this family are not supported.\n\nA note on other families: the runtime has no model-family-specific code beyond the Llama-family transformer above, so a GGUF built on that same architecture — Mistral 7B is the common example — will generally load, but it is not something we test or claim. Mixture-of-experts models such as Mixtral are **not** supported: the expert-routing path is not implemented.\n\n## Supported quantizations\n\nGGUF tensors can be stored at several precisions. `xrt` dequantizes each with ggml-compatible math:\n\n| Format | Block size | Notes |\n|---|---|---|\n| F32 | — | Reference precision |\n| F16 | — | Half precision |\n| BF16 | — | Brain float |\n| Q8_0 | 32 | 8-bit symmetric |\n| Q4_0 | 32 | 4-bit symmetric |\n| Q4_K | 256 | 4-bit K-quant, 6-bit scales |\n| Q5_K | 256 | 5-bit K-quant, 6-bit scales |\n| Q6_K | 256 | 6-bit K-quant, 8-bit scales |\n\nLower-bit formats (Q4_K, Q4_0) trade a little quality for much smaller files and faster memory-bound decode; F16/Q8_0 keep more fidelity. For most local use, a `Q4_K_M` or `Q6_K` build is a good balance.\n\n## Getting models from HuggingFace\n\n`xrt-hub` downloads and caches GGUF files. Cache location: `~/.cache/xrt/models/` (resolved from `HOME` / `USERPROFILE`).\n\n```bash\n# by exact filename\ncargo run --release -p xrt-cli -- download \\\n  --repo Qwen/Qwen2-0.5B-Instruct-GGUF --file qwen2-0_5b-instruct-q4_k_m.gguf\n\n# or let the hub pick a file matching a quantization\ncargo run --release -p xrt-cli -- download \\\n  --repo Qwen/Qwen2-0.5B-Instruct-GGUF --quantization q4_k_m\n```\n\nFor **gated or private repos**, set a HuggingFace token in the environment — `HF_TOKEN` (or `HUGGING_FACE_HUB_TOKEN`) is picked up automatically.\n\n## Local files and aliases\n\nAny command that takes `--model` accepts a direct path to a `.gguf` file. It also resolves a small set of built-in **aliases** to files under a local model root (configurable via `XRT_LOCAL_MODEL_ROOT` / `XENO_AI_MODEL_ROOT`), so a short name can stand in for a full path on a machine that keeps a shared model library.\n\nRelated: [KV cache & policies](/docs/rt/kv-cache) · [CLI reference](/docs/rt/cli-reference)."
        },
        {
          "slug": "kv-cache",
          "title": "KV cache & policies",
          "description": "The paged KV cache, its precision modes, and the agent-adaptive policy system.",
          "body": "# KV cache & policies\n\nEvery generated token attends to all previous tokens, so the runtime keeps their keys and values in a **KV cache**. XENO RT uses a **paged** cache (block-level allocation) from day one, and adds precision modes and policies tuned for long agentic sessions.\n\n## Cache precision modes\n\nSet the KV cache mode with the `XRT_KV_CACHE_MODE` environment variable (or per-session in the library). Accepted values:\n\n| Mode | Meaning |\n|---|---|\n| `f32` | Full-precision keys and values (default) |\n| `q8` | 8-bit quantized KV — roughly half the memory |\n| `kq4_vq8` | 4-bit keys, 8-bit values — smallest footprint |\n| `agent_adaptive` | Hybrid: hot tokens in full precision, the rest quantized |\n\n> **Backend caveat.** The quantized and adaptive modes (`q8`, `kq4_vq8`, `agent_adaptive`) are implemented on the **CUDA-resident backend**. On the CPU backend they transparently fall back to `f32`. Use them with a CUDA build (see [Backends & GPU](/docs/rt/backends)).\n\n## Agent-adaptive caching\n\nThe headline mode, `agent_adaptive`, exists because agent transcripts are long and repetitive: a big system prompt, tool schemas, and tool results that must stay intact, plus a rolling window of recent tokens that matters most. Adaptive caching keeps a **hot set** (recent window + pinned spans) in full precision and stores everything else in the compact 4-bit-key / 8-bit-value form, migrating tokens between the two as the window slides.\n\nWhich tokens count as hot is driven by a **cache policy**.\n\n## Cache policies\n\nA policy bundles the recent-window size and which prompt regions to pin. Select one with the `cache_policy` field on a request (server or CLI):\n\n| Policy | Recent window | Pins |\n|---|---|---|\n| `default_chat` | 256 | none (uniform cache) |\n| `agent_adaptive` | 256 | system prompt, tool schema, tool results |\n| `long_context` | 384 | system prompt, tool schema, tool results |\n| `memory_saver` | 192 | system prompt, tool results |\n\nThe `recent_window_tokens` request field overrides the window size. Policies classify the prompt into **spans** (system, developer, tool-schema, tool-result, user, assistant, retrieval, scratch); the server derives these spans automatically from the chat messages it renders.\n\n```bash\n# CLI: run generation under a policy\ncargo run --release -p xrt-cli -- generate \\\n  --model model.gguf --prompt \"...\" \\\n  --cache-policy agent_adaptive --recent-window-tokens 256\n```\n\n## Benchmarking cache modes\n\nThe `bench` subcommand sweeps cache modes head-to-head and reports prefill latency and tok/s per mode — the fastest way to see the memory-vs-speed trade-off on your hardware. See [CLI reference](/docs/rt/cli-reference)."
        },
        {
          "slug": "backends",
          "title": "Backends & GPU",
          "description": "CPU and CUDA-resident backends, backend selection, and GPU memory budgeting.",
          "body": "# Backends & GPU\n\nXENO RT runs the same models on either a CPU backend or a CUDA GPU backend. Selection is explicit and always falls back safely.\n\n## Backend selection\n\nChoose a backend with the `--backend` flag or the `XRT_BACKEND` environment variable. Accepted values:\n\n| Value | Behaviour |\n|---|---|\n| `auto` | Resolves to CPU today (default) |\n| `cpu` | CPU kernels — always available |\n| `cuda-resident` (`cuda`, `gpu`) | Weights resident on the GPU, GPU decode |\n| `external-openai` | Reserved — **not implemented**; selecting it returns an error |\n\n```bash\ncargo run --release -p xrt-server --features cuda -- \\\n  --model model.gguf --backend cuda-resident\n```\n\nThe server's `/v1/runtime/status` reports both the *requested* and the *active* backend, so you can confirm what actually loaded.\n\n## The CPU backend\n\nThe CPU path is the always-on default and needs no special build. It uses hand-tuned, auto-vectorization-friendly kernels:\n\n- **Tiled matmul** with a wide inner loop and **rayon** row-parallel execution.\n- Chunked, SIMD-friendly RMSNorm and softmax.\n- f32 accumulation for numerically stable quantized ops.\n- mmap zero-copy weight loading — no eager copies at startup.\n\n## The CUDA-resident backend\n\nBuilt with `--features cuda` and requiring CUDA Toolkit 12.x, this backend uploads model weights into VRAM and runs the forward pass with PTX kernels (matmul, RMSNorm, RoPE, softmax, SiLU, residual add, embedding lookup) via cudarc. It supports dense F32/F16/BF16/Q8_0/Q4_0/Q4_K/Q5_K/Q6_K models and is what unlocks the quantized and adaptive [KV cache modes](/docs/rt/kv-cache).\n\n## GPU memory budgeting\n\nBefore uploading, the backend preflights VRAM and refuses to load a model that would exceed a safe budget, so it fails fast with a clear message instead of an out-of-memory crash mid-run. The budget is tunable via environment variables:\n\n| Variable | Default | Meaning |\n|---|---|---|\n| `XRT_CUDA_DEVICE` | `0` | GPU device ordinal |\n| `XRT_GPU_MEMORY_FRACTION` | `0.90` | Fraction of VRAM the runtime may use |\n| `XRT_GPU_RESERVED_MB` | `1024` | Headroom reserved and never allocated |\n| `XRT_GPU_KV_FRACTION` | `0.30` | Share of the budget earmarked for the KV cache |\n\n`/v1/runtime/status` returns a full GPU resource report — device name, free/total VRAM, model-weight bytes, KV budget, and current allocation — useful for capacity planning.\n\n## Not yet on the GPU\n\nFlash Attention, a continuous-batching scheduler, and a Metal (Apple Silicon) backend are on the roadmap but not shipping in this beta. Broader (non-dense) GGUF layouts on CUDA are also not wired yet.\n\nRelated: [Configuration & environment](/docs/rt/configuration)."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "server-api",
          "title": "The OpenAI-compatible server",
          "description": "Every endpoint of xrt-server — chat, completions, streaming, models, and runtime control.",
          "body": "# The OpenAI-compatible server\n\n`xrt-server` exposes the runtime over HTTP with an OpenAI-compatible surface plus a few runtime-control endpoints. It binds to `127.0.0.1:3000` by default.\n\n```bash\ncargo run --release -p xrt-server -- \\\n  --model model.gguf --host 0.0.0.0 --port 3000\n```\n\nYou can start the server **without** a model and load one later over HTTP (see `/v1/runtime/load`).\n\n## Endpoints\n\n| Method | Path | Purpose |\n|---|---|---|\n| GET | `/v1/models` | List the loaded model (OpenAI shape) |\n| POST | `/v1/completions` | Text completion |\n| POST | `/v1/chat/completions` | Chat completion |\n| GET | `/v1/runtime/status` | Runtime + backend + GPU status |\n| POST | `/v1/runtime/load` | Load / swap a model at runtime |\n| POST | `/v1/runtime/unload` | Unload the current model |\n| POST | `/v1/images/remove-background` | Image background removal (see [Vision](/docs/rt/vision)) |\n\n## Chat completions\n\n```bash\ncurl http://localhost:3000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"local\",\n    \"messages\": [\n      {\"role\": \"system\", \"content\": \"You are helpful.\"},\n      {\"role\": \"user\", \"content\": \"Write a haiku about Rust.\"}\n    ],\n    \"max_tokens\": 128,\n    \"temperature\": 0.8\n  }'\n```\n\nThe response is a standard `chat.completion` object with `choices[].message`, `finish_reason`, and a `usage` block (prompt / completion / total tokens). The server applies the model's chat template automatically.\n\n### Request fields\n\nStandard OpenAI fields are supported: `model`, `messages`, `max_tokens`, `temperature`, `top_p`, `stream`, `seed`, `tools`, `tool_choice`. XENO RT also accepts extensions: `top_k`, `repetition_penalty`, and the KV-cache controls `cache_policy` and `recent_window_tokens` (see [KV cache & policies](/docs/rt/kv-cache)).\n\n## Streaming\n\nSet `\"stream\": true` and the endpoint returns Server-Sent Events: a sequence of `chat.completion.chunk` objects with incremental `delta.content`, terminated by a final chunk carrying `finish_reason` and a `data: [DONE]` sentinel — exactly as OpenAI clients expect. `/v1/completions` streams `text_completion.chunk` events the same way.\n\n## Function calling\n\nPass OpenAI-style `tools`. The server injects the tool schemas into the prompt and, if the model replies with a tool call, parses it out and returns it as `message.tool_calls` with `finish_reason: \"tool_calls\"`. This is a **best-effort, prompt-based** implementation (not grammar-constrained decoding), so reliability depends on the model; small models may not always emit clean tool JSON. When tools are present and no temperature is given, the server lowers temperature to make structured output more likely.\n\n## Runtime control\n\n`/v1/runtime/load` swaps the active model without restarting the process — accepting either a `model_path` or an `hf_repo` + `hf_file` pair (and an optional `mmproj_path` for a [vision projector](/docs/rt/vision)):\n\n```bash\ncurl http://localhost:3000/v1/runtime/load \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"hf_repo\":\"Qwen/Qwen2-0.5B-Instruct-GGUF\",\"hf_file\":\"qwen2-0_5b-instruct-q4_k_m.gguf\"}'\n```\n\n`/v1/runtime/status` reports readiness, the loaded model, the KV cache mode, requested vs active backend, and the GPU resource status. `/v1/runtime/unload` frees the model.\n\n> There is no built-in authentication — run `xrt-server` on a trusted network or behind your own proxy."
        },
        {
          "slug": "vision",
          "title": "Vision & multimodal",
          "description": "Image inputs in chat and the background-removal endpoint.",
          "body": "# Vision & multimodal\n\nXENO RT has two image capabilities: **multimodal chat** (images as chat content) and an **image-domain task endpoint** (background removal). Both are newer than the text path — treat them as beta.\n\n## Multimodal chat inputs\n\nWhen the server is started with a **vision projector** (`--mmproj <projection.gguf>` alongside a compatible model), chat messages may include image parts using OpenAI's multipart content shape:\n\n```json\n{\n  \"model\": \"local\",\n  \"messages\": [{\n    \"role\": \"user\",\n    \"content\": [\n      {\"type\": \"text\", \"text\": \"What is in this image?\"},\n      {\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/png;base64,...\"}}\n    ]\n  }]\n}\n```\n\nImage sources may be `http(s)://`, `file://`, or `data:` URLs. The server decodes, resizes, center-crops, and normalizes each image, then expands the model's image-patch placeholders in the prompt automatically.\n\nIf a request contains an image but no vision model is loaded, the server returns a clear `400`. **Audio and video parts are not supported** and are rejected explicitly.\n\n## Background removal\n\n`POST /v1/images/remove-background` runs a BiRefNet ONNX matte model and returns the image with its background cut to transparency. The heavy inference runs off the async executor, so it does not stall the server.\n\n```bash\ncurl http://localhost:3000/v1/images/remove-background \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"image_b64\":\"<base64 png/jpeg/webp>\", \"use_gpu\": true}'\n```\n\nProvide the source as either `image_b64` (base64 bytes) or `image_url` (`http(s)`, `file://`, or `data:`). Optional fields: `model_path` (override the ONNX file) and `use_gpu` (default `true`, tries CUDA first when the build supports it). The response returns the cutout as base64 PNG plus its `width` and `height`.\n\n> **Model must be present.** The BiRefNet ONNX file is **not auto-downloaded**. It defaults to `~/.xeno/models/birefnet-general/model.onnx`; if it is missing, the endpoint returns a precondition error telling you to provide it. Point `model_path` at any local ONNX file to override the location.\n\n## What's implemented\n\nBackground removal is the one image task shipping today. Other image-domain tasks (upscale, denoise, face restoration, colorize, inpaint) are planned but not yet available."
        },
        {
          "slug": "cli-reference",
          "title": "CLI reference",
          "description": "The xrt-cli commands — generate, chat, bench, download — and their flags.",
          "body": "# CLI reference\n\n`xrt-cli` has four subcommands. `generate`, `chat`, and `bench` take either a local `--model <path.gguf>` or a HuggingFace `--hf-repo` + `--hf-file` pair (auto-downloading on first use). `download` uses `--repo` with `--file` or `--quantization`.\n\n## generate\n\nOne-shot text generation; streams output and prints timing.\n\n```bash\ncargo run --release -p xrt-cli -- generate \\\n  --model model.gguf --prompt \"...\" \\\n  --max-tokens 128 --temperature 0.8 --top-k 40 --top-p 0.95 \\\n  --repetition-penalty 1.1 --seed 42 \\\n  --cache-policy default_chat --backend auto\n```\n\n| Flag | Default | Meaning |\n|---|---|---|\n| `--prompt` | required | The prompt text |\n| `--max-tokens` | 128 | Max tokens to generate |\n| `--temperature` | 0.8 | Sampling temperature (0 = greedy) |\n| `--top-k` | 40 | Keep top-K candidates |\n| `--top-p` | 0.95 | Nucleus sampling threshold |\n| `--repetition-penalty` | 1.1 | Penalize repeats |\n| `--seed` | — | Deterministic sampling seed |\n| `--cache-policy` | — | KV cache policy (see [KV cache](/docs/rt/kv-cache)) |\n| `--recent-window-tokens` | — | Override the policy window |\n| `--backend` | `auto` | `auto` / `cpu` / `cuda-resident` |\n\n## chat\n\nInteractive REPL that applies the model's chat template (ChatML fallback) and keeps the running conversation.\n\n```bash\ncargo run --release -p xrt-cli -- chat --model model.gguf --system \"Be concise.\"\n```\n\nSame sampling and backend flags as `generate`, plus `--system` for the system message. Default `--max-tokens` here is 512.\n\n## bench\n\nSweeps one or more KV cache modes (and optionally backends) over a single prompt and reports prefill latency, total time, and tok/s per combination — the tool for measuring the memory-vs-speed trade-offs.\n\n```bash\ncargo run --release -p xrt-cli -- bench \\\n  --model model.gguf --prompt \"...\" \\\n  --cache-modes f32,q8,agent_adaptive --json\n```\n\n`--cache-modes` takes a comma-separated list (default `f32,q8,agent_adaptive`); `--backends` sweeps backends (default `auto`); `--json` emits a structured report including environment and per-run GPU status. Its default `--temperature` is 0.2. Remember that quantized/adaptive modes only diverge from `f32` on a CUDA build.\n\n## download\n\nFetch a GGUF from HuggingFace into `~/.cache/xrt/models/` and print its path.\n\n```bash\ncargo run --release -p xrt-cli -- download --repo <repo> --file <file.gguf>\n# or select by quantization\ncargo run --release -p xrt-cli -- download --repo <repo> --quantization q4_k_m\n```\n\nProvide exactly one of `--file` or `--quantization`. Set `HF_TOKEN` for gated repos."
        },
        {
          "slug": "bindings",
          "title": "C and Python bindings",
          "description": "Embed the runtime in-process from C or Python.",
          "body": "# C and Python bindings\n\nBeyond the CLI and server, XENO RT can be driven **in-process** from other languages. Two workspace crates provide this.\n\n## Python (xrt-python)\n\nBuilt with PyO3, this crate compiles to a Python module named `xrt` (e.g. via `maturin` against the `xrt-python` crate). The API mirrors the Rust runtime:\n\n```python\nimport xrt\n\nrt = xrt.Runtime(\"model.gguf\")\nprint(rt.model_name, rt.vocab_size)\n\nsess = rt.session()\nout = sess.generate(\"Hello!\", max_tokens=128, temperature=0.8,\n                    top_k=40, top_p=0.95, repetition_penalty=1.1)\nprint(out)\n\n# piece-wise streaming\nfor piece in sess.stream(\"Tell me a joke\", max_tokens=64):\n    print(piece, end=\"\", flush=True)\n\n# chat templating\nmsgs = [\n    {\"role\": \"system\", \"content\": \"You are helpful.\"},\n    {\"role\": \"user\", \"content\": \"Hi!\"},\n]\nprompt = rt.format_chat(msgs)\nprint(sess.generate(prompt, max_tokens=256))\n```\n\n`Runtime` exposes `model_name` / `vocab_size` (properties) and `encode` / `decode` / `format_chat`. A `Session` owns its KV cache; call `sess.reset()` to clear it.\n\n## C (xrt-capi)\n\nA minimal C ABI (built as `cdylib` + `staticlib`, with a header at `crates/xrt-capi/include/xrt.h`). Functions return `0` on success or `-1` on error; call `xrt_last_error()` for the message.\n\n```c\nXrtRuntime *rt = NULL;\nif (xrt_runtime_load(\"model.gguf\", &rt) != 0) {\n    fprintf(stderr, \"error: %s\\n\", xrt_last_error());\n    return 1;\n}\nXrtSession *sess = NULL;\nxrt_session_new(rt, &sess);\n\nchar *output = NULL;\nxrt_generate(sess, \"Hello!\", 128, 0.8f, &output);\nprintf(\"%s\\n\", output);\nxrt_string_free(output);\n\nxrt_session_free(sess);\nxrt_runtime_free(rt);\n```\n\nThe surface covers loading a runtime, creating/resetting/freeing sessions, blocking `xrt_generate`, and a callback-based `xrt_generate_stream`. Always free returned strings with `xrt_string_free`.\n\n> The bindings currently expose the core text-generation path (load, session, generate, stream). Advanced controls — cache policies, backend selection, multimodal — are available through the [server API](/docs/rt/server-api) rather than the bindings today."
        },
        {
          "slug": "configuration",
          "title": "Configuration & environment",
          "description": "Sampling parameters, environment variables, and logging.",
          "body": "# Configuration & environment\n\n## Sampling parameters\n\nThese apply across the CLI, the server request body, and the bindings. Runtime defaults:\n\n| Parameter | CLI flag | API field | Default |\n|---|---|---|---|\n| Temperature | `--temperature` | `temperature` | 0.8 |\n| Top-K | `--top-k` | `top_k` | 40 |\n| Top-P | `--top-p` | `top_p` | 0.95 |\n| Repetition penalty | `--repetition-penalty` | `repetition_penalty` | 1.1 |\n| Max tokens | `--max-tokens` | `max_tokens` | 128 |\n| Seed | `--seed` | `seed` | none (random) |\n\nTemperature `0` is greedy decoding. `top_k` and `repetition_penalty` are XENO RT extensions beyond the OpenAI schema. (Some CLI subcommands override the max-tokens default — `chat` uses 512 — and `bench` uses temperature 0.2; see [CLI reference](/docs/rt/cli-reference).)\n\n## Environment variables\n\nThe variables the runtime actually reads:\n\n### Backend & inference\n| Variable | Default | Purpose |\n|---|---|---|\n| `XRT_BACKEND` | `auto` | Backend: `auto` / `cpu` / `cuda-resident` |\n| `XRT_KV_CACHE_MODE` | `f32` | KV cache mode: `f32` / `q8` / `kq4_vq8` / `agent_adaptive` |\n\n### GPU budgeting (CUDA build)\n| Variable | Default | Purpose |\n|---|---|---|\n| `XRT_CUDA_DEVICE` | `0` | GPU device ordinal |\n| `XRT_GPU_MEMORY_FRACTION` | `0.90` | Max fraction of VRAM to use |\n| `XRT_GPU_RESERVED_MB` | `1024` | Reserved VRAM headroom (MB) |\n| `XRT_GPU_KV_FRACTION` | `0.30` | Budget share for the KV cache |\n| `XRT_CUDA_PROFILE` | off | Emit per-stage CUDA timing logs |\n\n### Models & downloads\n| Variable | Purpose |\n|---|---|\n| `HF_TOKEN` / `HUGGING_FACE_HUB_TOKEN` | Auth for gated/private HuggingFace repos |\n| `XRT_LOCAL_MODEL_ROOT` / `XENO_AI_MODEL_ROOT` | Root directory for resolving local model aliases |\n| `XRT_HEAP_WEIGHTS` | Set to `1` to copy weights to heap memory (Windows streaming-bandwidth experiment; off by default) |\n\nThe model download cache lives at `~/.cache/xrt/models/` (derived from `HOME` / `USERPROFILE`).\n\n## Logging\n\nXENO RT logs through the standard `tracing` stack. Control verbosity with the conventional `RUST_LOG` filter:\n\n```bash\nRUST_LOG=info cargo run --release -p xrt-server -- --model model.gguf\nRUST_LOG=debug ...   # more detail\n```\n\nRelated: [Backends & GPU](/docs/rt/backends) · [KV cache & policies](/docs/rt/kv-cache)."
        }
      ]
    },
    {
      "title": "Help",
      "pages": [
        {
          "slug": "troubleshooting",
          "title": "Troubleshooting & FAQ",
          "description": "Answers to common questions and failure modes.",
          "body": "# Troubleshooting & FAQ\n\n**The server returns 503 \"no model is loaded.\"** You started `xrt-server` without `--model`. Either pass one at launch, or load one at runtime via `POST /v1/runtime/load` (see [Server API](/docs/rt/server-api)).\n\n**My quantized KV cache mode seems to make no difference.** The `q8`, `kq4_vq8`, and `agent_adaptive` modes are implemented on the **CUDA-resident backend**; on CPU they fall back to `f32`. Build with `--features cuda` and select `--backend cuda-resident`. See [KV cache & policies](/docs/rt/kv-cache).\n\n**Selecting the `cuda-resident` backend fails to load the model.** The GPU budget preflight refuses uploads that would exceed safe VRAM. Check `/v1/runtime/status` for the GPU report, and relax `XRT_GPU_MEMORY_FRACTION` / `XRT_GPU_RESERVED_MB`, use a smaller quantization, or pick another `XRT_CUDA_DEVICE`. See [Backends & GPU](/docs/rt/backends).\n\n**`external-openai` backend errors out.** That backend value is reserved but not implemented. Use `auto`, `cpu`, or `cuda-resident`.\n\n**The background-removal endpoint returns a precondition error.** The BiRefNet ONNX model is not bundled or auto-downloaded. Place it at `~/.xeno/models/birefnet-general/model.onnx` or pass `model_path`. See [Vision & multimodal](/docs/rt/vision).\n\n**Image inputs to chat are rejected.** Multimodal chat needs a vision projector loaded via `--mmproj`. Audio and video content parts are not supported at all.\n\n**A HuggingFace download fails with an auth error.** The repo is gated or private — set `HF_TOKEN`.\n\n**Tool calls come back as plain text.** Function calling is best-effort and prompt-based, not grammar-constrained; smaller models may not emit clean tool JSON. Try a more capable model or lower the temperature.\n\n**My model won't load.** XENO RT supports the **Llama / Qwen** family in GGUF with the listed quantizations. Other architectures or formats (e.g. safetensors) are not supported in this beta. See [Models & quantization](/docs/rt/models).\n\n**How do I make output deterministic?** Set a fixed `seed` and use `temperature 0` (greedy).\n\n## Not yet available\n\nThe roadmap includes Flash Attention, continuous batching, a Metal (Apple Silicon) backend, speculative decoding, LoRA adapters, safetensors import, distributed inference, and Docker images — none are in this beta. XENO RT is v0.2.0 and licensed under Apache-2.0."
        }
      ]
    }
  ]
};

export default rt;
