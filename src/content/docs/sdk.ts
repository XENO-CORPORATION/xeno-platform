import type { ProductDocs } from './_types';

const sdk: ProductDocs = {
  "slug": "sdk",
  "productName": "XENO Agent SDK",
  "tagline": "The embeddable agent runtime behind every XENO app — agent loop, tools, permissions, sessions, memory, and providers in one library.",
  "version": "0.9.24",
  "updated": "2026-08-30",
  "seo": {
    "title": "XENO Agent SDK documentation",
    "description": "Embed the XENO agent runtime in any app: the agent loop, tool registry, permission engine, sessions, memory, soul, providers, MCP, Electron bridge, and a framework-free UI — all app-agnostic and additive-only."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What the XENO Agent SDK is, what it ships, and where it fits in the XENO ecosystem.",
          "body": "# Introduction\n\n**XENO Agent SDK** (`@xenosystem/agent-sdk`) is the embeddable agent runtime that powers AI features across the XENO platform. It is built from the same engine that drives XENO Agent CLI, exposed as a programmatic library so any app — a terminal tool, an Electron main process, a renderer sidebar, or a Web Worker — can run a real agent loop with tools, permissions, sessions, memory, and auditing.\n\nIt is not a standalone application. It is a library embedded in 5+ consumer apps (Hub, Pixel, Motion, Sound, Agent CLI), and every public export is treated as a contract — see [Versioning & API stability](/docs/sdk/versioning-stability).\n\n## What it gives you\n\n- **A real agent loop** — `createXenoAgent()` returns a configured `AgentLoop` that reads context, calls tools, observes results, and iterates. Drive it with a blocking `run()` or an `AsyncIterable` `runStream()`.\n- **A tool registry** — register your app's operations as tools; the SDK ships a large built-in tool set (file I/O, search, shell, web, image, LSP, git, and more) and supports namespacing so `pixel.*` and `motion.*` never collide.\n- **A permission engine** — modes (`default`, `acceptEdits`, `bypassPermissions`, `plan`, `auto`), directory sandboxing, and an approval callback so the host owns the UX.\n- **Durable sessions** — transcripts, checkpoints, locks, turn-restore for crash recovery, and a versioned session format.\n- **Memory & identity** — a hierarchical memory manager and a layered identity/persona system, both token-budgeted for the context window.\n- **A Soul subsystem** — episodic memory plus self-taught skills with optional Ed25519-signed provenance.\n- **Pluggable providers** — an `LLMProvider` contract and a dual local/cloud provider, all OpenAI-compatible.\n- **Embedding surfaces** — an Electron bridge, a framework-free UI renderer, an MCP client and server, and cross-app orchestration primitives.\n\n## App-agnostic by design\n\nThe core loop, tools, sessions, permissions, memory, and providers run identically in Node, Electron main, Electron renderer, and Web Workers. Platform-specific surfaces are isolated: the UI renderer ships only under the [`/ui` subpath](/docs/sdk/package-exports) and is the only surface that touches the DOM, so importing the core never pulls it in. It has no framework peer — see [Electron &amp; UI](/docs/sdk/electron-ui). The Electron bridge is re-exported from the root barrel for convenience but only lazy-loads the `electron` package on demand, so importing it outside an Electron process is safe.\n\n## Where it fits\n\nThe SDK is **Layer 3 (Agent & Automation)** of the XENO ecosystem. It routes model calls through the XENO API (`api.xenostudio.ai`) or a local runtime (xeno-rt / Ollama), and it is embedded by the Layer 5 creative apps to put an agent sidebar in every product.\n\n## Next steps\n\n- [Installation](/docs/sdk/installation)\n- [Quickstart](/docs/sdk/quickstart) — create an agent and run your first turn\n- [The agent loop](/docs/sdk/agent-loop)\n\n> XENO Agent SDK is proprietary software (package license `UNLICENSED`), © 2026 XENO Corporation. This documentation describes the shipping 0.9.x line."
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Install the SDK from npm, the ESM-only requirements, and the subpath exports.",
          "body": "# Installation\n\nThe SDK is published to npm as **`@xenosystem/agent-sdk`**.\n\n```bash\nnpm install @xenosystem/agent-sdk\n```\n\n> **Scope change.** The SDK publishes as `@xenosystem/agent-sdk` (currently 0.9.x). The older `@xeno-corporation/xeno-agent-sdk` still resolves but is frozen at 0.7.0, so installing it gets you a materially older build. Update the specifier in both your `package.json` and your imports — the exported API is otherwise unchanged.\n\n## Requirements\n\n- **Node.js 20 or newer** (`engines.node >= 20.0.0`).\n- **ESM-first.** The package is `\"type\": \"module\"`, so `import` is the primary form — but every subpath also ships a CommonJS build, so `require` works for interop.\n- **TypeScript 5.x** recommended. Full type definitions ship in the package; every subpath has its own `.d.ts`.\n\n## No peer dependencies\n\nThe SDK declares **no peer dependencies at all** — including the UI. The [`/ui` subpath](/docs/sdk/electron-ui) is a framework-free renderer that talks to the DOM directly, so there is no React (or Vue, or Svelte) install step and no framework lock-in. Use it as-is, or drive the controller from components you already have.\n\n## Importing\n\nThe root barrel re-exports the whole stable surface:\n\n```ts\nimport { createXenoAgent, AgentLoop, ToolRegistry } from \"@xenosystem/agent-sdk\";\n```\n\nFocused subpaths keep dependency graphs small and intent explicit:\n\n```ts\nimport { SoulEngine } from \"@xenosystem/agent-sdk/soul\";\nimport { DualLLMProvider } from \"@xenosystem/agent-sdk/providers\";\nimport { ElectronAgentBridge } from \"@xenosystem/agent-sdk/electron\";\nimport { createAgentUiController, mountAgentUi } from \"@xenosystem/agent-sdk/ui\";\n```\n\nSee [Package exports](/docs/sdk/package-exports) for the full map of subpaths.\n\n## Authentication\n\nBy default the agent loop talks to the XENO API. Supply your key via the `XENO_API_KEY` environment variable or the `apiKey` option to `createXenoAgent()`; point at a different OpenAI-compatible endpoint with `baseURL`. See [Providers & models](/docs/sdk/providers).\n\nNext: [Quickstart →](/docs/sdk/quickstart)"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Create an agent with createXenoAgent, run a turn, and stream runtime events.",
          "body": "# Quickstart\n\nThe primary entry point is **`createXenoAgent()`**. It wires up the permission engine, tool registry, identity, memory, and system prompt, and hands you a ready `AgentLoop`.\n\n## Create an agent and run a turn\n\n```ts\nimport { createXenoAgent } from \"@xenosystem/agent-sdk\";\n\nconst { agent } = await createXenoAgent({\n  cwd: process.cwd(),\n  // model is optional — omit to use the SDK default; pass any id your provider supports\n  model: \"claude-sonnet-4-6\",\n  permissionConfig: { mode: \"default\" },\n});\n\nconst answer = await agent.run(\n  \"List the TypeScript files and summarize the entry point.\"\n);\nconsole.log(answer);\n```\n\n`createXenoAgent()` resolves auth from `apiKey` / `XENO_API_KEY` and the endpoint from `baseURL` (defaulting to the XENO API). `run(userMessage)` executes a full turn — model call, tool calls, iteration — and resolves to the final assistant text.\n\nThe factory returns more than the agent:\n\n```ts\nconst { agent, systemPrompt, permissionEngine, toolRegistry, identity, memory } =\n  await createXenoAgent({ cwd: process.cwd() });\n```\n\n## Stream runtime events\n\nFor a live UI, use `runStream()` — an `AsyncIterable` of typed runtime events in order:\n\n```ts\nfor await (const event of agent.runStream(\"Refactor utils.ts and run the tests\")) {\n  switch (event.type) {\n    case \"model.text.delta\":\n      process.stdout.write(event.text);\n      break;\n    case \"tool.started\":\n      console.log(`\\n▶ ${event.toolName}`);\n      break;\n    case \"tool.completed\":\n      console.log(`  ${event.success ? \"✓\" : \"✗\"} ${event.toolName} (${event.elapsedMs}ms)`);\n      break;\n    case \"turn.completed\":\n    case \"turn.failed\":\n    case \"turn.interrupted\":\n      break;\n  }\n}\n```\n\nEach turn emits `turn.started`, then interleaved `model.text.delta` / `tool.started` / `tool.permission` / `tool.completed` / `diff.completed`, then exactly one terminal `turn.completed` / `turn.failed` / `turn.interrupted`. Breaking out of the loop early cancels the turn. See [The agent loop](/docs/sdk/agent-loop).\n\n## Register an app tool\n\nApps extend the agent by registering their operations as tools:\n\n```ts\nimport { createXenoAgent, ToolRegistry } from \"@xenosystem/agent-sdk\";\n\nconst registry = new ToolRegistry();\nregistry.register({\n  definition: {\n    name: \"layer.remove-bg\",\n    description: \"Remove the background from the active layer\",\n    input_schema: {\n      type: \"object\",\n      properties: { layerId: { type: \"string\" } },\n      required: [\"layerId\"],\n    },\n  },\n  execute: async (input) => {\n    await engine.removeBackground(input.layerId as string);\n    return { success: true, output: `Removed background from ${input.layerId}` };\n  },\n});\n\nconst { agent } = await createXenoAgent({ toolRegistry: registry });\n```\n\nSee [Tools & the registry](/docs/sdk/tools) for the full tool contract.\n\n## Next steps\n\n- [The agent loop](/docs/sdk/agent-loop)\n- [Tools & the registry](/docs/sdk/tools)\n- [Permissions & sandboxing](/docs/sdk/permissions)\n- [Embedding in apps](/docs/sdk/embedding-apps)"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "agent-loop",
          "title": "The agent loop",
          "description": "createXenoAgent, the AgentLoop, run vs runStream, runtime events, and cancellation.",
          "body": "# The agent loop\n\nThe `AgentLoop` is the heart of the SDK: a provider-agnostic turn loop that calls the model, dispatches tools, observes results, and iterates until the task is done or it needs approval.\n\n## Creating one\n\n`createXenoAgent(options)` is the recommended constructor — it resolves environment-backed configuration in the factory (so the loop never reads `process.env` itself) and assembles every subsystem. Key options:\n\n| Option | Purpose |\n|--------|---------|\n| `cwd` | Working directory for tools and project state |\n| `model` / `fallbackModels` | Primary model id and ordered fallbacks tried after retryable failures |\n| `apiKey` / `baseURL` | Auth and OpenAI-compatible endpoint |\n| `maxTokens` / `maxIterations` | Turn bounds (defaults: `SDK_DEFAULT_MAX_TOKENS` = 8192 / `SDK_DEFAULT_MAX_ITERATIONS` = 50) |\n| `effort` | Reasoning effort (`low` / `medium` / `high` / `xhigh` / `max`) where supported |\n| `executionMode` | `agent` (full tool loop) or `chatOnly` |\n| `permissionConfig` / `sandbox` | Governance — see [Permissions](/docs/sdk/permissions) |\n| `toolRegistry` | Your tools — see [Tools](/docs/sdk/tools) |\n| `provider` | A custom `LLMProvider` — see [Providers](/docs/sdk/providers) |\n| `identity` / `memory` | Persona and memory config, or `false` to disable |\n\nThe result object is `{ agent, systemPrompt, permissionEngine, toolRegistry, identity?, memory? }`.\n\nAn `AgentLoop` can also be constructed directly (`new AgentLoop(config)`) when an embedded host wants to own configuration end-to-end — this is the app-agnostic path used by Electron main processes.\n\n## run vs runStream\n\n```ts\n// Blocking: resolves to the final assistant text\nconst text: string = await agent.run(\"…\");\n\n// Streaming: yields typed runtime events in order\nfor await (const event of agent.runStream(\"…\", { /* RunStreamOptions */ })) { … }\n```\n\nBoth drive the same loop and the same event bus. Callbacks (`onText`, `onToolStart`, `onToolEnd`, `onIteration`, `onError`) and an external `runtimeEventBus` / `onRuntimeEvent` sink keep firing regardless of which you call.\n\n## Runtime events\n\n`runStream` (and the event bus) emit `XenoRuntimeEvent`s:\n\n| Event | Payload highlights |\n|-------|--------------------|\n| `turn.started` | `model`, `effort?`, `promptPreview` |\n| `model.text.delta` | `text`, `iteration?` |\n| `tool.started` | `toolName`, `inputPreview`, `iteration?` |\n| `tool.permission` | `toolName`, `allowed`, `reason`, `layer` (`sandbox` \\| `permission`) |\n| `tool.completed` | `toolName`, `success`, `outputPreview`, `error?`, `elapsedMs` |\n| `diff.completed` | `filesChanged`, `summary` |\n| `turn.completed` | `outputPreview`, `tokenUsage` (terminal) |\n| `turn.failed` / `turn.interrupted` | `error` (terminal — exactly one per turn) |\n\nA failed turn surfaces as a terminal `turn.failed` event rather than rejecting the iterator, so a streaming consumer never has to wrap the loop in try/catch for turn failures.\n\n## Cancellation\n\n`agent.cancelCurrentTurn()` aborts the in-flight request and ends the turn. Consumer `break`/`return` out of a `runStream` loop calls it for you.\n\n## Extending the loop\n\nThree stable, additive extension seams let an embedder reshape governance without forking the loop: **tool middleware**, **prompt sections**, and **completion guards**. Register them at construction or at runtime with `agent.use(...)`, `agent.registerPromptSection(...)`, and `agent.registerCompletionGuard(...)`. See [Extension seams](/docs/sdk/extension-seams).\n\nRelated: [Tools](/docs/sdk/tools) · [Sessions](/docs/sdk/sessions) · [Providers](/docs/sdk/providers)."
        },
        {
          "slug": "tools",
          "title": "Tools & the registry",
          "description": "The RegisteredTool contract, the built-in tool set, namespacing, MCP tools, and input validation.",
          "body": "# Tools & the registry\n\nTools are how the agent acts. Every tool is a `RegisteredTool`, and the `ToolRegistry` is the container the loop consults.\n\n## The tool contract\n\n```ts\ninterface ToolDefinition {\n  name: string;\n  description: string;               // what the model sees\n  input_schema: {\n    type: \"object\";\n    properties: Record<string, unknown>;\n    required?: string[];\n  };\n}\n\ntype ToolExecutor = (input: Record<string, unknown>) => Promise<ToolResult>;\n\ninterface RegisteredTool {\n  definition: ToolDefinition;\n  execute: ToolExecutor;\n}\n\ninterface ToolResult {\n  success: boolean;\n  output: string;\n  error?: string;\n  assistantContent?: ToolAssistantContentBlock[];      // e.g. images/resources shown to the model\n  assistantOnlyContent?: ToolAssistantContentBlock[];\n}\n```\n\nA tool declares its schema for the model, and its `execute` returns a structured `ToolResult`. Whether a call needs approval is decided by the [permission engine](/docs/sdk/permissions), not a per-tool flag.\n\n## The registry\n\n```ts\nimport { ToolRegistry, createDefaultToolRegistry } from \"@xenosystem/agent-sdk\";\n\nconst registry = new ToolRegistry();\nregistry.register(myTool);\nregistry.registerAll([toolA, toolB]);\n```\n\nCommon methods: `register`, `registerAll`, `registerAlias`, `unregister`, `get`, `has`, `listNames`, `getDefinitions`, `getDefinitionsByNamespace(ns)`, `listNamespaces`, `execute(name, input)`, `onChange(listener)`, and `size`. `registerAlias` registers a callable compatibility name that is excluded from `getDefinitions()` so the model is never offered the same tool under two names.\n\n## The default tool set\n\n`createDefaultToolRegistry({ cwd, askUser, dispatchAgent, webSearchApiKey, memoryManager, validateInputs })` builds the standard registry. It is far more than the original six file tools — the shipped built-ins include:\n\n- **Files & code**: `Read`, `Write`, `Edit`, `Glob`, `Grep`, `ReadImage`\n- **Shell**: `Bash` (with a persistent-shell session, background execution, and command-safety helpers)\n- **Web**: `WebSearch` (multi-backend — DuckDuckGo with no key, upgrading to Brave when `webSearchApiKey` is supplied) and `WebFetch`\n- **Reasoning & delegation**: `Think`, `AskUser`, `DispatchAgent`\n- **Memory**: memory read/write tools (when a `MemoryManager` is provided)\n- **Analysis**: `SqliteAnalyze`, `ElfAnalyze`, `GcodeAnalyze`, `HtmlSanitizerAudit`, `ArtifactCompare`\n- **Language tooling**: LSP definition/references/hover/diagnostics tools\n- **Git**: status / diff / log / commit / branch (with runtime `cwd` binding)\n\nEach built-in also has a factory (`createReadTool`, `createBashTool`, …) if you need a custom-configured instance.\n\n## Namespacing for app tools\n\nApp operations use a `\"<namespace>.<operation>\"` convention (`pixel.brush.draw`, `motion.timeline.cut`) so tools from different apps never collide. `getDefinitionsByNamespace(\"pixel\")` and `listNamespaces()` let you introspect them.\n\n## Tools from MCP servers\n\nTools contributed by connected MCP servers are adapted into `RegisteredTool`s and synced into the registry, appearing to the agent alongside the built-ins and subject to the same permission checks. See [MCP](/docs/sdk/mcp).\n\n## Opt-in input validation\n\nEnable JSON-Schema-subset validation of model-supplied tool inputs:\n\n```ts\nconst { agent } = await createXenoAgent({ validateToolInputs: true });\n// or per-registry: new ToolRegistry({ validateInputs: true }) / registry.setValidateInputs(true)\n```\n\nOn a violation the registry returns a structured, self-correctable error naming the offending field and expected type, instead of executing. It validates `type` / `enum` / `required` / `properties` / `items` / `additionalProperties:false` and ignores unknown keywords. **Defaults to off** for backward compatibility. The validator (`validateJsonSchema`, `formatJsonSchemaErrors`) is exported for direct use.\n\nRelated: [Permissions](/docs/sdk/permissions) · [Extension seams](/docs/sdk/extension-seams)."
        },
        {
          "slug": "permissions",
          "title": "Permissions & sandboxing",
          "description": "The permission engine, its modes, directory sandboxing, and the host approval callback.",
          "body": "# Permissions & sandboxing\n\nAgents must not run unsupervised. The `PermissionEngine` gates every tool call, and a directory sandbox bounds what the agent can touch.\n\n## Permission modes\n\nSet `permissionConfig.mode`:\n\n| Mode | Behavior |\n|------|----------|\n| `default` | Ask before writes, edits, and shell commands; read-only tools run automatically |\n| `acceptEdits` | Auto-approve file writes and edits; still ask before shell commands |\n| `bypassPermissions` | Approve everything without prompting — trusted, sandboxed environments only |\n| `plan` | Read-only; the agent can plan and suggest but not mutate |\n| `auto` | Automated policy mode for non-interactive runs |\n\n```ts\nconst { agent } = await createXenoAgent({ permissionConfig: { mode: \"acceptEdits\" } });\n```\n\n`PermissionConfig` also carries per-tool / per-pattern `rules` (`{ tool, pattern?, decision }` where decision is `allow` / `ask` / `deny`) and an optional permission `profile`.\n\n## The engine\n\n`createXenoAgent()` builds and returns a `PermissionEngine` for you (in the result object), loading any persisted project-level approvals for `cwd`. You can also pass your own via `permissionEngine`. The engine tracks the working directory, remembered approvals, and allowed directories.\n\n## Directory sandbox\n\nRestrict an agent to specific directories and capabilities:\n\n```ts\nconst { agent } = await createXenoAgent({\n  sandbox: {\n    allowedDirectories: [\"/home/user/project\"],\n    capabilities: { fileRead: true, fileWrite: true, terminal: false, appLaunch: false },\n  },\n});\n```\n\nEvery tool invocation is checked against the sandbox before execution. A denial surfaces as a `tool.permission` runtime event with `layer: \"sandbox\"`.\n\n## Host approval callback\n\nWhen the sandbox (or engine) needs a decision, `onPermissionRequest` lets the host render its own prompt — a CLI question, an Electron dialog, a web modal — and the loop pauses until it resolves:\n\n```ts\nconst { agent } = await createXenoAgent({\n  sandbox: { allowedDirectories: [], capabilities: { fileRead: true, fileWrite: true, terminal: false, appLaunch: false } },\n  onPermissionRequest: async (ctx) => {\n    const ok = await showDialog(`Allow ${ctx.toolName} to access ${ctx.directory}?`);\n    return { granted: ok };\n  },\n});\n```\n\nIf the callback grants access, the directory is added to the sandbox's temporary directories and the tool is retried. If omitted, sandbox denials are immediate.\n\n## Auditability\n\nEvery permission decision is observable through the runtime event stream (`tool.permission`) and, in a full host, the audit logger. The SDK never assumes \"yes\" in a non-interactive context — hosts opt into automatic behavior deliberately via mode or an approval callback.\n\nRelated: [Tools](/docs/sdk/tools) · [The agent loop](/docs/sdk/agent-loop)."
        },
        {
          "slug": "sessions",
          "title": "Sessions, checkpoints & recovery",
          "description": "Durable sessions with transcripts, checkpoints, locks, turn-restore, and a versioned format.",
          "body": "# Sessions, checkpoints & recovery\n\nA session is a durable, resumable record of a conversation. The `session` subsystem persists transcripts, snapshots progress, prevents concurrent writes, and can restore a session after a crash.\n\nImport from the root or the focused `/session` subpath:\n\n```ts\nimport { SessionManager, generateSessionId } from \"@xenosystem/agent-sdk/session\";\n```\n\n## The pieces\n\n| Export | Role |\n|--------|------|\n| `SessionManager` | Create / resume a session and coordinate its lifecycle |\n| `generateSessionId` / `parseSessionId` / `isValidSessionId` | Session ids with embedded timestamps |\n| `TranscriptWriter` | Append-only conversation transcript on disk |\n| `CheckpointManager` | Snapshot / restore checkpoints for long workflows |\n| `SessionLock` (aliased `SessionLockManager`) | PID lock so two processes never write the same session |\n| `SessionRegistry` | Track all sessions for a project |\n| `TurnRestoreManager` | Restore an earlier turn for crash recovery, with file previews |\n\nWire a session into an agent via the `session` option on `createXenoAgent()` (a `SessionIntegrationConfig`), so transcripts and checkpoints are written as the loop runs.\n\n## Turn restore (crash recovery)\n\n`TurnRestoreManager` exposes what can be restored, the restore points, and file previews so a host can offer \"resume from before that turn\" after an interruption.\n\n## Versioned format\n\nThe session/message format is versioned. `SESSION_FORMAT_VERSION` is written into the session metadata and the `session_start` transcript event. Readers treat the **absence** of a version field as version 1, so pre-existing sessions survive upgrades; `readSessionFormatVersion(meta)` encodes that rule. Serialized formats are never broken silently — see [Versioning & API stability](/docs/sdk/versioning-stability).\n\n## Auto-checkpointing\n\n`AutoCheckpointHandler` (from the core barrel) periodically checkpoints a session during a long run, so a host can drive durable progress without managing checkpoint timing by hand.\n\nRelated: [Memory & identity](/docs/sdk/memory-identity) · [The agent loop](/docs/sdk/agent-loop)."
        },
        {
          "slug": "memory-identity",
          "title": "Memory & identity",
          "description": "The hierarchical memory manager and the layered identity/persona system, both token-budgeted.",
          "body": "# Memory & identity\n\nThe SDK gives an agent continuity (memory) and character (identity). Both are assembled by `createXenoAgent()` and injected into the system prompt under configurable token budgets so they never crowd out the conversation.\n\n## Memory levels\n\n`MemoryManager` maintains a hierarchy that widens in scope and persistence:\n\n| Level | Scope | Persists |\n|-------|-------|----------|\n| Conversation | Current turn | No |\n| Session | Current session | Until the session ends |\n| Project | Current project | Yes |\n| Global | All projects | Yes |\n\nConfigure it through the `memory` option (or pass `memory: false` to disable):\n\n```ts\nconst { agent, memory } = await createXenoAgent({\n  memory: {\n    globalDir: \"/path/to/global\",\n    includeProjectSessionContext: true,\n  },\n});\n```\n\nThe manager loads memory for the prompt (`loadForPrompt`), can pull in prior project-session context, and formats it all into the injected memory block. The result object's `memory.prompt` is exactly what was injected. Memory can also be exposed to the agent as tools (the memory read/write tools) so it can record and recall during a turn.\n\n## Identity layers\n\nIdentity is resolved by `IdentityLoader` + `IdentityResolver` across layers — **global**, **project**, **role**, and **session** — highest precedence winning. The resolved persona (`ResolvedIdentity`) shapes the agent's behavior and is folded into the system prompt.\n\n```ts\nconst { agent, identity } = await createXenoAgent({\n  role: \"reviewer\",\n  identity: { globalDir: \"/path/to/global\" },   // or identity: false to disable\n});\n```\n\nSelecting a `role` picks up that role's identity layer (and, if configured, its own memory store). Identity files are markdown with YAML frontmatter; the loader/resolver handle discovery and precedence.\n\n## Budgeted injection\n\nMemory and identity are both budget-constrained: the system prompt leaves room for the conversation, and injection is capped so long-lived project/global memory can't overflow the context window.\n\nFor the earned layer that goes beyond stored notes — episodic memory and self-taught skills — see [Soul](/docs/sdk/soul)."
        },
        {
          "slug": "soul",
          "title": "Soul — earned memory & skills",
          "description": "The SoulEngine: episodic memory, self-taught skills, and Ed25519-signed provenance.",
          "body": "# Soul — earned memory & skills\n\nThe **Soul** subsystem is the earned layer of an agent: it records what the agent *did* as episodes, distills reusable *skills* from successful non-trivial tasks, and injects the most relevant of both before a new task. It pairs with xeno-anima's authored `mind.xeno` — \"a Mind is given, a Soul is earned.\"\n\nSoul is app-agnostic (`node:fs` / `node:crypto` only) and layered on the SDK's existing vector memory store, so it adds no new runtime dependency. Import from the root or `/soul`:\n\n```ts\nimport { SoulEngine, createEd25519Signer } from \"@xenosystem/agent-sdk/soul\";\n```\n\n## The engine\n\n```ts\nconst soul = await SoulEngine.create({\n  dir,                         // optional on-disk persistence (episodes.jsonl + skills.json)\n  complete,                    // a minimal SoulCompletion callback for skill synthesis\n  signer: createEd25519Signer(),\n});\n\n// Before a task: pull relevant past skills/episodes into context\nconst augmentation = await soul.augmentContext(task);\n\n// After a task: record it (and maybe distill a skill)\nawait soul.onTaskComplete({ task, transcript, outcome: \"success\" });\n```\n\n- **`augmentContext(query)`** retrieves the most relevant past skills and episodes to inject before a new task.\n- **`onTaskComplete(...)`** records an `Episode` and, on a successful non-trivial task, distills a reusable `Skill`.\n\n## Stores and synthesis\n\n- `EpisodicStore` and `SkillStore` are semantic stores over the vector memory store.\n- `synthesizeSkill` is the LLM-backed distillation step. It takes an injected, minimal `SoulCompletion` (decoupled from the full `LLMProvider`), parses tolerantly, and never throws.\n\n## Signed provenance\n\n`createEd25519Signer()` produces a signer; `verifySoulRecord` validates it. Episodes and skills carry tamper-evident Ed25519 signatures, so a Soul's earned record is verifiable and portable.\n\n## Wiring note\n\nThe Soul engine is **not auto-wired into the agent loop**. Consumers call `augmentContext` / `onTaskComplete` at their own lifecycle points (before/after a turn). Loop integration is a separate, additively reviewed step.\n\nRelated: [Memory & identity](/docs/sdk/memory-identity)."
        },
        {
          "slug": "extension-seams",
          "title": "Extension seams",
          "description": "Tool middleware, prompt sections, and completion guards — reshape governance without forking the loop.",
          "body": "# Extension seams\n\nThe `AgentLoop` exposes three **stable, additive, typed** extension seams so an embedder can reshape governance *outside* the SDK without forking the loop. With no seam registered, the default code paths are unchanged. Permission, sandbox, path-safety, and audit still apply to every call that reaches the inner executor.\n\nRegister at construction (`createXenoAgent({ … })` or the `AgentLoop` constructor) or at runtime on the instance.\n\n## 1. Tool middleware\n\nOnion-composed around every tool execution. A middleware can **block** (return its own `ToolResult` without calling `next()`), **rewrite input** (mutate `ctx.input` before `next()`), **observe results**, or **inject model-visible guidance** (`ctx.addGuidance(text)`).\n\n```ts\nagent.use(async (ctx, next) => {\n  if (ctx.toolName === \"Bash\" && /curl|wget/.test(String(ctx.input.command))) {\n    return { success: false, output: \"\", error: \"Network fetch disabled this turn\" };\n  }\n  return next();\n});\n```\n\nThe first-registered middleware is the outermost layer. When none are registered the orchestrator runs directly (zero overhead).\n\n## 2. Prompt sections\n\nOrdered, token-budgeted system-prompt sections appended after the base prompt.\n\n```ts\nagent.registerPromptSection({\n  name: \"house-style\",\n  order: 10,\n  provide: (ctx) => \"Always run the linter before proposing a commit.\",\n});\n```\n\nSections are concatenated in ascending `order` and the combined suffix is truncated as a whole to `promptSectionsTokenBudget` (default `DEFAULT_PROMPT_SECTIONS_TOKEN_BUDGET = 4000`). Re-registering a `name` replaces it; a throwing provider is skipped so it can never break prompt construction.\n\n## 3. Completion guards\n\nConsulted when the model wants to end the turn — a guard can veto and either nudge the loop to continue or stop it outright.\n\n```ts\nagent.registerCompletionGuard({\n  name: \"require-tests\",\n  order: 0,\n  evaluate: (ctx) => {\n    if (ctx.stats.toolFailures > 0) {\n      return { allow: false, guidance: \"Fix the failing tool calls before finishing.\" };\n    }\n    return { allow: true };\n  },\n});\n```\n\nGuards run in `order`, first veto wins. A veto appends `guidance` as a synthetic user message and continues (subject to `maxCompletionGuardReminders`, default 2); `stop: true` ends the turn immediately with `guidance` as the final text. The evaluation context carries `stats` and the message history so a guard can weigh whether a proposed answer is supported by tool evidence. A throwing guard is treated as allowing — it can never wedge the loop.\n\n## Why seams instead of forks\n\nThese seams exist so `xeno-agent-cli` (and any embedder) can reimplement research-budget limits, evidence ledgers, and end-of-turn verification on top of the SDK rather than patching the agent loop. They are **stable**, not experimental.\n\nRelated: [The agent loop](/docs/sdk/agent-loop) · [Tools](/docs/sdk/tools)."
        }
      ]
    },
    {
      "title": "Embedding & integrating",
      "pages": [
        {
          "slug": "embedding-apps",
          "title": "Embedding in apps",
          "description": "AppAgentFactory, per-app tool definitions, and app context injection for the creative suite.",
          "body": "# Embedding in apps\n\nThe integration layer turns the low-level `createXenoAgent()` into a high-level way to drop an agent into a specific XENO app, with the right tools and system prompt already assembled.\n\n## AppAgentFactory\n\n`AppAgentFactory` creates app-specific agents. It has a dedicated method per app type and returns an `AppAgentResult` (the agent plus its registry and context):\n\n```ts\nimport { AppAgentFactory } from \"@xenosystem/agent-sdk\";\n\nconst factory = new AppAgentFactory();\nconst { agent, toolRegistry } = await factory.createPixelAgent({\n  imageAdapter: myPixelEngineAdapter,\n  // plus any createXenoAgent options to merge (model, permissionConfig, …)\n});\n```\n\nFactory methods cover the creative and productivity suite: `createPixelAgent`, `createMotionAgent`, `createSoundAgent`, `createArchitectAgent`, `create3DAgent`, `createEngineAgent`, `createWorkflowAgent`, `createDocsAgent`, `createSheetsAgent`, `createSlidesAgent`, and `createNotesAgent`. Each takes a typed options object (`PixelAgentOptions`, `MotionAgentOptions`, …) where you pass the app's engine adapter, and internally calls `createXenoAgent()` with app-appropriate tools and prompts.\n\n## Per-app tool definitions\n\nThe integrations layer exports tool-definition builders for each app type (`createDocsTools`, `createSheetsTools`, `create3DTools`, `createArchitectTools`, `createEngineTools`, `createNotesTools`, `createSlidesTools`, `createWorkflowTools`, …) so an app can register its domain operations without hand-writing every schema. These follow the [tool namespacing convention](/docs/sdk/tools) so app tools never collide.\n\n## App context injection\n\n`AppContextInjector` enriches the agent's system prompt with live app state — the active layer, the selected clip, the current track, the open document — via typed context inputs. This is what lets a user say \"remove the background from *this* layer\" and have the agent know which layer *this* is.\n\n## Local-first LLM\n\n`LocalLLMProvider` connects an embedded agent to a local xeno-rt runtime with cloud fallback, so an app can run entirely offline when a local model is available and transparently fall back to the XENO API otherwise. See [Providers & models](/docs/sdk/providers).\n\nRelated: [Electron & UI](/docs/sdk/electron-ui) · [Providers](/docs/sdk/providers)."
        },
        {
          "slug": "electron-ui",
          "title": "Electron & UI",
          "description": "The ElectronAgentBridge and IPC protocol, plus the framework-free /ui renderer for the agent sidebar.",
          "body": "# Electron & UI\n\nTwo surfaces make it turnkey to put an agent sidebar in an Electron app: a **main-process bridge** and a **framework-free renderer**. The Electron bridge is available from both the root barrel and the `/electron` subpath, and it only lazy-requires the `electron` package on demand — so importing it outside an Electron main process is safe.\n\n## The Electron bridge (main process)\n\n`ElectronAgentBridge` runs the agent in the Electron main process and streams to the renderer over a typed IPC protocol:\n\n```ts\nimport { ElectronAgentBridge, setupAIHandlers, readXenoApiKey } from \"@xenosystem/agent-sdk/electron\";\n\nconst bridge = new ElectronAgentBridge({\n  appContext: () => ({ /* live app state injected into the prompt */ }),\n  onPermissionRequest: async (info) => showPermissionDialog(info),\n  onText: (text) => sendToRenderer(text),\n  onToolStart: (name, input) => sendToRenderer({ name }),\n  onToolEnd: (name, result) => sendToRenderer({ name }),\n  onComplete: () => setIdle(),\n  onError: (err) => showError(err.message),\n});\n```\n\nThe bridge speaks two typed channel sets — `AppToAgentMessages` (renderer → agent) and `AgentToAppMessages` (agent → renderer) — with `PermissionRequestInfo` and an `IpcHandler` contract, so the two processes stay type-safe. `setupAIHandlers()` wires the standard IPC handlers in one call (auto-importing `ipcMain` from `electron` if you don't supply it), and `readXenoApiKey()` resolves the stored key.\n\n## The UI renderer (renderer process)\n\nThe `/ui` subpath is a **framework-free** agent chat surface. It has **no peer dependencies** — no React, no Vue, no build step — so it drops into an Electron renderer, a plain page, or any framework you already use.\n\nMount it onto an element in two calls:\n\n```ts\nimport { createAgentUiController, mountAgentUi } from \"@xenosystem/agent-sdk/ui\";\n\n// `agent` satisfies AgentUiAgent: { tokenUsage, run(prompt), cancelCurrentTurn() }\nconst controller = createAgentUiController({\n  agent: agentLoop,\n  enablePermissionRequests: true,\n});\n\nconst ui = mountAgentUi(document.getElementById(\"sidebar\")!, controller, {\n  agentName: \"XENO Agent\",\n  inputPlaceholder: \"Ask anything…\",\n  injectStyles: true,\n});\n\n// on teardown\nui.dispose();\ncontroller.dispose();\n```\n\n`mountAgentUi()` returns an `AgentUiDomHandle` — `element`, `render()`, `focusInput()`, `dispose()`. Pass `styleNonce` when your CSP requires one, or `injectStyles: false` and ship `xenoUiCss` yourself.\n\n## Driving it from your own components\n\nThe controller is the whole contract, so you can skip `mountAgentUi()` and render the state however you like:\n\n```ts\nimport { createAgentUiView, dispatchAgentUiAction } from \"@xenosystem/agent-sdk/ui\";\n\nconst unsubscribe = controller.subscribe((snapshot) => {\n  const view = createAgentUiView(snapshot, { agentName: \"XENO Agent\" });\n  paint(view); // your renderer — React, Svelte, plain DOM\n});\n\nawait dispatchAgentUiAction(controller, { type: \"send\", text: \"Summarise this file\" });\nawait dispatchAgentUiAction(controller, { type: \"permission\", requestId, decision });\n```\n\n`AgentUiAction` is a closed union — `send`, `cancel`, `clear`, `retry`, `permission`. The controller also exposes them directly: `sendMessage()`, `cancelExecution()`, `clearMessages()`, `retryLastMessage()`, `resolvePermission()`, plus `getSnapshot()` and `subscribe()`.\n\nStyling ships alongside: `xenoUiCss` (the stylesheet as a string) and `xenoUiTokens` (the CSS custom properties). `parseMarkdown()` and `isSafeMarkdownHref()` are exported for custom renderers.\n\n## Renderer API version\n\n`XENO_UI_RENDERER_API_VERSION` is `1`, and `controller.apiVersion` reports it. Assert on it if you render the snapshot yourself — the shape is versioned independently of the package.\n\nRelated: [Embedding in apps](/docs/sdk/embedding-apps) · [The agent loop](/docs/sdk/agent-loop)."
        },
        {
          "slug": "providers",
          "title": "Providers & models",
          "description": "The LLMProvider contract, the dual local/cloud provider, and OpenAI-compatible endpoints.",
          "body": "# Providers & models\n\nThe SDK is model-agnostic and provider-agnostic. By default the loop uses a built-in OpenAI-compatible transport pointed at the XENO API, but you can route every chat call through your own backend.\n\n## The LLMProvider contract\n\nImplement `LLMProvider` and pass it to `createXenoAgent({ provider })` (or `AgentLoopConfig.provider`):\n\n```ts\nimport { createXenoAgent, type LLMProvider } from \"@xenosystem/agent-sdk\";\n\nconst echo: LLMProvider = {\n  id: \"echo\",\n  async complete(model, messages) {\n    // return an LLMCompletionResult\n    return { /* … */ };\n  },\n};\n\nconst { agent } = await createXenoAgent({ provider: echo });\n```\n\nA provider exposes `complete(model, messages, tools?, onText?, signal?)`, an `id`, and optionally `completeLocal`, a `model`, and `capabilities` (e.g. whether it can stream tokens via `onText`). When no `provider` is supplied the default transport is used unchanged — the internal client structurally implements `LLMProvider`, so existing callers behave identically.\n\n## Dual local/cloud provider\n\n`DualLLMProvider` (from the root or `/providers`) prefers a local runtime and falls back to the cloud:\n\n```ts\nimport { DualLLMProvider } from \"@xenosystem/agent-sdk/providers\";\n\nconst provider = new DualLLMProvider({ mode: \"auto\" }); // \"auto\" | \"local\" | \"cloud\"\nconst response = await provider.chat({ messages: [{ role: \"user\", content: \"Hello!\" }] });\n```\n\n- `local` — only the local xeno-rt endpoint (default `http://localhost:3338`).\n- `cloud` — only `api.xenostudio.ai`.\n- `auto` — prefer local, fall back to cloud on failure.\n\nSwitch at runtime with `setMode` / `getMode`, and stream tokens with `chatStream`. To use it as the loop's provider, adapt it to the `LLMProvider` contract.\n\n## Local & offline\n\n`LocalLLMProvider` (from the integrations layer) connects an embedded app to a local xeno-rt runtime with cloud fallback. Because xeno-rt and Ollama are OpenAI-compatible, pointing the default transport at them is just a matter of setting `baseURL` — your code and prompts never leave the machine.\n\n## Choosing a model\n\nPass `model` to `createXenoAgent()` (and `fallbackModels` for ordered fallbacks tried after retryable transport/provider failures). The SDK does not hardcode a model catalog — use any id your chosen provider serves. `effort` (`low` … `max`) tunes reasoning where the model supports it.\n\nRelated: [The agent loop](/docs/sdk/agent-loop) · [Embedding in apps](/docs/sdk/embedding-apps)."
        },
        {
          "slug": "mcp",
          "title": "MCP client & server",
          "description": "Connect external MCP servers for new tools, and expose the SDK's own tools over MCP.",
          "body": "# MCP client & server\n\nThe SDK speaks the Model Context Protocol in **both directions**: it is an MCP client (connect external servers to give the agent new tools, prompts, and resources) and an MCP server (expose the SDK's own tools to external clients). Import from the root or the `/mcp` subpath.\n\n## As an MCP client\n\n`MCPManager` connects to configured servers and syncs their capabilities into the tool registry:\n\n```ts\nimport { MCPManager, loadConfiguredMcpServers } from \"@xenosystem/agent-sdk/mcp\";\n```\n\nConfigured servers come from an MCP config file (`loadMcpConfigFile` / `saveMcpConfigFile` / `loadConfiguredMcpServers`, with helpers to edit it). Servers carry an approval decision (`approveMcpServer` / `denyMcpServer` / `getMcpApprovalDecision`) that gates whether an untrusted server's tools are allowed.\n\nTransports: `StdioTransport` (spawn a local server) and `SSETransport` (remote server over Server-Sent Events).\n\nTools, prompts, and resources from a connected server are adapted into `RegisteredTool`s and pushed into the registry with `syncMcpToolsToRegistry`, so they appear to the agent alongside the [built-ins](/docs/sdk/tools) and are subject to the same [permission engine](/docs/sdk/permissions). An access policy (`MCPRegistryAccessPolicy`) controls which entries are exposed.\n\n## As an MCP server\n\n`MCPServer` exposes the SDK's tools (and registered prompts/resources) to any MCP client — an editor, another agent, Claude Desktop:\n\n```ts\nimport { MCPServer } from \"@xenosystem/agent-sdk/mcp\";\n```\n\nIt implements the standard JSON-RPC MCP surface (initialize, tools/list, tools/call, prompts, resources, and elicitation), with typed request/result shapes for each. Elicitation lets a server ask the client for input mid-call.\n\nRelated: [Tools](/docs/sdk/tools) · [Orchestration & app-server](/docs/sdk/orchestration)."
        },
        {
          "slug": "orchestration",
          "title": "Orchestration & app-server",
          "description": "Cross-app message routing, the agent protocol, the JSON-RPC app-server, and the run control-plane.",
          "body": "# Orchestration & app-server\n\nBeyond a single agent, the SDK ships primitives for coordinating many agents across processes and windows, driving an agent over RPC, and tracking runs.\n\n## Cross-app routing\n\n`CrossAppRouter` routes JSON-serializable messages between agents living in different processes or windows — the substrate for \"one request spans Pixel + Motion + Sound.\" Messages (`CrossAppMessage`) carry sender, recipient, task, correlation id, and timestamp; handlers (`CrossAppHandler`) receive them. Errors surface as `CrossAppRouterError`.\n\n```ts\nimport { CrossAppRouter } from \"@xenosystem/agent-sdk\";\n```\n\n## The agent protocol\n\n`AgentRegistry` + `AgentProtocol` implement an agent-to-agent layer: agents advertise an `AgentCard`, group into teams, report load, and exchange typed messages. Tasks let one agent dispatch structured work to another and get typed results back.\n\n## The app-server (JSON-RPC)\n\n`XenoAppServer` exposes an agent over JSON-RPC so external clients — an editor, the browser, XENO Hub — can drive it over a stream:\n\n```ts\nimport { XenoAppServer } from \"@xenosystem/agent-sdk\";\n\nconst server = new XenoAppServer({ defaultAgentOptions: { /* thread run options */ } });\nawait server.start(process.stdin, process.stdout);\n```\n\nIt announces a `server.ready` notification with a protocol version and processes newline-delimited JSON-RPC 2.0 requests, backed by the SDK's thread runner and the unified exec manager.\n\n## The run control-plane\n\nThe `/control-plane` subsystem is a durable record of agent runs — a `run-store` of `AgentRunRecord`s (schema-versioned) with rich lifecycle status (`queued`, `starting`, `running`, `blocked`, `waiting_for_user` / `_tool` / `_hook` / `_permission`, `paused`, `completed`, `failed`, `cancelled`, `interrupted`, `detached`), usage accounting (token counts and estimated cost), parent/root run attribution, and a lock. This is what a host uses to list, resume, and supervise background and nested runs.\n\n## Hooks & subagent definitions\n\nTwo more extensibility subsystems support orchestration:\n\n- **Hooks** (`HookRunner`, `runHooks`) run commands or HTTP calls at lifecycle events with per-hook trust states.\n- **Agent definitions** (the `agents` subsystem) load reusable subagents from markdown-with-YAML files across `project` / `user` / `plugin` / `built-in` scopes, each with a tool policy, model, and isolation setting — the definitions the loop can dispatch to via delegation.\n\n> The plugin system (`PluginHost`, marketplace, scaffolding) is **experimental** and design-complete but **not yet wired into the agent loop** — see [Versioning & API stability](/docs/sdk/versioning-stability).\n\nRelated: [MCP](/docs/sdk/mcp) · [The agent loop](/docs/sdk/agent-loop)."
        }
      ]
    },
    {
      "title": "Coordination and automation",
      "pages": [
        {
          "slug": "goal-loop-handoff",
          "title": "Goal, Loop, and Handoff",
          "description": "The durable coordination contract shared by the SDK, CLI, ACP, and Agent Interface.",
          "body": "# Goal, Loop, and Handoff\n\nImport the canonical coordination contract from `@xenosystem/agent-sdk/coordination`.\n\n```ts\nimport {\n  DurableXenoCoordinationStore,\n  XenoExecutionLeaseSession,\n} from '@xenosystem/agent-sdk/coordination'\n\nconst store = new DurableXenoCoordinationStore()\nconst goal = await store.createGoal({\n  sessionId: 'session-123',\n  objective: 'Ship a verified release',\n  successCriteria: ['tests pass', 'packaged process passes'],\n})\nconst loop = await store.startLoop({\n  sessionId: goal.sessionId,\n  goalId: goal.id,\n  kind: 'goal-continuation',\n})\n```\n\nA **Goal** owns the objective, success criteria, milestones, tasks, steering, progress, and evidence-backed verification. A **Loop** owns repeated work; goal-continuation and agentic-development loops are unbounded unless the caller explicitly supplies a limit. A failed iteration is durable evidence, not automatic termination. A **Handoff** transfers the single live execution lease and advances a monotonically increasing fencing epoch.\n\nCreate an `XenoExecutionLeaseSession` for every process that can execute tools. Keep it alive for that executor and call `assertOwned()` immediately before side effects. Preparing a handoff refuses active operations whose retry policy is unsafe; after a target claim, the source is fenced.\n\nThe store fsyncs a candidate and atomically replaces versioned state, uses optimistic versions plus a tokenized cross-process lock, and preserves goals, iterations, handoff lineage, epochs, and lifecycle events across restart. Goal completion is explicit and requires criterion evidence; an assistant message never completes it by itself."
        },
        {
          "slug": "governed-automation",
          "title": "Governed browser and computer automation",
          "description": "Capability leases, target preflight, evidence, idempotency, and the native-adapter certification boundary.",
          "body": "# Governed browser and computer automation\n\nThe `@xenosystem/agent-sdk/automation` package connects agent intent to browser or computer drivers without allowing a model, adapter, or transport to widen authority.\n\n```ts\nimport {\n  XenoGovernedAutomationExecutor,\n  XenoLoopbackAutomationAdapter,\n} from '@xenosystem/agent-sdk/automation'\n```\n\nThe executor validates identity and the Secure Execution Contract, atomically binds the real tab or device target, validates browser/computer policy, persists required before evidence, consumes the exact capability lease once, delegates one declared operation, validates the resulting target, and persists bounded after evidence. Missing required evidence returns `evidence-incomplete`, never trusted success.\n\nBrowser policy separates read domains from action domains, applies explicit denies first, controls redirects/uploads/downloads/recording, and intersects with the run network policy. `operationId` provides durable idempotency; reuse with different parameters or authority fails with `AUTOMATION_OPERATION_CONFLICT`. Human stop aborts the SDK operation and invokes the adapter stop hook for the originally bound target.\n\n`XenoLoopbackAutomationAdapter` can connect to an explicit loopback XENO Browser server with its per-launch bearer token. It rejects redirects, ambient browser credentials, unbounded responses, and non-JSON replies.\n\n**Certification boundary:** the source contract and conformance tests ship in 0.9.24. A native browser or computer adapter is available only when the Host supplies it, and `certified: true` requires candidate-bound evidence for that exact adapter build. Protocol tests alone do not prove Windows, Linux, or macOS containment."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "package-exports",
          "title": "Package exports",
          "description": "The subpath export map and the major exports available from each entry point.",
          "body": "# Package exports\n\nThe root barrel (`@xenosystem/agent-sdk`) re-exports most of the stable surface. Focused subpaths keep dependency graphs tight and make intent explicit. All entry points ship TypeScript definitions.\n\n## Subpath map\n\n| Import path | What it exposes |\n|-------------|-----------------|\n| `.` (root) | The core surface plus most subsystems, re-exported for convenience |\n| `./providers` | `DualLLMProvider` and its types (also ships a CommonJS build) |\n| `./ui` | Framework-free agent UI: controller, DOM mount, view model, styles (NOT in the root barrel) |\n| `./electron` | `ElectronAgentBridge`, `setupAIHandlers`, IPC protocol types (also re-exported from root) |\n| `./session` | `SessionManager`, `TranscriptWriter`, `CheckpointManager`, ids, turn-restore |\n| `./mcp` | `MCPManager` (client), `MCPServer`, transports, config, adapters |\n| `./soul` | `SoulEngine`, episodic/skill stores, Ed25519 signing |\n| `./control-plane` | Run store, run records/status, locks |\n| `./agents` | Subagent definitions (scan/load markdown+YAML) |\n| `./hooks` | `HookRunner`, hook types, command/HTTP hook runners |\n| `./coordination` | Durable Goal, Loop, Handoff state and live execution leases |\n| `./automation` | Governed browser/computer operations, policies, evidence, and adapters |\n| `./utils` | Logger, cost helpers, clipboard |\n\n> The `/ui` renderer is the one surface that is subpath-only — it is intentionally excluded from the root barrel so the core never touches the DOM. It has no framework peer. Every other subpath above is also reachable from the root import.\n\n## Major root exports by area\n\n- **Entry & loop**: `createXenoAgent`, `AgentLoop`, `SDK_DEFAULT_MAX_TOKENS`, `SDK_DEFAULT_MAX_ITERATIONS`, delegation (`createDelegatedXenoAgent`), the reducer, and subagent-workflow helpers.\n- **Tools**: `ToolRegistry`, `createDefaultToolRegistry`, every built-in tool factory, `validateJsonSchema`, `formatJsonSchemaErrors`.\n- **Governance**: `PermissionEngine` and the extension-seam types (tool middleware, prompt-section provider, completion guard).\n- **Providers**: `LLMProvider`, `DualLLMProvider`, `LocalLLMProvider`, and their types.\n- **Runtime**: the `XenoRuntimeEvent` union, event bus/sink types, `BackgroundProcessManager`, `AutoCheckpointHandler`, unified exec.\n- **Integrations**: `AppAgentFactory`, per-app tool builders, `AppContextInjector`.\n- **Orchestration**: `CrossAppRouter`, `AgentRegistry` / `AgentProtocol`, `XenoAppServer`.\n- **Diagnostics**: `AgentDebugger`, `AgentEvaluator`, `CodeValidator`, cost/usage helpers.\n- **Version**: `SDK_VERSION` (a string constant you can read at runtime).\n\nUse `npm view @xenosystem/agent-sdk` and your editor's go-to-definition against the shipped `.d.ts` files for the exact, version-accurate surface.\n\nRelated: [Introduction](/docs/sdk/introduction) · [Versioning & API stability](/docs/sdk/versioning-stability)."
        },
        {
          "slug": "versioning-stability",
          "title": "Versioning & API stability",
          "description": "Stability tiers, the additive-only contract, the deprecation policy, and semantic versioning.",
          "body": "# Versioning & API stability\n\nThis SDK is embedded in 5+ consumer apps, so **every public export is a contract**. The SDK holds itself to **additive-only** changes to its public surface, and classifies exports into three stability tiers.\n\n## Stability tiers\n\n| Tier | Marker | Guarantee |\n|------|--------|-----------|\n| **Stable** (default) | none | Not removed, renamed, or signature-changed without a major bump. Serialized formats stay readable. |\n| **Internal** | `@internal` JSDoc | Implementation detail. Stays exported (removing one is itself a break) but may change shape across minor versions. Don't build on it. |\n| **Experimental** | `@experimental` JSDoc | Intentionally unstable while the design settles. Safe to try; pin a version if you depend on it. |\n\nStable examples: `createXenoAgent`, `AgentLoop`, `ToolRegistry`, `PermissionEngine`, `SessionManager`, `LLMProvider`, `runStream`, `validateJsonSchema`. The current experimental surface is the **plugin system** (`PluginHost`, marketplace, scaffolding) — design-complete but **not yet wired into the agent loop**, so installing a plugin does not yet contribute tools or prompt sections to a running loop (see `docs/plugins-status.md` in the repo).\n\n> The extension seams (tool middleware, prompt sections, completion guards) are **stable**, not experimental — they shipped with explicit additive contracts.\n\n## The additive-only contract\n\n1. **Never remove an export** without first deprecating it (`@deprecated`, naming the replacement) for at least one minor release, then removing it only in the next major.\n2. **Never change a function signature** — add an overload or a new function instead.\n3. **Never rename a type** — add an alias for the better name and deprecate the old one.\n4. **Never break a serialized format** — bump the format version and keep reading older versions tolerantly (absence of a version field means version 1, e.g. `SESSION_FORMAT_VERSION` / `readSessionFormatVersion`).\n\nA `@deprecated` export keeps working exactly as before during its window; the tag is the only change.\n\n## Semantic versioning\n\n- **PATCH** — bug fixes, no surface change.\n- **MINOR** — additive features (new exports, new optional options/fields), `@deprecated` annotations, and changes to `@internal` / `@experimental` items.\n- **MAJOR** — removal of a deprecated export or any other break to the stable surface.\n\nThe SDK is currently on the **0.9.x** line. Within `0.x` it still holds to additive-only changes to the public surface; the changelog calls out anything otherwise. Because a bump here ripples into every consuming app, releases are reported to the orchestrator so apps can update and rebuild.\n\nRelated: [Package exports](/docs/sdk/package-exports) · [Introduction](/docs/sdk/introduction)."
        }
      ]
    }
  ]
};

export default sdk;
