import type { ProductDocs } from './_types';

const pixel: ProductDocs = {
  "slug": "pixel",
  "productName": "XENO Pixel",
  "tagline": "The unified raster and vector image editor — Photoshop-class layers, selections, and on-device AI in a fast desktop app.",
  "seo": {
    "title": "XENO Pixel documentation",
    "description": "Learn XENO Pixel — the desktop image editor with layers, masks, non-destructive adjustments, a stamp-based brush engine, on-device AI, and a programmatic agent API. Install, core concepts, AI features, and full reference."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Pixel is, and where it fits in the XENO ecosystem.",
          "body": "# Introduction\n\n**XENO Pixel** is a professional desktop image editor from XENO Corporation. It brings raster and vector editing into one application — layers, masks, non-destructive adjustments, a low-latency brush engine, selections, text, shapes, and on-device AI — with an interface that follows Photoshop conventions so existing muscle memory carries over.\n\nIt is a native desktop app built on Electron and React, not a web page or a command-line tool. You open documents, paint, composite layers, and export — and every operation is also callable programmatically so agents and scripts can drive the same engine.\n\n## What it does\n\n- **Raster and vector in one app** — paint with a stamp-based brush engine, and draw editable shapes, paths, lines, and text in the same document.\n- **A real layer system** — raster layers, groups, layer masks, vector masks, adjustment layers, smart objects, and layer styles, composited with a broad blend-mode set.\n- **Non-destructive editing** — adjustment layers and smart filters re-render from source rather than baking pixels.\n- **Selections that hold up** — marquee, lasso, magic wand, magnetic lasso, color range, and Select Subject, with refine, feather, grow, and saved selection channels.\n- **On-device AI** — background removal, upscaling, denoise, colorize, depth, and more, run locally through the XENO runtime with honest availability reporting.\n- **Agent-ready** — a typed command bridge exposes documents, layers, selections, tools, filters, and export to automation.\n\n## Where it fits\n\nXENO Pixel is a **Layer 5 (Creative App)** in the XENO ecosystem. It builds on `@xeno/core` for rendering, compositing, color, and formats; consumes XENO's AI runtime for model inference; and can be launched standalone or from [XENO Hub](/docs/hub), which shares authentication, credits, and workspace context. It can also hand images to sibling apps like XENO Motion.\n\n## Maturity\n\nXENO Pixel is currently in **beta (v0.6.3)**. The core editor — documents, layers, selections, tools, adjustments, history, project save/load, and export — is shipping and usable day to day. Several advanced subsystems are still maturing: GPU-accelerated compositing, some file-format edge cases, and parts of the AI pipeline. Where a feature is partial or unavailable, the app says so in the UI rather than pretending, and this documentation does the same.\n\n## Next steps\n\n- [Install XENO Pixel](/docs/pixel/installation)\n- [Quickstart](/docs/pixel/quickstart) — your first document, edit, and export\n- [The interface](/docs/pixel/interface) — a tour of the workspace\n\n> XENO Pixel is proprietary software, © 2026 XENO Corporation. All rights reserved."
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Install XENO Pixel from the download page, through XENO Hub, or build from source.",
          "body": "# Installation\n\nXENO Pixel is a desktop application. The primary distribution today is a **Windows installer**, delivered through the download page and through XENO Hub.\n\n## From the download page\n\nDownload the latest installer from the XENO Pixel product page:\n\n- [xenostudio.ai/products/pixel/download](https://xenostudio.ai/products/pixel/download)\n\nThe download page reads the current version dynamically, so it always offers the latest build (for example, `XENO Pixel Setup 0.6.3.exe`). Run the installer and launch XENO Pixel from the Start menu.\n\n## Through XENO Hub\n\nIf you use [XENO Hub](/docs/hub), install and launch XENO Pixel from Hub's Studio section. Hub manages the install, shares your authentication and credit balance, and shows an **Update** button when a newer version is available — it checks for updates on startup and periodically after that.\n\n## From source\n\nXENO Pixel is a standard Electron + Vite project. With Node.js installed:\n\n```bash\ngit clone https://github.com/XENO-CORPORATION/xeno-pixel\ncd xeno-pixel\nnpm install\nnpm run dev        # launch in development with hot reload\n```\n\nProduction builds and installers:\n\n```bash\nnpm run build          # production build\nnpm run package:win    # build a Windows installer\nnpm run package:mac    # macOS build\nnpm run package:linux  # Linux build\n```\n\n> macOS and Linux packaging targets exist in the build config, but Windows is the actively shipped installer channel. Treat mac/linux builds as build-from-source targets for now.\n\n## Updating\n\nWhen launched from XENO Hub, updates are surfaced automatically. For a standalone install, download the latest installer from the download page and run it over your existing install — your preferences, custom shortcuts, and recent files are preserved.\n\nNext: [Quickstart →](/docs/pixel/quickstart)"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Create a document, make an edit, and save or export your work.",
          "body": "# Quickstart\n\nThis walks through the first loop: create a document, paint on a layer, and save and export. It assumes XENO Pixel is [installed](/docs/pixel/installation).\n\n## 1. Create a document\n\nWhen no document is open, the **Start screen** offers New Project, Open File, and your recent projects. To create a document:\n\n- Press `Ctrl+N` for a quick new document, or\n- Press `Ctrl+Alt+N` to open the **New Project** dialog, where you set width, height, DPI, color profile, and background — or pick a preset.\n\nClick Create and an empty canvas appears with a background layer.\n\n## 2. Paint something\n\n- Press `B` to select the **Brush**. Adjust size with `[` and `]`.\n- Drag on the canvas to paint. The brush uses full-resolution pointer input, so a pen tablet's pressure and tilt are captured.\n- Press `E` to erase, `X` to swap foreground/background colors, and `D` to reset to black/white.\n\nEvery stroke is a discrete history step — press `Ctrl+Z` to undo and `Ctrl+Shift+Z` to redo.\n\n## 3. Add a layer and adjust\n\n- Press `Ctrl+Shift+N` for a new layer, or duplicate the current one with `Ctrl+J`.\n- Open Filter → Adjustments → Brightness/Contrast to tune tones, or add a non-destructive **adjustment layer** so you can re-edit it later (see [Non-destructive editing](/docs/pixel/non-destructive-editing)).\n\n## 4. Save your project\n\nSave the editable project — layers, masks, and all — as a single `.xpixel` file:\n\n```text\nCtrl+S        Save\nCtrl+Shift+S  Save As\n```\n\nA `.xpixel` file is one self-contained document; there is no companion asset folder. XENO Pixel also autosaves while you work and can recover your document after an unexpected quit — see [Documents and color](/docs/pixel/documents-and-color).\n\n## 5. Export a flat image\n\nTo produce a PNG, JPEG, or other deliverable:\n\n```text\nCtrl+Shift+E   Export As…\n```\n\nChoose a format in the Export dialog. For many outputs at once, use File → Batch Export. See [File formats](/docs/pixel/file-formats) for what each format preserves.\n\n## Next steps\n\n- [The interface](/docs/pixel/interface) — panels, tools, and menus\n- [Layers](/docs/pixel/layers) — the heart of the editor\n- [Tools](/docs/pixel/tools) — the full tool set"
        },
        {
          "slug": "interface",
          "title": "The interface",
          "description": "A tour of the workspace: menu bar, tools, panels, canvas, and options bars.",
          "body": "# The interface\n\nXENO Pixel follows a familiar professional layout: a menu bar on top, a tools sidebar on the left, panels on the right, and the document canvas in the center. The design is deliberately dense and monochromatic — the focus is your image, not chrome.\n\n## Menu bar\n\nThe top menu bar groups commands into standard menus:\n\n- **File** — New, Open, Place Embedded, Import Brushes (.abr), Save / Save As, Export As, Batch Export, Print, Close.\n- **Edit** — Undo/Redo, Cut/Copy/Paste, Fill, Free Transform, Liquify, Puppet Warp, Preferences, Keyboard Shortcuts.\n- **Select** — All, Deselect, Reselect, Inverse, Color Range, Modify (Expand/Contract/Smooth/Feather/Border), Refine Edge, Grow, Select Similar.\n- **Image** — Canvas Size, Image Size, and the **AI** submenu (see [AI features](/docs/pixel/ai-features)).\n- **Layer** — new layers, groups, masks, adjustment layers, styles, smart objects.\n- **Filter** — Blur, Sharpen, Adjustments, Liquify, Invert, Grayscale.\n- **View** — Zoom, Fit to Screen, Actual Pixels, Rulers, Guides, Grid, Perspective Grid.\n\n## Tools sidebar\n\nThe left sidebar holds the tools, grouped by purpose (selection/navigation, paint/vector, view). Hover any tool to see its name and shortcut. At the bottom are the foreground/background color swatches — press `X` to swap them. See [Tools](/docs/pixel/tools) for the full set.\n\n## Panels\n\nPanels on the right give you inspection and control over the active document. The main ones include:\n\n- **Layers** — the layer stack, blend mode, opacity, masks, and locks.\n- **History** — every step, with undo/redo and snapshots.\n- **Properties** — context-sensitive settings for the current layer or tool.\n- **Color palette** and the advanced **Color Picker**.\n- **Navigator** and **Info** — overview thumbnail and live cursor/color/measurement readouts.\n- **Actions** — record and replay macros.\n- **Timeline** — frame-based sprite animation.\n- **Brush Library** — brush presets, including imported `.abr` tips.\n- **Agent** — the in-app AI assistant panel (`Ctrl+Shift+A`; see [Agent and automation](/docs/pixel/agent-automation)).\n\n## Tool options and quick actions\n\n- The **Tool Options bar** sits under the menu bar and exposes settings for the active tool (brush size, hardness, selection mode, and so on).\n- A floating **Quick Actions bar** hovers over the canvas while a document is open, for common operations without diving into menus.\n\n## Canvas and viewport\n\nThe canvas supports smooth zoom from 1% to 6400% with zoom-to-cursor, plus fit-to-window (`Ctrl+0`) and actual-pixels (`Ctrl+1`). Rulers, guides, a pixel grid, and a perspective grid help you align work. Multiple documents open as tabs.\n\nNext: [Documents and color →](/docs/pixel/documents-and-color)"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "documents-and-color",
          "title": "Documents and color",
          "description": "Creating documents, color modes and bit depth, the .xpixel project format, and crash recovery.",
          "body": "# Documents and color\n\nA XENO Pixel **document** is a stack of layers plus metadata: dimensions, resolution, color mode, bit depth, and a color profile.\n\n## Creating and sizing documents\n\nCreate a document with `Ctrl+N` (quick) or `Ctrl+Alt+N` (full New Project dialog with size, DPI, profile, background, and presets). Once open, you can change dimensions two ways:\n\n- **Image Size** — resample the whole document to new pixel dimensions.\n- **Canvas Size** — change the canvas bounds without resampling, anchored to any of nine positions.\n\n## Color mode, bit depth, and resolution\n\nDocument metadata carries:\n\n- **Color mode** — RGB, CMYK, grayscale, or Lab.\n- **Bit depth** — 8, 16, or 32 bits per channel.\n- **Color profile** — sRGB or a named/custom ICC profile.\n- **Resolution** — DPI, used to derive physical print dimensions.\n\n> Honest note on depth: the metadata records these modes, but the editing engine today operates primarily on 8-bit RGBA pixel buffers internally. Deep 16/32-bit and non-RGB pipelines are on the roadmap; for now, treat color mode and bit depth as document intent and export targets rather than a guarantee that every edit is performed at that depth.\n\n## The .xpixel project format\n\nSave your editable work as a `.xpixel` file (`Ctrl+S` / `Ctrl+Shift+S`). Key properties:\n\n- **Single file.** A `.xpixel` is one JSON document with embedded, base64-encoded raster payloads — there is no separate asset folder, so the project moves as one file.\n- **Full fidelity within XENO Pixel.** Layers, groups, masks, adjustment layers, smart objects, styles, and viewport state are preserved across save and reopen.\n- **Legacy compatibility.** Files saved by early builds as `.xeno-pixel` still open — the loader accepts both extensions. New saves always write `.xpixel`.\n\nTo bring in images instead of a project, use File → Open or Place Embedded, or drag files onto the canvas. See [File formats](/docs/pixel/file-formats) for supported types.\n\n## Autosave and crash recovery\n\nWhile you edit, an autosave sidecar is written next to your project on an interval (only when there are unsaved changes), and a lock file marks that the app is running. If XENO Pixel exits unexpectedly, the next launch detects the stale lock and shows a **Crash Recovery** dialog offering to Recover your autosaved work or Discard it. Recovery covers both the current and legacy sidecar names.\n\nNext: [Layers →](/docs/pixel/layers)"
        },
        {
          "slug": "layers",
          "title": "Layers",
          "description": "Raster layers, groups, masks, adjustment layers, smart objects, styles, and blend modes.",
          "body": "# Layers\n\nLayers are the core of the editor. XENO Pixel supports a full professional layer model, managed from the **Layers** panel.\n\n## Layer types\n\n- **Raster layers** — pixel layers you paint, fill, and filter.\n- **Groups** — nest layers into folders with their own opacity, blend mode, and clipping.\n- **Adjustment layers** — non-destructive tonal/color edits applied to everything below (see [Non-destructive editing](/docs/pixel/non-destructive-editing)).\n- **Smart objects** — containers that embed a source bitmap plus a transform, re-rasterizing from the source so scaling stays crisp.\n- **Shape, text, and line layers** — editable vector content (see [Tools](/docs/pixel/tools)).\n\n## Common layer operations\n\n```text\nCtrl+Shift+N   New layer\nCtrl+J         Duplicate layer\nDelete         Delete layer\nCtrl+E         Merge down\nCtrl+Shift+F   Flatten image\nCtrl+G         Group layers\n```\n\nEach layer has visibility and lock toggles, an opacity value, a blend mode, an optional color label, and clipping-to-below. You can reorder, rename, and clip layers directly in the Layers panel.\n\n## Masks\n\nTwo kinds of masks are supported:\n\n- **Layer (raster) masks** — a grayscale mask hides or reveals pixels non-destructively. Add one from the Layers panel or programmatically.\n- **Vector masks** — a path-based mask for crisp, resolution-independent edges.\n\n## Layer styles\n\nApply live, editable effects to a layer: **drop shadow, inner shadow, outer glow, inner glow, stroke, and bevel & emboss**. Styles can be edited, removed, and copied from one layer to another.\n\n## Blend modes\n\nThe Layers panel exposes a broad blend-mode set spanning the Normal, Darken, Lighten, Contrast, Inversion, and Component families: Normal, Multiply, Screen, Overlay, Darken, Lighten, Color Burn, Color Dodge, Hard Light, Soft Light, Vivid Light, Linear Light, Pin Light, Hard Mix, Subtract, Divide, Grain Merge, Difference, Exclusion, Hue, Saturation, Color, and Luminosity — 23 layer modes, plus **Pass Through** for groups. A few Photoshop modes (Linear Burn, Linear Dodge/Add, Dissolve, Lighter/Darker Color) are not yet included.\n\n## Layer comps\n\nCapture named snapshots of layer **visibility, position, and appearance** as **layer comps**, then switch between them — useful for presenting variations of a composition from a single document.\n\nNext: [Tools →](/docs/pixel/tools)"
        },
        {
          "slug": "tools",
          "title": "Tools",
          "description": "The full tool set — painting, retouching, vector, text, and navigation.",
          "body": "# Tools\n\nTools live in the left sidebar and each has a keyboard shortcut. This page covers what each does; see [Keyboard shortcuts](/docs/pixel/keyboard-shortcuts) for the complete list and how to customize it.\n\n## Selection and navigation\n\n| Tool | Shortcut | Purpose |\n|------|----------|---------|\n| Move | `V` | Move layer content and selections |\n| Rectangular Marquee | `M` | Rectangular selection |\n| Elliptical Marquee | — | Elliptical selection |\n| Lasso | `L` | Freehand / polygon selection |\n| Magic Wand | `W` | Color-similarity selection |\n| Crop | `C` | Crop the document |\n| Eyedropper | `I` | Sample a color from the canvas |\n| Hand | `H` | Pan the viewport |\n| Zoom | `Z` | Zoom in/out |\n\nSelection tools are covered in depth in [Selections and masks](/docs/pixel/selections-and-masks).\n\n## Painting and retouching\n\n| Tool | Shortcut | Purpose |\n|------|----------|---------|\n| Brush | `B` | Paint with the stamp-based brush engine |\n| Pencil | `N` | Hard-edged painting |\n| Eraser | `E` | Erase pixels |\n| Fill | `K` | Flood fill a region |\n| Gradient | `G` | Draw linear, radial, angle, or diamond gradients |\n| Clone Stamp | `S` | Copy pixels from a sampled source |\n| Healing Brush | `J` | Blend sampled pixels into the target |\n\n### The brush engine\n\nThe brush is stamp-based with a stroke-buffer pipeline (flow builds up within a stroke, then composites at stroke opacity). It captures full-resolution pointer input including pen **pressure and tilt**, and supports pressure-driven size and opacity. Brush size is adjustable live with `[` and `]`. You can import Photoshop `.abr` brush tips via File → Import Brushes; imported tips bring their geometry, with some advanced dynamics reported as unsupported where they can't yet be reproduced.\n\nRelated painting features include **symmetry** modes and **pattern** fills.\n\n## Vector, text, and measurement\n\n| Tool | Shortcut | Purpose |\n|------|----------|---------|\n| Pen | `P` | Draw editable cubic-bezier paths |\n| Shape | `U` | Rectangle, ellipse, polygon, and star presets |\n| Line | — | Draw straight lines with caps |\n| Text | `T` | Editable text layers |\n| Transform | `Ctrl+T` | Free-transform layer content |\n| Measure | — | Measure distance and angle |\n\n### Vector and text notes\n\nShapes and paths are editable vector layers. Text is created as an editable text layer and shaped with HarfBuzz for correct glyph layout. Deep typography (full paragraph layout, complex-script parity) and lossless SVG round-tripping are still maturing — see [File formats](/docs/pixel/file-formats).\n\n## Warp tools\n\nBeyond Free Transform, XENO Pixel offers **Liquify** (Filter → Liquify, `Shift+Ctrl+X`) for push/twirl/bloat/pucker brushing, and **Puppet Warp** (Edit → Puppet Warp) for pin-based mesh deformation.\n\n> Every tool here is also callable programmatically — see [Agent and automation](/docs/pixel/agent-automation).\n\nNext: [Selections and masks →](/docs/pixel/selections-and-masks)"
        },
        {
          "slug": "selections-and-masks",
          "title": "Selections and masks",
          "description": "Marquee, lasso, magic wand, color range, Select Subject, refine, and saved selection channels.",
          "body": "# Selections and masks\n\nSelections constrain where edits apply. XENO Pixel stores selections as a grayscale (0–255 per pixel) alpha mask, so soft and feathered edges are exact, and displays them with animated marching ants.\n\n## Making selections\n\n- **Rectangular / Elliptical Marquee** — geometric selections (`M`, and the ellipse tool).\n- **Lasso** — freehand and polygon selections (`L`).\n- **Magnetic Lasso** — snaps to edges as you trace, using an edge-cost map.\n- **Magic Wand** — selects contiguous or similar colors by tolerance (`W`).\n- **Color Range** (Select → Color Range) — sample colors with an eyedropper or pick a preset (reds, yellows, greens, cyans, blues, magentas, highlights, midtones, shadows, skin tones), with fuzziness, localized clusters, and range controls.\n- **Select Subject** — an AI segmentation that isolates the main subject (see [AI features](/docs/pixel/ai-features)).\n\nSelection tools honor the standard **replace / add / subtract / intersect** modes.\n\n## Selection commands\n\n```text\nCtrl+A         Select All\nCtrl+D         Deselect\nCtrl+Shift+D   Reselect\nCtrl+Shift+I   Invert selection\nShift+F6       Feather\n```\n\n## Refining a selection\n\nSelect → Modify and the refine commands let you reshape a selection non-destructively:\n\n- **Expand, Contract, Smooth, Feather, Border**\n- **Refine Edge**\n- **Grow** and **Select Similar**\n\n## Saving and loading selections\n\nSave the current selection as a named channel and load it back later. Saved selections travel with the document, so you can build up a library of reusable masks for a composition.\n\n## Selections and layer masks\n\nA selection can be converted into a **layer mask** to hide or reveal pixels non-destructively, and layer masks can be edited with any painting tool. See [Layers](/docs/pixel/layers) for the mask model.\n\nNext: [Non-destructive editing →](/docs/pixel/non-destructive-editing)"
        },
        {
          "slug": "non-destructive-editing",
          "title": "Non-destructive editing",
          "description": "Adjustment layers, smart objects, smart filters, and destructive filters compared.",
          "body": "# Non-destructive editing\n\nXENO Pixel favors edits you can revisit. Three mechanisms let you change an image without permanently altering pixels: adjustment layers, smart objects, and smart filters.\n\n## Adjustment layers\n\nAn **adjustment layer** applies a tonal or color transform to all layers below it, re-rendered live from an internal lookup table. Because nothing is baked, you can re-open the adjustment and change its parameters at any time, or mask it to a region.\n\nAvailable adjustment types:\n\n- **Brightness/Contrast**\n- **Hue/Saturation**\n- **Levels** (input/output black & white, gamma)\n- **Curves** (per-channel RGB, red, green, blue)\n- **Color Balance** (shadows/midtones/highlights, preserve-luminosity)\n- **Invert**\n- **Black & White** (per-color weights)\n\n## Smart objects and smart filters\n\nConvert a raster layer to a **smart object** to embed its source pixels plus a transform. Scaling down and back up re-rasterizes from the original, so quality is preserved. Filters applied to a smart object become **smart filters** — a re-editable, toggleable stack layered on top of the source rather than burned into it.\n\n## Destructive filters\n\nFor a direct, baked edit, the Filter menu applies operations straight to the active layer:\n\n- **Blur** — Gaussian Blur\n- **Sharpen** — Unsharp Mask, Smart Sharpen\n- **Adjustments** — Brightness/Contrast, Hue/Saturation, Levels, Color Balance\n- **Invert, Grayscale**\n- **Liquify** and **Puppet Warp** (mesh/pin warps)\n\nWhen applied to a smart object, these same filters attach as smart filters; when applied to a plain raster layer, they modify pixels directly (and remain undoable via history).\n\n## Where this is heading\n\nA foundational filter-graph exists under the hood, and adjustment layers, smart filters, masks, and smart objects are steadily converging into a single, document-wide non-destructive graph. Today you get robust adjustment layers and smart filters; a fully unified, always-re-evaluable DAG across every effect is an in-progress goal rather than a finished guarantee.\n\nNext: [AI features →](/docs/pixel/ai-features)"
        }
      ]
    },
    {
      "title": "AI",
      "pages": [
        {
          "slug": "ai-features",
          "title": "AI features",
          "description": "On-device and cloud AI — background removal, upscaling, and more — with honest availability status.",
          "body": "# AI features\n\nXENO Pixel integrates AI operations directly into the editor, mostly under **Image → AI**. Models run on-device through the XENO runtime where possible, with a cloud path for generative editing. The app is deliberately honest about what is production-ready versus still being wired — the AI menu labels partial and unavailable features, and this page mirrors that status exactly.\n\n## Availability status\n\n| Operation | Status | Notes |\n|-----------|--------|-------|\n| Remove Background | Production | Uses the XENO runtime when available, with a bundled offline background-removal fallback. |\n| Upscale (2x/4x) | Partial | Model path wired with provider fallback; model-specific validation incomplete. |\n| Upscale HQ (SwinIR) | Partial | Dedicated path wired; I/O validation incomplete. |\n| Denoise (NAFNet) | Partial | Wired with fallback; validation incomplete. |\n| Style Transfer | Partial | Wired; style-model validation incomplete. |\n| Colorize (DDColor) | Partial | Wired; I/O validation incomplete. |\n| Restore Face (CodeFormer) | Partial | Dedicated path wired; face validation incomplete. |\n| Estimate Depth | Partial | Wired; output validation incomplete. |\n| Select Subject (segment) | Partial | Mask path wired; full promptable SAM flow not production-ready. |\n| Inpaint | Unavailable | LaMa mask-aware preprocessing not yet implemented. |\n| Content-Aware Fill | Unavailable | Depends on inpaint. |\n| Detect Faces | Unavailable | Bounding-box postprocessing not implemented. |\n\n**Production** features are ready to use. **Partial** features run through a wired model path but may not be fully validated across inputs. **Unavailable** features are intentionally blocked — the menu item is disabled with a reason rather than silently failing.\n\n> Performance note: on-device inference currently runs on the CPU execution provider. Hardware acceleration (DirectML/CUDA/WebGPU) is planned; expect heavier models to take longer than a GPU path would.\n\n## Running an AI operation\n\nOpen a document, then choose the operation from **Image → AI**. Results generally replace the active layer's pixels (or, for generative edits, are added as a new layer so your original stays intact).\n\n## AI text edits (cloud)\n\nBeyond the local models, XENO Pixel can edit a layer from a natural-language prompt: it sends the layer's pixels as a reference image plus your prompt to a cloud image model through the XENO API, and returns a generated image. This path requires connectivity and XENO credits.\n\n## Managing models\n\nOpen **Image → AI → Manage Models** to download, delete, and check the status of local models. Downloads report progress, and the app checks whether a model's files are actually present on disk before claiming it is ready — so the UI reflects real availability, not optimism.\n\n## The Agent panel\n\nXENO Pixel also has an in-app **Agent** panel (`Ctrl+Shift+A`) for natural-language interaction with the editor. See [Agent and automation](/docs/pixel/agent-automation) for what it can do today and where it is going.\n\nNext: [Agent and automation →](/docs/pixel/agent-automation)"
        }
      ]
    },
    {
      "title": "Automation and integration",
      "pages": [
        {
          "slug": "agent-automation",
          "title": "Agent and automation",
          "description": "The programmatic tool bridge, the in-app agent panel, and Hub/Motion integration.",
          "body": "# Agent and automation\n\nA core design rule of XENO Pixel is that **every editor operation is callable programmatically**, not just through mouse and keyboard. Tool logic lives in the engine, separate from the React UI, so agents and scripts can drive the same code paths a person uses.\n\n## The agent tool bridge\n\nThe tool bridge exposes editor operations as discrete, typed commands. Each command validates its parameters and, when it changes the document, pushes a proper history entry — exactly as if a human had performed the action. The command surface spans the whole editor:\n\n- **Documents** — create, import, import raw RGBA, load/save project, resize image, resize canvas, crop, place smart object.\n- **History** — undo, redo.\n- **View** — set viewport zoom/pan, pan viewport.\n- **Selections** — select all/deselect/invert, rectangular/elliptical/lasso/magic-wand/magnetic-lasso selections, refine (feather/expand/contract/smooth/border), save/load selection channels, clear contents.\n- **Layers** — create, set active, update properties (name/visibility/lock/opacity/blend/clip/color-label), reorder, delete, duplicate, merge down, add/remove masks, set/remove vector masks, layer styles (set/remove/copy), group/ungroup, layer comps, smart-object source editing.\n- **Tools** — set active tool, brush settings, brush/erase strokes, fill, gradient, clone stamp, healing brush, blur/sharpen/history retouch, smudge, sample color, transform, pen paths, text layers, lines, measure, shapes.\n- **Color** — set foreground/background, swap.\n- **Adjustments & filters** — add/edit adjustment layers, apply raster or smart-object filters.\n- **Warps** — liquify, puppet warp.\n- **AI** — apply AI operations (subject to the [availability rules](/docs/pixel/ai-features)).\n- **Export** — export the document, batch-export layers.\n- **Introspection** — query feature availability and agent command coverage.\n\nBecause the bridge can report its own **command coverage** and each operation's status (covered / partial / disabled), an agent can discover what it is allowed to do before attempting it.\n\n> Status: the command registry, parameter schemas, and dispatcher are real and tested. The streaming/tool-use wiring to the embedded XENO agent SDK is still in progress, so full LLM-driven tool calling is maturing. The pieces an agent calls are in place ahead of the model that will call them autonomously.\n\n## The in-app Agent panel\n\nThe **Agent** panel (`Ctrl+Shift+A`) is a natural-language sidebar inside XENO Pixel. Today it dispatches your requests to registered editor tools via keyword matching; when the full agent SDK is wired, that dispatch will be replaced by an LLM producing structured tool calls against the same bridge described above.\n\n## Hub and sibling-app integration\n\nXENO Pixel exposes bridges for the wider ecosystem:\n\n- **Hub bridge** — when launched from [XENO Hub](/docs/hub), Pixel shares authentication, credits, and workspace context, and accepts a queued command surface from Hub. Files saved in a workspace are visible to sibling apps.\n- **Motion bridge** — send a rendered image straight to XENO Motion (optionally placing it on the timeline) for a Pixel → Motion handoff.\n- **CLI open** — Pixel can be launched with a file path (for example from Hub or a shell) and opens that image on start.\n\nNext: [File formats →](/docs/pixel/file-formats)"
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "file-formats",
          "title": "File formats",
          "description": "What XENO Pixel can import and export, with honest fidelity notes for each format.",
          "body": "# File formats\n\nXENO Pixel reads and writes common image formats, plus its own `.xpixel` project format. Support varies by format — some are complete, others are useful subsets. This page is explicit about which is which, because silent data loss is worse than a clear limitation.\n\n## Project format\n\n- **.xpixel** — the native, single-file project format. Preserves the full editable document (layers, masks, adjustment layers, smart objects, styles, viewport). Early `.xeno-pixel` files still open. This is the format to use for work in progress. See [Documents and color](/docs/pixel/documents-and-color).\n\n## Import\n\n| Format | Support |\n|--------|---------|\n| PNG, JPEG, WebP, BMP, GIF, AVIF | Full raster import. |\n| PSD / PSB | Imported via the bundled library. Layer structure, masks, groups, and raster content come through; vectors, editable text styling, some adjustments, and effects may be flattened or dropped. Color modes are read as RGB. |\n| TIFF | Baseline cases supported; some variants (BigTIFF, tiled, planar-separated, float) are not yet decoded. |\n| EXR | A subset of channels/compression is read; multipart, deep, tiled, and several production compressions are not yet supported. |\n| SVG | Imported as vector shapes (path/shape data); full SVG document fidelity (all gradients, clips, masks, text) is partial. |\n| RAW | Detection only. Camera RAW **decoding is not yet integrated**, so RAW files are recognized but not developed. |\n\n## Export\n\n| Format | Support |\n|--------|---------|\n| PNG, JPEG, WebP | Full export. Recommended for final deliverables. |\n| PSD | Exported via the bundled library as RGB; non-raster concepts and some effects are not fully round-tripped. |\n| TIFF, EXR | Subset encoders — usable for common cases, not the full format matrix. |\n| SVG | Partial vector export. |\n\n### Batch export\n\nFile → Batch Export renders multiple outputs at once (PNG, JPEG, WebP), which pairs well with the [agent bridge](/docs/pixel/agent-automation) for scripted output across many layers or documents.\n\n## Practical guidance\n\n- Keep your editable master as **.xpixel**.\n- Export flattened deliverables as **PNG / JPEG / WebP** for reliable, full-fidelity output.\n- Treat **PSD, TIFF, EXR, SVG** as interoperability bridges — great for moving work around, but verify anything critical, since some structure may not survive the round trip.\n- **RAW** files can be opened for recognition but not yet developed in-app.\n\nNext: [Keyboard shortcuts →](/docs/pixel/keyboard-shortcuts)"
        },
        {
          "slug": "keyboard-shortcuts",
          "title": "Keyboard shortcuts",
          "description": "The default shortcut set and how to customize, import, and export bindings.",
          "body": "# Keyboard shortcuts\n\nXENO Pixel ships Photoshop-compatible default shortcuts, and every binding is fully customizable. Open **Edit → Keyboard Shortcuts** to review, remap, resolve conflicts, and import/export bindings.\n\n## Tools\n\n| Key | Tool | Key | Tool |\n|-----|------|-----|------|\n| `V` | Move | `B` | Brush |\n| `M` | Rectangular Marquee | `N` | Pencil |\n| `L` | Lasso | `E` | Eraser |\n| `W` | Magic Wand | `K` | Fill |\n| `C` | Crop | `G` | Gradient |\n| `I` | Eyedropper | `S` | Clone Stamp |\n| `H` | Hand | `J` | Healing Brush |\n| `Z` | Zoom | `P` | Pen |\n| `T` | Text | `U` | Shape |\n| `[` / `]` | Brush size − / + | `X` | Swap colors |\n| `D` | Default colors | | |\n\n## File\n\n| Shortcut | Action |\n|----------|--------|\n| `Ctrl+N` | New document |\n| `Ctrl+Alt+N` | New Project (full dialog) |\n| `Ctrl+O` | Open |\n| `Ctrl+S` / `Ctrl+Shift+S` | Save / Save As |\n| `Ctrl+Shift+E` | Export |\n| `Ctrl+P` | Print |\n| `Ctrl+W` | Close |\n| `Ctrl+,` | Preferences |\n\n## Edit and transform\n\n| Shortcut | Action |\n|----------|--------|\n| `Ctrl+Z` | Undo |\n| `Ctrl+Shift+Z` | Redo (also `Ctrl+Y`) |\n| `Ctrl+X` / `Ctrl+C` / `Ctrl+V` | Cut / Copy / Paste |\n| `Shift+F5` | Fill |\n| `Ctrl+T` | Free Transform |\n| `Shift+Ctrl+X` | Liquify |\n\n## Select\n\n| Shortcut | Action |\n|----------|--------|\n| `Ctrl+A` | Select All |\n| `Ctrl+D` | Deselect |\n| `Ctrl+Shift+D` | Reselect |\n| `Ctrl+Shift+I` | Inverse |\n| `Shift+F6` | Feather |\n\n## Layers\n\n| Shortcut | Action |\n|----------|--------|\n| `Ctrl+Shift+N` | New Layer |\n| `Ctrl+J` | Duplicate Layer |\n| `Delete` | Delete Layer |\n| `Ctrl+E` | Merge Down |\n| `Ctrl+Shift+F` | Flatten Image |\n| `Ctrl+G` | Group Layers |\n\n## View and window\n\n| Shortcut | Action |\n|----------|--------|\n| `Ctrl+=` / `Ctrl+-` | Zoom In / Out |\n| `Ctrl+0` | Fit to Screen |\n| `Ctrl+1` | Actual Pixels |\n| `Ctrl+R` | Toggle Rulers |\n| `Ctrl+'` | Toggle Grid |\n| `Ctrl+;` | Toggle Guides |\n| `Ctrl+Shift+G` | Toggle Perspective Grid |\n| `Ctrl+Shift+A` | Agent Panel |\n\n## Customizing shortcuts\n\nThe shortcut system follows a VS Code-style model:\n\n- **Remap** any action to a new key combination.\n- **Conflict detection** warns when two actions share a binding.\n- **Reset** a single action, a category, or everything to defaults.\n- **Import/Export** your bindings as JSON to move them between machines.\n\nCustomizations persist locally across restarts.\n\nNext: [Troubleshooting and FAQ →](/docs/pixel/troubleshooting)"
        },
        {
          "slug": "troubleshooting",
          "title": "Troubleshooting and FAQ",
          "description": "Answers to common questions and how to recover from problems.",
          "body": "# Troubleshooting and FAQ\n\n## An AI feature is greyed out or says Partial/Unavailable\n\nThat is intentional. XENO Pixel reports each AI operation's real status instead of failing silently. **Unavailable** operations (Inpaint, Content-Aware Fill, Detect Faces) are blocked until their processing is production-ready; **Partial** operations run but may not be fully validated. See [AI features](/docs/pixel/ai-features) for the full table.\n\n## AI operations feel slow\n\nOn-device inference currently uses the CPU execution provider — hardware acceleration is planned but not yet enabled. Larger images and heavier models will take longer than a GPU path would.\n\n## XENO Pixel crashed — did I lose my work?\n\nProbably not. XENO Pixel autosaves while you edit. On the next launch it detects the unclean exit and shows a **Crash Recovery** dialog offering to recover your autosaved document. See [Documents and color](/docs/pixel/documents-and-color).\n\n## My RAW file opens but looks wrong / won't develop\n\nCamera RAW **decoding is not yet integrated**. RAW files are detected but not developed in-app. Convert to TIFF or another supported format in your camera's software for now — see [File formats](/docs/pixel/file-formats).\n\n## My PSD lost text, vectors, or effects\n\nPSD/PSB import and export go through a bundled library with known limits: some vectors, editable text styling, adjustments, and effects can be flattened or dropped, and export is RGB. For a fully editable master, save as **.xpixel** instead and use PSD only for interchange.\n\n## Colors look off, or 16/32-bit editing isn't behaving\n\nDocument color mode and bit depth are recorded as metadata, but the editing engine today works primarily in 8-bit RGBA internally. Deep high-bit-depth and CMYK/Lab editing pipelines are on the roadmap. Keep critical high-bit-depth or CMYK originals in a dedicated tool until those pipelines land.\n\n## A tool or panel isn't where I expect\n\nHover any tool for its name and shortcut, and check **Edit → Keyboard Shortcuts** — bindings are customizable and may have been changed. You can reset everything to defaults from that dialog.\n\n## The image won't zoom past a limit\n\nThe viewport clamps between 1% and 6400%. Use `Ctrl+0` to fit to window and `Ctrl+1` for actual pixels.\n\n## Where do I report a bug or request a feature?\n\nXENO Pixel is in active beta. File issues through your XENO support channel or the XENO Pixel repository. Include your version (see the product page), the document type, and the exact steps — especially for format import/export, which has the most known edge cases.\n\n## Is my work portable?\n\nYes — `.xpixel` is a single self-contained file, and early `.xeno-pixel` files still open. For deliverables, export to PNG/JPEG/WebP for reliable, full-fidelity output."
        }
      ]
    }
  ]
};

export default pixel;
