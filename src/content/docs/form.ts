import type { ProductDocs } from './_types';

const form: ProductDocs = {
  "slug": "form",
  "productName": "XENO Form",
  "tagline": "3D modeling, sculpting, animation, and rendering — one app for the whole 3D pipeline.",
  "seo": {
    "title": "XENO Form Documentation",
    "description": "Documentation for XENO Form — an alpha-stage 3D modeling, sculpting, animation, and rendering application. Modeling, modifiers, sculpt and weight painting, a node material editor, a CPU path tracer, and USD/glTF/OBJ/STL I/O."
  },
  "sections": [
    {
      "title": "Getting Started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Form is, what actually runs today at v0.3.0, and where it is headed.",
          "body": "## What XENO Form is\n\nXENO Form is a 3D application that aims to unify polygon modeling, sculpting, UV mapping, materials, animation, simulation, and rendering in a single tool — the ground the industry currently splits across Blender, Maya, Cinema 4D, 3ds Max, Houdini, and ZBrush.\n\nIt is part of the XENO creative suite. It imports the shared `@xeno/core` library (history/undo manager, id generation, shared utilities), is designed to be launched from XENO Hub, and is built to hand meshes, materials, and rigged characters to sibling apps like XENO Motion and XENO Engine via USD.\n\n## Maturity: alpha (v0.3.0)\n\nXENO Form is in **active alpha**. A large surface of the app is functional and covered by an extensive test suite, but it is not a stable release, and much of the long-term architecture is still in development. This documentation describes **what actually runs today**, and marks planned work explicitly.\n\n### What works today\n\n- Real-time 3D viewport with orbit / pan / zoom, PBR shading, and soft shadows\n- Mesh primitives: cube, sphere, cylinder, cone, torus, plane, circle, icosphere, grid\n- Object mode and edit mode with vertex / edge / face selection and transform gizmos\n- A non-destructive modifier stack (subdivision, mirror, bevel, boolean, array, solidify, decimate, remesh, displacement, geometry-nodes slot)\n- Sculpting with a multi-brush system and pressure sensitivity\n- A UV editor with projection and seam editing\n- A node-based PBR material editor that evaluates the graph to shader parameters\n- An animation timeline with keyframes and playback\n- Armature / bone rigging, shape keys, and camera animation paths (data model + Properties)\n- XPBD cloth simulation, weight painting, and vertex color painting\n- Texture baking (normal, AO, diffuse, position, curvature, displacement, shadow, combined)\n- A CPU path tracer with BVH acceleration and progressive rendering\n- Export for OBJ, glTF / GLB, STL, and USD (USDA ASCII); USDA import\n- Undo / redo, autosave, and crash recovery via the shared `@xeno/core` history manager\n\n### Planned and not yet shipped\n\n- The **Rust native engine** (napi-rs + wgpu). Today rendering runs on **Three.js / WebGL2** in the renderer process (`viewportEngine.ts`). The Rust/wgpu path is the target architecture, not the current one.\n- The advanced viewport pipeline in the architecture (deferred rendering, SSAO/GTAO, bloom, cascaded shadow maps). The shipping viewport uses Three.js forward rendering with PCF soft shadows and ACES tone mapping only.\n- GPU-accelerated sculpting and a GPU/CUDA path tracer (the current path tracer is CPU-based)\n- **AI 3D** features (text-to-mesh, AI texturing, auto-rig, pose estimation, style transfer). Three mesh operations are wired to the `xeno-lib` native module; the rest are roadmap. See [AI and mesh processing](/docs/3d/reference#ai-and-mesh-processing).\n- Full geometry reconstruction on import for OBJ / glTF / STL (today those importers create scene objects but do not fully rebuild the mesh)\n- Python scripting (PyO3), FBX / Alembic / PLY / USDC / USDZ I/O, and agent embedding\n\n## The rest of these docs\n\n- [Installation and quickstart](/docs/3d/installation) — build and run the app, make a first scene.\n- [The interface](/docs/3d/interface) — a tour of the viewport, outliner, properties, tools, and modes.\n- [Modeling](/docs/3d/modeling) — objects, primitives, edit mode, transforms, and the modifier stack.\n- [Sculpting and painting](/docs/3d/sculpting-and-painting) — sculpt brushes, weight paint, vertex color, and cloth.\n- [Materials and rendering](/docs/3d/materials-and-rendering) — the node material editor, viewport shading, and the path tracer.\n- [Animation and rigging](/docs/3d/animation-and-rigging) — the timeline, armatures, shape keys, and camera paths.\n- [Reference](/docs/3d/reference) — file formats, the project format, keyboard shortcuts, and feature status."
        },
        {
          "slug": "installation",
          "title": "Installation and quickstart",
          "description": "Build XENO Form from source, run it in development, and create your first scene.",
          "body": "## Requirements\n\nXENO Form is an Electron 34 desktop app (React 19 + TypeScript). To build and run it you need:\n\n- A recent Node.js LTS and npm\n- A GPU and drivers that support **WebGL2** — the viewport renders through Three.js today\n- Windows, macOS, or Linux (Electron packaging targets NSIS / DMG / AppImage)\n\nBecause XENO Form is in alpha, the primary way to run it is **from source**. The long-term distribution channel is XENO Hub, which manages install, auth, and updates for the whole suite.\n\n## Build and run from source\n\nFrom the `xeno-3d` repository root:\n\n```bash\nnpm install\nnpm run dev       # launch in development with hot-reload\n```\n\n`npm run dev` starts the app under `electron-vite` with HMR — the fastest loop for trying things out.\n\n### Other scripts\n\n```bash\nnpm run build       # production build (electron-vite build)\nnpm run typecheck   # tsc --noEmit\nnpm run test        # run the vitest suite\nnpm run package:win     # build a Windows installer\nnpm run package:mac     # build a macOS DMG\nnpm run package:linux   # build a Linux AppImage\n```\n\nThe engine logic is heavily unit-tested (cloth, path tracer, USD round-trip, material evaluation, file I/O, validation, texture baking, weight painting, and the scene store). If you change engine code, run `npm run test`.\n\n## Your first scene\n\nWhen the app opens you get a default scene containing a **Camera**, a **Sun Light**, and a **Cube** — the familiar starting point.\n\n1. Rotate the view by dragging in the viewport; the tools sidebar and header let you switch tools and shading.\n2. Add geometry from the primitives set — cube, sphere, cylinder, cone, torus, plane, circle, icosphere, or grid.\n3. Press `G`, `R`, or `S` to move, rotate, or scale the selection; constrain to an axis with `X`, `Y`, or `Z`.\n4. Press `Tab` to drop into edit mode and work on vertices, edges, and faces.\n5. Add a modifier from the Modifiers tab of the Properties panel — try Subdivision or Mirror.\n6. Press `F12` to render the current view to a PNG.\n\nSee [The interface](/docs/3d/interface) for a full tour, and [Keyboard shortcuts](/docs/3d/reference#keyboard-shortcuts) for the complete key map.\n\n## Saving your work\n\nSave with `Ctrl+S` (Save As with `Ctrl+Shift+S`). XENO Form projects use the **`.xeno3d`** extension. Autosave is on by default (every two minutes), and the app checks for and offers to recover an unsaved session after a crash. See [Files and formats](/docs/3d/reference#files-and-formats)."
        },
        {
          "slug": "interface",
          "title": "The interface",
          "description": "A tour of the viewport, outliner, properties panel, tools, interaction modes, and shading.",
          "body": "## Layout\n\nThe XENO Form window is organized around a central **3D viewport**, with panels around it:\n\n- **Tools sidebar** — the active tool (select, transform, edit operations, sculpt, UV, annotate).\n- **Scene outliner** — the object hierarchy. Supports multi-select, expand/collapse, and a context menu.\n- **Properties panel** — tabbed editors for the active object and scene.\n- **Timeline** — the current frame, playback, and keyframes.\n- **Status bar** — scene statistics (object, mesh, vertex, face, triangle, light, and camera counts).\n\n## The viewport\n\nThe viewport is a Three.js WebGL2 renderer with a physically based look: PCF soft shadow mapping and ACES filmic tone mapping with an adjustable exposure. Navigate with orbit, pan, and dolly; frame the selection with the numpad-period key.\n\n### View presets\n\nStandard view presets are bound to the numpad-style keys: front (`1`), right (`3`), top (`7`), and the camera view (`0`). Toggle perspective / orthographic with `5`.\n\n### Shading modes\n\nFour viewport shading modes are available:\n\n- **Solid** — fast shading (the default)\n- **Material** — preview materials with lighting\n- **Rendered** — full lit preview\n- **Wireframe** — edges only\n\nOverlays (grid, floor, axes, face orientation, and X-ray) can be toggled independently.\n\n## Interaction modes\n\nThe active object can be edited in several modes. `Tab` cycles Object to Edit to Sculpt; the paint modes are selected from the mode selector.\n\n| Mode | Purpose |\n|------|---------|\n| Object | Move, rotate, scale, and organize whole objects |\n| Edit | Vertex / edge / face editing (extrude, inset, knife, and more) |\n| Sculpt | Brush-based sculpting |\n| Weight paint | Paint vertex-group weights for rigging |\n| Vertex paint | Paint per-vertex colors |\n| Texture paint | Paint into textures |\n\nIn edit mode, `1` / `2` / `3` switch between vertex, edge, and face selection.\n\n## The Properties panel\n\nThe Properties panel is tabbed. The tabs are:\n\n- **Object** — transform (location / rotation / scale) and object data\n- **Modifiers** — the non-destructive modifier stack ([Modeling](/docs/3d/modeling))\n- **Materials** — material slots and the node material editor ([Materials and rendering](/docs/3d/materials-and-rendering))\n- **Particles** — particle settings\n- **Physics** — cloth and simulation settings\n- **Constraints** — object constraints (track-to, copy transforms, limits, child-of, floor, and more)\n- **Data** — mesh / object data such as vertex groups and shape keys\n- **Render** — output resolution, path-tracer settings, color management\n\n## Selection\n\nSelect objects in the viewport or the outliner. Multi-selection is supported — the first selected object is the **active** object, which drives the Properties panel."
        }
      ]
    },
    {
      "title": "Core Concepts",
      "pages": [
        {
          "slug": "modeling",
          "title": "Modeling",
          "description": "Objects, mesh primitives, edit-mode operations, transforms, and the non-destructive modifier stack.",
          "body": "## Objects and the scene\n\nEverything in a scene is a **scene object** with a type: mesh, light, camera, empty, armature, curve, volume, or grease pencil. Objects form a hierarchy — reorganize them in the [outliner](/docs/3d/interface). Each object carries its own transform (position, rotation, scale), modifier stack, material assignments, shape keys, and constraints.\n\n### Adding meshes\n\nAdd a mesh primitive and it is created with configurable parameters:\n\n| Primitive | Key parameters |\n|-----------|----------------|\n| Cube | size |\n| Sphere (UV) | segments, rings, radius |\n| Icosphere | subdivisions, radius |\n| Cylinder | vertices, radius, depth, cap fill |\n| Cone | vertices, base/tip radius, depth, cap fill |\n| Torus | major/minor segments, major/minor radius |\n| Plane | size, subdivisions |\n| Circle | vertices, radius, fill type |\n| Grid | X/Y subdivisions, size |\n\n## Transforms\n\nMove, rotate, and scale with the gizmo. The transform tools are bound to `G` (grab/move), `R` (rotate), and `S` (scale); constrain to an axis by pressing `X`, `Y`, or `Z` during the operation. Transforms respect a configurable **pivot point** and an optional **snap** target.\n\nProportional editing (`O`) spreads a transform smoothly across nearby geometry with a selectable falloff.\n\n## Edit mode\n\nPress `Tab` to enter edit mode. Select vertices, edges, or faces (`1` / `2` / `3`) and run mesh operations. The engine exposes a broad set of edit operations, including:\n\n- Extrude (region and individual), inset faces\n- Bevel edges and vertices\n- Loop cut, knife, bridge edge loops\n- Fill, grid fill, poke faces\n- Merge, split, dissolve, collapse\n- Triangulate, tris-to-quads\n- Recalculate / flip normals, separate\n- Mark / clear seams and sharp edges\n\nCore operations run on the real geometry — extrude and inset, for example, operate on the selected faces of the actual mesh (`viewportEngine.extrudeFaces` / `insetFaces`), not just a preview.\n\n## The modifier stack\n\nModifiers are **non-destructive**: they sit in an ordered stack on the object and can be toggled, reordered, or removed at any time without altering the base mesh. Add them from the Modifiers tab. The modifiers offered in the Add-Modifier menu are:\n\n| Modifier | What it does |\n|----------|--------------|\n| Subdivision Surface | Catmull-Clark or simple subdivision |\n| Mirror | Mirror across X / Y / Z with merge and clipping |\n| Bevel | Round or chamfer edges |\n| Boolean | Union / intersect / difference against a target object |\n| Array | Repeat with an offset and count |\n| Solidify | Give a surface thickness |\n| Decimate | Reduce polygon count |\n| Remesh | Rebuild topology |\n| Displacement | Push geometry along a height/vector map |\n| Geometry Nodes | A slot for a procedural node graph |\n\n## Constraints\n\nObjects can carry constraints — track-to, copy location/rotation/scale/transforms, limit location/rotation/scale, child-of, floor, follow-path, and others — each with an influence value and an optional target object. Manage them from the Constraints tab.\n\n## History\n\nStructural operations push an undo entry through the shared `@xeno/core` history manager (up to 50 steps). Undo with `Ctrl+Z`, redo with `Ctrl+Shift+Z` or `Ctrl+Y`."
        },
        {
          "slug": "sculpting-and-painting",
          "title": "Sculpting and painting",
          "description": "The sculpt brush system, weight painting for rigs, vertex color painting, and cloth simulation.",
          "body": "## Sculpting\n\nEnter Sculpt mode (`Tab` cycles into it, or press `X` in object mode to grab the sculpt tool) to shape a mesh with brushes. The sculpt engine defines **fifteen brush types**:\n\ndraw, smooth, flatten, inflate, grab, pinch, crease, clay strips, blob, layer, nudge, snake hook, thumb, mask, and multires displacement. (The sculpt panel exposes the first fourteen.)\n\nEach brush is configured by:\n\n- **Radius** and **strength**\n- **Hardness** of the falloff\n- **Pressure** — use pen pressure with a configurable pressure curve\n- **Symmetry** across any combination of X / Y / Z axes\n- **Spacing** between brush dabs\n- **Auto-smooth** applied after the stroke\n- A **dynamic topology** option with a detail size and method (relative / constant / brush)\n\nMasking is supported so you can protect areas of the mesh from a stroke.\n\n> Note: sculpting today runs on the CPU via the Three.js path. GPU-compute sculpting for very high polygon counts is part of the planned Rust engine ([Introduction](/docs/3d/introduction#planned-and-not-yet-shipped)).\n\n## Weight painting\n\nWeight painting assigns per-vertex weights to **vertex groups**, which drive how an armature deforms a mesh. In Weight Paint mode you paint weights with a brush that has four modes:\n\n- **Add** and **Subtract** — increase or decrease weight\n- **Mix** — blend toward the target weight\n- **Blur** — smooth weights across neighbors\n\nWeights are shown with a **thermal color ramp** (cold to hot) overlay, so you can read influence at a glance. Painting can **mirror across X** for symmetric characters and **auto-normalize** so each vertex's weights sum correctly. The engine also provides weight smoothing and uniform-weight fills.\n\n## Vertex color painting\n\nPaint colors directly onto mesh vertices across one or more **vertex color layers**. Each layer has a blend mode (multiply / mix / add / overlay) and opacity. The brush supports paint, blur, smudge, and sample modes with a primary and secondary color, optional pressure, and a viewport preview.\n\n## Cloth simulation\n\nXENO Form includes an **XPBD cloth solver**. A cloth object is a particle grid connected by **distance** and **bending** constraints, integrated over configurable substeps for stability. You can:\n\n- Set mass, structural and bending stiffness, and damping\n- Apply gravity and a **wind** force with turbulence\n- **Pin** vertices (for example, pin the top row to hang a curtain)\n- Enable collision against scene objects and **self-collision**\n- Add pressure and a shrink factor\n\nThe simulation steps and produces updated positions and normals for the mesh. It is exposed both as an interactive preview and as reusable engine functions (`createClothGrid`, `simulateStep`, `pinTopRow`, and related helpers)."
        },
        {
          "slug": "materials-and-rendering",
          "title": "Materials and rendering",
          "description": "The node-based PBR material editor, viewport shading, texture baking, HDRI lighting, and the CPU path tracer.",
          "body": "## Materials\n\nEvery mesh can hold one or more **material slots**. A slot has quick PBR properties — base color, metallic, roughness, emission and strength, alpha, and a normal map — and can additionally be driven by a **node graph** for full control.\n\n### The node material editor\n\nThe material editor is a visual node graph. Node types include:\n\n- **Shaders**: Principled BSDF, Diffuse BSDF, Glossy BSDF, Emission, Mix Shader\n- **Textures**: image, noise, voronoi, gradient, checker\n- **Color**: color ramp, mix color, and adjustment nodes\n- **Vector / math**: math, vector math, mapping, texture coordinate\n- **Utility**: fresnel, bump, normal map\n- **Output**: Material Output\n\nWhen you wire nodes together, the graph is **evaluated to concrete PBR parameters**: the editor runs a topological sort over the connected nodes and resolves the base color, metallic, roughness, and emission that feed the Material Output (it can also generate GLSL for those connections). This is what lets the viewport preview a graph-driven material.\n\n## Environment lighting\n\nThe scene environment can be a solid color or an **HDRI** image, with controls for rotation and intensity. Radiance `.hdr` (RGBE) images are parsed for image-based lighting; the environment config carries spherical-harmonic coefficients for diffuse irradiance used by the renderer and path tracer.\n\n## Viewport rendering\n\nThe interactive viewport is a Three.js WebGL2 renderer configured for a physically based look: PCF soft shadow mapping, ACES tone mapping, and an adjustable exposure. Use the Rendered shading mode for a fuller preview while you work. Capture the viewport to a PNG at any time with `F12`.\n\n## The path tracer\n\nFor final-quality output, XENO Form includes a **CPU path tracer**. It builds a **BVH** acceleration structure using the surface-area heuristic (SAH), intersects triangles with the Moller-Trumbore test, and integrates light with cosine-weighted hemisphere sampling. Rendering is **progressive** — samples accumulate over time and the image refines — and the result is tone-mapped with ACES. Start a progressive render with `Shift+F12`.\n\nRender settings (Render tab) cover output resolution and percentage, exposure, samples-per-pixel and max bounces, output format, and color management.\n\n> The GPU (wgpu compute) and CUDA path-tracer backends described in the architecture are planned; today the path tracer runs on the CPU.\n\n## Texture baking\n\nBake surface and lighting information into textures in UV space. Supported bake types are normal, ambient occlusion, diffuse, position, curvature, displacement, shadow, and a combined bake. You choose the output resolution, a UV-island **margin** to prevent seam bleeding, and the sample count. Baking rasterizes triangles into UV space and evaluates the chosen signal per texel."
        },
        {
          "slug": "animation-and-rigging",
          "title": "Animation and rigging",
          "description": "The timeline and keyframes, armatures and bones, shape keys, and camera animation paths.",
          "body": "## The timeline\n\nAnimation is driven by a **timeline** with a current frame, a frame range, a frame rate, and playback. Press `Space` to play or pause. Scrubbing the timeline updates the scene to that frame.\n\n## Keyframes\n\nAnimate a property by setting **keyframes**. A keyframe records a frame, the target object and property, and a value, with an interpolation mode of **linear**, **bezier**, or **constant** (stepped). Bezier keys carry left/right handles for curve shaping. The timeline shows keys per object so you can retime animation.\n\n## Armatures and rigging\n\nRig a character with an **armature** — a hierarchy of **bones**. Each bone has a head and tail position, a roll, a parent, and flags for whether it deforms the mesh and participates in an IK chain (with target and pole-target references). Armatures can display as octahedral, stick, or wire.\n\nBuild the skeleton, bind a mesh to it, and refine influence in [Weight Paint mode](/docs/3d/sculpting-and-painting#weight-painting). Note that skeletal deformation and IK solving are part of the data model and are still maturing.\n\n## Shape keys\n\n**Shape keys** (morph targets / blend shapes) store per-vertex position deltas relative to a **Basis** shape. Blend a key in with a value between its min and max to morph the mesh — the classic approach for facial animation and correctives. The first shape key you add on an object becomes the Basis automatically, and the Data tab lists shape keys with their values.\n\n## Camera animation\n\nAnimate a camera along a path with control points. A camera animation carries a path type (linear, bezier, catmull-rom, or b-spline), a set of points each with position, look-at target, and field of view, a duration in frames, and an easing function. Options include following the path orientation, tracking a target object, camera roll, and depth-of-field with focal distance and aperture — enough to block out dolly, track, and crane moves."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "reference",
          "title": "Reference",
          "description": "File formats and I/O, the project format, keyboard shortcuts, AI/mesh-processing status, and feature maturity.",
          "body": "## Files and formats\n\nXENO Form reads and writes several interchange formats. The table reflects what is implemented **today** versus planned. Export is the most complete path; mesh **import** for OBJ / glTF / STL currently creates scene objects (names, transforms, hierarchy) but does not yet fully reconstruct the mesh geometry into the viewport.\n\n| Format | Import | Export | Notes |\n|--------|--------|--------|-------|\n| OBJ / MTL | Partial | Yes | Export writes mesh + materials; import validates and adds a scene object |\n| glTF / GLB | Partial | Yes | Export full; import extracts nodes (names, transforms) as objects |\n| STL | Partial | Yes | Export binary and ASCII with mesh validation; import adds a placeholder object |\n| USD (USDA) | Yes | Yes | ASCII round-trip — scene hierarchy, transforms, mesh data, cameras, lights |\n| HDR (Radiance) | Yes | — | RGBE parsed for environment lighting |\n| OpenEXR | — | Yes | Path-tracer / render image output |\n| USDC / USDZ | Planned | Planned | Binary / packaged USD |\n| FBX | Planned | Planned | Full scene, animation, rigging |\n| Alembic (ABC) | Planned | Planned | Baked animation / simulation caches |\n| PLY | Planned | Planned | Point clouds, scans |\n\nThe open-file dialog lists the full set of extensions (including planned ones), but only the formats above will load at this stage. USD support is centered on the **USDA** ASCII format, which parses and writes prims for transforms, meshes, cameras, and lights and round-trips cleanly.\n\n### The project format\n\nNative XENO Form projects use the **`.xeno3d`** extension. Saving serializes the scene state; opening restores it. Autosave runs on an interval (two minutes by default), and the app checks for an unsaved session on startup and offers **crash recovery**.\n\n## Keyboard shortcuts\n\nTranscribed from the app's key handler. Text-input fields swallow these keys, so shortcuts only fire when the viewport (not a text box) has focus.\n\n### Global (with Ctrl or Cmd)\n\n| Keys | Action |\n|------|--------|\n| Ctrl+Z | Undo |\n| Ctrl+Shift+Z | Redo |\n| Ctrl+Y | Redo |\n| Ctrl+S | Save |\n| Ctrl+Shift+S | Save As |\n| Ctrl+D | Duplicate selected object |\n\n### Tools and transforms\n\n| Key | Action |\n|-----|--------|\n| W | Select tool |\n| G | Move (translate gizmo) |\n| R | Rotate |\n| S | Scale |\n| E | Extrude (runs extrude-region in edit mode) |\n| I | Inset (edit mode) |\n| K | Knife tool |\n| B | Box select |\n| C | Circle select |\n| U | UV tool |\n| D | Annotate tool |\n| X | Sculpt tool (object mode) / constrain to X axis (edit or transform) |\n| Y | Constrain to Y axis (edit or transform) |\n| Z | Constrain to Z axis (edit or transform) |\n| O | Toggle proportional editing |\n| Delete / Backspace | Delete selected object |\n\n### Modes, view, and playback\n\n| Key | Action |\n|-----|--------|\n| Tab | Cycle Object to Edit to Sculpt |\n| 1 | Vertex select (edit) / Front view (object) |\n| 2 | Edge select (edit) |\n| 3 | Face select (edit) / Right view (object) |\n| 5 | Toggle perspective / orthographic |\n| 7 | Top view |\n| 0 | Camera view |\n| . | Frame selected |\n| Space | Play / pause animation |\n| F12 | Render viewport to PNG |\n| Shift+F12 | Start progressive path-traced render |\n\n## AI and mesh processing\n\nXENO Form wires **three** mesh operations to the `xeno-lib` native module: auto-retopology, UV unwrapping (ABF++ / LSCM), and mesh decimation. These are gated on the native module: when `@xeno/lib` is not installed, each call returns a clear error (\"xeno-lib is not installed…\") rather than failing silently, so the UI degrades gracefully.\n\nThe broader AI 3D vision in the architecture — text-to-mesh, AI texturing, auto-rig, pose estimation, style transfer — is roadmap and is not yet wired into the app. Treat AI features as in development.\n\n## Feature status at a glance\n\n| Area | Status |\n|------|--------|\n| Viewport (Three.js / WebGL2, PCF soft shadows, ACES) | Working |\n| Mesh primitives, object/edit mode, gizmos | Working |\n| Modifier stack (10 in the Add-Modifier menu) | Working |\n| Sculpting (15 brush types, pressure, masks) | Working |\n| UV editor, node material editor | Working |\n| Weight paint, vertex color, cloth (XPBD) | Working |\n| Texture baking, CPU path tracer (BVH + SAH) | Working |\n| Animation timeline, keyframes, playback | Working |\n| Armatures, shape keys, camera paths | Partial (data model + Properties) |\n| OBJ / glTF / STL export, USDA round-trip, `.xeno3d` projects, autosave, crash recovery | Working |\n| OBJ / glTF / STL mesh import (full geometry) | Partial |\n| Rust/wgpu engine, deferred pipeline, GPU sculpt, GPU/CUDA path tracer | Planned |\n| AI 3D (text-to-mesh, auto-rig, AI texture), Python scripting, FBX/Alembic/PLY | Planned |\n\nSee [Introduction](/docs/3d/introduction) for the fuller picture of what alpha means for XENO Form."
        }
      ]
    }
  ]
};

export default form;
