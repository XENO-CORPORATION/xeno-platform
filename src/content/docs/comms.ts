import type { ProductDocs } from './_types';

const comms: ProductDocs = {
  "slug": "comms",
  "productName": "XENO Comms",
  "tagline": "Universal human and agent messaging — DMs, groups, media, voice notes, and receipts, with AI agents modeled as first-class members.",
  "seo": {
    "title": "XENO Comms documentation",
    "description": "Install and use XENO Comms — the beta messenger where humans and AI agents share one identity model. Conversations, media, receipts, multi-device sync, phone discovery, encryption status, and the client SDK."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Comms is, what makes it different, its real maturity, and where it fits in the XENO ecosystem.",
          "body": "## Introduction\n\nXENO Comms is a universal communications platform. It starts as a WhatsApp and Telegram grade messenger — reliable sync, push notifications, direct messages, groups, media, voice notes, receipts, and a local cache — and is designed to grow from there into Slack, Microsoft Teams, Google Chat, and Discord style workspaces.\n\nThe first shipping target is the Windows desktop app. Mobile (iOS and Android) is built into the architecture and codebase, but its release is paused until the Windows messenger is proven. Web and the Hub/Shell embed come after that.\n\n## What makes it different\n\nThe defining architectural decision is that agents are modeled as first-class members, not bots bolted onto a chat window. There is a single users table with a kind column (human or agent), so a human and an agent are the same kind of entity in the data model, and an agent-authored message is its own first-class message kind. The design intent is that an agent has an identity and a profile, can be a conversation member, and can hold conversations with humans and other agents — and eventually act across other XENO apps without leaving the conversation.\n\nThat is the direction, not a description of a finished feature. The [Agents](/docs/comms/agents) page is precise about what is live today versus what is gated for a later phase.\n\n## Where it fits\n\nXENO Comms is Layer 6 (Office, Productivity and Communications) of the XENO ecosystem, and it is also intended to be the realtime substrate other apps build multiplayer on top of. It does not own identity, billing, or credits — those live on the XENO platform (xenostudio.ai). Comms validates platform tokens and owns only communication state: conversations, messages, devices, receipts, and presence.\n\n## Maturity\n\nXENO Comms is in beta (v0.1.0) with a live backend. Read this before you rely on it:\n\n- The Windows desktop app is the active launch target. Mobile is paused; macOS and Linux desktop are not launch-ready.\n- Encrypted conversations use an alpha sealed-message path, not production Signal-protocol end-to-end encryption. Encryption is gated behind server feature flags and is off by default in the launch configuration. See [Encryption](/docs/comms/encryption).\n- Live agent participation is gated off by default at launch (agent invocation is disabled pending token validation, grants, and cost controls). See [Agents](/docs/comms/agents).\n- Voice and video calls are not part of the first messaging launch.\n- Workspaces, channels, threads, full-text search, the web app, and the Hub/Shell embed are later phases.\n\nWherever a capability is planned but not shipping, this documentation says so plainly.\n\n## Next steps\n\n- [Getting started](/docs/comms/installation) — install, sign in, and send your first message\n- [Conversations](/docs/comms/conversations) — DMs, groups, and how chats are organized\n- [Encryption](/docs/comms/encryption) — what \"encrypted\" means today\n\n> XENO Comms is proprietary software, © 2026 XENO Corporation. All rights reserved."
        },
        {
          "slug": "installation",
          "title": "Getting started",
          "description": "Install the Windows desktop app, sign in, register your device, and send your first message.",
          "body": "## Getting started\n\nThe active, shipping surface is the Windows desktop app, built with Electron. This page covers installing it, signing in, and sending your first message.\n\n## Install on Windows\n\nDownload the signed installer from the XENO Comms download page on xenostudio.ai and run it.\n\nUse only production builds from XENO. Do not use local alpha or developer builds for real conversations — they may point at development infrastructure and do not carry the privacy guarantees of a release build.\n\n## Updates\n\nThe desktop app auto-updates. It reads its release feed from the XENO platform CDN (updates.xenostudio.ai/apps/comms/). When a newer version is published, the app detects it and offers the update — you do not need to re-download the installer manually. The download page always resolves to the latest published version.\n\n## Sign in\n\n1. Open XENO Comms and choose Sign in.\n2. Complete XENO platform authentication in the browser (sign-in is hosted on xenostudio.ai).\n3. Return to the app when the browser redirects to `xeno-comms://auth`.\n\nXENO Comms does not own your login. It authenticates you against the XENO platform — the same identity you use elsewhere in the ecosystem — and receives a token it uses to talk to the Comms API. If sign-in fails, check your network, your app version, and whether XENO platform auth is reachable.\n\nOn first sign-in, the app registers this machine as one of your devices, so messages can sync to all of them and so you can revoke a device you no longer trust. See [Devices and sync](/docs/comms/devices-and-sync).\n\n## Start a conversation\n\nFrom the Chats list you can start a direct message with another user, or create a group with several members. You can add members by their XENO user, and — where they have made themselves discoverable — by phone number. See [Contacts and discovery](/docs/comms/contacts-and-discovery).\n\nThere is also a Saved chat: a private conversation with only you, useful for notes, links, and files you want on every device.\n\n## Send your first message\n\nType in the composer and send. You can send text with an emoji picker (recent emojis are remembered), attach images, video, and files, record a voice note, and edit or delete a message after sending. As you type, the people in the conversation see a typing indicator; when your message is delivered and read, you get receipts back. See [Messaging and media](/docs/comms/messaging-and-media).\n\nText sends are cached locally, so if you close the app or lose your connection, a message you were sending is kept and can be retried rather than lost.\n\n## Other surfaces\n\nThese are part of the codebase but are not the current install target:\n\n- Mobile (iOS and Android) — React Native builds; release paused until the Windows app is proven.\n- macOS and Linux desktop — Electron builds exist, but signing, packaging, update, and push are not separately proven for launch.\n- Web (chat.xenostudio.ai) and the Hub/Shell embed — later phases.\n\n## Running the backend locally (developers)\n\nIf you are developing against Comms rather than using the app, the repository ships a real local stack (Postgres, ScyllaDB, and NATS — not mocks). See [Developer reference](/docs/comms/developer) for the client SDK, and the repository's BACKEND.md for running the gateway, API, and workers.\n\n## Next steps\n\n- [Conversations](/docs/comms/conversations)\n- [Messaging and media](/docs/comms/messaging-and-media)\n- [Devices and sync](/docs/comms/devices-and-sync)"
        }
      ]
    },
    {
      "title": "Using XENO Comms",
      "pages": [
        {
          "slug": "conversations",
          "title": "Conversations",
          "description": "Direct messages, groups, saved chats, roles, and how the chat list is organized.",
          "body": "## Conversations\n\nEverything in XENO Comms is a conversation. The same underlying model powers private messaging today and is designed to power workspace channels later, so the concepts here carry forward.\n\n## Conversation types\n\n- Direct message (dm) — exactly two members. Private one-to-one messaging. A member can be a human or an agent user.\n- Group — a private group in the WhatsApp/Telegram sense, with multiple members and roles.\n- Saved — a private conversation with only you; a place for notes and files that syncs across your devices.\n\nTwo more types exist in the data model but belong to later phases and are not part of the messenger launch:\n\n- Channel — Slack/Teams style workspace channel (later phase).\n- Thread — a thread attached to a parent message (later phase).\n\nThey are present in the schema so that adding workspaces later does not require a painful migration. Today, you work with DMs, groups, and saved chats.\n\n## The chat list\n\nThe desktop app organizes your conversations under Chats, with quick scopes to filter them:\n\n- All\n- Unread\n- Favorites\n- Groups\n\nConversations can be muted, archived, and pinned to the top of the list, and the Favorites scope filters those you have marked.\n\n## Members and roles\n\nGroup members have a role: admin or member. Admins manage the conversation — adding and removing members and changing group settings. You can:\n\n- Add a member.\n- Remove a member (admin).\n- Change a member's role (admin).\n- Leave a conversation.\n\n## Conversation preferences\n\nEach conversation carries your personal preferences, independent of other members — for example your notification level for that chat (all, mentions, or muted), plus archived, pinned, and mark-unread state. See [Privacy and safety](/docs/comms/privacy-and-safety) for notification behavior and [Encryption](/docs/comms/encryption) for the per-conversation security state.\n\n## Next steps\n\n- [Messaging and media](/docs/comms/messaging-and-media)\n- [Agents](/docs/comms/agents) — agents as conversation members\n- [Privacy and safety](/docs/comms/privacy-and-safety)"
        },
        {
          "slug": "messaging-and-media",
          "title": "Messaging and media",
          "description": "Text, emoji, media and voice notes, editing and deleting, receipts, typing, and disappearing messages.",
          "body": "## Messaging and media\n\n## Message kinds\n\nA message is one of a small set of kinds:\n\n- text — plain text (with emoji).\n- media — an image, video, or file attachment.\n- voice_note — a recorded voice message.\n- system — a conversation event (someone joined, left, and so on).\n- agent — a message authored by an agent member.\n\n## Sending text\n\nType in the composer and send. The desktop app includes an emoji picker with categories and a Recent list that remembers the emoji you use most. A text message is limited to 16 KB.\n\nMessages are sent with a per-device client message id, which makes sending idempotent: if a send is retried after a flaky connection, the server returns the original message rather than posting a duplicate.\n\n## Editing and deleting\n\nYou can edit a message after sending it, and you can delete a message. Both are first-class operations that propagate to everyone in the conversation in realtime.\n\n## Media and voice notes\n\nAttachments never travel over the realtime connection — they go to object storage. The flow is:\n\n1. The app requests a signed upload URL for the file.\n2. The app uploads the bytes directly to storage.\n3. A worker scans the file and, once it is marked ready, the attachment can be referenced by a message.\n\nBecause of the scan-and-quarantine step, a rejected or still-scanning file cannot be attached to a message. Voice notes are stored as attachments; transcription is a later integration and does not block sending.\n\n## Receipts and typing\n\nXENO Comms gives you durable delivery and read receipts:\n\n- Delivered means a device accepted the message.\n- Read means a user explicitly opened the conversation past that message.\n\nTyping indicators show when someone is composing. Typing is the only thing clients publish directly over the realtime socket; durable events like messages and read receipts always go through the API, so they cannot be forged from a socket.\n\nYou can turn read receipts off in your privacy settings — see [Privacy and safety](/docs/comms/privacy-and-safety).\n\n## Disappearing messages\n\nConversations support a disappearing-message timer. The desktop app offers preset durations (one hour, one day, one week); the underlying model allows timers up to 90 days.\n\n## Offline and retry\n\nText sends are kept in a local outbox in OS-backed secure storage. If the app restarts while a message is pending, it comes back as a retryable item rather than vanishing. Media has its own local outbox as well.\n\n## Next steps\n\n- [Devices and sync](/docs/comms/devices-and-sync)\n- [Encryption](/docs/comms/encryption)\n- [Privacy and safety](/docs/comms/privacy-and-safety)"
        },
        {
          "slug": "devices-and-sync",
          "title": "Devices and sync",
          "description": "Multi-device registration, pairing, revocation, and how messages sync and back-fill.",
          "body": "## Devices and sync\n\n## Multiple devices, one account\n\nYou can sign in on several devices, and each is registered separately. Device kinds include windows, macos, linux, ios, android, web, embed, and agent. Every message you send or receive is meant to be present on all of your devices.\n\nYou can view your devices under Settings and revoke any device you no longer trust — a lost machine, an old install, or one you do not recognize. Revoking a device ends its sessions and stops it receiving messages.\n\n## Pairing a desktop\n\nWhen you already have an account on one device, you can pair a new desktop through the platform sign-in or pairing flow. A device pairing code is generated on one device and claimed on another to bind the new desktop to your account. After pairing, confirm the new device appears in your Devices list.\n\n## How sync works\n\nWhen a device connects or reconnects, it catches up from where it left off:\n\n1. The device reconnects with its device id and its last known sync cursor.\n2. It asks for the conversations it is authorized to see.\n3. It pulls any messages it missed since its cursor, in order, and receives the next cursor to continue from.\n4. It acknowledges what it received, which drives delivery receipts, and marks explicit reads as you open conversations.\n\nDelivery is tracked per device (so each device backfills independently), while read state is tracked per user (so reading on one device clears unread everywhere).\n\n## Reliability\n\nRealtime delivery is decoupled from any single provider being healthy. Every server-originated event is written to a durable outbox before it is fanned out, and workers replay that outbox — so a momentary hiccup in the realtime path or in push does not drop your messages.\n\n## Local cache\n\nThe desktop app keeps an on-disk cache of your conversations, drafts, and recently viewed attachments so the app opens with your history and survives restarts and offline periods.\n\n## Next steps\n\n- [Contacts and discovery](/docs/comms/contacts-and-discovery)\n- [Privacy and safety](/docs/comms/privacy-and-safety) — revoking devices, account deletion"
        },
        {
          "slug": "contacts-and-discovery",
          "title": "Contacts and discovery",
          "description": "Phone-number identity, verification, contact discovery, and discovery privacy.",
          "body": "## Contacts and discovery\n\nXENO Comms lets people find each other the way a phone messenger does — by phone number — while keeping raw phone numbers off the server and putting you in control of who can find you.\n\n## Phone identity\n\nYou can bind one or more verified phone numbers to your account. Verification is by SMS or voice code. Once verified, a number becomes a phone identity attached to your account.\n\nComms does not store your raw phone number. It stores a hash of it, plus your country calling code and the last four digits for display. That is enough to show \"+1 ••• ••• 1234\" and to power discovery, without keeping the full number.\n\n> In local development, a dev-only verification code is returned so you can test the flow without a live SMS/voice provider. In production, verification requires the configured provider — Comms never pretends a code was delivered when it was not.\n\n## Contact discovery\n\nDiscovery lets you find which of your contacts are on XENO Comms. Your device sends SHA-256 hashes of contacts' phone numbers (in E.164 form), and the service returns only the users whose privacy settings permit being discovered by you. Raw numbers are never uploaded — only hashes.\n\n## Discovery privacy\n\nEach of your verified numbers has its own discovery visibility:\n\n- everyone — anyone who has your number can find you.\n- contacts — only people you have as contacts.\n- nobody — you are not discoverable by number.\n\nYou control this per number, and you can change it at any time.\n\n## Using discovery when starting a chat\n\nWhen you create a conversation, you can add members by their XENO user, and you can enter phone numbers (prefixed with +). Numbers are resolved through discovery to the matching users; a number that resolves to a discoverable user is added as that user.\n\n## Next steps\n\n- [Conversations](/docs/comms/conversations)\n- [Privacy and safety](/docs/comms/privacy-and-safety)"
        },
        {
          "slug": "encryption",
          "title": "Encryption",
          "description": "What \"encrypted\" means in XENO Comms today — the alpha sealed-message path, off by default at launch — and what production E2EE still requires.",
          "body": "## Encryption\n\nThis page is deliberately precise, because encryption is the one place where an overstated claim is a real harm. XENO Comms is in beta, and its encryption is honestly described as an alpha sealed-message path — not production Signal-protocol end-to-end encryption.\n\n## Gated off by default at launch\n\nEncrypted conversation creation and encrypted message sends are gated behind server feature flags and are turned off by default in the launch configuration (the operator flags for E2EE conversation creation and E2EE message sends default to disabled) until E2EE launch approval. In the shipping launch build, conversations therefore default to unencrypted, cloud-synced messaging. The encrypted path below is implemented and can be enabled by operators, but it is not the default experience today.\n\n## What the implemented path does\n\nA conversation has a security state: none, ready, or enabled. When the encrypted path is enabled for a conversation, the following is what the implementation does:\n\n- Devices generate their private key material locally. The server never receives private keys.\n- Each device publishes public key material: an identity key, a signed pre-key with its signature, and a batch of one-time pre-keys.\n- Before starting an encrypted session, clients fetch the other members' device key bundles, claiming one one-time pre-key per device, and verify the signed pre-keys.\n- Encrypted conversations reject plaintext at the API. The server will not store a plaintext body for a conversation marked encrypted — it accepts only the encrypted payload and its header.\n- The client encrypts a message for every active target device and, on the receiving side, decrypts and renders the message.\n- Private keys are stored in OS-backed secure storage — Electron safeStorage on desktop. Packaged desktop fails closed if secure storage is unavailable.\n- You can derive a safety number from identity keys to compare with the other party, and the app can flag when a conversation's device roster changes.\n- Notifications for encrypted conversations are redacted to generic copy — the server never puts encrypted content into a push.\n\n## What is NOT done yet\n\nThe current implementation proves the end-to-end product path, but it is not the final, audited protocol. Before XENO can claim production-grade E2EE, the following are still required:\n\n- Full double-ratchet session state (libsignal-grade), not the alpha sealed-message construction.\n- Group sender keys for efficient encrypted group messaging.\n- QR-code or independently reviewed device-to-device key verification.\n- An independent security review.\n\nUntil that work lands and is reviewed, XENO Comms and its documentation must not claim production Signal-protocol E2EE. This is a hard rule in the project, not a disclaimer of convenience.\n\n## Consequences of encryption\n\nBecause the server cannot read encrypted conversations:\n\n- Search, link previews, and support tooling cannot inspect encrypted message plaintext.\n- Push notifications for encrypted conversations show generic copy.\n- Moderation staff review metadata and user-provided evidence only — staff must never request your private keys.\n\n## The roadmap goal\n\nThe target is to have both qualities Telegram splits between its two modes: cloud-grade multi-device sync for normal conversations, and first-class end-to-end encryption for private DMs and groups, with key verification and disappearing messages. Full encryption rollout is a later phase (Phase 1B).\n\n## Next steps\n\n- [Privacy and safety](/docs/comms/privacy-and-safety)\n- [Help and limitations](/docs/comms/help)"
        },
        {
          "slug": "agents",
          "title": "Agents",
          "description": "How agents are modeled as first-class members rather than bots — and exactly how far that goes in the launch build.",
          "body": "## Agents\n\nThe defining idea of XENO Comms is that agents are modeled as members, not bots. This page is precise about what that means in the data model versus what is live in the launch build.\n\n## First-class in the model\n\nIn most chat apps a bot is a second-class citizen — a webhook with a name. In XENO Comms there is a single users table with a kind column, so a human and an agent are the same kind of entity. Concretely, the model already provides:\n\n- a user kind of agent, so an agent is a real user;\n- a first-class message kind of agent, so an agent's message is treated exactly like a human's in the conversation model;\n- a per-message credit-cost field, so agent work can be metered.\n\nBecause conversation membership is by user id, a user with kind agent can be a member of a DM or group.\n\n## The AgentGateway\n\nAgents do not connect through the same realtime gateway humans use. There is a dedicated AgentGateway service (a separate Elixir/Phoenix app) with an agent socket and a per-conversation agent channel that delivers scoped events to the right agent and handles event acknowledgement. It exists in the repo today.\n\n## What is gated off at launch\n\nThis is the honest limit: end-to-end agent participation is disabled by default in the launch configuration. The operator flag for agent invocation defaults to off until AgentGateway token validation, agent grants, and cost controls are approved. So while the model and the gateway are in place, agents acting inside conversations are not turned on in the shipping build yet.\n\n## What comes later\n\nIn a later phase, the AgentGateway gains depth: agent grants and token validation, a mention dispatcher that invokes the XENO agent runtime (xeno-agent-sdk), channel subscriptions, cost controls, and agents that act across other XENO apps (Pixel, Motion, Sound, and others) from inside the conversation. That cross-app reach is the long-term payoff of making agents members: you can ask an agent to do work in another app without leaving the chat.\n\n## Next steps\n\n- [Conversations](/docs/comms/conversations)\n- [Developer reference](/docs/comms/developer) — the client SDK and realtime model"
        }
      ]
    },
    {
      "title": "Privacy and safety",
      "pages": [
        {
          "slug": "privacy-and-safety",
          "title": "Privacy and safety",
          "description": "Read receipts, presence, blocking, reporting, notifications, and account export and deletion.",
          "body": "## Privacy and safety\n\nXENO Comms puts the privacy controls a modern messenger needs in your Account settings, and backs them with server-side enforcement.\n\n## Your privacy controls\n\nFrom Account settings you can:\n\n- control read receipts (whether others see when you have read a message);\n- control presence visibility (whether others see when you are online);\n- choose who can invite you to groups;\n- manage phone discovery visibility per number (see [Contacts and discovery](/docs/comms/contacts-and-discovery));\n- block and unblock users;\n- report abuse;\n- export your account data;\n- delete your Comms account.\n\n## Blocking\n\nBlocking a user is a first-class action with its own list you can manage. You can block and later unblock; blocked conversations do not notify you.\n\n## Reporting\n\nUse Report on a conversation or message when you encounter spam, abuse, harassment, impersonation, illegal content, or other safety issues. Reports carry a reason (spam, abuse, harassment, illegal, other) and move through review states (open, reviewing, resolved, dismissed). Staff review metadata and the evidence you provide — and, importantly, staff must never request your end-to-end encryption private keys.\n\nMedia is scanned on upload; files that are rejected or quarantined cannot be attached to messages.\n\n## Notifications\n\nEach conversation has a notification level:\n\n- all — notify on every message.\n- mentions — notify only when a message mentions you.\n- muted — do not notify.\n\nMuted and blocked conversations do not push. Encrypted conversations always use generic notification copy, because the server never sees their contents. Notification delivery depends on the platform push providers (APNs, FCM, and WNS on Windows) and can be turned off globally during incidents by operators.\n\n## Account export\n\nYou can export your Comms account data. The export contains the account metadata Comms owns about you.\n\n## Account deletion\n\nDeleting your Comms account:\n\n- disables the account;\n- clears your profile identity fields;\n- revokes your devices, sessions, push tokens, and phone numbers;\n- removes your contact imports.\n\nWhat deletion does not do: it cannot remove copies of messages already delivered to other conversation members, and it does not erase moderation evidence or audit records that must be retained for safety, security, and abuse investigation.\n\n## What Comms collects, briefly\n\nComms collects what it needs to run messaging: your account profile, device data, phone identity (hashed), conversation metadata, message content (encrypted payloads only, for encrypted conversations), push tokens (encrypted at rest), and moderation/audit records. It does not sell personal data and does not use advertising IDs. The full, authoritative statement lives in the product's privacy policy.\n\n## Next steps\n\n- [Encryption](/docs/comms/encryption)\n- [Help and limitations](/docs/comms/help)"
        }
      ]
    },
    {
      "title": "Developer reference",
      "pages": [
        {
          "slug": "architecture",
          "title": "Architecture",
          "description": "The polyglot backend — gateway, API, workers, and storage — and how a message flows through it.",
          "body": "## Architecture\n\nXENO Comms is polyglot by design: each layer uses the language best suited to it.\n\n## Services\n\n| Service | Language | Owns |\n|---|---|---|\n| Gateway | Elixir / Phoenix | WebSocket sessions, conversation subscriptions, presence, typing, realtime fanout |\n| AgentGateway | Elixir / Phoenix | Agent subscriptions, scoped agent event delivery, event acknowledgement |\n| API | Rust / Axum | Authenticated REST, conversation and message writes, device/session registration, signed upload URLs |\n| Workers | Go | Push notifications, media processing, audit mirroring, outbox replay |\n\n## Storage\n\n| Store | Role |\n|---|---|\n| PostgreSQL 16 | Low-cardinality transactional state (users, devices, conversation membership, pointers) |\n| ScyllaDB | Messages, receipts, reactions, sync windows, audit log — the message-scale tables from day one |\n| NATS JetStream | Event backbone for realtime fanout and worker jobs |\n| Dragonfly | Cache and rate-limit state — never a source of truth |\n| OpenSearch | Search (later phase) |\n| Cloudflare R2 | Attachment bytes and derived assets |\n\nNote: some ScyllaDB tables (for example reactions and threads) are provisioned in the schema ahead of the user-facing features that will use them, so later phases do not require a painful migration.\n\n## How a message is written\n\nThe Rust API is the only writer for user-created messages. A send flows like this:\n\n1. The client posts the message to the API.\n2. The API validates the token, the device, and conversation membership.\n3. It inserts the message into ScyllaDB (the source of truth for message bodies) and updates the Postgres pointers (last message, ordering).\n4. It writes the realtime event to a durable outbox, then publishes to NATS.\n5. The Phoenix Gateway fans the event out to connected clients.\n6. Go workers pick up push, media, and audit jobs from the same event.\n\nKey invariants: Postgres holds pointers and low-cardinality state only; ScyllaDB is the message source of truth; the cache never becomes a source of truth; and every server-originated realtime event is written to the outbox before fanout so recovery is not coupled to any provider's availability.\n\n## Identity and auth\n\nComms does not own OAuth, billing, credits, or model catalog logic — those stay on the XENO platform. It owns communication identity: users (human and agent), user identities (lookup hashes), devices, sessions, and push tokens. A client obtains a platform access token, calls the Comms API with it as a bearer token, and the API validates it against the platform before upserting local identity rows. The WebSocket gateway is joined with the same bearer token plus the device id, and only joins conversations the token authorizes.\n\n## Observability\n\nServices ship OpenTelemetry instrumentation: traces (per request/job/socket, tagged with user, device, conversation, and agent ids), structured logs correlated to trace ids, and RED plus saturation metrics for queues, sockets, and DB pools.\n\n## Next steps\n\n- [Developer reference](/docs/comms/developer) — the client SDK, REST endpoints, and realtime events\n- [Agents](/docs/comms/agents)"
        },
        {
          "slug": "developer",
          "title": "Developer reference",
          "description": "The TypeScript client SDK (CommsClient), the REST endpoint map, and the realtime channel model.",
          "body": "## Developer reference\n\nThe Comms surfaces are built on a shared TypeScript client SDK, so building against Comms means building against the same client the app uses. This page covers the client SDK, the REST surface it wraps, and the realtime model.\n\n> The client package is `@xeno-corporation/xeno-comms-client`, version 0.1.0. Types come from `@xeno-corporation/xeno-comms-types`. Both are workspace packages in the xeno-comms monorepo (not published to a public registry).\n\n## Creating a client\n\nCommsClient is constructed with your API base URL, the realtime gateway URL, a device id (a UUID), and a function that returns a platform access token:\n\n```ts\nimport { CommsClient } from \"@xeno-corporation/xeno-comms-client\";\n\nconst client = new CommsClient({\n  apiBaseUrl: \"https://api.comms.xenostudio.ai\",\n  gatewayUrl: \"wss://gateway.comms.xenostudio.ai/socket\",\n  deviceId: myDeviceId,\n  getAccessToken: async () => await getPlatformToken(),\n});\n```\n\nThe config is validated on construction: apiBaseUrl must be http/https and gatewayUrl must be ws/wss. Insecure private-network URLs are rejected unless you explicitly opt in with allowInsecurePrivateNetwork (for local development only).\n\n## Devices and identity\n\n```ts\nawait client.registerDevice({ kind: \"windows\", label, displayName /* ... */ });\nawait client.listDevices();\nawait client.revokeDevice(deviceId);\nawait client.publishDevicePrekeys({ /* identity + signed + one-time prekeys */ });\nawait client.registerPushToken({ provider: \"wns\" /* ... */ });\n```\n\nPush providers are apns, fcm, wns, and web_push. Device kinds are ios, android, windows, macos, linux, web, embed, and agent.\n\n## Phone identity and discovery\n\n```ts\nconst req = await client.requestPhoneVerification({ /* number, channel: sms|voice */ });\nawait client.verifyPhoneNumber(req.requestId, { code });\nawait client.listPhoneNumbers();\nawait client.updatePhonePrivacy(phoneNumberId, { visibility: \"contacts\" });\nawait client.discoverPhoneNumberUsers([\"+15551234567\"]); // hashes client-side\n```\n\nDiscovery visibility is everyone, contacts, or nobody. Numbers are SHA-256 hashed client-side before they reach the server.\n\n## Conversations and members\n\n```ts\nawait client.listConversations();\nawait client.createDirectConversation({ participantUserIds: [otherUserId] });\nawait client.createGroupConversation({ title, participantUserIds });\nawait client.createSavedConversation();\nawait client.updateConversation(id, { /* title, disappearing timer */ });\nawait client.updateConversationPreferences(id, { notificationLevel: \"mentions\" });\nawait client.listConversationMembers(id);\nawait client.addConversationMember(id, { userId });\nawait client.updateConversationMember(id, userId, { role: \"admin\" });\nawait client.removeConversationMember(id, userId);\nawait client.leaveConversation(id);\n```\n\n## Messages, attachments, receipts\n\n```ts\nawait client.listMessages(conversationId, 50);\nawait client.sendMessage({ conversationId, body, clientMessageId });\nawait client.editMessage(conversationId, messageId, { body });\nawait client.deleteMessage(conversationId, messageId);\n\n// media\nconst upload = await client.createAttachment(conversationId, { /* metadata */ });\n// upload bytes to the returned signed URL, then:\nawait client.completeAttachment(upload.attachmentId);\nawait client.listAttachments(conversationId);\nconst dl = await client.getAttachmentDownload(attachmentId);\n\n// sync + receipts\nconst page = await client.sync({ cursor, limit });\nawait client.markDelivered({ conversationId, messageId, syncCursor });\nawait client.markRead({ conversationId, messageId, syncCursor });\n```\n\nMessage kinds are text, media, voice_note, system, and agent. sendMessage takes a per-device clientMessageId, making retries idempotent — a duplicate send returns the original message.\n\n## Encryption helpers\n\nThe encryption mode is chosen when the conversation is created (via the `e2ee` field, one of none, ready, or enabled) — there is no separate endpoint to switch mode afterward. For an encrypted conversation, the client fetches key bundles and records verification state (see [Encryption](/docs/comms/encryption)):\n\n```ts\n// choose the mode at creation:\nawait client.createDirectConversation({ participantUserIds: [otherUserId], e2ee: \"ready\" });\n\n// key bundles + device roster for an encrypted session:\nawait client.getConversationPrekeyBundles(conversationId); // claims one prekey per device\nawait client.getConversationDeviceRoster(conversationId);\n\n// safety-number verification / device-roster trust (this is NOT a mode switch):\nawait client.updateConversationSecurityState(conversationId, {\n  safetyNumber,        // record the compared safety number\n  deviceRosterHash,    // trust the current device roster\n  // clearSafetyVerification, clearDeviceRosterTrust also supported\n});\nawait client.listConversationSecurityEvents(conversationId);\n```\n\n## Realtime\n\nRealtime is a Phoenix Channels connection. Open it and join per conversation with typed handlers:\n\n```ts\nconst conn = client.createRealtimeConnection();\nawait conn.connect();\nconst sub = await conn.joinConversation(conversationId, {\n  onMessageNew: (e) => { /* ... */ },\n  onMessageUpdated: (e) => { /* ... */ },\n  onMessageDeleted: (e) => { /* ... */ },\n  onTypingStarted: (e) => { /* ... */ },\n  onTypingStopped: (e) => { /* ... */ },\n  onDeliveryReceipt: (e) => { /* ... */ },\n  onReadReceipt: (e) => { /* ... */ },\n  onPresenceState: (p) => { /* ... */ },\n  onPresenceDiff: (p) => { /* ... */ },\n});\n\nawait sub.startTyping();\nawait sub.markRead(messageId, syncCursor);\nawait sub.leave();\n```\n\nOver the socket, clients may publish typing only. Durable events — messages and read/delivery receipts — go through the API; receipts you send through the subscription are routed to the API for you.\n\n## REST endpoint map\n\nThe SDK wraps a versioned REST API under /v1. The main routes:\n\n| Area | Endpoints |\n|---|---|\n| Health | GET /healthz, GET /readyz |\n| Account | GET /v1/account/export, POST /v1/account/delete, GET \\| PATCH /v1/account/privacy |\n| Devices | GET /v1/devices, POST /v1/devices/register, POST /v1/devices/prekeys, POST /v1/devices/push-tokens, POST /v1/devices/:id/revoke |\n| Phone | POST /v1/phone/verification-requests (+ /:id/verify), GET /v1/phone/numbers, POST /v1/phone/numbers/:id/privacy, POST /v1/contacts/phone-discovery |\n| Pairing | POST /v1/device-pairing/codes, POST /v1/device-pairing/claim |\n| Conversations | GET /v1/conversations, POST /v1/conversations/dm \\| group \\| saved, PATCH /v1/conversations/:id, /members, /preferences, /security-state (PATCH), /security-events (GET), /leave |\n| Messages | GET \\| POST /v1/conversations/:id/messages, PATCH \\| DELETE /v1/conversations/:id/messages/:messageId |\n| Attachments | GET \\| POST /v1/conversations/:id/attachments, POST /v1/attachments/:id/complete, GET /v1/attachments/:id/download |\n| Sync + receipts | GET /v1/sync, POST /v1/conversations/:id/receipts/delivered \\| read |\n| Safety | GET /v1/blocked-users, POST /v1/users/:id/block \\| unblock, POST /v1/reports |\n| Encryption | GET /v1/conversations/:id/prekey-bundles, GET /v1/conversations/:id/device-roster |\n\nAdmin moderation routes (/v1/admin/moderation/*, /v1/admin/media/quarantine/*) exist for authorized reviewers and require admin credentials (X-Xeno-Admin-Token / X-Xeno-Admin-Actor headers).\n\n## Next steps\n\n- [Architecture](/docs/comms/architecture)\n- [Encryption](/docs/comms/encryption)"
        }
      ]
    },
    {
      "title": "Help",
      "pages": [
        {
          "slug": "help",
          "title": "Help and limitations",
          "description": "Support, known limitations of the beta, and answers to common questions.",
          "body": "## Help and limitations\n\n## Getting support\n\nIf you hit a problem, contact the XENO Comms support channel. When you write in, include:\n\n- your user id;\n- your device id;\n- your platform and app version;\n- a UTC timestamp of when it happened;\n- a short description of what you expected and what occurred.\n\nNever send passwords, tokens, private keys, or full phone numbers to support.\n\n## Known limitations (beta)\n\nXENO Comms is beta (v0.1.0). The honest limits right now:\n\nEncryption\n- Encrypted conversations use an alpha sealed-message path, not production Signal-protocol sessions, and are gated off by default in the launch build. Group sender keys, full double-ratchet state, reviewed key verification, and a security review are still required before production E2EE claims. See [Encryption](/docs/comms/encryption).\n\nAgents\n- Agents are first-class in the data model and the AgentGateway exists, but live agent participation is disabled by default at launch pending token validation, grants, and cost controls. See [Agents](/docs/comms/agents).\n\nPlatforms\n- Windows desktop is the active launch target. Mobile is paused until Windows is proven. macOS and Linux desktop are not launch-ready.\n\nFeatures not in the first launch\n- Voice and video calls.\n- Slack/Teams style workspaces, channels, threads, and public communities.\n- Full-text search, the web app, and the Hub/Shell embed.\n- Matrix-style federation.\n\nMedia and notifications\n- Media upload depends on production object storage and the media scanner; rejected or quarantined files cannot be attached.\n- Push depends on APNs, FCM, and WNS availability; encrypted conversations use generic notification copy. Media, push, and new registrations can each be disabled by operators during incidents.\n\n## FAQ\n\nIs XENO Comms end-to-end encrypted?\nThe encrypted path is implemented but is off by default in the launch build, and even when enabled it is a working alpha sealed-message path — not the final, audited Signal-protocol E2EE. The product does not claim production E2EE. See [Encryption](/docs/comms/encryption).\n\nCan I use it on my phone?\nMobile apps exist in the codebase, but their release is paused until the Windows desktop app is proven. Use the Windows desktop app today.\n\nHow do agents fit in?\nAgents share the same identity model as humans (a users table with a kind column), and the AgentGateway exists — but live agent participation is gated off by default at launch. Deeper cross-app agent actions come in a later phase. See [Agents](/docs/comms/agents).\n\nWhere are my messages stored, and do they sync?\nMessages are stored server-side (in ScyllaDB) for non-encrypted conversations so they sync across your devices; encrypted conversations store only ciphertext the server cannot read. Either way, a local cache keeps your history on-device and survives restarts. See [Devices and sync](/docs/comms/devices-and-sync).\n\nWhat happens if I delete my account?\nYour account is disabled, your identity fields cleared, and your devices, sessions, push tokens, and phone numbers revoked. Messages already delivered to others, and moderation/audit records that must be retained for safety, are not erased. See [Privacy and safety](/docs/comms/privacy-and-safety).\n\nCan I lose a message if my connection drops mid-send?\nText sends are queued in a local outbox in secure storage and come back as retryable items after a restart, and the server side uses a durable outbox for realtime events — so a dropped connection should not silently lose a message.\n\n## Next steps\n\n- [Introduction](/docs/comms/introduction)\n- [Privacy and safety](/docs/comms/privacy-and-safety)"
        }
      ]
    }
  ]
};

export default comms;
