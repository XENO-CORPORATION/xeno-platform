import type { ProductDocs } from './_types';

const sound: ProductDocs = {
  "slug": "sound",
  "productName": "XENO Sound",
  "tagline": "Full-spectrum audio workstation — from beat-making to broadcast mastering, in one app.",
  "seo": {
    "title": "XENO Sound Documentation — DAW & Audio Editor",
    "description": "Documentation for XENO Sound, the XENO Corporation audio workstation: arrangement and session views, mixing, effects, recording, MIDI, and AI audio tools. Alpha (v0.2.0)."
  },
  "sections": [
    {
      "title": "Getting Started",
      "pages": [
        {
          "slug": "overview",
          "title": "Overview",
          "description": "What XENO Sound is, its two working modes, and where it stands in development.",
          "body": "## What XENO Sound is\n\nXENO Sound is the audio workstation in the XENO Corporation creative suite — one desktop application that spans the whole audio workflow, from making beats to editing podcasts to mixing and mastering. It is built as an Electron 34 desktop app with a React 19 interface and a Web Audio engine, and it is a sibling to XENO Pixel (image), XENO Motion (video), and XENO Canvas (design).\n\nThe goal is a single tool that scales from simple to complex: a bedroom producer sketching a loop and a mastering engineer finalizing a release should both feel at home, without switching apps.\n\n## Maturity: alpha (v0.2.0)\n\nXENO Sound is at **v0.2.0** and under active development. A broad slice of the workstation is already implemented and covered by tests — multi-track arrangement, a mixer with effects, recording, MIDI and pattern sequencing, non-destructive clip editing, offline WAV export, and a set of AI audio operations. Other areas are partial or aspirational.\n\nThroughout these docs we flag what is **working today** versus what is **in development** so you always know what to expect. Where the app depends on a sibling repo (for example AI models from xeno-lib), that dependency is called out.\n\n## Two modes, one engine\n\nXENO Sound has two top-level ways to work, switchable from the transport bar (or the View menu). Both share the same audio engine, mixer, effects, and instruments.\n\n### Arrangement mode\n\nA linear, left-to-right timeline — the classic DAW layout for recording, editing, mixing, and mastering. You place audio clips on tracks along a time ruler, edit them non-destructively, and mix through channel strips. This is the mode for podcasts, voiceover, scoring, and album work.\n\n### Session mode\n\nA non-linear clip grid inspired by Ableton Live's Session View — a matrix of clip slots per track for beat-making, live experimentation, and performance.\n\nSee [Arrangement and Session](/docs/sound/arrangement-and-session) for how the two views work.\n\n## What's in the box today\n\n- **Projects and tracks** — a project data model with audio tracks, a master track, clips, patterns, automation, and a `.xsnd` project format. See [Projects and Tracks](/docs/sound/projects-and-tracks).\n- **Transport and editing** — play/pause/stop/record/loop/metronome, BBT/time/samples position display, clip split with automatic micro-fades, and crossfades. See [Arrangement and Session](/docs/sound/arrangement-and-session).\n- **Mixing and effects** — per-track volume/pan/mute/solo, an effects rack with EQ, compressor, reverb, and delay, sends/returns, and a plugin host. See [Mixing and Effects](/docs/sound/mixing-and-effects).\n- **Recording** — input arming, monitoring, punch in/out, and count-in pre-roll. See [Recording](/docs/sound/recording).\n- **AI and the agent** — denoise, stem separation, transcription, pitch detection, and auto-master, plus an agent tool surface. See [AI and the Agent](/docs/sound/ai-and-agent).\n\n## Design language\n\nThe interface follows the XENO design system: a dark, monochromatic palette, rectangular controls (no circular knobs), compact headers, and no decorative gradients or shadows on panels. The look is deliberately restrained so meters, waveforms, and your audio stay the focus.\n\nReady to make sound? Head to the [Quickstart](/docs/sound/quickstart)."
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Run XENO Sound from source and make your first project — import, play, add an effect, and export.",
          "body": "## Running XENO Sound\n\nXENO Sound is an Electron desktop application. During the alpha it runs from source in the repo. From the `xeno-sound` directory:\n\n```bash\nnpm install\nnpm run dev\n```\n\n`npm run dev` launches the app in development via `electron-vite`. Other useful scripts:\n\n```bash\nnpm run build       # production build (electron-vite build)\nnpm run typecheck   # tsc --noEmit\nnpm run test        # vitest run\nnpm run package:win # build a Windows installer (electron-builder)\n```\n\nPackaging targets exist for Windows, macOS, and Linux (`package:win`, `package:mac`, `package:linux`). When distributed, XENO Sound is launched from XENO Hub, which manages auth and workspace context.\n\n### Audio needs a gesture\n\nBrowsers (and therefore Electron) don't allow audio to start until you interact with the window. The first click or keypress initializes the audio engine — so if you press Space before touching anything, the very first press just wakes the engine.\n\n## Your first session\n\nWhen the app opens it creates a default project called **Untitled Session** with two empty audio tracks plus a master track, at 48 kHz / 32-bit float, 120 BPM, 4/4. You can start working immediately.\n\n### 1. Import audio\n\nUse the **Media Browser** on the left to bring in audio files. XENO Sound detects and decodes WAV, MP3, FLAC, OGG, AIFF, M4A/AAC, Opus, and WebM (format is detected from the file header, not just the extension). Drop a clip onto a track in the timeline.\n\n### 2. Play\n\nPress **Space** to play and pause. Press **Home** to return to the start. The transport bar shows the position — click the small format label to cycle between **BBT** (bars/beats/ticks), **HMS** (time), and **SPL** (samples).\n\nSet the tempo by double-clicking the large **BPM** readout and typing a value, or scroll over it to nudge it (20–999).\n\n### 3. Mix and add an effect\n\nThe **Mixer** along the bottom gives each track a fader, pan, mute, and solo. Select a track and open the **Effects Rack** on the right to add an effect — the built-ins are Parametric EQ, Compressor, Reverb, and Stereo Delay. See [Mixing and Effects](/docs/sound/mixing-and-effects).\n\n### 4. Export\n\nPress **Ctrl+Shift+E** (or File → Export Audio…) to open the export dialog. XENO Sound renders your mix offline — faster than real time — to a **WAV** file at 16/24/32-bit, mono or stereo, with optional normalization and dithering. See [Reference](/docs/sound/reference) for format details and current limits.\n\n## Where to go next\n\n- [Projects and Tracks](/docs/sound/projects-and-tracks) — the project model and `.xsnd` format.\n- [Arrangement and Session](/docs/sound/arrangement-and-session) — the two views, transport, and the pattern editor.\n- [Recording](/docs/sound/recording) — capturing audio from an input.\n- [AI and the Agent](/docs/sound/ai-and-agent) — denoise, stems, transcription, and tool automation."
        }
      ]
    },
    {
      "title": "Core Concepts",
      "pages": [
        {
          "slug": "projects-and-tracks",
          "title": "Projects and Tracks",
          "description": "The project data model — tracks, clips, the master track — and the .xsnd project format with autosave.",
          "body": "## The project model\n\nEverything you do in XENO Sound lives inside a **project**. A project holds:\n\n- **Metadata** — name, sample rate, bit depth, tempo, and time signature.\n- **Tracks** — an ordered list of tracks, each with its own clips, volume, pan, mute/solo state, effect chain, and sends.\n- **A master track** — the final mix bus that everything routes into.\n- **Patterns** — step/piano-roll patterns for music production (see [Arrangement and Session](/docs/sound/arrangement-and-session)).\n- **Automation lanes** — parameter automation over time.\n- **Mixer state** — sends and bus channels.\n\nA new project starts at 48 kHz / 32-bit float, 120 BPM, 4/4, with two audio tracks and a master track. Tempo is clamped to 20–999 BPM; track volume to 0–1.5; pan to -1…+1. Invalid values (NaN, infinities) are rejected rather than applied, so a bad number can never corrupt playback.\n\n## Tracks\n\nA track has a `type` — `audio`, `pattern`, `bus`, or `master`. Audio tracks hold audio clips; pattern tracks host sequenced patterns. Each track carries:\n\n- `volume`, `pan`, `mute`, `solo`, `armed`\n- `effectIds` — the effects in its chain\n- `sendIds` — its aux sends\n- `clips` and `patternClips`\n\nSolo is mix-wide: when any track is soloed, all non-soloed, non-master tracks are silenced. The engine mirrors mute/solo/volume/pan changes to the live audio graph as you make them.\n\n## Clips\n\nA **clip** is a non-destructive reference into an audio buffer. It has a `startTime` on the timeline, a source `offset`, a `duration`, a `gain`, and fade-in/out durations. Because clips reference their source rather than copying it, editing is non-destructive — the underlying audio is never altered.\n\nEditing operations that exist today include:\n\n- **Split** — cut a clip at a point; both halves get automatic 1 ms micro-fades at the split boundary to prevent clicks, and each half keeps the appropriate original fade.\n- **Crossfade** — overlapping clips can be crossfaded with linear, equal-power, or S-curve curves.\n- **Clip gain and fades** — per-clip gain and fade-in/out with linear, equal-power, S-curve, logarithmic, or exponential shapes.\n\n## The audio file pool\n\nImported audio is tracked by an `AudioFileManager` that reference-counts files against the clips that use them. It can report unused files and remove them — handy for keeping a project lean before you archive or share it.\n\n## The .xsnd project format\n\nProjects save to a **`.xsnd`** file. The format is:\n\n- **JSON** — human-readable, with a `version` field for forward/backward compatibility (opening a file from a newer version than your app is refused with a clear message). Gzip compression is planned.\n- **Audio referenced by path** — audio buffers are *not* embedded. Each project stores its audio in a sibling folder named `<Project>_Audio/`, and clips reference files by relative path. This mirrors how REAPER, Pro Tools, and Ableton handle media.\n\n```\nMySong.xsnd          <- JSON project file\nMySong_Audio/        <- referenced audio\n  recording_001.wav\n  sample_002.wav\n```\n\nThe serializer captures tracks, clips, patterns, automation, mixer sends/buses, effects, and the master track. It never saves the `armed` recording state, so reopening a project won't leave a track hot.\n\n> Note: the format version is currently 1. `.xsnd` is XENO Sound's on-disk extension in the shipping code (the ecosystem CLAUDE.md reserves `.xsound` for the app; the implementation uses `.xsnd`).\n\n## Autosave and crash recovery\n\nXENO Sound keeps an autosave interval (5 minutes by default, configurable in Preferences, 0 to disable) and includes a crash-recovery path so an unexpected shutdown doesn't lose recent work. Save/load and serialization are implemented in the engine; as an alpha, some menu wiring around save-as flows is still being finished."
        },
        {
          "slug": "arrangement-and-session",
          "title": "Arrangement and Session",
          "description": "The two working views, the transport, and the pattern editor for step sequencing and MIDI.",
          "body": "## Two views\n\nXENO Sound gives you two ways to arrange sound, toggled from the transport bar's Arrangement/Session buttons (Arrangement is the list-style icon; Session is the grid). Both drive the same engine, so a mix or an effect you set up in one is present in the other.\n\n### Arrangement view\n\nThe linear timeline. Tracks stack vertically; time runs left to right along a ruler. You place, move, trim, split, and crossfade audio clips here, and record onto tracks. This is the view for editing and mixing a piece with a fixed structure — podcasts, scores, albums.\n\n### Session view\n\nA clip grid: each track is a column of clip slots, and rows form scenes you can trigger. It's designed for beat-making, sketching ideas, and live-style experimentation where you launch clips rather than lay them on a fixed timeline.\n\n## The transport\n\nThe transport bar sits below the menu bar and holds the core playback controls:\n\n- **Play/Pause** (Space), **Stop**, **Return to start** (Home), and **Record** (R).\n- **Loop** and **Metronome** toggles.\n- **Punch** in/out and a **Pre-roll** count-in selector (Off / 1 / 2 / 4 bars) for recording — see [Recording](/docs/sound/recording).\n- A large, editable **BPM** display — double-click to type a tempo, or scroll to nudge it.\n- **Time signature** and a **position readout** that cycles between BBT (bars:beats:ticks), HMS (hours:minutes:seconds), and SPL (samples) when you click its label.\n- A **CPU/DSP load** indicator and the project's sample-rate / bit-depth.\n\nTiming uses a lookahead scheduler (the \"Tale of Two Clocks\" pattern) so notes and clips are scheduled sample-accurately against the audio clock rather than the less precise UI timer.\n\n## The pattern editor\n\nBelow the timeline sits the **pattern editor**, XENO Sound's music-production surface. Patterns are tick-based at 480 PPQ (pulses per quarter note), with helpers to snap to grid divisions (1/4, 1/8, 1/16, triplets) and convert between ticks and seconds at any tempo.\n\nA pattern contains:\n\n- **Channels** — step-sequencer lanes, each mapped to a sample or a synth preset, with per-step velocity, plus per-channel volume/pan/mute/solo.\n- **Notes** — piano-roll MIDI notes with note number, start tick, duration, and velocity.\n- **Swing** — a groove amount for the whole pattern.\n\nRelated production tools available from the menus:\n\n- **Step Sequencer** and **Piano Roll** editing for patterns.\n- **Drum Machine** — a pad grid for one-shot drum programming.\n- **Sampler** — a sample-playback instrument.\n- **Score Editor** — a notation view of the active pattern's notes.\n- **Chord Detection** and **Audio to MIDI** — analyze audio and turn it into notes.\n\n### MIDI files\n\nXENO Sound reads and writes Standard MIDI Files. It parses Type 0 and Type 1 files (tempo and note events, PPQ-based division; SMPTE division is rejected with a clear error) and can generate a Type 1 file from your pattern notes, so patterns round-trip to and from other tools.\n\n### The synth engine\n\nPattern channels can play a built-in subtractive synth. It ships with presets including Init Saw, Sub Bass, Pluck, Pad, Lead, Kick, Snare, and Hi-Hat, each with amplitude envelope, filter, and gain settings. Note scheduling uses a pre-allocated event pool so playback doesn't allocate memory on the audio path.\n\n> The README describes a wider instrument roadmap (wavetable, FM, granular). The subtractive synth, sampler, and drum machine are the instruments present in the current build."
        },
        {
          "slug": "mixing-and-effects",
          "title": "Mixing and Effects",
          "description": "The mixer, the built-in effects rack, sends and returns, metering, and the plugin host.",
          "body": "## The mixer\n\nThe **Mixer** runs along the bottom of the window as a row of channel strips — one per track, plus the master. Each strip gives you a fader (volume), pan, mute, and solo. Changes apply to the live audio graph immediately: adjusting a fader re-gains that track's node in real time, and solo silences every other non-master track.\n\nVolume is clamped to 0–1.5 (so you have headroom above unity) and pan to -1…+1. The master strip controls the final output gain.\n\n## The effects rack\n\nSelect a track and the **Effects Rack** on the right shows its effect chain. You can add, remove, reorder, enable/disable, and adjust the parameters of effects in the chain. The built-in effects are:\n\n- **Parametric EQ** — a 4-band EQ. The default bands are a low shelf at 80 Hz, two peaking bands, and a high shelf at 10 kHz, each with frequency, gain, and Q. The EQ supports the full biquad band-type set (lowpass, highpass, bandpass, low/high shelf, peaking, notch, allpass).\n- **Compressor** — threshold, ratio, attack, release, knee, and makeup gain, with optional auto-makeup. Defaults are -24 dB threshold, 4:1 ratio, 10 ms attack, 100 ms release, 6 dB knee.\n- **Reverb** — a convolution reverb with mix, decay, and pre-delay controls, plus an algorithmic path.\n- **Stereo Delay** — delay time, feedback, and mix, with a ping-pong mode.\n\nAlso present in the DSP layer: a **sidechain compressor** (keyed by another track), **tape / analog emulation**, and **spatial audio / surround** panning tools, reachable from the Effects and Instruments menus.\n\nEach effect is defined by a descriptor (id, category, and typed parameters), so effects present a consistent, machine-readable parameter surface — which is also what lets the [agent](/docs/sound/ai-and-agent) add and drive them.\n\n## Sends, returns, and buses\n\nBeyond the channel fader, tracks can route to aux buses via **sends** (pre- or post-fader), and buses can carry their own effects and feed the master. A routing matrix (Ctrl+Shift+R) visualizes and edits signal flow across tracks and buses. Group tracks and a routing/send-return layer let you build submixes.\n\n## Metering\n\nXENO Sound includes several meters:\n\n- **Meter bridge** — peak, RMS, and LUFS meters alongside the mix.\n- **Advanced metering** — EBU R128 loudness with integrated LUFS, loudness range (LRA), momentary/short-term readings, and **true-peak** detection via interpolation, including inter-sample clipping detection (over 0 dBTP). This follows the EBU R128 / ITU-R BS.1770 conventions.\n- **Correlation meter** and a **spectrum analyzer** / **spectrogram** for stereo and frequency analysis.\n\n## The plugin host\n\nA **plugin host** manages audio-effect plugins. It registers the built-in effects (EQ, Compressor, Reverb, Stereo Delay) and can register custom plugins described by a manifest (id, name, vendor, version, category, I/O counts, parameters, and a processor URL) so additional WASM-based effects can be loaded and instantiated on a track. This is the seam for extending XENO Sound's effect set.\n\n## Workflow panels\n\nA few production-workflow panels round out mixing: **Mixer Scenes** (save and recall full mixer snapshots, with per-track recall-safe flags), and **Session Notes** (attach text notes to the project, markers, tracks, or regions, searchable and color-tagged). Both serialize with the project."
        },
        {
          "slug": "recording",
          "title": "Recording",
          "description": "Capturing audio from an input — arming, monitoring, punch in/out, count-in, and the metronome.",
          "body": "## Recording audio\n\nXENO Sound records from your audio inputs onto tracks using the browser MediaStream + AudioWorklet path. The recording engine handles input enumeration, arming, input-level metering, and writing captured audio back as clips.\n\n### Arming and monitoring\n\nTo record, **arm** the track you want to capture to. The record engine tracks which tracks are armed, exposes input-level metering (so you can set levels before rolling), and supports input **monitoring** so you can hear yourself through the signal path. Each armed track can be assigned an input device.\n\n### Recording\n\nPress **R** (or the record button in the transport) to toggle recording. When you stop, captured audio is turned into an `AudioBuffer` and added to the armed track as a new clip at the position where recording began. Recorded clips behave like any imported clip — you can split, fade, and move them.\n\n## Punch in / out\n\nEnable **Punch** in the transport to record only within a defined region. Punch in/out lets you drop into record at a precise point and drop out at another — the standard way to fix a phrase without re-recording a whole take. The record engine takes explicit punch-in and punch-out times.\n\n## Count-in (pre-roll)\n\nThe **Pre-roll** selector gives you a count-in before recording starts, so you have time to get ready and lock to the tempo. Options are Off, 1, 2, or 4 bars (invalid values reset to Off). The default in Preferences is a 1-bar count-in.\n\n## The metronome\n\nToggle the **Metronome** in the transport to hear a click. The metronome schedules clicks against the audio clock with an accented downbeat, following the project tempo and time signature. It's independent of recording — you can practice or play to a click any time.\n\n## Frozen tracks\n\nHeavy tracks can be **frozen** — rendered to a cached buffer so their effects and instruments stop consuming CPU during playback. The record and playback paths are freeze-aware: a frozen track plays from its cached buffer, and editing a frozen track auto-unfreezes it so you never edit stale audio. Freezing reports progress as it renders.\n\n## Preferences that affect recording\n\nUnder Preferences → Recording you can set the default input device, the default pre-roll bars, whether count-in is on, and the record file format. Audio preferences (sample rate, bit depth, buffer size, device) live under Preferences → Audio; the defaults are 48 kHz, 32-bit, 512-sample buffer."
        }
      ]
    },
    {
      "title": "AI & Automation",
      "pages": [
        {
          "slug": "ai-and-agent",
          "title": "AI and the Agent",
          "description": "The AI audio operations (denoise, stems, transcription, pitch, auto-master) and the agent tool surface.",
          "body": "## AI audio operations\n\nXENO Sound includes a set of AI-assisted audio operations, reachable from the **Track → AI**, **Effects → AI**, and **Analyze** menus. They fall into two groups by where they run.\n\n### Model-backed operations (via xeno-lib)\n\nThese run in the Electron main process through **xeno-lib** over IPC — the renderer sends audio samples, the main process runs the model, and results come back. All processing is offline; nothing runs on the real-time audio thread.\n\n- **Denoise** — AI noise reduction (RNNoise). Denoising a track opens an A/B comparison so you can audition the cleaned result against the original before applying it as a new clip.\n- **Separate Stems** — splits a track into four stems (vocals, drums, bass, other) using an HTDemucs-style model, creating four new tracks and muting the original.\n- **Transcribe** — speech-to-text with timestamps (Whisper), shown in a transcription panel with time-coded segments and detected language.\n\n> These depend on xeno-lib being installed. When it isn't present, the model-backed IPC handlers surface a clear \"xeno-lib is not installed\" error (and the denoise path can fall back to a no-op) rather than crashing — expected in an alpha or a stripped-down build.\n\n### In-app analysis operations\n\nThese run directly in the renderer using signal processing, so they work without external models:\n\n- **Pitch Detection** — fundamental-frequency tracking over time via an autocorrelation-based approach, producing a per-frame pitch curve.\n- **Auto-Master** — analyzes the mix for loudness and applies gain toward a target LUFS with limiting, adding the mastered result as a new track. This is a pragmatic approximation rather than a reference-grade mastering chain — treat it as a fast starting point.\n\nAI operations report progress through a shared progress dialog and surface errors with a retry path.\n\n## The agent\n\nXENO Sound exposes a **programmatic tool surface** and an agent sidebar (toggle with Ctrl+Shift+A). The design principle across XENO apps is agent-first: every meaningful operation should be callable in code, not just by mouse. The sidebar registers these tools today:\n\n| Tool | What it does |\n|------|--------------|\n| `set_bpm` | Change project tempo (20–999) |\n| `set_volume` | Set a track's volume (0–1.5) |\n| `mute_track` / `solo_track` | Toggle mute/solo on a track |\n| `add_effect` | Add EQ, compressor, reverb, or delay to a track |\n| `denoise_track` | Run AI denoise on a track |\n| `separate_stems` | Split a track into 4 stems |\n| `transcribe_track` | Transcribe a track to text |\n| `export_audio` | Open export for the mixdown |\n\nEach tool has a real executor that calls into the project, transport, and mixer stores and returns a human-readable result. Tools resolve a track by id **or** by name (exact, then partial), so \"mute the drums\" can find a track named \"Drums.\" Invalid arguments return a helpful message listing valid options rather than failing silently.\n\n### Current state\n\nThe tool registry and executors are **live and deterministic** — the tools genuinely change the project when invoked. The natural-language layer that turns a chat request into tool calls (via xeno-agent-sdk) is the **future** piece; today the sidebar is the UI shell and command surface over that tool set. As the SDK integration lands, the same tools will be driven by an LLM without changing the tool contracts."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "reference",
          "title": "Reference",
          "description": "Keyboard shortcuts, export formats and options, and audio defaults.",
          "body": "## Keyboard shortcuts\n\n### Verified global shortcuts\n\nThese are bound by the app's global key handler and work anywhere except while typing in a text field:\n\n| Shortcut | Action |\n|----------|--------|\n| Space | Play / Pause (stops a running pattern) |\n| Home | Return to start / stop |\n| Enter | Stop |\n| R | Toggle record |\n| Ctrl+Shift+R | Toggle routing matrix |\n| Ctrl+Shift+E | Open export dialog |\n| Ctrl+Shift+A | Toggle agent panel |\n| Ctrl+, | Open Preferences |\n\n### Transport-bar interactions\n\n| Interaction | Action |\n|-------------|--------|\n| Double-click BPM | Edit tempo (type a value) |\n| Scroll over BPM | Nudge tempo up/down |\n| Click position label | Cycle BBT / HMS / SPL display |\n| Arrangement / Session buttons | Switch views |\n| Loop / Metronome / Punch buttons | Toggle those modes |\n\n### Menu accelerators\n\nThe menus display additional accelerators — New (Ctrl+N), Open (Ctrl+O), Save (Ctrl+S), Save As (Ctrl+Shift+S), the Edit clipboard set, view toggles, and track commands. As an alpha, some of these labels are shown ahead of being wired to a global handler; the table above lists what is confirmed active today. Menu items with an action (Export, Preferences, the AI submenu, Advanced Metering, Session Notes, Mixer Scenes, Plugin Host, and the Track/Instruments/Analyze tools) invoke their features directly.\n\n## Export\n\nOpen export with **Ctrl+Shift+E** or File → Export Audio…. XENO Sound renders your project **offline** using an `OfflineAudioContext` — faster than real time — and reports render/encode/write progress.\n\n### Formats\n\n| Format | Status | Notes |\n|--------|--------|-------|\n| WAV | Available | 16 / 24 / 32-bit, lossless |\n| MP3 | Not yet — falls back to WAV | needs a WASM LAME encoder |\n| FLAC | Not yet — falls back to WAV | needs a WASM encoder |\n| OGG Vorbis | Not yet — falls back to WAV | needs a WASM Vorbis encoder |\n| AAC | Defined, not offered in dialog | needs a native encoder |\n\n**WAV is the only format that actually encodes today.** MP3, FLAC, and OGG appear in the export dialog, but choosing one currently renders a WAV and shows a warning (\"…encoding requires xeno-lib. Exported as WAV instead.\"). AAC exists in the export format table but isn't listed in the dialog yet. These are honest placeholders pending xeno-lib audio codecs — not broken exports.\n\n### Export options\n\n- **Sample rate** and **bit depth** (16/24/32 for WAV).\n- **Channels** — mono or stereo.\n- **Normalize** — normalize output to a target peak (off by default).\n- **Dither** — apply dithering when reducing bit depth.\n- **Tail seconds** — extra time captured so reverb/delay tails aren't cut off.\n\nExport results report the output path, duration, file size, measured peak level, whether the audio clipped, and total export time.\n\n## Audio and project defaults\n\n| Setting | Default |\n|---------|---------|\n| Sample rate | 48 kHz |\n| Bit depth | 32-bit float |\n| Buffer size | 512 samples |\n| Tempo | 120 BPM (range 20–999) |\n| Time signature | 4/4 |\n| Autosave interval | 5 minutes (0 to disable) |\n| Undo history | 200 steps |\n| Pre-roll | 1 bar |\n| Default export format | WAV |\n\nThese are configurable under **Preferences** (Ctrl+,), which is organized into General, Audio, Recording, MIDI, Timeline, Mixer, and Export categories.\n\n## Supported import formats\n\nAudio import detects format from file headers and supports: WAV, MP3, FLAC, OGG, AIFF, M4A/AAC, Opus, and WebM.\n\n## Project file\n\n- **Extension:** `.xsnd`\n- **Encoding:** JSON with a `version` field (currently 1; gzip planned)\n- **Audio:** referenced by relative path in a sibling `<Project>_Audio/` folder\n- **Compatibility:** opening a file saved by a newer version than your app is refused with a clear message"
        }
      ]
    }
  ]
};

export default sound;
