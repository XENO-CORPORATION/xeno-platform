import type { ProductDocs } from './_types';

const engine: ProductDocs = {
  "slug": "engine",
  "productName": "XENO Engine",
  "tagline": "AI-native game engine — an entity-component scene editor, TypeScript scripting, and a broad runtime toolkit, built from zero for the XENO ecosystem.",
  "seo": {
    "title": "XENO Engine documentation",
    "description": "Learn XENO Engine (alpha) — the entity-component scene editor, TypeScript game scripting, play mode, and the rendering, physics, animation, audio, AI, particle, terrain, and export subsystems."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Engine is, what actually ships in the alpha, and where it fits in the XENO ecosystem.",
          "body": "# Introduction\n\n**XENO Engine** is an AI-native game engine from XENO Corporation — a scene editor and runtime toolkit built from zero for the XENO ecosystem instead of retrofitted for it. You model in XENO 3D, texture in XENO Pixel, compose audio in XENO Sound, animate in XENO Motion, script in TypeScript, and ship to desktop and web.\n\nThis is the documentation for the current **alpha (v0.2.0)**. It describes what runs today and is explicit about what is still on the roadmap.\n\n## What ships today\n\nThe v0.2.0 build is an **Electron + React 19 + TypeScript** editor. Its core is real and usable:\n\n- **An entity/component scene editor** — a hierarchy of entities, each a bag of typed components (transform, mesh renderer, rigid body, collider, light, camera, script, audio, particles, animator, terrain, sprite, tilemap, and more).\n- **A 3D viewport** — rendered with a Three.js WebGL preview, with transform gizmos, a grid, and shaded/wireframe/unlit shading modes.\n- **TypeScript game scripting** — attach scripts with lifecycle hooks (`onStart`, `onUpdate`, collision/trigger callbacks) that run in a sandboxed runtime.\n- **Play mode** — enter/exit play with full scene-state isolation, driven by a fixed-timestep game loop.\n- **Undo/redo, copy/paste, prefabs, tags and layers, multi-select, and a JSON scene format** that is friendly to version control.\n- **A broad subsystem toolkit** — materials, post-processing, lighting, animation, audio, AI behavior trees, particles, terrain, 2D sprites, input, in-game UI, asset pipeline, profiler, and web/native export — see [Engine systems](/docs/engine/engine-systems).\n\n## What is planned, not shipped\n\nThe README describes the engine's **architecture target**: a Rust runtime compiled as a native N-API module, rendering through `wgpu` with a deferred PBR pipeline, Rapier physics, `kira` audio, and a V8/`deno_core` scripting host. **None of that native runtime ships in v0.2.0.** Today the viewport is a Three.js preview and scripts run in the editor process. Wherever a subsystem's production form depends on that native runtime, this documentation says so.\n\nAlso planned, not shipped: **multiplayer/networking** (the data model exists; the transport does not), skeletal GPU skinning, and the full wgpu deferred renderer.\n\n## Where it fits\n\nXENO Engine is **Layer 5 (Creative Apps)** of the XENO ecosystem. It imports `@xeno/core` for its shared history manager, is designed to consume `xeno-lib` AI models and embed `xeno-agent-sdk` for NPC dialogue, and is launched from **XENO Hub** for auth and workspace context.\n\n## Next steps\n\n- [Install & launch](/docs/engine/installation)\n- [Quickstart](/docs/engine/quickstart) — build and play your first scene\n- [Editor & scenes](/docs/engine/editor-and-scenes)\n\n> XENO Engine is proprietary software, © 2026 XENO Corporation. It is currently in **alpha (v0.2.0)** — APIs, formats, and features may change."
        },
        {
          "slug": "installation",
          "title": "Install & launch",
          "description": "How to get the XENO Engine editor running — via XENO Hub, a desktop build, or from source.",
          "body": "# Install & launch\n\nXENO Engine is a desktop application (Electron). Because it is in **alpha**, the most reliable way to run it today is from source; packaged installers are produced by the release pipeline and distributed through XENO Hub and xenostudio.ai as builds are cut.\n\n## Via XENO Hub (recommended when available)\n\nXENO Engine is launched from **XENO Hub**, the desktop launcher for the ecosystem. Hub provides auth, credits, and workspace context, checks `updates.xenostudio.ai/apps/engine/version.json` for new builds, and shows an **Update** button when a newer version is available. When an Engine build is published, it appears in Hub's app list.\n\n## Desktop download\n\nWhen a build is published, download pages live at:\n\n- Overview — `xenostudio.ai/products/xeno-engine/`\n- Download — `xenostudio.ai/products/xeno-engine/download/`\n- Release notes — `xenostudio.ai/products/xeno-engine/release-notes/`\n\nThe download page reads `version.json` on load, so the version and installer link stay current. Installers are built with electron-builder as NSIS (Windows), DMG (macOS), and AppImage (Linux).\n\n## From source\n\nXENO Engine requires **Node.js 20+**. Clone the repo and run the editor in dev mode with hot reload:\n\n```bash\ngit clone https://github.com/XENO-CORPORATION/xeno-engine\ncd xeno-engine\nnpm install\nnpm run dev\n```\n\nOther useful scripts (from `package.json`):\n\n```bash\nnpm run build        # production build (electron-vite)\nnpm run typecheck    # tsc --noEmit\nnpm run test         # vitest run\nnpm run package:win  # build + electron-builder --win\nnpm run package:mac  # build + electron-builder --mac\nnpm run package:linux\n```\n\n## About the native runtime\n\nThe README's architecture references a **Rust native module** (built with `cargo`) alongside the Electron shell. That native runtime is not part of the v0.2.0 build — there is no `native/` crate in the repo yet, and the editor runs entirely on its TypeScript + Three.js stack. You do **not** need a Rust toolchain to run the alpha.\n\n## Next steps\n\n- [Quickstart](/docs/engine/quickstart)\n- [Keyboard shortcuts](/docs/engine/editor-and-scenes#keyboard-shortcuts)"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Open the editor, build a scene from a template, attach a script, and press play.",
          "body": "# Quickstart\n\nThis walkthrough takes you from an empty editor to a scene you can play. It assumes you have the editor running — see [Install & launch](/docs/engine/installation).\n\n## 1. The editor layout\n\nWhen the editor loads, you get a fixed layout:\n\n- **Title bar** and **menu bar** (File, Edit, Entity, Component, Prefab, Window) across the top.\n- **Toolbar** — play/pause/stop controls and the transform tools.\n- **Hierarchy** (left) — the scene graph tree. A new scene starts with a `Scene` root containing a `Main Camera`, a `Directional Light`, an `Environment` group (a ground plane, a cube, and a sphere), and a `Player`.\n- **Viewport** (center) — the 3D Three.js preview with grid and gizmos.\n- **Assets / Console** (bottom) — the project asset browser and the log console.\n- **Inspector** (right) — the components of the selected entity.\n\n## 2. Start from a template (optional)\n\nXENO Engine ships four starter templates you can apply to a fresh scene:\n\n| Template id | Genre | What it sets up |\n|-------------|-------|-----------------|\n| `fps` | First-Person Shooter | FPS camera, weapon, enemies, walled arena |\n| `platformer` | Side-scroller | Platforms, coins, jump physics, side camera |\n| `topdown` | Overhead action/RPG | Player, enemies, ground plane, top camera |\n| `puzzle` | Grid puzzle | A grid of tiles, movable pieces, goal markers |\n\nEach template comes with a pre-built entity hierarchy, a recommended camera mode, and grid settings. See [Play mode & the game loop](/docs/engine/scripting#play-mode-and-the-game-loop).\n\n## 3. Create and place an entity\n\nAdd an entity under the scene root, then move it with the transform tools:\n\n1. Create an entity (via the Entity menu / hierarchy). It gets a `Transform` component by default.\n2. Press **W** to move, **E** to rotate, **R** to scale, **Q** to return to select. Drag the gizmo in the viewport.\n3. Switch between **world** and **local** transform space with **X**.\n4. Press **F** to frame the selected entity in the viewport.\n\n## 4. Add components\n\nWith an entity selected, use the Inspector (or the Component menu) to add components — for example a **Mesh Renderer** (mesh + material with PBR color/roughness/metalness overrides), a **Rigid Body** (dynamic/static/kinematic), a **Collider**, or a **Light**. Every add/remove/edit is undoable with **Ctrl+Z** / **Ctrl+Y**. See the full list in [Entities & components](/docs/engine/entities-and-components).\n\n## 5. Attach a script\n\nAdd a **Script** component and point it at a script file. XENO Engine includes ready-to-edit templates such as `PlayerController.ts` and `EnemyAI.ts` that read input and move the entity. Scripts expose tunable parameters via `// @param` comments. See [Scripting](/docs/engine/scripting).\n\n## 6. Press play\n\nHit **Ctrl+P** to enter play mode. The engine snapshots your scene and runs the [game loop](/docs/engine/scripting#play-mode-and-the-game-loop) — a fixed 60 Hz physics step plus per-frame script and animation updates — executing your `onUpdate` hooks. **Ctrl+P** again pauses; **Ctrl+Shift+P** stops and restores the exact pre-play scene state, so nothing you did while playing leaks back into your saved scene.\n\n## 7. Save\n\nPress **Ctrl+S** to save. Scenes serialize to a JSON scene format that round-trips cleanly and is friendly to version control.\n\n## Next steps\n\n- [Editor & scenes](/docs/engine/editor-and-scenes)\n- [Entities & components](/docs/engine/entities-and-components)\n- [Engine systems](/docs/engine/engine-systems)"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "editor-and-scenes",
          "title": "Editor & scenes",
          "description": "The editor workspace, the entity hierarchy, transform tools, prefabs, tags and layers, the scene format, and keyboard shortcuts.",
          "body": "# Editor & scenes\n\nThe XENO Engine editor is where you assemble scenes. This page covers the workspace, how the scene graph works, and the editing model.\n\n## The workspace\n\nThe editor is a fixed multi-panel layout: title bar, menu bar, and toolbar across the top; the **Hierarchy** on the left; the **Viewport** in the center with the **Assets/Console** panels beneath it; and the **Inspector** on the right. The chrome follows a monochromatic design system (near-black background, white at varying opacity) so nothing competes with the full-color game viewport.\n\n### Viewport\n\nThe viewport is a Three.js WebGL preview of the scene. It supports three shading modes — **shaded**, **wireframe**, and **unlit** — a toggleable grid, transform gizmos, and a 2D mode for sprite/tilemap work. The editor camera runs in **orbit** (default) or **fly** mode; press **F** to frame the selection.\n\n> The production viewport target is a Rust `wgpu` deferred renderer (G-buffer, cascaded shadows, SSAO, SSR, GI, volumetrics). That is planned — the alpha renders through Three.js. See [Engine systems](/docs/engine/engine-systems).\n\n## The scene graph\n\nA scene is a tree of **entities**. Every scene has a `Scene` root; everything else nests beneath it. Each entity has:\n\n- a **name**, a **tag**, and a **layer**;\n- **visibility** and **lock** flags;\n- a set of typed **components** (always at least a `Transform`);\n- optional **children**.\n\nThe Hierarchy panel lets you expand/collapse, select, multi-select, reparent by drag, rename, duplicate, and delete. Locked entities are protected from deletion.\n\n### Selection\n\nBeyond click selection, the editor supports **Select All** (Ctrl+A), **Deselect** (Esc), **Invert selection** (Ctrl+I), and *select-by-component* (select every entity that has, say, a Light). Multi-selected entities can be edited together — a single Inspector change is applied across the whole selection.\n\n## Transform tools\n\nThe toolbar and keyboard expose four tools: **Select (Q)**, **Move (W)**, **Rotate (E)**, and **Scale (R)**. Transforms operate in **world** or **local** space (toggle with **X**), and can snap to a configurable grid via the snap settings.\n\n## Prefabs\n\nA **prefab** is a reusable entity template captured from an existing entity tree. You can:\n\n- **create** a prefab from a selected entity,\n- **instantiate** it into a scene (instances track their source),\n- **apply overrides** back to the source, or\n- **revert** an instance to the prefab.\n\nPer-property overrides let an instance diverge without breaking its link to the base.\n\n## Tags & layers\n\nEntities carry a **tag** (for identification/grouping — defaults include `Untagged`, `Player`, `Enemy`, `Ground`, `MainCamera`) and a **layer** (for rendering/collision filtering — defaults include `Default`, `Water`, `UI`, `PostProcessing`). Both lists are editable; core entries such as `Untagged` are protected from removal.\n\n## Scene format\n\nScenes serialize to a **JSON scene format** through the scene serializer. The format is designed to be VCS-friendly and to round-trip losslessly — entity IDs, component data, and hierarchy order are all preserved. The engine's reserved single-file project extension in the ecosystem convention is `.xengine`.\n\n## Undo & history\n\nEvery editor mutation — create, delete, duplicate, rename, reparent, component add/remove/edit, tag/layer changes — is recorded on a shared **history manager** (from `@xeno/core`) and is fully undoable. Redo is cleared when you take a new action after undoing.\n\n## Keyboard shortcuts\n\nFrom the editor's global key handling and the File menu:\n\n| Shortcut | Action |\n|----------|--------|\n| Q / W / E / R | Select / Move / Rotate / Scale tool |\n| X | Toggle world/local transform space |\n| F | Frame selected entity |\n| Ctrl+Z / Ctrl+Y | Undo / Redo |\n| Ctrl+C / Ctrl+X / Ctrl+V | Copy / Cut / Paste entity |\n| Ctrl+D | Duplicate |\n| Ctrl+A | Select all |\n| Ctrl+I | Invert selection |\n| Esc | Deselect all (when stopped) |\n| Delete | Delete selected |\n| Ctrl+S | Save scene |\n| Ctrl+P | Play / Pause |\n| Ctrl+Shift+P | Stop |\n| Ctrl+N | New scene (File menu) |\n| Ctrl+O | Open scene (File menu) |\n| Ctrl+Shift+S | Save scene as (File menu) |\n\n## Next steps\n\n- [Entities & components](/docs/engine/entities-and-components)\n- [Scripting](/docs/engine/scripting)"
        },
        {
          "slug": "entities-and-components",
          "title": "Entities & components",
          "description": "The entity-component model and the full catalog of components you can attach in the alpha.",
          "body": "# Entities & components\n\nXENO Engine is built on an **entity-component** model. An entity is an identity with a name, tag, layer, flags, and children; its behavior and appearance come entirely from the **components** attached to it. In the alpha, the editor manages components as typed data on each entity.\n\n> The high-performance archetype ECS (Structure-of-Arrays storage, parallel system scheduler, 100K+ entities) described in the README is the **Rust runtime target**. The alpha implements the same entity/component *model* in TypeScript so scenes author correctly against it.\n\n## The transform\n\nEvery entity has a **Transform**: `position`, `rotation` (Euler degrees), and `scale`, each an `[x, y, z]` triple. The transform is the one component you cannot remove.\n\n## Component catalog\n\nThese are the component types you can add in v0.2.0, from the editor's component model:\n\n| Component | Purpose | Key fields |\n|-----------|---------|-----------|\n| **Mesh Renderer** | Draw a mesh with a material | `mesh`, `material`, `castShadows`, `receiveShadows`, PBR overrides (`color`, `roughness`, `metalness`) |\n| **Rigid Body** | Physics body | `type` (dynamic/static/kinematic), `mass`, `drag`, `angularDrag`, `useGravity` |\n| **Collider** | Collision shape | `shape` (box/sphere/capsule/mesh/convex), `isTrigger`, `center`, `size`, `physicsMaterial` |\n| **Light** | Light source | `lightType` (directional/point/spot), `intensity`, `range`, `castShadows` |\n| **Camera** | 3D camera | `fov`, `near`, `far`, `isMain` |\n| **Script** | Attach a TypeScript behavior | `file`, `enabled`, `parameters` |\n| **Audio Source** | Play a clip | `clip`, `volume`, `spatialBlend`, `loop`, `playOnAwake` |\n| **Particle System** | Emit particles | `maxParticles`, `emissionRate`, `lifetime`, `startSpeed`, `startSize`, `simulationSpace`, `loop`, `playOnAwake` |\n| **Animator** | Drive an animation controller | `controller`, `currentState`, `speed`, `applyRootMotion` |\n| **Terrain** | Heightmap terrain | `size`, `resolution`, `maxHeight`, `enableWater`, `waterHeight`, `textureLayerCount` |\n| **Sprite Renderer** | 2D sprite | `sprite`, `color`, `flipX`, `flipY`, `pixelsPerUnit`, `sortingLayer`, `orderInLayer` |\n| **Tilemap** | 2D tile grid | `tileSize`, `gridCols`, `gridRows`, `tilesetId` |\n| **Network Identity** | Multiplayer identity (planned) | `netId`, `ownerId`, `hasAuthority`, `replicationMode` |\n| **Post-Process Volume** | Local/global post-processing | `isGlobal`, `priority`, `blendDistance`, `profileId` |\n| **NPC** | AI character wiring | `profileId`, `dialogueTreeId`, `behaviorTreeId`, `faction`, `disposition` |\n| **Camera 2D** | Orthographic/pixel-perfect camera | `orthographicSize`, `pixelPerfect`, `referenceWidth`, `referenceHeight` |\n\nAdding a component fills in sensible defaults; removing or editing it is undoable. The **Network Identity** component is present so scenes can be authored for multiplayer, but the networking transport itself is not implemented — see [Engine systems](/docs/engine/engine-systems#networking-planned).\n\n## Editing components\n\nSelect an entity and its components appear in the **Inspector**. Edits are applied immediately and pushed to the undo history. With multiple entities selected, an edit to a shared component is applied to every selected entity that has it (bulk editing).\n\n## Scene statistics\n\nThe editor estimates **scene statistics** — entity count, vertices, triangles, draw calls, and material count — from the visible mesh renderers, so you can see the rough cost of a scene as you build it.\n\n## Next steps\n\n- [Scripting](/docs/engine/scripting)\n- [Engine systems](/docs/engine/engine-systems)"
        },
        {
          "slug": "scripting",
          "title": "Scripting & play mode",
          "description": "Write TypeScript game scripts with lifecycle hooks, expose parameters, and run them under the fixed-timestep game loop in play mode.",
          "body": "# Scripting & play mode\n\nGameplay in XENO Engine is written in **TypeScript**. You attach a script to an entity with a **Script** component, and the script runs under the engine's game loop during play mode.\n\n## The script lifecycle\n\nA script is an object that implements any subset of these lifecycle hooks:\n\n| Hook | When it runs |\n|------|--------------|\n| `onStart()` | Once, when the script is attached |\n| `onUpdate(deltaTime)` | Each update tick (documented as fixed 60 Hz) |\n| `onLateUpdate(deltaTime)` | Every frame (variable timestep) |\n| `onDestroy()` | When the script is detached or the entity is destroyed |\n| `onCollisionEnter(other)` / `onCollisionExit(other)` | On collision begin/end |\n| `onTriggerEnter(other)` / `onTriggerExit(other)` | On entering/leaving a trigger zone |\n\n## The engine API surface\n\nScripts receive a controlled engine API — they cannot reach Node.js, the filesystem, or the network. The exposed surface includes:\n\n- **`self`** — a reference to the script's entity, with `getPosition`/`setPosition`, `getRotation`/`setRotation`, `getScale`/`setScale`, plus `id`, `name`, and `tag`.\n- **`input`** — `isActionPressed`, `isActionJustPressed`, `isActionJustReleased`, `getAxis`, `getMousePosition`, `getMouseDelta`.\n- **`audio`** — `play(clip, options)`, `stop`, `setVolume`.\n- **`scene`** — `findByName`, `findByTag`, `instantiate(prefab, position?)`, `destroy(entity)`.\n- **`log` / `warn` / `error`**, plus `deltaTime` and `time`.\n\nA minimal script looks like this:\n\n```ts\n// @param speed: number = 5\n\nreturn {\n  onStart() {\n    log('PlayerController started');\n  },\n  onUpdate(dt) {\n    const moveX = input.getAxis('MoveHorizontal');\n    const moveZ = input.getAxis('MoveVertical');\n    const p = self.getPosition();\n    self.setPosition(p[0] + moveX * 5 * dt, p[1], p[2] + moveZ * 5 * dt);\n    if (input.isActionJustPressed('Jump')) log('Jump!');\n  },\n};\n```\n\n## Script parameters\n\nExpose tunable values to the Inspector with `// @param` comments — `// @param speed: number = 5`. Supported parameter types are `number`, `string`, `boolean`, and `vec3`. Each parameter carries a default and can be overridden per entity.\n\n## Built-in templates\n\nThe engine ships editable starter scripts you can attach directly:\n\n- `PlayerController.ts`\n- `EnemyAI.ts`\n- `CameraFollow.ts`\n- `Collectible.ts`\n- `Spawner.ts`\n- `GameManager.ts`\n\n## Sandbox & safety\n\nScripts are transpiled and executed in a sandboxed runtime with configurable safety limits: a per-update execution-time budget (16 ms by default), caps on instances/modules/source size, and an injected loop-iteration guard against infinite loops. A script that throws repeatedly is auto-disabled after a threshold of consecutive errors, so one bad script can't take down play mode.\n\n> Today scripts run sandboxed **in the editor process**. The production target delegates to a native **V8/`deno_core`** isolate for true isolation and hot-reload; that host is planned, not shipped.\n\n## Play mode and the game loop\n\nPress **Ctrl+P** to enter play mode. On enter, the engine **snapshots** your entities and prefabs; on stop (**Ctrl+Shift+P**) it restores that snapshot exactly — play-mode changes never leak into your saved scene. Pause is **Ctrl+P** again, and you can **step a single frame** while paused.\n\nWhile playing, the loop uses an accumulator: it advances the **physics step at a fixed 60 Hz**, then runs **script updates** and **animation evaluation** for the rendered frame. Fixing the physics timestep keeps simulation stable regardless of frame rate.\n\n> The alpha's physics step is a lightweight gravity/velocity integrator with basic collision. The production target delegates physics to **Rapier** in the native runtime; that is planned.\n\n## Next steps\n\n- [Engine systems](/docs/engine/engine-systems)\n- [Entities & components](/docs/engine/entities-and-components)"
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "engine-systems",
          "title": "Engine systems",
          "description": "A reference catalog of the engine subsystems in v0.2.0 — what each one does and its maturity.",
          "body": "# Engine systems\n\nXENO Engine exposes its runtime as a set of subsystems. In v0.2.0 each is a TypeScript module in the editor; many are data-model + runtime helpers that are intended to be backed by the native Rust runtime over time. This page is a reference to what exists today and how mature it is.\n\n## Rendering & materials\n\n- **Scene renderer** — the Three.js viewport that syncs entities to renderable objects and reports render stats. This is the WebGL **preview**; the `wgpu` deferred pipeline is the planned production target.\n- **Post-processing** — a post-process stack with profiles and presets for bloom, color grading, vignette, ambient occlusion, anti-aliasing, tonemapping, depth of field, motion blur, chromatic aberration, film grain, fog, and screen-space reflections.\n- **Scene lighting** — directional/point/spot/area light definitions, light and reflection probes, environment and procedural-sky settings, and lightmap settings.\n- **Material system** — a PBR material library (metallic-roughness) with texture slots, blend modes, preview rendering, and duplication.\n- **Light gizmos** — editor gizmo geometry for directional/point/spot lights.\n\n## Simulation\n\n- **Game loop** — the play-mode runner: a fixed 60 Hz physics step plus per-frame script and animation updates. See [Scripting & play mode](/docs/engine/scripting#play-mode-and-the-game-loop).\n- **Physics debug** — wireframe visualization for colliders and raycasts, with a physics-body/collider data model. Full rigid-body simulation via **Rapier** is planned in the native runtime.\n\n## Animation\n\n- **Animation system** — animation clips with keyframe tracks and interpolation (step/linear/bezier/hermite), an animator controller with a parameter-driven state machine and transitions, 1D/2D/direct blend trees, animation events, and IK constraint types (FABRIK, CCD, look-at, aim). GPU skeletal skinning is planned in the native runtime.\n\n## Audio\n\n- **Audio system** — 3D spatial audio over the Web Audio API: audio clips, per-source config, reverb zones, and a listener model. The production target uses the `kira` Rust audio library.\n\n## AI\n\n- **Behavior tree editor** — behavior-tree nodes (selector, sequence, parallel, decorator) with a node library, preset trees, and a blackboard runtime.\n- **AI NPC system** — dialogue trees (nodes, connections, conditions, actions, variables), a quest manager (objectives, rewards), and NPC profiles (stats, mood, schedule, factions, barks). NPC dialogue is designed to be driven by `xeno-agent-sdk` LLM proxying.\n\n## Particles & VFX\n\n- **Particle editor** — a particle-system runtime with emission modules, shapes, forces, gradients/curves, trails, texture-sheet animation, and presets (fire, smoke, sparks, rain, snow).\n- **GPU compute particles** — a compute-shader particle path for high particle counts, with automatic fallback to CPU instancing when WebGL2 float textures are unavailable.\n\n## World building\n\n- **Terrain system** — heightmap terrain with brush sculpting (raise/lower/smooth/flatten), multi-layer texturing, foliage instances, and water configuration.\n- **2D sprite system** — sprite renderer, sprite animation/atlases, tilemaps with tilesets and auto-tiling, 2D rigid bodies/colliders, a pixel-perfect camera, parallax layers, and sorting layers.\n\n## Input & UI\n\n- **Input system** — action-based input with bindings and rebinding, a default input preset, and axis/button action types.\n- **Game UI** — an in-game UI framework: UI elements/screens, styles, event handlers, data binding, and helpers to build a default HUD and pause menu.\n\n## Content pipeline\n\n- **Asset pipeline** — an import pipeline for models, textures, audio, and scripts, with per-kind metadata, import settings, and asset limits.\n- **Scene format** — the JSON scene serializer (see [Editor & scenes](/docs/engine/editor-and-scenes#scene-format)), including scene diffing and prefab references.\n\n## Networking (planned)\n\n- **Networking system** — a network client with a config/identity model, network transforms, players, rooms, RPC definitions, and stats. **The data model exists; the transport is not implemented.** Server-authoritative physics, client prediction, and reconciliation are planned.\n\n## Tooling & export\n\n- **Performance profiler** — frame/system profiling, memory snapshots, and bottleneck reports with profile sessions and summaries.\n- **Build system** — packaging config for building a game as an Electron app or web page, with build targets/profiles and generated Electron main/package/builder files.\n- **Web export** — a WASM + WebGPU web-build pipeline that generates an HTML template, a game bundle, and an asset manifest.\n\n## Next steps\n\n- [Scripting & play mode](/docs/engine/scripting)\n- [Status & FAQ](/docs/engine/status-and-faq)"
        }
      ]
    },
    {
      "title": "Help",
      "pages": [
        {
          "slug": "status-and-faq",
          "title": "Status & FAQ",
          "description": "Where the engine really is, what's shipped vs planned, and common questions for the alpha.",
          "body": "# Status & FAQ\n\n## Where is the engine right now?\n\nXENO Engine is in **alpha (v0.2.0)**. What works today is the **editor and its subsystem toolkit**: an entity/component scene editor, a Three.js viewport, TypeScript scripting, play mode with a fixed-timestep loop, undo/redo, prefabs, tags/layers, a JSON scene format, and the broad set of runtime subsystems in [Engine systems](/docs/engine/engine-systems). It is a foundation you can build scenes and gameplay against — not a shipping production engine yet.\n\n## Is the Rust/wgpu engine real?\n\nNot yet. The README describes the **architecture target** — a Rust runtime compiled as a native N-API module, rendering through `wgpu` with a deferred PBR pipeline, Rapier physics, `kira` audio, and a V8/`deno_core` scripting host. In v0.2.0 the viewport is a **Three.js WebGL preview**, physics is a lightweight integrator, and scripts run sandboxed in the editor process. Those subsystems are designed to be swapped for the native runtime as it lands.\n\n## What language do I write games in?\n\n**TypeScript.** Scripts implement lifecycle hooks (`onStart`, `onUpdate`, collision/trigger callbacks) and call a controlled engine API. See [Scripting & play mode](/docs/engine/scripting). A visual (Blueprints-style) node graph that compiles to TypeScript is on the roadmap.\n\n## Can I make 2D games?\n\nYes — the 2D sprite system provides a sprite renderer, sprite animation and atlases, tilemaps with auto-tiling, 2D physics components, and a pixel-perfect camera. Toggle the viewport's 2D mode while authoring.\n\n## Does multiplayer work?\n\nNot yet. The **Network Identity** component and the networking data model (rooms, players, transforms, RPCs, replication modes) exist so scenes can be authored for it, but the actual transport, server-authoritative physics, and client prediction are **planned, not implemented**.\n\n## Can I export a game?\n\nThe build and web-export subsystems exist and can generate an Electron app package or a WASM + WebGPU web bundle (HTML template, game bundle, asset manifest). Treat export as **early** in the alpha — it matures alongside the native runtime.\n\n## How does it fit with the rest of XENO?\n\nXENO Engine is a Layer 5 creative app. It imports `@xeno/core` (the shared history manager), is designed to consume `xeno-lib` AI models and embed `xeno-agent-sdk` for NPC dialogue, imports assets from XENO 3D / Pixel / Sound / Motion, and is launched from **XENO Hub**. See [Introduction](/docs/engine/introduction).\n\n## Where are updates published?\n\nBuilds are distributed through XENO Hub and the product pages on `xenostudio.ai/products/xeno-engine/`. Hub polls `updates.xenostudio.ai/apps/engine/version.json` and surfaces an update when a newer build is available. See [Install & launch](/docs/engine/installation).\n\n## What's the license?\n\nProprietary — © 2026 XENO Corporation. All rights reserved. (The repo's `package.json` is marked `private` / `UNLICENSED`.)"
        }
      ]
    }
  ]
};

export default engine;
