import type { ProductDocs } from './_types';

const architect: ProductDocs = {
  "slug": "architect",
  "productName": "XENO Architect",
  "tagline": "AI-native BIM and CAD — model, document, and analyze a whole building in one app.",
  "seo": {
    "title": "XENO Architect Documentation — AI-native BIM & CAD",
    "description": "Documentation for XENO Architect, the AI-native architecture and CAD tool. BIM modeling, 2D documentation, MEP, analysis, and IFC/DXF interoperability in a single Electron desktop app. Alpha (v0.2.0)."
  },
  "sections": [
    {
      "title": "Getting Started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Architect is, what it does today, and where it is on the road to a full BIM/CAD tool.",
          "body": "## What XENO Architect is\n\nXENO Architect is an AI-native architecture and CAD application. The goal is a single desktop app for the whole architectural workflow — from massing to construction documents — built BIM-native from the ground up, so one model is the single source of truth for 3D, drawings, schedules, and analysis. It is XENO's answer to Revit, ArchiCAD, SketchUp, Rhino, and Vectorworks.\n\nArchitect is part of the wider XENO ecosystem (see [Interoperability](/docs/architect/interoperability)). It is a Layer 5 creative app that imports the shared `@xeno/core` toolkit, launches from XENO Hub, and can call XENO's local/cloud AI runtimes.\n\n## Maturity: alpha (v0.2.0)\n\nArchitect is under active development. The core is real and usable: you can place BIM elements, orbit a shaded 3D model, generate plan/section/elevation views, run clash detection, and import/export IFC and DXF. But this is an alpha — several headline capabilities are partial or in development, and the app is honest about that (disabled ribbon buttons are labelled, and this documentation flags what is and is not wired). See [Status and roadmap](/docs/architect/status) for the current, code-accurate picture.\n\nOne architectural note worth stating up front: the 3D viewport currently runs on Three.js inside the Electron renderer. The Rust/wgpu native engine described in the project vision is the target architecture and is not yet compiled or integrated. Everything you use today runs in the TypeScript layer.\n\n## What you can do today\n\n- Model a building with parametric BIM elements: walls, floors, roofs, doors, windows, stairs, columns, beams, curtain walls, and rooms. Every element carries geometry and metadata (IFC class, materials, computed area/volume).\n- Work in a real-time 3D viewport: orbit, pan, zoom, switch between perspective and orthographic views, apply PBR materials, and run a sun study with shadows.\n- Document the model: auto-generated 2D plan/section/elevation drawings, dimensions, tags, schedules, sheets, and PDF export.\n- Lay out MEP: electrical, plumbing, and HVAC data with automatic electrical wire routing (A* pathfinding).\n- Analyze: clash detection, energy model export (gbXML), accessibility checks, and quantity/cost takeoffs.\n- Interoperate: import IFC and DWG/DXF; export IFC, gbXML, and PDF; save projects to the native `.xarch` format.\n\n## How this documentation is organized\n\n- Getting Started — this page and [Installation and quickstart](/docs/architect/installation).\n- Core Concepts — [The BIM model](/docs/architect/bim-model) and [The workspace](/docs/architect/workspace) (interface + keyboard shortcuts).\n- Building and Documenting — [Modeling](/docs/architect/modeling), [Documentation and views](/docs/architect/documentation), and [MEP and analysis](/docs/architect/mep-analysis).\n- Reference — [Interoperability](/docs/architect/interoperability) (import/export, file format, AI) and [Status and roadmap](/docs/architect/status).\n\nIf you are brand new, start with [Installation and quickstart](/docs/architect/installation) and model your first walls in a few minutes."
        },
        {
          "slug": "installation",
          "title": "Installation and quickstart",
          "description": "Install XENO Architect, launch it, and model your first walls, doors, and a floor.",
          "body": "## Requirements\n\nXENO Architect is a desktop application built on Electron 34 (React 19 + TypeScript). It ships as a native installer per platform:\n\n- Windows — NSIS installer (`XENO Architect Setup X.Y.Z.exe`)\n- macOS — DMG\n- Linux — AppImage\n\nThe app window is frameless with a minimum size of 900x600 and opens at 1400x900 by default. Window position and size are remembered between sessions.\n\n## Install\n\n### From XENO Hub (recommended)\n\nXENO Architect is distributed through XENO Hub, the ecosystem launcher. Hub manages auth, credits, and updates. When a new version is published, Hub detects it (it checks the release feed every few minutes) and shows an Update button.\n\n### Direct download\n\nInstallers are also published on `xenostudio.ai/products/architect/download/`. The download page auto-detects your OS and always points at the latest release.\n\n### Run from source (developers)\n\nThe repository is a standard electron-vite project:\n\n```bash\nnpm install\nnpm run dev        # launch in dev mode with hot reload\nnpm run build      # production build\nnpm run typecheck  # tsc --noEmit\nnpm test           # vitest\n```\n\nBuild installers with `npm run package` (or `package:win` / `package:mac` / `package:linux`).\n\n## Quickstart: your first model\n\nWhen Architect launches you get a new Untitled Project with sensible defaults already loaded: five levels (Foundation, Ground Floor, First Floor, Second Floor, Roof), a structural grid (axes A–D and 1–3), a materials library, and a set of compound wall types. See [The BIM model](/docs/architect/bim-model) for what those defaults mean.\n\n1. Draw walls. Press `W` (or click Architecture in the ribbon and pick Wall). Walls are placed on the active level (Ground Floor by default).\n2. Add a floor. Press `Shift+F` for the Floor tool.\n3. Insert doors and windows. Press `D` for a door, `Shift+W` for a window. These are hosted elements — they belong to a wall, and deleting the host wall removes them too.\n4. Add stairs and structure. `Shift+S` for stairs, `C` for a column, `B` for a beam.\n5. Orbit and inspect. Press `Q` to orbit, `H` to pan, and use number keys `1`–`6` to snap to perspective, top, front, right, left, and back views.\n6. Select and edit. Press `V` to select. When you select something, the ribbon automatically switches to the Modify tab (move, copy, rotate, mirror, align, offset, trim, split, array). Edit numeric properties in the Properties panel on the right.\n7. Save. `Ctrl+S`. Architect writes a `.xarch` project bundle (a directory containing `manifest.json`, `state.json`, and an `assets/` folder). Reopen with `Ctrl+O`.\n\nThe title bar shows the project name, and Architect writes auto-save recovery snapshots in the background (keeping the last 20).\n\nFor the full tool list and every shortcut, see [The workspace](/docs/architect/workspace)."
        }
      ]
    },
    {
      "title": "Core Concepts",
      "pages": [
        {
          "slug": "bim-model",
          "title": "The BIM model",
          "description": "How Architect structures a building: projects, sites, levels, grids, BIM elements, materials, and IFC metadata.",
          "body": "## BIM, not just geometry\n\nXENO Architect is BIM-native: a wall is not just a box, it is a building object that carries geometry, metadata, and relationships. This is the core difference from a pure modeler. Every element in the model is a `BimElement` with:\n\n- Geometry — position, rotation (degrees), and dimensions (width / height / depth, in millimetres).\n- Identity — an IFC class (e.g. `IfcWall`) and a 22-character IFC GlobalId (`ifcGuid`) generated on creation.\n- Hosting — the `levelId` it lives on, an optional `hostId` (a door hosted in a wall), and `connectedIds` for joined elements.\n- Material — a `materialId`, plus an optional `wallTypeId` for compound walls.\n- IFC property sets — an `ifcProperties` map (e.g. `Pset_WallCommon.FireRating`, `Pset_WallCommon.IsExternal`).\n- Computed quantities — `area` (m2) and `volume` (m3), recomputed from dimensions with a `dirty` flag so derived values never go stale.\n\n## The project hierarchy\n\nA project is organized top-down:\n\n```\nProject\n └── Site\n      └── Building\n           └── Levels[]   (ordered by elevation)\n                ├── Walls, Floors, Roofs\n                ├── Doors, Windows (hosted in walls)\n                ├── Stairs, Columns, Beams, Curtain walls\n                ├── Rooms (bounded by walls, area/volume computed)\n                └── MEP elements (electrical, plumbing, HVAC)\n```\n\nAlongside the physical model, a project also holds Views (plan / section / elevation / 3D), Sheets (title block + placed views), Schedules, Materials, Wall types, phases, and worksets.\n\n## Levels\n\nLevels are horizontal datums measured in millimetres from the project datum (0.0). A fresh project has five:\n\n| Level | Elevation | Structural |\n|-------|-----------|-----------|\n| Foundation | -600 mm | yes |\n| Ground Floor | 0 mm | yes |\n| First Floor | 3200 mm | yes |\n| Second Floor | 6400 mm | yes |\n| Roof | 9600 mm | no |\n\nElements are placed on the active level.\n\n## Grids\n\nThe structural grid is a set of named axes. Defaults are vertical axes A (0), B (6000), C (12000), D (18000) and horizontal axes 1 (0), 2 (6000), 3 (12000) — a 6 m orthogonal grid.\n\n## Materials\n\nMaterials carry both visual (PBR: albedo, roughness, metallic, opacity) and physical properties used by analysis (density, thermal conductivity, compressive strength, fire rating, U-value). The default library includes concrete, facing brick, glulam, structural steel, double glazing, mineral wool, limestone, gypsum board, anodized aluminium, and a CLT panel.\n\n## Compound wall types\n\nWalls reference a wall type — a stack of material layers, each with a thickness, a `function` (structure / substrate / insulation / finish / membrane), and a `priority` that governs how walls join. Defaults include an External Cavity Wall (facing brick + mineral wool + concrete + gypsum), an Internal Partition, a Concrete Shear Wall, and a Curtain Wall System.\n\n## IFC class mapping\n\nEvery element type maps to an IFC class so the model round-trips to and from IFC — `wall → IfcWall`, `floor → IfcSlab`, `roof → IfcRoof`, `door → IfcDoor`, `window → IfcWindow`, `stairs → IfcStairFlight`, `column → IfcColumn`, `beam → IfcBeam`, `curtainWall → IfcCurtainWall`, and MEP/site types to their IFC equivalents (e.g. `pipe → IfcPipeSegment`, `duct → IfcDuctSegment`). See [Interoperability](/docs/architect/interoperability) for import/export.\n\n## Units and coordinates\n\n- World coordinates: right-handed, millimetres as the base stored unit.\n- Display units are user-selectable (mm / cm / m / ft-in / inches); the stored model is always in mm.\n- Angles: degrees in the UI.\n- Level elevations: absolute from the project datum (0.0)."
        },
        {
          "slug": "workspace",
          "title": "The workspace",
          "description": "The Architect interface — menu bar, ribbon, tools sidebar, viewport, project browser, properties — and every keyboard shortcut.",
          "body": "## Layout\n\nArchitect uses a Revit-style layout under a monochromatic dark theme:\n\n```\n+------------------------------------------------------+\n| Menu Bar                                  [_] [ ] [x] |\n+------------------------------------------------------+\n| Ribbon Bar (contextual tabs + tool panels)           |\n+------+---------------------------------------+--------+\n| Tools|            3D Viewport                | Prop-  |\n| side |                                       | erties |\n| bar  +---------------------------------------+ panel  |\n|      |         Project Browser (tree)        |        |\n+------+---------------------------------------+--------+\n| Status Bar                                            |\n+------------------------------------------------------+\n```\n\n- Menu bar — File, Edit, View, Architecture, Structure, Annotate, Tools, Collaborate, Help, plus the app title and window controls.\n- Ribbon bar — contextual tabs (below). This is the primary way to pick tools.\n- Tools sidebar — a compact tool palette on the left.\n- Viewport — the 3D scene (Three.js) with a toolbar for view direction.\n- Project browser — a tree of levels, views, and families under the viewport.\n- Properties panel — edits the selected element's dimensions, rotation, material, and parameters.\n- Status bar — model info, units, and cursor coordinates.\n\n## Ribbon tabs\n\nThe ribbon groups tools into tabs that mirror the phases of design: Architecture (walls, floors, roofs, doors, windows, stairs, columns, curtain walls, rooms), Structure (structural elements; Loads and Check are present but disabled), MEP (electrical, plumbing, HVAC, and Auto Route), Site (boundary, topo, road, parking, landscape), Annotate (dimension, measure, section, elevation, text, tag, callout, revision cloud), Analyze (sun, energy, schedule, clash, quantity takeoff, accessibility, areas, render), AI (Floor Plan and Furnish), and Collaborate (Links & Groups working; Worksharing labelled Coming Soon). The Modify tab appears automatically when you have a selection.\n\n## Keyboard shortcuts\n\nThese are the shortcuts wired in the app today. Typing in a text input never triggers a tool.\n\n### Tools\n\n| Key | Tool | Key | Tool |\n|-----|------|-----|------|\n| V | Select | H | Pan |\n| Q | Orbit | M | Measure |\n| W | Wall | R | Roof |\n| D | Door | C | Column |\n| B | Beam | T | Topography |\n| Shift+F | Floor | Shift+W | Window |\n| Shift+S | Stairs | Shift+C | Curtain wall |\n| Shift+P | Section | Shift+D | Dimension |\n| Shift+R | Room | | |\n\n### View and toggles\n\n| Key | Action | Key | Action |\n|-----|--------|-----|--------|\n| 1 | Perspective | 2 | Top |\n| 3 | Front | 4 | Right |\n| 5 | Left | 6 | Back |\n| G | Toggle grid | O | Toggle ortho |\n| S | Toggle snap | + / − | Zoom in / out |\n\n### Editing\n\n| Key | Action |\n|-----|--------|\n| Esc | Deselect and return to the Select tool |\n| Delete / Backspace | Delete selected (cascades to hosted elements) |\n| Ctrl+D | Duplicate selected |\n| Ctrl+Z | Undo |\n| Ctrl+Shift+Z or Ctrl+Y | Redo |\n\n### Menu commands\n\nFile and project commands live in the menus: `Ctrl+O` Open, `Ctrl+S` Save, `Ctrl+I` Import IFC, `Ctrl+E` Export IFC, `Ctrl+P` Export PDF. New Project (`Ctrl+N`) and Save As (`Ctrl+Shift+S`) appear in the menu but are placeholders in this build.\n\n> Note: this list reflects the shortcuts wired in the code, not the idealized table in the repository README. Trust this page."
        }
      ]
    },
    {
      "title": "Building and Documenting",
      "pages": [
        {
          "slug": "modeling",
          "title": "Modeling",
          "description": "Placing and editing BIM elements — walls, floors, roofs, doors, windows, stairs, structure, curtain walls, rooms — plus materials and modify tools.",
          "body": "## Placing elements\n\nPick a tool from the ribbon or its shortcut, then draw in the viewport. New elements are added to the active level and given an IFC class and GlobalId automatically. Each element's area and volume are computed on creation and kept in sync when you change its dimensions.\n\n### Walls\n\nPress `W`. Walls are parametric and reference a wall type — a compound layer stack (see [The BIM model](/docs/architect/bim-model)). Wall joins are tracked so adjoining walls resolve their layers by priority. There is a dedicated wall-editing mode with grips, snap targets, and a live dimension readout while dragging.\n\n### Floors and roofs\n\n`Shift+F` places a floor slab (with selectable floor types); `R` places a roof generated from parameters (footprint, slope, type).\n\n### Doors and windows\n\n`D` and `Shift+W`. These are hosted components — they belong to a wall and cut an opening in it. Architect ships default door and window families with types and hardware sets. Because they are hosted, deleting the host wall deletes them too.\n\n### Stairs\n\n`Shift+S`. Stairs are calculated from parameters (straight / L / U / spiral) rather than drawn by hand.\n\n### Structure and curtain walls\n\n`C` for columns and `B` for beams. Curtain walls (`Shift+C`) generate a mullion/panel grid as a single system.\n\n### Rooms\n\n`Shift+R`. Rooms are detected from the surrounding walls; their area and volume are computed from the bounded region.\n\n### Site and topography\n\n`T` places a topographic surface. The Site ribbon tab adds boundaries, roads, parking, and landscape.\n\n## Selecting and editing\n\nPress `V` to select; click an element, or Shift-click to extend the selection. Selecting anything switches the ribbon to the Modify tab: Move, Copy, Rotate, Mirror, Align, Offset, Trim, Split, Array, and Paint (apply a material to a face). Common edits also have direct shortcuts — `Ctrl+D` duplicates the selection and `Delete` removes it (cascading to hosted children) — and the Properties panel lets you type exact width, height, depth, rotation, and material.\n\n## Materials\n\nApply materials from the library (see [The BIM model](/docs/architect/bim-model) for the default set and their physical properties). The Paint tool assigns a material to a specific face of an element. Material choices drive both the PBR appearance in the viewport and downstream analysis (U-values for energy, densities for quantities).\n\n## Undo, redo, and saving\n\nEvery modeling operation is undoable (`Ctrl+Z` / `Ctrl+Shift+Z`), with the Edit menu showing a description of the next undo/redo step. Projects save to the `.xarch` bundle format, and background auto-save keeps recovery snapshots. See [Interoperability](/docs/architect/interoperability) for the file format and import/export."
        },
        {
          "slug": "documentation",
          "title": "Documentation and views",
          "description": "Turning the 3D model into drawings — plans, sections, elevations, dimensions, tags, schedules, sheets — plus rendering, sun studies, and PDF output.",
          "body": "## Views generated from the model\n\nArchitect's 2D documentation is generated from the same BIM model, so drawings stay consistent with the 3D geometry. Views are rendered as an SVG overlay on the viewport.\n\n- Floor plans — generated per level with a configurable view range (cut plane, depth) that controls what is shown.\n- Sections — placed with the Section tool (`Shift+P`); a section view is generated from the cut plane.\n- Elevations — placed with the Elevation tool from the Annotate tab.\n\nView direction in 3D is controlled with the number keys (`1`–`6`) and the viewport toolbar.\n\n## Annotation\n\nFrom the Annotate ribbon tab: the aligned Dimension tool (`Shift+D`) with dimension styles (the engine also supports dimension chains and spot elevations), ad-hoc Measure (`M`), element Tags with configurable styles, and Callouts and revision clouds for detailing and revision tracking.\n\n## Schedules\n\nSchedules are tabular views of the model — door, window, room, and other categories with fields, filters, and sorting. Default schedule definitions ship with the app, and the Tools menu exposes wall, door, window, and room-finish schedules. Schedules read live from the model, so quantities update as you edit.\n\n## Sheets and PDF export\n\nA sheet carries a title block and a set of placed views at a chosen size and scale, and is the unit of construction-document output. Export PDF (`Ctrl+P`, File → Export PDF) renders your sheets to a PDF document with the project name and number.\n\n## 3D viewport and rendering\n\nThe viewport is a real-time PBR scene (running on Three.js today; the Rust/wgpu engine is the target — see [Status and roadmap](/docs/architect/status)). It supports orbit/pan/zoom, orthographic and perspective projection, PBR materials, and shadows.\n\n### Render to image\n\nAnalyze → Render captures the current viewport as a PNG at 1920x1080 and downloads it — a direct viewport capture. A progressive CPU path tracer for higher-quality offline renders (Lambertian + GGX BRDF, soft shadows, indirect lighting, ACES tone mapping) exists as an engine module but is not yet wired to a UI button — see [Status and roadmap](/docs/architect/status).\n\n### Sun study\n\nToggle the sun study from Analyze → Sun (or Tools → Sun Study). It computes sun position from latitude/longitude, date, and time (London coordinates by default) and casts shadows in the viewport, with configurable shadow opacity and north angle.\n\n## Where documentation output goes\n\nDrawings and models leave Architect as PDF (sheets), IFC (full model), gbXML (energy), and DXF. Rendered stills are PNG. See [Interoperability](/docs/architect/interoperability) for the complete import/export matrix."
        },
        {
          "slug": "mep-analysis",
          "title": "MEP and analysis",
          "description": "Electrical, plumbing, and HVAC layout with automatic wire routing, plus clash detection, energy, accessibility, and quantity/cost analysis.",
          "body": "## MEP layout\n\nThe MEP ribbon tab places mechanical, electrical, and plumbing elements. These are backed by data models with engineering calculations; placement and the automatic electrical routing are the most complete part.\n\n### Electrical\n\nPlace panels, outlets, switches, and lighting fixtures. The app ships default electrical panels, and circuits connect devices back to panels.\n\n### Automatic wire routing\n\nMEP → Auto Route runs A* pathfinding to route conduit from devices to their panel:\n\n- A discretized 2D grid per level (50 mm cells by default).\n- Obstacle avoidance — walls and columns on the active level become blocked cells.\n- Prefers routes along walls and orthogonal (Manhattan) runs, with an optional 45-degree diagonal.\n- NEC 40% conduit fill calculation and multi-circuit conduit sharing.\n\nAuto Route builds obstacles from the visible walls and columns on the active level, computes the level bounds, and routes every circuit for each panel, adding the resulting wire segments to the model.\n\n### Plumbing and HVAC\n\nPlumbing provides fixtures, pipe segments, fittings, risers, and hot-water systems. HVAC provides ducts, diffusers/air terminals, equipment, and zones. These are data-model and calculation features; automatic pipe/duct routing is not yet implemented (only electrical auto-routing is).\n\n## Analysis\n\nFrom the Analyze tab and the Tools menu:\n\n### Clash detection\n\nTools → Clash Detection runs a hard-clash test across all elements using spatial checks, then reports a summary broken down into critical / major / minor. A set of default clash tests ships with the app.\n\n### Energy\n\nThe energy engine builds thermal zones and computes performance from the model's U-values and climate data (climate presets included, London by default). Export the energy model as gbXML (File → Export gbXML) for use in external energy tools.\n\n### Accessibility compliance\n\nAn accessibility checker evaluates the model against a selected standard and produces a compliance report.\n\n### Quantity surveying\n\nThe Quantity Takeoff tool computes material takeoffs and a bill of quantities from the model, priced against a unit-cost library — turning the BIM model directly into a costed schedule of quantities.\n\n## Structural analysis (in development)\n\nStructural loads, code checks, and load-path visualization appear in the Structure ribbon and menus but are currently disabled. Physics-based structural simulation (Rapier) is part of the target Rust engine, which is not yet integrated. See [Status and roadmap](/docs/architect/status)."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "interoperability",
          "title": "Interoperability",
          "description": "Import and export formats, the .xarch project file, and how Architect's AI features work.",
          "body": "## The .xarch project file\n\nArchitect saves projects in its native `.xarch` format. Unlike a single opaque file, a `.xarch` project is a directory bundle:\n\n```\nproject.xarch/\n ├── manifest.json   — metadata and format version\n ├── state.json      — the full application state (elements, levels, grids,\n │                       materials, wall types, views, sheets, schedules, ...)\n └── assets/         — linked external files (textures, reference images)\n```\n\n`Ctrl+S` saves (prompting for a location the first time), `Ctrl+O` opens. Architect also writes auto-save recovery snapshots to a sibling `-autosave` folder in the background, keeping the most recent 20.\n\n## Import\n\nFile → Import (or the shortcuts below). Imported geometry is added to the active level.\n\n| Format | Extension | Command | Notes |\n|--------|-----------|---------|-------|\n| IFC | `.ifc` | Import IFC (`Ctrl+I`) | Parsed to BIM elements |\n| DWG / DXF | `.dwg` / `.dxf` | Import DWG/DXF | Line work is converted into walls (per DXF layer) |\n| Point cloud | `.e57` | — | Present in the menu but disabled (in development) |\n\nThe file dialog also lists SKP, OBJ, glTF/GLB, and USD extensions as openable file types; IFC and DXF are the import paths wired end-to-end today.\n\n## Export\n\n| Format | Command | Use case |\n|--------|---------|----------|\n| IFC | Export IFC (`Ctrl+E`) | BIM exchange with structural/MEP engineers |\n| gbXML | Export gbXML | Energy modeling in external tools |\n| PDF | Export PDF (`Ctrl+P`) | Construction documents from sheets |\n| PNG | Analyze → Render | Rendered viewport still (1920x1080) |\n\nThe export dialog also lists glTF and OBJ filters, but the File-menu glTF export has no wired handler yet — treat it as planned. IFC export writes a valid IFC file from the model, carrying each element's IFC class, GlobalId, and property sets.\n\n## AI features\n\nArchitect's AI runs through XENO's shared LLM layer (the `DualLLMProvider` from `xeno-agent-sdk`), which routes to a local model via xeno-rt or to the cloud (`api.xenostudio.ai`), with an automatic mode that prefers local when available. AI features require `@xeno-corporation/xeno-agent-sdk` to be installed; without it, the rest of the app still works but AI is unavailable.\n\nThree AI operations are exposed today:\n\n- Chat — an embedded assistant scoped to building design, BIM, and construction documentation.\n- Floor-plan generation — describe a layout in natural language and get a described room layout with dimensions, placements, and circulation.\n- Building analysis — energy, code-compliance, or cost analysis of the current design, returned as an LLM assessment.\n\nThese are text-based assists in this alpha: the AI reasons about the design and returns descriptions and suggestions. Direct generation of geometry (auto-placing a generated floor plan, auto-furnishing rooms) is in development — the AI ribbon tab exposes Floor Plan and Furnish, but the geometry-writing side is not complete. See [Status and roadmap](/docs/architect/status).\n\n## Ecosystem integration\n\nArchitect imports `@xeno/core` (shared panel system, color pipeline, UI, plugins), is launched and credited through XENO Hub, and its AI uses xeno-rt (local) and the XENO cloud. See the ecosystem report for the full map."
        },
        {
          "slug": "status",
          "title": "Status and roadmap",
          "description": "An honest, code-accurate view of what works in v0.2.0, what is partial, and what is planned.",
          "body": "## Where Architect is\n\nXENO Architect is alpha (v0.2.0), under active development. The BIM core, 3D viewport, 2D documentation, MEP electrical routing, and IFC/DXF interoperability are real and usable. Several advanced features are partial or in development, and the app labels disabled tools honestly. This page is the ground truth — where the marketing README and the code disagree, the code wins.\n\n## The engine: current vs target\n\nThe project's long-term architecture is a Rust native engine (compiled via napi-rs) that renders through wgpu (Vulkan/Metal/DX12) and hosts the BIM kernel, OpenCASCADE geometry, Rapier physics, and a PyO3 Python runtime. None of that Rust engine is compiled or integrated yet. Today:\n\n- The 3D viewport runs on Three.js in the Electron renderer.\n- All BIM, drafting, MEP, and analysis logic runs in the TypeScript engine layer (each module is intended to be replaced by a Rust equivalent later).\n- Geometry is not yet backed by a NURBS/B-Rep kernel (OCCT); it is parametric box/mesh geometry.\n\n## Feature status\n\n| Feature | Status |\n|---------|--------|\n| BIM elements (walls, floors, roofs, doors, windows, stairs, columns, beams, curtain walls, rooms) | Working |\n| 3D viewport (Three.js PBR, orbit/pan/zoom, shadows) | Working |\n| 2D views (plan, section, elevation, SVG overlay) | Working |\n| Materials (PBR + physical properties) | Working |\n| Dimensions, tags, schedules, sheets | Working |\n| Render to image (PNG viewport capture) | Working |\n| Sun study (sun path + shadows) | Working |\n| Undo/redo, project save/load (.xarch), auto-save recovery | Working |\n| Import IFC / DWG-DXF; export IFC / gbXML / PDF | Working |\n| Clash detection, accessibility check, quantity/cost takeoff | Working |\n| MEP data models (electrical, plumbing, HVAC) | Working (data + calcs) |\n| Automatic electrical wire routing (A*) | Working |\n| Automatic pipe/duct routing | Planned |\n| Progressive path-traced offline rendering | Engine module present, not wired to UI |\n| Python scripting (Pyodide WASM) | Engine module present, no editor UI |\n| AI floor-plan generation / building analysis | Working as LLM text assist |\n| AI geometry generation / auto-furnish | In development |\n| Structural analysis (loads, code check, load paths) | Planned (disabled in UI) |\n| Collaboration / worksharing (realtime) | Data model only; UI disabled |\n| glTF export | Planned (dialog filter present, no File-menu handler) |\n| Point cloud (E57) import | Planned (menu, disabled) |\n| Rust/wgpu native engine (napi-rs, OCCT, Rapier, PyO3) | Planned (target architecture) |\n\n## FAQ\n\nIs the Rust engine in the current build? No. The viewport and all logic run in TypeScript/Three.js today. The Rust engine is the target architecture.\n\nWhy do the README shortcuts differ from the app? The README lists an idealized table. The [workspace page](/docs/architect/workspace) documents the shortcuts actually wired in the code — trust those.\n\nWhat file extension do projects use? `.xarch` — a directory bundle, not a single file. (Some ecosystem docs reserve `.xarchitect`; the shipping build writes `.xarch`.)\n\nCan I script the model? A Pyodide (Python-in-WebAssembly) engine module exists, but there is no script-editor UI yet, so scripting is not exposed to users in this build.\n\nDoes AI need to be online? AI routes through xeno-agent-sdk's dual provider — it can use a local model via xeno-rt or the cloud. It requires the agent SDK to be installed.\n\nIs my model portable? Yes — export IFC for BIM exchange and PDF for drawings. IFC round-trips class, GlobalId, and property sets.\n\nFor the current release notes and downloads, see `xenostudio.ai/products/architect/`."
        }
      ]
    }
  ]
};

export default architect;
