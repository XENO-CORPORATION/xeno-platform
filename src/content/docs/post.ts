import type { ProductDocs } from './_types';

const post: ProductDocs = {
  "slug": "post",
  "productName": "XENO Post",
  "tagline": "The open-source, AI-native social media command center — schedule, publish, listen, and respond across many platforms, self-hosted and agent-ready.",
  "seo": {
    "title": "XENO Post documentation",
    "description": "Install, configure, and operate XENO Post — the self-hostable, AGPL-3.0 social media management platform with a capability-driven composer, BullMQ scheduling, unified inbox, a first-class AGENT role, a native MCP server, and a versioned REST API."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Post is, where it fits in the XENO ecosystem, and its current maturity.",
          "body": "XENO Post is the open-source, AI-native social media command center. It lets you schedule, publish, listen, and respond across many social platforms from one place — self-hosted, agentic, and without a per-seat subscription.\n\nIt is a self-hostable, AGPL-3.0 alternative to tools like Postiz, Mixpost, Buffer, Hootsuite, Later, Sprout Social, and Publer. It runs on its own with just Postgres + Redis, and — when the rest of the XENO Corporation ecosystem is reachable — can unlock local AI inference and deeper media integrations.\n\n## What it does\n\n- Compose once, target many — a capability-driven composer builds a platform-agnostic post, then materializes a per-channel target for each account you select, with independently editable copy.\n- Schedule reliably — a BullMQ producer queues publish jobs on Redis; a worker publishes at the scheduled moment with exponential-backoff retries.\n- Unify engagement — inbound DMs, comments, and mentions normalize into a single inbox of conversations and messages (for connectors that implement ingestion).\n- Generate with AI — caption, hashtags, alt-text, rewrite, and thread-split tasks, backed by your choice of provider (local xeno-rt, OpenAI, Anthropic, Google, or Ollama).\n- Automate agentically — a native MCP server and a first-class AGENT role make LLM agents members of your workspace, not bolted-on service accounts.\n- Audit everything — an immutable, hash-chained audit log records every privileged action, human or agent.\n\n## Where it fits\n\nXENO Post is Layer 6 (Office, Productivity & Communications) of the XENO ecosystem. It is a standalone product first and integrates natively second — detecting sibling services at boot and falling through to non-XENO equivalents when they are absent.\n\n## Maturity\n\nXENO Post is alpha (v0.1.0, released 2026-06-27) and under active development. The v0.1.0 backend, worker, web dashboard, MCP server, REST API, SDKs, and the production X (Twitter) connector are implemented and tested; most other connectors and the mobile/desktop clients are in progress. Where a feature is planned rather than shipped, these docs say so explicitly. See [Status and roadmap](/docs/post/roadmap) for the honest picture.\n\n## Next steps\n\n- [Install XENO Post](/docs/post/installation) — Docker Compose self-host\n- [Publish your first post](/docs/post/first-post) — connect a channel and go live\n- [Core concepts](/docs/post/concepts) — Workspace, Channel, Post, Target, Inbox, Monitor\n\n> XENO Post is AGPL-3.0. Self-hosting is unconditionally free, with no feature paywall and no \"enterprise edition\" gates."
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Self-host XENO Post with Docker Compose in a few minutes.",
          "body": "The fastest way to a working XENO Post is Docker Compose. You need Docker 24+ and roughly 2 CPU / 2 GB RAM free.\n\n## 1. Clone and configure\n\n```bash\ngit clone https://github.com/XENO-CORPORATION/xeno-post.git\ncd xeno-post\ncp .env.example .env\n```\n\nOpen `.env` and set, at minimum:\n\n- `XENO_POST_KMS_KEY` — a 32-byte base64 string. Generate one with `openssl rand -base64 32`. This is the master key that encrypts every stored OAuth token. Do not lose it — without it, your encrypted channel credentials are unreadable.\n- `POSTGRES_PASSWORD` — any strong password.\n- `PUBLIC_BASE_URL` — the URL your browser will hit.\n\nIf you want AI features now, also set one of `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, or point `XENO_RT_URL` at a running xeno-rt instance. AI is optional — the rest of the product works without it.\n\n## 2. Start the stack\n\n```bash\ndocker compose up -d\n```\n\nThe compose file ships five services: `postgres`, `redis`, `api`, `worker`, and `web`. The web app listens on port 4300 and the API on 4301 by default — a deliberately lean footprint next to heavier competitors that require many more containers.\n\n## 3. Run migrations\n\n```bash\ndocker compose exec api pnpm db:migrate\n```\n\nMigrations are forward-only (append-only Prisma migrations). Run this on the first boot.\n\n## 4. Verify\n\n```bash\ncurl http://localhost:4301/health\n# -> {\"status\":\"ok\",\"version\":\"0.1.0\",\"uptime\":123.4,\n#     \"checks\":{\"db\":{\"status\":\"ok\"},\"redis\":{\"status\":\"ok\"},\"storage\":{\"status\":\"ok\"}}}\n```\n\nThe API also exposes `/health/live`, `/health/ready`, and a Prometheus `/metrics` endpoint (these stay unprefixed; the versioned API lives under `/api/v1`). Then open `http://localhost:4300` in a browser — you create the first user, who becomes the OWNER of the default workspace. A first-run onboarding wizard walks you through connecting a channel and drafting a first post.\n\n## From source (development)\n\nXENO Post is a pnpm + Turborepo monorepo. Requires Node 22, pnpm 10, Postgres 16+, Redis 7+.\n\n```bash\npnpm install\npnpm db:migrate\npnpm dev          # runs api, web, and worker concurrently via turbo\n```\n\n## Updating\n\n```bash\ngit pull\ndocker compose pull\ndocker compose up -d\ndocker compose exec api pnpm db:migrate\n```\n\nSee [Self-hosting and operations](/docs/post/self-hosting) for backups, security, and production deployment.\n\n## Next\n\n- [Publish your first post](/docs/post/first-post)\n- [Core concepts](/docs/post/concepts)"
        },
        {
          "slug": "first-post",
          "title": "Your first post",
          "description": "Connect a channel, compose, and publish — from a fresh install to a live post.",
          "body": "This walkthrough goes from a fresh install to a published post on a real account.\n\n## 1. Sign in\n\nAfter [installing](/docs/post/installation), open `http://localhost:4300`. Create a user — you become the OWNER of a workspace. You can rename it or create more workspaces later in Settings.\n\n## 2. Connect a channel\n\nGo to Channels → Add channel and pick a platform. Each connected account is a Channel with its own encrypted credentials and declared capability set.\n\nMost platforms use an OAuth developer app. Bluesky is the easiest first connection because it needs no OAuth app — an app password is enough:\n\n1. At `https://bsky.app/settings/app-passwords`, create an app password named, e.g., `xeno-post`.\n2. In XENO Post: Channels → Add channel → Bluesky.\n3. Enter your handle (`yourname.bsky.social`) and the app password.\n4. Click Connect. XENO Post fetches your profile, envelope-encrypts the credentials with your `XENO_POST_KMS_KEY`, and stores them.\n\nThe channel appears with status active and its capability matrix.\n\n## 3. Compose\n\nClick New post. The composer shows a text area with a live character count, a media drawer, and a per-channel preview pane. Type a sentence, optionally drop in an image, and select your channel on the right.\n\nThe composer is capability-driven: it reads each selected connector's capability set to decide which controls are enabled. With only Bluesky selected, a control the platform does not support (such as native polls) stays disabled. The character cap shown is the intersection of the caps of every selected channel.\n\n## 4. Schedule or publish now\n\n- Publish now enqueues a BullMQ publish job immediately; the worker picks it up shortly after.\n- Schedule lets you pick a date, time, and timezone; the job waits in Redis until then.\n\nClick Publish now and watch the state advance through `draft → queued → publishing → published`. The permalink appears in the activity view alongside the real post on the platform.\n\n## 5. Watch analytics roll in\n\nFor connectors that implement metric collection, the analytics collector automatically schedules pulls at +1h, +6h, +24h, and +7d after publish, writing time-series metric rows you can read back per post and per channel.\n\n## Where to go next\n\n- [Core concepts](/docs/post/concepts) — the nouns behind everything you just did\n- [Channels and connectors](/docs/post/connectors) — per-platform capabilities and status\n- [AI features](/docs/post/ai-features) — generate captions, hashtags, and threads\n- [Command-line and SDKs](/docs/post/cli-and-sdks) — publish from a script"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "concepts",
          "title": "Core concepts",
          "description": "Workspace, Channel, Post, Target, Inbox, and Monitor — plus the seven roles.",
          "body": "A handful of nouns appear everywhere in XENO Post. Learn them once and the rest of the product follows.\n\n## Workspace\n\nThe multi-tenancy boundary. A workspace owns channels, posts, members, AI provider keys, the audit log, and notifications. One user can belong to many workspaces; all permissions are workspace-scoped. The data model is a single multi-tenant Postgres database with a `workspaceId` discriminator on every row.\n\n## Channel\n\nA connected social account on a specific platform. Each channel stores:\n\n- envelope-encrypted credentials (AES-256-GCM, per-workspace data key)\n- a declared capability set describing what the platform supports (text length, media constraints, polls, threads, and so on)\n- rate-limit / quota state, tracked by the connector kit\n- its last error timestamp and message\n\nThe same vendor can expose multiple channel types when the identity, scopes, and rate-limit pools differ — for example LinkedIn splits into personal and organization connectors, and Meta into Facebook and Instagram.\n\n## Post\n\nA composition that targets one or more channels. A post is platform-agnostic until publish time. Its state flows through a strict machine: `draft → queued → publishing → published | partial | failed | cancelled`. `partial` records the case where one target succeeded and another failed — both outcomes are stored.\n\n## Target (PostTarget)\n\nThe per-channel materialization of a Post. Select three channels and you get three targets. Each target can override the body (different copy for LinkedIn than for X), carry platform-specific settings, and track its own platform post id, permalink, attempt count, and metrics. This is why XENO Post is not a blunt cross-poster — every target is independently editable.\n\n## Inbox\n\nInbound DMs, comments, and mentions across your channels, normalized into `Conversation` and `Message` records. Connectors that implement inbox ingestion pull items on a schedule with a per-channel watermark and duplicate protection, so the same message is never ingested twice.\n\n## Monitor\n\nA listening rule (keyword, account, topic, or feed). Matches produce mention records you can review. Listening is opt-in per workspace and, like the inbox, driven by connector ingestion.\n\n## The seven roles\n\nMembership is locked at seven roles — six human plus one agent:\n\n| Role     | Can do |\n|----------|--------|\n| OWNER    | Everything, including delete-workspace. |\n| ADMIN    | Manage members and connections. Cannot delete the workspace. |\n| EDITOR   | Create and publish posts; manage AI provider keys. |\n| REVIEWER | Approve or reject posts in flight; cannot create. |\n| VIEWER   | Read-only across the workspace. |\n| CLIENT   | External reviewer — read, comment, and approve on shared posts. |\n| AGENT    | An LLM identity. Like an editor, but with explicit rate and token budgets and full audit attribution. |\n\nThe AGENT role is XENO Post's defining choice: agents are first-class members, not service accounts. Every action they take is logged with the agent's identity. See [Agents and MCP](/docs/post/mcp).\n\n## Audit log\n\nEvery privileged action — human or agent — appends to an immutable, hash-chained `AuditEvent` table. Entries reference token ids, never token values; no plaintext credential ever touches a log. This is a v0.1 guarantee, not a later add-on."
        },
        {
          "slug": "connectors",
          "title": "Channels and connectors",
          "description": "How platform connectors work, capability-driven composing, and current connector status.",
          "body": "Every social platform is a connector — its own npm package that implements a shared `PostConnector` interface from `@xeno-post/connector-kit`. Adding a platform means adding a package, never editing another connector or the core API. This is the plugin-first rule that keeps XENO Post extensible.\n\n## The connector contract\n\nA connector implements a fixed surface (authorize, complete authorization, refresh token, revoke, upload media, validate, publish, delete) and declares an honest capability set. Undeclared capabilities are simply unavailable — the composer will not offer a control a connector has not declared. Connectors may also optionally implement ingestion methods (`collectMetrics`, `fetchInboxItems`, `replyToInboxItem`) to power analytics and the unified inbox.\n\n## Capability-driven composing\n\nThe composer never hardcodes platform names. It reads the capability sets of the selected channels and enables a control only when every selected channel supports it. The character limit shown is the intersection of the selected channels' limits, so you cannot compose something one target will reject. Validation runs again at schedule time against each connector's declared constraints.\n\n## OAuth and the token vault\n\nConnecting a channel runs an OAuth flow (or an app-password flow, where the platform supports one, such as Bluesky). The channels module holds a Redis-backed, CSRF-safe OAuth state store with a short (10-minute) TTL, and stores the resulting credentials in an envelope-encrypted token vault. A refresh worker renews expiring tokens under a Redis distributed lock so two workers never refresh the same channel at once.\n\n## Connector status (v0.1.0)\n\nXENO Post is alpha, and connectors are at different stages:\n\n- X (Twitter v2) — the reference production connector. OAuth 2.0 PKCE, chunked media upload, text/media/poll/quote/reply/thread publishing, public-metrics analytics, mentions and DMs inbox polling, a rate-limit accountant, retry backoff, and a circuit breaker. Its contract, auth, publish, and media test suites pass.\n- Bluesky (AT Protocol) — connects via app password; implements publishing plus metrics and mention/reply ingestion.\n- Substantial connector implementations also exist in the tree — at varying completeness and not all fully tested — for Mastodon, TikTok, YouTube, Threads, LinkedIn (personal and organization), Meta (Facebook and Instagram), Pinterest, Reddit, Discord, Telegram, WhatsApp, Slack, Nostr, and Farcaster, with additional packages for Lemmy, Pixelfed, Tumblr, Google Business, and CMS targets (WordPress, Ghost, Medium, DEV.to, Hashnode). The umbrella `linkedin` and `meta` packages are thin placeholders; the real code lives in the split per-identity packages.\n\nBecause the composer is capability-driven, a channel only offers what its connector actually declares — so partial connectors are safe to use for exactly the surface they implement. See [Status and roadmap](/docs/post/roadmap) for what is landing next.\n\n## Writing your own connector\n\nConnectors are out-of-tree by design: anyone can publish `@yourname/xeno-post-connector-<platform>`. The `@xeno-post/connector-kit` package ships the base class, the rate-limit accountant, and a contract-test harness every connector runs against its own fixtures. Install third-party connectors with `xeno-post plugins install` — see [Command-line and SDKs](/docs/post/cli-and-sdks)."
        },
        {
          "slug": "ai-features",
          "title": "AI features",
          "description": "AI generation tasks, provider choices, and brand voice — with graceful fallback when unconfigured.",
          "body": "AI in XENO Post is first-class but never mandatory. Every AI capability is optional: if no provider is configured, the product returns a typed `AI_NOT_CONFIGURED` error rather than breaking.\n\n## Generation tasks\n\nThe generate endpoint supports five tasks:\n\n- caption — write distinct caption variants that fit the target platform's character limit\n- hashtags — produce concise, de-duplicated hashtag sets\n- alt-text — write accessible image descriptions\n- rewrite — restyle existing copy\n- thread-split — split long text into a thread; this task is deterministic (it does not require an LLM to break text at platform limits)\n\nGenerate from the composer's AI panel — choose a task and tone, get variants, and insert one — or call the API directly:\n\n```\nPOST /api/v1/workspaces/:wid/ai/generate\n{ \"task\": \"caption\", \"input\": \"Announce our v2 launch\", \"count\": 3, \"platform\": \"x\", \"tone\": \"punchy\" }\n```\n\nThere is also `POST /api/v1/workspaces/:wid/ai/assistant` for a chat-style assistant. Both are workspace-scoped, rate-limited per workspace via Redis, and audit-logged with metadata only — prompts and outputs are never written to the audit log.\n\n## Providers\n\nXENO Post is provider-agnostic. You bring your own key (BYOK) per workspace, with zero markup. Supported provider ids for stored keys:\n\n- `xeno-rt` — local inference via a reachable xeno-rt runtime (preferred when detected)\n- `openai`\n- `anthropic`\n- `google`\n- `ollama` — local models\n\nManage them at Settings → AI, or via `GET`/`POST /api/v1/workspaces/:wid/ai/providers`. When you are signed in with a XENO account, AI usage can instead bill your XENO credits through the platform-managed `xeno-cloud` provider — see [Self-hosting and operations](/docs/post/self-hosting).\n\n## Provider resolution\n\nAt generation time XENO Post resolves a provider in priority order: a reachable xeno-rt runtime is auto-detected and preferred; otherwise it falls back to whichever BYOK key you have stored. If nothing is configured, the call returns the typed `AI_NOT_CONFIGURED` error and the UI shows an inline settings hint — it never silently fails or throws an opaque 500.\n\n## Brand voice\n\nEach workspace can store a brand voice — tone descriptors and exemplars — that AI generation uses to stay on-brand. Read and update it through the API, or through the MCP tool `xeno-post.workspace.brand_voice` so agents write in your voice too.\n\n## Ecosystem AI unlocks\n\nWhen sibling XENO services are reachable, the AI surface can expand beyond text (for example image generation via xeno-rt, and media editing via other XENO creative apps). These are optional ecosystem integrations layered on top of the standalone AI surface described above."
        }
      ]
    },
    {
      "title": "Automation and developer surface",
      "pages": [
        {
          "slug": "rest-api",
          "title": "REST API and keys",
          "description": "The versioned /api/v1 surface, authentication, API keys, scopes, and webhooks.",
          "body": "The XENO Post REST surface lives at `/api/v1`. It is versioned and OpenAPI 3.1 described — the primary integration point for the SDKs, agents, and third-party tools.\n\n## Interactive reference\n\nThe API process serves the spec and a Swagger UI:\n\n```\nGET /api/v1/openapi.json    # raw OpenAPI 3.1 document (auto-generated)\nGET /api/v1/docs            # interactive Swagger reference\n```\n\nThe OpenAPI document is generated on every build — do not hand-edit it.\n\n> A GraphQL endpoint is on the roadmap; in v0.1.0 the shipped, stable surfaces are REST plus MCP.\n\n## Authentication\n\nTwo auth modes:\n\n1. Session (web app) — Argon2id password hashing, a short 15-minute JWT access token, and a rotating 30-day refresh token with family-id theft detection. Optional TOTP MFA via `POST /auth/mfa/setup` and `/auth/mfa/verify`. Core auth routes: `POST /auth/signup`, `/auth/login`, `/auth/refresh`, `/auth/logout`, and `GET /auth/me`.\n2. API key (SDKs, agents, scripts) — a bearer token prefixed `xpost_`, presented as `Authorization: Bearer xpost_...`.\n\n## API keys and scopes\n\nCreate a key in Settings → API keys. The full secret is shown once; only a SHA-256 hash, a display prefix, and the last four characters are stored. Keys are workspace-scoped — one key can never reach a second workspace — and carry explicit, least-privilege scopes drawn from a fixed permission namespace, for example `post:read`, `post:create`, `post:update`, `post:publish`, `post:delete`, `channel:read`, `media:read`, `media:create`, `media:delete`, `inbox:read`, `inbox:update`, and `brand-voice:read`. Revoke a key at any time; each key has its own Redis rate-limit bucket and a throttled `lastUsedAt`.\n\nEvery request authenticated by an API key emits an audit event tagged with the key's prefix, so you can answer \"what did key `xpost_...` do this week?\".\n\n## Errors\n\nNon-2xx responses use problem-details JSON with a machine-readable `type`, `title`, `status`, and `detail`, including per-field validation violations for composer errors (for example, a channel character-limit breach).\n\n## Webhooks\n\nPrefer outbound webhooks over polling. XENO Post fires HMAC-SHA256-signed POSTs on events (post published, inbox message received, mention found, approval transitions). The signature is sent as `sha256=<hex>`. Delivery retries with exponential backoff on 5xx or network errors up to a capped number of attempts, incrementing a failure counter per subscription.\n\n## SDKs\n\nFirst-party clients ship for Node (`@xeno-post/sdk`) and Python (`xeno-post`). Both use bearer API-key auth and workspace-scoped paths over the same REST surface. See [Command-line and SDKs](/docs/post/cli-and-sdks)."
        },
        {
          "slug": "mcp",
          "title": "Agents and MCP",
          "description": "The native MCP server, the tools it exposes, and the first-class AGENT role.",
          "body": "XENO Post is MCP-native. Agents are first-class members of a workspace (the AGENT role), and the surface they talk to is a real Model Context Protocol server — not a glued-on tools endpoint. Any MCP-aware client (Claude, ChatGPT, Cursor, xeno-agent-cli) can drive the platform.\n\n## Endpoints\n\n- `GET /.well-known/mcp.json` — the capability manifest.\n- `POST /mcp` — a JSON-RPC 2.0 endpoint. Authenticated with the same credentials as REST (an API key, ideally a dedicated agent key with the narrowest scopes it needs).\n\nBoth stay outside the `/api/v1` prefix.\n\n## Tools (v0.1.0)\n\nThe running MCP server registers 15 tools, each namespaced `xeno-post.<area>.<verb>`, permission-gated by the caller's scopes, and audit-logged on every call:\n\n| Tool | Required permission | Purpose |\n|------|--------------------|---------|\n| `xeno-post.channel.list` | `channel:read` | List channels and their capability sets |\n| `xeno-post.channel.get` | `channel:read` | Fetch a single channel |\n| `xeno-post.post.compose` | `post:create` | Create or update a draft (no publish) |\n| `xeno-post.post.schedule` | `post:update` | Schedule a draft |\n| `xeno-post.post.publish_now` | `post:publish` | Publish immediately |\n| `xeno-post.post.list` | `post:read` | Query posts by state / range / channel |\n| `xeno-post.post.get` | `post:read` | Fetch a post and its targets |\n| `xeno-post.post.delete` | `post:delete` | Delete a post |\n| `xeno-post.post.metrics` | `post:read` | Read a post's metrics |\n| `xeno-post.media.list` | `media:read` | List workspace media assets |\n| `xeno-post.inbox.list` | `inbox:read` | Paginated inbox query |\n| `xeno-post.inbox.reply` | `inbox:update` | Reply on the underlying platform |\n| `xeno-post.ai.generate` | `post:create` | Run an AI generation task |\n| `xeno-post.analytics.summary` | `post:read` | High-level metrics across a date range |\n| `xeno-post.workspace.brand_voice` | `brand-voice:read` | Read or update the workspace brand voice |\n\nThese mirror the REST surface. (The repository's forward-looking docs describe a broader tool set targeted for a later release; the list above is what v0.1.0 actually exposes.)\n\n## The AGENT role\n\nAn agent is a member of the workspace with its own identity, its own `xpost_` API key, and explicit scopes — you grant, say, `post:create` and `inbox:read` and nothing more. Agents also carry budgets (such as maximum tokens or posts per day), enforced at the MCP gate and again downstream at the AI layer, so a runaway agent is stopped before it burns tokens or floods your channels.\n\nBecause the agent has a first-class identity, the audit log can always answer which agent did something, using which tool call — none of which is reconstructable if an agent shares a human's login. You can run many agents in one workspace, each with different scopes and separate audit signatures.\n\n## Pointing a client at XENO Post\n\nRegister the `POST /mcp` URL and an agent API key in your MCP client's config, then restart it."
        },
        {
          "slug": "cli-and-sdks",
          "title": "Command-line and SDKs",
          "description": "Drive XENO Post from the terminal and from Node or Python.",
          "body": "XENO Post ships a terminal client for scripting common operations against a running instance, plus first-party Node and Python SDKs for programmatic use.\n\n## CLI commands\n\nThe CLI (binary `xeno-post`, currently at an early 0.0.1) exposes these commands:\n\n| Command | Description |\n|---------|-------------|\n| `login` | Authenticate against a XENO Post instance. |\n| `whoami` | Print the authenticated user and current workspace. |\n| `connect` | Connect a social account to the current workspace. |\n| `draft` | Create a draft post. |\n| `schedule` | Schedule a draft for future publication. |\n| `publish-now` | Publish a draft immediately. |\n| `list` | List posts in the current workspace. |\n| `plugins` | Manage plugins — `plugins list`, `plugins install`, `plugins remove`. |\n\nUsage notes drawn from the real command definitions:\n\n- `connect` takes the platform as a positional argument, plus an optional `--instance` for federated platforms.\n- `draft` takes the post content as a positional argument, a required `--to` (comma-separated channels), and an optional `--at` for scheduling.\n\nA typical flow:\n\n```bash\nxeno-post login\nxeno-post connect mastodon --instance mastodon.social\nxeno-post draft \"Hello world\" --to x,linkedin-personal --at \"2026-08-01T09:00\"\nxeno-post list\n```\n\nRun `xeno-post --help` for the full, authoritative option list. The CLI authenticates the same way scripts do — a bearer `xpost_` API key.\n\n## Node SDK\n\n```bash\nnpm install @xeno-post/sdk\n```\n\nThe Node SDK is a typed REST client. It authenticates with a workspace-scoped API key and calls the same `/api/v1` surface.\n\n## Python SDK\n\n```bash\npip install xeno-post\n```\n\nThe Python SDK mirrors the Node client's shape and auth model.\n\n## Plugins\n\nConnectors and AI providers are installable plugins. Use `xeno-post plugins install <package>` to add a third-party connector such as `@yourname/xeno-post-connector-<platform>`; the connector registry discovers installed packages dynamically. See [Channels and connectors](/docs/post/connectors) for the connector contract, and [REST API and keys](/docs/post/rest-api) for the underlying HTTP surface."
        }
      ]
    },
    {
      "title": "Operating XENO Post",
      "pages": [
        {
          "slug": "self-hosting",
          "title": "Self-hosting and operations",
          "description": "Production configuration, security, backups, and the optional XENO account link.",
          "body": "XENO Post is designed to run on your own infrastructure with only Postgres + Redis. Every XENO ecosystem integration is an optional unlock, detected at boot — the product never hard-requires a sibling service.\n\n## Configuration\n\nAll configuration is via environment variables in `.env` (see `.env.example` for the full annotated list). The essentials:\n\n- `XENO_POST_KMS_KEY` — master key for envelope-encrypting OAuth tokens. Back it up securely and separately from your database. Losing it makes stored channel credentials unrecoverable.\n- `POSTGRES_PASSWORD`, and the standard Postgres/Redis connection settings.\n- `PUBLIC_BASE_URL` — the externally reachable URL (used for OAuth redirects).\n- Optional AI: `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` / `XENO_RT_URL`.\n- Optional SMTP for the mailer (publish-failure, approval, and invitation emails).\n\n## Security model\n\n- Envelope encryption — a per-workspace AES-256-GCM data key wraps every stored credential; the data keys are wrapped by the `XENO_POST_KMS_KEY` master key. A rotation helper is provided.\n- Immutable audit log — every privileged action appends to a hash-chained `AuditEvent` table, with verification tooling. Logs reference token ids, never values.\n- Least-privilege keys — API keys carry explicit scopes and are workspace-scoped; they never inherit owner permissions implicitly.\n\n## Health and observability\n\nThe API exposes `/health`, `/health/live`, `/health/ready`, and a Prometheus `/metrics` endpoint; the worker runs its own health server. Requests carry a request id and structured logging, with optional env-gated Sentry.\n\n## Production deployment\n\nProduction Dockerfiles for api, web, and worker ship with the repo, along with `docker-compose.prod.yml` and a tag-based GHCR release workflow. Deploy templates and self-host docs are included. For backups, at minimum snapshot the Postgres data volume nightly and copy media storage off-host; the repo's self-hosting docs cover backup/restore, updates, and hardening in more detail.\n\n## Media storage\n\nMedia defaults to local disk, with an optional S3/R2-compatible backend. Uploads are deduplicated by SHA-256; image metadata and thumbnails are generated when Sharp is available, and media is validated against connector capabilities at schedule time.\n\n## The optional XENO account link\n\nSigning in with a XENO account auto-provisions a personal workspace linked to that account. The dashboard header then shows your live XENO credit balance, and AI usage can bill your XENO credits (the `xeno-cloud` provider) instead of a BYOK key. The link durably stores the rotating refresh token and refreshes the access token via the OIDC token endpoint when it expires, so it does not go stale. This is entirely optional — self-hosting without a XENO account is fully supported.\n\n## Licensing\n\nXENO Post is AGPL-3.0. Self-hosting is unconditionally free with no feature paywall. If you offer XENO Post as a hosted service to third parties, AGPL requires you to publish your modifications."
        },
        {
          "slug": "roadmap",
          "title": "Status and roadmap",
          "description": "An honest account of what ships in v0.1.0 and what is still in development.",
          "body": "XENO Post is alpha (v0.1.0, released 2026-06-27). This page states plainly what is implemented and what is not, so you can plan around it.\n\n## Shipped in v0.1.0\n\nBackend (API)\n\n- Auth: Argon2id, 15-minute JWT access + rotating 30-day refresh with theft detection, TOTP MFA.\n- Workspaces, members, and email-link invitations with the 7-role permission matrix.\n- Immutable, hash-chained audit log with verification tooling.\n- Envelope encryption (per-workspace AES-256-GCM data keys under a master KMS key).\n- Posts module with the full state machine, per-channel target materialization, capability-intersected validation, thread linking, CSV bulk import, publish-now and duplicate.\n- Schedule module: idempotent BullMQ producer, calendar day-grouped view, best-time-to-post helper, queue/DB reconciliation.\n- Channels module: dynamic connector registry, Redis OAuth state store, envelope-encrypted token vault, locked refresh worker.\n- Media pipeline: local-disk default with optional S3/R2, multipart upload, SHA-256 dedupe, thumbnails via Sharp.\n- AI generate + assistant endpoints with provider resolution and typed `AI_NOT_CONFIGURED`.\n- Public `xpost_` API keys with scopes; stable `/api/v1/openapi.json` and `/api/v1/docs`.\n- Native MCP server (JSON-RPC 2.0) with 15 tools.\n- Notifications + SMTP mailer; role-targeted approval policies.\n- Health, readiness, and Prometheus metrics endpoints.\n\nWorker\n\n- End-to-end publish handler with exponential-backoff retries and permanent-failure alerting.\n- Analytics collector scheduled at +1h/+6h/+24h/+7d.\n- HMAC-signed outbound webhooks with retry.\n\nWeb\n\n- Capability-driven composer with per-platform preview and an AI panel.\n- Channels connect grid, calendar, first-run onboarding wizard, command palette (Cmd/Ctrl+K), notification bell.\n\nConnectors — X (Twitter) is the production reference connector (full publish/media/analytics/inbox, tests passing). Bluesky publishes and ingests metrics/mentions. Many other connectors have substantial code in the tree at varying completeness.\n\nClients / SDKs — Node (`@xeno-post/sdk`) and Python (`xeno-post`) SDKs, plus the terminal CLI.\n\nXENO account — optional credit-billed AI and workspace auto-provisioning.\n\n## In development / not yet complete\n\n- Additional platform connectors (LinkedIn, Meta Facebook/Instagram, Pinterest, Reddit, Discord, Telegram, WhatsApp, Slack, and the decentralized and CMS targets) are at varying stages and not all fully tested.\n- The mobile (Expo / React Native) and desktop (Electron) apps are scaffolded, not released.\n- A GraphQL endpoint and an auto-generated MCP tool reference are planned; REST + MCP are the stable surfaces today.\n- Social listening at scale, richer analytics reporting, and the full agent orchestration loop are on the roadmap.\n- The CLI reports an early binary version (0.0.1) and its command surface will expand.\n\n## How to read these docs\n\nWhere a feature is described as shipped, it is backed by v0.1.0 code and tests. Where these docs say \"planned\", \"in development\", or \"roadmap\", treat it as not yet available. When in doubt, the repository's `CHANGELOG.md` is the authoritative record of what landed in each release.\n\n## Getting help\n\n- File issues and read the source at the XENO-CORPORATION/xeno-post repository.\n- Start from [Introduction](/docs/post/introduction) and [Installation](/docs/post/installation) if you are new.\n- For automation, see [Agents and MCP](/docs/post/mcp) and [REST API and keys](/docs/post/rest-api)."
        }
      ]
    }
  ]
};

export default post;
