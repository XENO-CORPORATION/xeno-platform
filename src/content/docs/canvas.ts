import type { ProductDocs } from './_types';

const canvas: ProductDocs = {
  "slug": "canvas",
  "productName": "XENO Canvas",
  "tagline": "Multiplayer product and UI design — the Figma surface, built into the XENO ecosystem.",
  "seo": {
    "title": "XENO Canvas documentation",
    "description": "Design in XENO Canvas — draw, auto-layout, tokens with theme modes, components and variants, gradients and vectors, live multiplayer, and dev handoff. Beta."
  },
  "sections": [
    {
      "title": "Getting started",
      "pages": [
        {
          "slug": "introduction",
          "title": "Introduction",
          "description": "What XENO Canvas is, and where it fits in the XENO ecosystem.",
          "body": "## Introduction\n\nXENO Canvas is a multiplayer product- and UI-design application — the XENO ecosystem's answer to Figma. It covers the live, multi-author, component-driven design flow: draw frames and shapes, systematize them with auto-layout, design tokens, and components, collaborate with other people in the same file, and hand the result to a developer as CSS or Tailwind.\n\nCanvas is a desktop app built on Electron, React 19, and TypeScript. Its native project format is the single-file `.xcanvas`.\n\n## What Canvas is (and is not)\n\nCanvas owns one specific surface: vector product design with components, tokens, auto-layout, and real-time collaboration. It deliberately does **not** try to be everything:\n\n- **Raster editing** (brushes, photo work) belongs to XENO Pixel. Canvas can place, mask, and embed images, but it is not a paint app.\n- **Video / motion graphics** belong to XENO Motion.\n- **3D scenes** belong to XENO Form (`xeno-form`).\n\nIf you have used Figma, Canvas will feel familiar on purpose — the keyboard defaults and canvas interactions follow Figma so your muscle memory transfers.\n\n## Where it fits\n\nCanvas sits in Layer 5 (Creative Apps) of the XENO ecosystem, alongside Pixel, Motion, and Sound. It is designed to consume shared XENO infrastructure — the `@xeno/core` vector kernel, the `xeno-comms` multiplayer gateway, and `xeno-rt` for AI — as those integrations come online. Today it runs fully standalone.\n\n## Maturity\n\nXENO Canvas is in **beta** — the current Windows build is downloadable from the [product page](/product/canvas). A large amount of the daily design loop already works — drawing, transforms, undo/redo, tokens, auto-layout, components and variants, fills and effects, vectors, pages, export, dev handoff, and cross-window multiplayer. Several headline features are still in development, including networked multiplayer, real boolean geometry, prototyping, plugins, and AI assist. See [Status and limitations](/docs/canvas/status-and-limitations) for an honest, current breakdown before you rely on Canvas for production work.\n\n## Next steps\n\n- [Installation](/docs/canvas/installation) — run Canvas from source\n- [Quickstart](/docs/canvas/quickstart) — your first frame, in five minutes\n- [Canvas and tools](/docs/canvas/canvas-and-tools) — the surface and the toolset"
        },
        {
          "slug": "installation",
          "title": "Installation",
          "description": "Install the Windows beta, or run XENO Canvas from source.",
          "body": "## Installation\n\n**Download the Windows beta** from the [product page](/product/canvas) — `XENO Canvas Setup <version>.exe` (NSIS). The installer registers the `.xcanvas` file type, so double-clicking a project opens it directly. The build is not yet code-signed, so Windows SmartScreen may ask you to confirm (More info → Run anyway); signing is pending a certificate; the app carries an `UNSIGNED BUILD` badge so a build is never mistaken for a signed one. macOS and Linux builds follow.\n\nAlternatively, run from source as an Electron development build.\n\n## Requirements\n\n- A recent Node.js LTS (Node 20+ recommended) and npm.\n- The `xeno-canvas` repository checked out locally.\n\n## Run from source\n\n```bash\ncd xeno-canvas\nnpm install\nnpm run dev\n```\n\n`npm run dev` boots the Electron shell (electron-vite) with the renderer served by Vite. The window opens on the [Home screen](/docs/canvas/quickstart) — recent projects, templates and search.\n\n## Verify your checkout\n\nCanvas ships with a strict type check and a Vitest suite. Both should be clean on a good checkout:\n\n```bash\nnpm run typecheck   # 0 errors\nnpm run test        # 523 passing (v0.38.0)\n```\n\nThe engine modules (matrix math, geometry, transforms, auto-layout, tokens, history, export, codegen, and the multiplayer sync) are unit-tested; a failing suite is a good early signal that something in your environment is off.\n\n## Building an installer (optional)\n\nThe repo is configured for `electron-builder` targeting Windows (nsis), macOS (dmg), and Linux (AppImage) via `npm run package` (and `package:win` / `package:mac` / `package:linux`). These builds are used for internal packaging; they are **not** yet notarized/signed or wired into the Hub auto-update pipeline. Prefer `npm run dev` for day-to-day use during the beta.\n\n## Next steps\n\n- [Quickstart](/docs/canvas/quickstart)\n- [Documents and pages](/docs/canvas/documents-and-pages) — the `.xcanvas` format, autosave, and crash recovery"
        },
        {
          "slug": "quickstart",
          "title": "Quickstart",
          "description": "Create a project, draw your first screen, and save it.",
          "body": "## Quickstart\n\nThis walks you from an empty window to a saved `.xcanvas` file.\n\n## 1. Create a project\n\nOn first launch a short welcome appears once — dismiss it with **Start designing** or **Escape**. On the Home screen, click **New project**. The setup dialog asks for a name and a starting-frame template:\n\n| Template | Size |\n|---|---|\n| Desktop | 1440 × 1024 |\n| Tablet | 1024 × 768 |\n| Mobile | 390 × 844 (iPhone 14) |\n| Empty | no starting frame |\n\nPick one and click **Create**. A frame appears on the canvas, and an autosave + lock session starts so your work can be recovered after a crash.\n\n## 2. Move around\n\n- **Pan** by scrolling, or hold **Space** and drag, or press **H** for the Hand tool.\n- **Zoom** with **Ctrl + wheel** toward the cursor. Zoom is clamped between 2% and 25600% (Figma's range).\n\n## 3. Draw something\n\nPress a tool key and click (for a default size) or drag (to size it):\n\n- **R** — rectangle\n- **O** — ellipse\n- **L** — line\n- **T** — text (double-click a text node to edit its characters)\n- **F** — frame\n\nAfter you draw one shape, Canvas returns to the Move tool (**V**) — Figma muscle memory. New shapes parent into the frame under your cursor.\n\n## 4. Select and transform\n\nPress **V**, then click a shape. The selection shows 8 resize handles: drag a handle to resize (hold **Shift** for aspect ratio, **Alt** to resize from center), or drag just outside a corner to rotate (**Shift** snaps to 15°). Nudge with the arrow keys (1px, or 10px with **Shift**). See [Canvas and tools](/docs/canvas/canvas-and-tools) for the full manipulator.\n\n## 5. Undo freely\n\nEverything you do is undoable. **Ctrl+Z** undoes, **Ctrl+Shift+Z** (or **Ctrl+Y**) redoes. See [Editing and history](/docs/canvas/editing-and-history).\n\n## 6. Save\n\n**File → Save** (or **Ctrl+S**) writes a `.xcanvas` file. The save-status pill in the title bar flips to *Saved*. Reopen it any time with **File → Open** (**Ctrl+O**).\n\n## Where to go next\n\n- [Design tokens](/docs/canvas/design-tokens) — colors and theme modes\n- [Auto-layout](/docs/canvas/auto-layout) — flexbox on the canvas\n- [Components](/docs/canvas/components) — reusable, systematized design\n- [Keyboard shortcuts](/docs/canvas/keyboard-shortcuts) — the full reference"
        }
      ]
    },
    {
      "title": "Core concepts",
      "pages": [
        {
          "slug": "canvas-and-tools",
          "title": "Canvas and tools",
          "description": "The infinite canvas, the toolset, selection, and the transform manipulator.",
          "body": "## Canvas and tools\n\nThe editing surface is an infinite, zoomable canvas rendered with Canvas2D. It draws your document's frames and shapes, plus overlays for selection, snapping guides, and remote collaborators.\n\n## The toolset\n\nEach tool has a single-key shortcut matching Figma:\n\n| Key | Tool |\n|---|---|\n| V | Move / select |\n| F | Frame |\n| R | Rectangle |\n| O | Ellipse |\n| L | Line |\n| P | Pen (vector paths) |\n| T | Text |\n| C | Comment |\n| H | Hand (pan) |\n\nDrawing tools create a real node by click (default size) or drag (sized to the drag rect), with a live dashed preview. After one shape, the tool reverts to Move. The floating toolbar also shows **Polygon** and **Place image** as coming-soon placeholders — they are visible but disabled.\n\n> The **Comment** tool is present in the toolset, but comment threads are not yet implemented — commenting lands with networked multiplayer. See [Status and limitations](/docs/canvas/status-and-limitations).\n\n## Panning and zooming\n\n- Scroll to pan; hold **Space** (or use **H**) to drag-pan.\n- **Ctrl + wheel** zooms toward the cursor, keeping the point under the pointer stable.\n- Zoom range is 2% to 25600%.\n\n## Selection\n\n- Click picks the topmost node under the cursor. **Shift**-click toggles a node in or out of the selection. Clicking empty canvas clears it.\n- Locked and hidden nodes are not pickable.\n- Drag on empty canvas to **marquee-select** top-level nodes; **Shift** adds to the current selection.\n- **Ctrl/Cmd+A** selects all top-level nodes on the current page. **Esc** clears the selection and returns to the Move tool.\n\nHit-testing is rotation-correct: the cursor is inverse-mapped into each node's local space, so rotated shapes select and resize the way they look.\n\n## The transform manipulator\n\nSelecting a node shows an oriented box with 8 handles (4 corners, 4 edges):\n\n- **Resize** by dragging a handle. The opposite handle stays pinned and the box grows along its own axes, so a rotated rectangle resizes correctly. Hold **Shift** to preserve aspect ratio, **Alt** to resize about the center. Results snap to the pixel grid, and a live W × H readout follows the cursor.\n- **Rotate** by dragging just outside a corner. **Shift** snaps to 15°. The box pivots about its center and shows a live angle readout.\n- **Multi-select** shows one axis-aligned bounding box that scales every selected node about the fixed corner or center. Drag anywhere on a selected node to move the whole set.\n\n## Object snapping\n\nWhile moving, the selection's edges and centers snap to other objects' edges and centers within a pixel threshold, drawing alignment guides. Hold **Ctrl/Cmd** to suspend snapping.\n\n## Panels\n\nThe right sidebar carries the **Properties** inspector (editable X/Y/W/H, rotation, opacity, fills, strokes, text, effects, auto-layout, instance overrides, and Inspect), the **Layers** tree, the **Actions** panel (align, distribute, order, group, components), the **Tokens** panel, and the **Agent** panel (a stub today). The **Pages** list sits above Layers. See the topic pages for each subsystem."
        },
        {
          "slug": "documents-and-pages",
          "title": "Documents, pages, and the .xcanvas format",
          "description": "The project file, multi-page documents, autosave, and crash recovery.",
          "body": "## Documents, pages, and the .xcanvas format\n\n## The document model\n\nA Canvas document is a graph of nodes on one or more pages. Node types include frame, group, rectangle, ellipse, line, text, image, vector, component, component-set, instance, and boolean-op. Every node carries a matrix transform (translation and rotation), a size, fills, strokes, effects, and optional constraints and auto-layout. The document also holds the shared **design tokens** and **theme modes** (see [Design tokens](/docs/canvas/design-tokens)).\n\n## Pages\n\nA document can hold multiple pages. The **Pages** panel sits above the Layers tree:\n\n- **Add** a page with the + button.\n- **Switch** pages by clicking; **double-click** to rename.\n- **Delete** a page with the trash button (you can never delete the last page — a document always has at least one). Deleting a page removes its nodes.\n\nAll page operations are undoable — history snapshots the page structure.\n\n## The .xcanvas file\n\nCanvas saves to a single-file JSON project with the extension `.xcanvas`. The envelope is tagged as a XENO Canvas project and carries a format version so future migrations can read older files. Images you drop in are embedded in the file.\n\nSave with **File → Save** (**Ctrl+S**) or **Save As** (**Ctrl+Shift+S**); open with **File → Open** (**Ctrl+O**). The title bar shows the project name with a dot when there are unsaved changes, and a save-status pill (*Unsaved changes* / *Saved*).\n\n## Autosave and crash recovery\n\nWhen you create or open a project, Canvas starts a session that:\n\n- Writes a versioned autosave sidecar (`.xcanvas.autosave`) every 30 seconds while the document is dirty, and makes a best-effort flush when the window is closing.\n- Holds a lock sidecar (`.xcanvas.lock`) so a second instance and a crash can be detected.\n\nIf Canvas exits uncleanly, the next launch probes for a stale autosave + lock and surfaces a **Crash Recovery** dialog with the project name and last-autosave time. Choose to recover the document, or discard it to clean up the sidecars and start fresh.\n\n## Safety\n\nThe main-process file layer only ever reads and writes the `.xcanvas`, `.xcanvas.autosave`, and `.xcanvas.lock` extensions, and every file IPC handler validates the path against traversal — a document can never make Canvas touch a file outside that allowlist.\n\n## Next steps\n\n- [Editing and history](/docs/canvas/editing-and-history)\n- [Dev handoff and export](/docs/canvas/dev-handoff-and-export)"
        },
        {
          "slug": "editing-and-history",
          "title": "Editing and history",
          "description": "Undo/redo, structural edits, z-order, grouping, and align/distribute.",
          "body": "## Editing and history\n\nCanvas is built on a command-based history: every mutation is a command with a *do* and an *undo*, recorded on an undo/redo stack. This is not a bolt-on — the commands map onto the same operation kinds that multiplayer replicates, so undo, redo, and collaboration all share one edit stream.\n\n## Undo and redo\n\n- **Ctrl+Z** undoes.\n- **Ctrl+Shift+Z** or **Ctrl+Y** redoes.\n\nThe Edit menu's Undo/Redo items enable and disable to reflect what's on the stack. History resets automatically when you swap documents (new / open / recover). A continuous drag collapses into a single undo step, and a run of arrow-key nudges coalesces into one step (within a short window) so undo doesn't unwind pixel by pixel.\n\n## Structural edits\n\nAll of these are undoable:\n\n- **Delete** — Delete / Backspace removes the selection and its subtree.\n- **Group / ungroup** — **Ctrl+G** / **Ctrl+Shift+G**. Grouping re-origins children into a group box; ungrouping restores their positions.\n- **Duplicate** — **Ctrl+D** deep-clones the selection with fresh IDs, offset and reselected.\n- **Copy / cut / paste** — **Ctrl+C / X / V**. The clipboard is in-app (deep subtree clone with fresh IDs).\n- **Z-order** — bring forward / send backward with **Ctrl+]** / **Ctrl+[**; add **Shift** for to-front / to-back. Ordering is per-parent correct.\n- **Reorder in the Layers panel** — drag a layer row to restack it, with a drop indicator.\n\n## Move and nudge\n\nDrag a selected node to move it live. Arrow keys nudge 1px, or 10px with **Shift**.\n\n## Align and distribute\n\nThe **Actions** panel offers six align modes (left, horizontal centers, right, top, vertical centers, bottom) and horizontal / vertical distribute. Align needs two or more nodes; distribute needs three or more. All are undoable.\n\n## Editable properties\n\nThe **Properties** inspector shows a selected node's name, type, position (X/Y), size (W/H), rotation, and opacity as editable fields — committing a value writes back through history (rotation pivots about the node's center). The **Layers** panel's visibility and lock toggles are also undoable.\n\n## Next steps\n\n- [Design tokens](/docs/canvas/design-tokens)\n- [Components](/docs/canvas/components)"
        }
      ]
    },
    {
      "title": "Building design systems",
      "pages": [
        {
          "slug": "design-tokens",
          "title": "Design tokens and theme modes",
          "description": "Shared tokens, theme modes, aliases, and binding fills and text to tokens.",
          "body": "## Design tokens and theme modes\n\nA design token is a named value — a color, a number, a text style, or a shadow — defined once and referenced everywhere. Change the token, and every node bound to it updates live. Tokens live on the document and are managed from the **Tokens** panel.\n\n## Token kinds\n\nCanvas manages four token kinds:\n\n- **Color** — full create / rename / edit / delete UI in the Tokens panel, with a live-resolved swatch.\n- **Number** — managed, resolvable, and aliasable.\n- **Typography** (text styles) — bindable to text layers.\n- **Shadow** (effect styles).\n\nThe token **resolver** turns a token reference into a concrete value, honoring the document's active theme mode, following alias chains, and detecting cycles. When a token can't resolve, its captured fallback keeps the node rendering.\n\n## Theme modes\n\nA token can carry different values per **mode** — for example a light and a dark value for the same color. In the Tokens panel:\n\n- The **Modes** row shows *Default* plus any modes you add (e.g. `dark`).\n- Click a mode to make it active; editing a token now edits its value for that mode.\n- Switching the mode **live-recolors** every bound node on the canvas.\n\n## Aliases\n\nCreate a token that points at another token (an alias). The resolver follows the chain, so you can build semantic layers — e.g. `button/bg` → `brand/primary` → a raw color — and editing the base propagates through.\n\n## Binding nodes to tokens\n\n- **Fills** — in Properties → Fill, bind a layer's fill to a color token (a fallback is captured), edit the raw color, or unbind. Editing the bound token recolors the layer instantly. Token colors resolve for rectangles, ellipses, text, line strokes, and frame backgrounds.\n- **Text styles** — in Properties → Text, bind a text layer to a typography token; editing the token restyles every bound layer live.\n\n> Binding an arbitrary number field (like a gap or radius) to a number token is a separate variables feature that is not yet wired — number tokens today are fully managed, resolvable, and aliasable, but not yet bound to per-field slots.\n\n## Export\n\nToken-bound colors carry into dev handoff: generated CSS and Tailwind emit `var(--token-name, fallback)` so the output stays design-system aware. See [Dev handoff and export](/docs/canvas/dev-handoff-and-export)."
        },
        {
          "slug": "auto-layout",
          "title": "Auto-layout",
          "description": "Flexbox on the canvas — direction, gap, padding, alignment, sizing, and wrapping.",
          "body": "## Auto-layout\n\nAuto-layout turns a frame into a flexbox container: its children lay out along a main axis with gaps and padding, and the frame can grow with its content. This is what makes buttons, lists, and cards behave responsively.\n\n## Adding auto-layout\n\n- Select a frame and add auto-layout from **Properties → Auto Layout**, or\n- Press **Shift+A** to wrap the current selection into a new auto-layout frame (on a single frame, Shift+A just enables it).\n\n## What you can control\n\nFrom the Auto Layout section in Properties:\n\n- **Direction** — horizontal or vertical.\n- **Gap** between children.\n- **Padding** — a value per side (top / right / bottom / left, independently).\n- **Cross-axis align** — start, center, end, or **stretch**.\n- **Main-axis justify** — start, center, end, or **space-between**.\n- **Sizing per axis** — **hug** (fit content) or **fixed**.\n\n## Per-child controls\n\nWhen a layer sits inside an auto-layout frame, Properties shows per-child options:\n\n- **Fill** (grow) — stretch the child to fill free space on the main axis; free space is distributed across multiple growers.\n- **Absolute** — opt a child out of the layout flow and position it freely.\n\n## Wrapping and nesting\n\nThe layout engine supports real **flex-wrap**: children wrap to a new line when they overflow a fixed main axis. Nested auto-layout frames reflow inner-first, so a card inside a list inside a page all settle correctly. Reflow runs whenever content or sizing changes — including when you draw a shape into an auto-layout frame — and every layout edit is undoable.\n\n## Next steps\n\n- [Components](/docs/canvas/components) — auto-layout is what makes instances grow with their content"
        },
        {
          "slug": "components",
          "title": "Components, instances, and variants",
          "description": "Create components, place live instances, override them, and combine variants.",
          "body": "## Components, instances, and variants\n\nComponents are the single source of truth for reusable design — a button, a card, an input. You define it once, place **instances** of it, and edits to the main component flow to every instance.\n\n## Creating a component\n\nSelect one or more layers and:\n\n- Press **Ctrl+Alt+K**, or use **Object → Convert to Component**, or the Create Component button in the **Actions** panel.\n\nThe selection is wrapped into a `component` node (its children are re-origined). A component behaves like a first-class container — editable like a group — and renders with a purple diamond marker. Components are containers everywhere in the app (geometry, history, document ops), so instances of them stay locked while the main stays editable.\n\n## Placing instances\n\nThe Actions panel's Components section lists your components, each with an **Insert instance** button. An instance references its component by ID and renders the component's child tree live — so editing the main component updates every instance, because instances resolve at render time.\n\n## Overrides\n\nAn instance can override parts of itself without touching the main component. Select an instance and use the **Overrides** section in Properties to change a child's **text** (characters) or **fill** color. Overridden children show an accent ring and a **reset** button. Overrides layer on top of the main and survive edits to the main component. (Overrides are driven from the inspector; click-into-instance selection on the canvas is a later addition.)\n\n## Variants and component sets\n\nSelect two or more components and choose **Combine as variants** in the Actions panel to build a **component set** (e.g. a button's default / hover / disabled states). Instances of a set get a **Variant** dropdown in the inspector that swaps the rendered variant; overrides and detach resolve through the active variant.\n\n## Detach\n\nPress **Ctrl+Alt+B** (or **Object → Detach Instance**) to replace an instance with a plain group of cloned, freely editable nodes (fresh IDs). This is undoable.\n\n## Not yet shipped\n\nComponent **properties** panels (boolean/text/instance-swap props), nested-instance swap, and publishing components to a shared **library** are on the roadmap — see [Status and limitations](/docs/canvas/status-and-limitations)."
        }
      ]
    },
    {
      "title": "Reference",
      "pages": [
        {
          "slug": "styling",
          "title": "Fills, effects, text, and vectors",
          "description": "Solid and gradient fills, strokes, shadows and blur, text, image fills, masks, and the pen tool.",
          "body": "## Fills, effects, text, and vectors\n\nStyling controls live in the **Properties** inspector for the selected node, and every change writes through history as one undo step.\n\n## Fills\n\n- **Solid** color, bindable to a color token (see [Design tokens](/docs/canvas/design-tokens)).\n- **Linear gradient** (angle-driven) and **radial gradient**, rendered on rectangles and ellipses. From Properties → Fill you can convert a solid to a linear gradient and edit its angle and two stops, or revert to solid.\n- **Image fills** — drag and drop an image onto the canvas to embed it as a media asset and create an image-fill node. Choose a fit mode in Properties: **fill**, **fit**, **crop**, or **tile**. Embedding is undoable.\n\n## Strokes\n\nRectangles and ellipses render their stroke (weight plus a token-resolved color). In Properties → Stroke you can add or remove a stroke and edit its weight, color, and alignment (inside / center / outside).\n\n## Effects\n\nIn Properties → Effects, add or remove a **drop shadow** (offset X/Y, blur, spread, color) and a **layer blur** (radius). Both composite on shapes via the canvas shadow/filter pipeline.\n\n## Masks\n\nMark a layer as **Use as Mask** (**Ctrl+Alt+M**, or Object → Use as Mask). It alpha-clips the siblings drawn after it within its parent.\n\n## Text\n\nDouble-click a text node (or finish drawing one with the **T** tool) to edit its characters in an inline overlay — Enter commits, Esc cancels. In Properties → Text you can set font family, size, weight, line-height, and horizontal alignment, and bind the layer to a typography token. (Per-range rich-text runs and web-font loading are not yet shipped.)\n\n## Vectors and the pen tool\n\nPress **P** for the pen tool:\n\n- Click to drop corner anchors; **drag** an anchor to pull a bezier handle.\n- Click the first anchor to close the path; **Enter** or double-click finishes an open path.\n- **Backspace** removes the last anchor; **Esc** (or switching tools) cancels.\n\nClosed paths get a fill; open paths get a stroke. Paths render via the browser's `Path2D` rasterizer with token-resolved colors and effects. **Double-click** a pen path to enter node-edit mode and **drag its anchor points** — the path and its bounds re-derive live, and it's undoable.\n\n> **Boolean operations** (union / subtract / intersect / exclude) are modeled but not yet computed — real path geometry comes from the `@xeno/core` kernel. See [Status and limitations](/docs/canvas/status-and-limitations)."
        },
        {
          "slug": "multiplayer",
          "title": "Multiplayer",
          "description": "Live collaboration today across windows, and what the networked server adds later.",
          "body": "## Multiplayer\n\nMultiplayer is Canvas's defining feature, and the architecture is real from the ground up: single-user is treated as the degenerate case of multi-user, and the same edit operations that drive undo/redo are the ones that replicate to peers.\n\n## What works today\n\nCanvas ships a working **local transport** built on the browser's `BroadcastChannel`. Two Canvas windows open on the same machine collaborate live:\n\n- **Op replication** — every local edit serializes the affected scope into a sync operation and broadcasts it; peers apply it and converge. Disjoint edits are exact; two people editing the same node resolve last-writer-wins.\n- **Undo that respects peers** — remote edits don't land on your undo stack, so you only ever undo your own work. Undo and redo re-broadcast so peers stay converged.\n- **Live presence** — remote cursors with name tags and remote selection outlines, each in that peer's color. Cursor updates are throttled to about 20 Hz.\n- **Session lifecycle** — connect, join the document's channel, subscribe to ops and presence, publish your join and selection, and tear down cleanly on close.\n\nEach window gets a random guest identity and color, so two windows read as two collaborators.\n\n## How it's wired\n\nAll of this rides a single seam, the `CommsClient` interface. The local `BroadcastChannel` client is the default; swapping in the production `xeno-comms` client is a contained change with no consumer edits.\n\n## What the networked server adds\n\nThe following arrive when the `xeno-comms` server integration lands and are **not** available today:\n\n- Cross-machine (real networked) collaboration and an auth handshake via XENO Hub.\n- Server-ordered sequencing and a true operation-CRDT for same-node merges.\n- Offline editing with reconnect reconciliation.\n- Follow / observe mode (viewport following).\n- Multiplayer **comments**.\n\nSee [Status and limitations](/docs/canvas/status-and-limitations) for the full picture."
        },
        {
          "slug": "dev-handoff-and-export",
          "title": "Dev handoff and export",
          "description": "Copy a node as CSS or Tailwind, and export selections to PNG or SVG.",
          "body": "## Dev handoff and export\n\n## Inspect: copy as CSS or Tailwind\n\nSelect a node and open **Properties → Inspect** (dev handoff). Canvas generates real, paste-able code for the node and offers a live preview with one-click **Copy**. Toggle between:\n\n- **CSS** — a declaration block, and\n- **Tailwind** — utility classes.\n\nThe codegen covers the layout box (position and size), rotation, solid and linear/radial gradient **fills**, **border** (from the stroke), corner **radius**, **box-shadow** and blur (from effects), **opacity**, **typography** (for text), and **flex** (for auto-layout frames). Token-bound colors emit `var(--token-name, fallback)` so the output stays tied to your design system rather than baking in a raw hex.\n\n> Additional targets (SwiftUI, Jetpack Compose, Style Dictionary), on-canvas measurements and redlines, and selection-to-full-tree export are on the roadmap.\n\n## Export PNG and SVG\n\nFrom the **File** menu:\n\n- **Export PNG** (**Ctrl+Shift+E**) renders the current **selection** — or the whole page when nothing is selected — through the shared scene renderer at 2× into an offscreen canvas, so the output is pixel-identical to what you see.\n- **Export SVG** serializes rectangles, ellipses, lines, text, vectors, and frames — with fills, strokes, and transforms — into an SVG file.\n\nBoth download directly via an object URL.\n\n## Next steps\n\n- [Keyboard shortcuts](/docs/canvas/keyboard-shortcuts)\n- [Status and limitations](/docs/canvas/status-and-limitations)"
        },
        {
          "slug": "keyboard-shortcuts",
          "title": "Keyboard shortcuts",
          "description": "The full shortcut reference, matching Figma defaults.",
          "body": "## Keyboard shortcuts\n\nCanvas follows Figma's default bindings so your muscle memory transfers. Press **?** in the app to open the in-app shortcut reference. Shortcuts are ignored while you're typing in a text field or the inline text editor.\n\n## Tools\n\n| Keys | Action |\n|---|---|\n| V | Move / select |\n| F | Frame |\n| R | Rectangle |\n| O | Ellipse |\n| L | Line |\n| P | Pen |\n| T | Text |\n| C | Comment |\n| H | Hand (pan) |\n| Space (hold) | Temporary pan |\n\n## Edit\n\n| Keys | Action |\n|---|---|\n| Ctrl+Z | Undo |\n| Ctrl+Shift+Z / Ctrl+Y | Redo |\n| Ctrl+C / X / V | Copy / cut / paste |\n| Ctrl+D | Duplicate |\n| Delete / Backspace | Delete |\n| Ctrl+A | Select all (current page) |\n| Esc | Clear selection / Move tool |\n\n## Arrange\n\n| Keys | Action |\n|---|---|\n| Ctrl+G | Group |\n| Ctrl+Shift+G | Ungroup |\n| Ctrl+] / Ctrl+[ | Bring forward / send backward |\n| Ctrl+Shift+] / Ctrl+Shift+[ | To front / to back |\n| Shift+A | Wrap in auto-layout |\n| Ctrl+Alt+K | Create component |\n| Ctrl+Alt+B | Detach instance |\n| Ctrl+Alt+M | Use as mask |\n\n## Transform and view\n\n| Keys | Action |\n|---|---|\n| Arrows | Nudge 1px |\n| Shift+Arrows | Nudge 10px |\n| Drag handle | Resize (Shift = ratio, Alt = from center) |\n| Drag outside corner | Rotate (Shift = 15°) |\n| Ctrl/Cmd + wheel | Zoom |\n| Ctrl/Cmd (while moving) | Suspend snapping |\n| ? | Open the shortcuts dialog |\n\n## File\n\n| Keys | Action |\n|---|---|\n| Ctrl+N | New project |\n| Ctrl+O | Open |\n| Ctrl+S | Save |\n| Ctrl+Shift+S | Save As |\n| Ctrl+Shift+E | Export PNG |\n| Ctrl+W | Close project |\n\n> Note: the pen (**P**), the **File** shortcuts, and **Ctrl+Alt+M** (mask) work in the app but are not all listed in the in-app **?** dialog yet — this table is the fuller reference."
        }
      ]
    },
    {
      "title": "Help",
      "pages": [
        {
          "slug": "status-and-limitations",
          "title": "Status and limitations",
          "description": "An honest, current map of what works in the beta and what is still in development.",
          "body": "## Status and limitations\n\nXENO Canvas is in **beta**, Windows-only for now. This page is the honest, current map of what you can rely on and what is still being built, so you don't hit a surprise wall. It tracks the product roadmap in the repository's `SPEC.md`.\n\n## Working today\n\n- Infinite Canvas2D viewport with pan and zoom (2%–25600%).\n- Drawing tools: frame, rectangle, ellipse, line, text, and the pen (vector paths).\n- Selection (click, Shift, marquee), rotation-correct hit-testing, and the full 8-handle resize / rotate manipulator with snapping.\n- Command-based **undo/redo**, group/ungroup, z-order, duplicate, copy/cut/paste, align/distribute, editable Properties fields.\n- **Design tokens** (color/number/typography/shadow) with theme modes, aliases, and fill/text binding.\n- **Auto-layout** (flex direction, gap, per-side padding, align/justify, hug/fixed, per-child fill and absolute, wrapping, nested reflow).\n- **Components**, live instances, per-instance overrides (text + fill), variants/component-sets, and detach.\n- **Fills** (solid, linear/radial gradient, image with fit modes), **strokes**, **effects** (drop shadow, layer blur), **masks**, text controls, and vector node-editing.\n- **Pages**, PNG/SVG **export**, and **dev handoff** (CSS + Tailwind, token-aware).\n- **Multiplayer** across two windows on one machine (local BroadcastChannel transport) with live cursors, selection, and multiplayer-aware undo.\n\n## In development / not yet available\n\n- **AI assist** — the Agent panel is a stub; generation, refactoring, and accessibility auditing (via `xeno-agent-sdk` and `xeno-rt`) are not wired yet.\n- **Networked multiplayer** — cross-machine collaboration, auth via Hub, server-ordered sequencing, a true op-CRDT, offline reconcile, and follow mode land with the `xeno-comms` server. Today's multiplayer is same-machine only. See [Multiplayer](/docs/canvas/multiplayer).\n- **Comments** — not implemented; the comment tool is hidden until it ships.\n- **Boolean operations** (union / subtract / intersect / exclude) — modeled but not computed; they need the `@xeno/core` path kernel.\n- **Prototyping and present mode** — interactions, smart-animate, and a runnable prototype are not yet built.\n- **Plugins** and **shared libraries** (publish / subscribe / update) are not yet built.\n- **Rich text** (per-range styling) and web/Google font loading; **Figma import**.\n- **Code signing and Hub auto-update** — the Windows installer is downloadable but not yet signed (expect a SmartScreen prompt) and doesn't auto-update yet; see [Installation](/docs/canvas/installation).\n- **Rulers and layout-grid overlays** — not yet built (zoom presets, pixel-grid toggle, Toggle UI, and the shortcuts/About dialogs all work).\n\n## Reporting issues\n\nCanvas keeps 0 typecheck errors and a green test suite as a shipping bar for each milestone. If something misbehaves, first confirm your checkout passes `npm run typecheck` and `npm run test`, then file the issue against the `xeno-canvas` repository with the steps and, where possible, the `.xcanvas` file."
        }
      ]
    }
  ]
};

export default canvas;
